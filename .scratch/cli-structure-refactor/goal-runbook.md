# CLI Structure Refactor Goal Runbook

## Source Baseline

| Path | SHA-256 |
| --- | --- |
| `.scratch/cli-structure-refactor/implementation-plan.md` | `7b419363d958d94fd06037bc2c2ecd45765b82bc2d208d9f3ae72d8e831561d3` |
| `.scratch/cli-structure-refactor/issues/01-research-github-cli-project-structure.md` | `13dd0dfa348e1cf8fa2fbd23f5c491650727eef4a3da9f3501af8f1e37fbe5dc` |
| `.scratch/cli-structure-refactor/issues/02-research-ycy-current-tree-and-ownership.md` | `c9ede864686f76aeed80efcc9ddc577cb72c650aa0abab5b6d13b2748fccfd99` |
| `.scratch/cli-structure-refactor/issues/03-choose-command-package-visibility-and-composition-root.md` | `c6992465f8298374fbe955f6cfd5fd25158cb551f33eb2c53449664bb0eb681d` |
| `.scratch/cli-structure-refactor/issues/04-choose-domain-command-tree-and-file-names.md` | `5802669121362475a09cb767a46c05a8d48e7ebde36b56a3c7ff652810a10be7` |
| `.scratch/cli-structure-refactor/issues/05-choose-shared-module-seams-and-dependency-direction.md` | `c7a4e8e19a2d42bfa90ea5d838b21cbbe1afdf125089bed250d99e333f0ffb68` |
| `.scratch/cli-structure-refactor/issues/06-choose-repository-support-area-layout.md` | `474fe4be6c75559c5e3e1493e1eb6ea0ababfe7e4b2d9484aad0550ab17c96db` |
| `.scratch/cli-structure-refactor/issues/07-choose-test-fixture-and-acceptance-topology.md` | `d8141f7b0e4824a67bf81380f251e5d2ceedd4e060bd4e8568842f200e6dabdf` |
| `.scratch/cli-structure-refactor/issues/08-prototype-target-tree-and-migration-slices.md` | `d8b512a896912123fe9019e4c0e0830d33002748e76780ac83bc6b25821f56d7` |
| `.scratch/cli-structure-refactor/issues/09-approve-structural-migration-gates.md` | `600447f7e1352eba527c1fc123b12eb9a865bd8378d13340f1ab9a9bfe45fb90` |
| `.scratch/cli-structure-refactor/map.md` | `d76a9ca55126c5fa47ca74561f023788a61f513287e3294c6992bdbcb05c7e9a` |
| `.scratch/cli-structure-refactor/prototypes/08-target-tree-and-migration-slices.md` | `e082655c37bed1a6219dcc8a5729559d7097fee5907b4ecbd6fb4203c5a8b9af` |
| `.scratch/cli-structure-refactor/research/02-ycy-current-tree-and-ownership.md` | `1f26f50ebbf81c349a2b106360c7edf20aa62eb74b7c431eb082ef99542b03c0` |
| `.scratch/cli-structure-refactor/research/github-cli-structure.md` | `c7c606d8f94572a79cb6083d0d76f59a47c4a4b09ad221a3fcd213c28e6b13a2` |
| `CLAUDE.md` | `35a0f66a6531e61b35dc7288884da35eeef79ad1039f5ffdfd29295cee8d566e` |

## State Rules

- `implementation-plan.md` 是 Gate 合同的唯一来源；本账本只记录状态和证据。
- 一次只执行 Goal Ledger 中唯一 `active` 的 Gate。
- 每轮向对应 Gate 的 Progress Log 追加 slice、修改、验证结果、风险和下一动作。
- `passed` 需要计划中每条 Exit condition 的明确证据；普通实现或验证失败保持 `active`。
- `blocked` 只用于计划声明的 Stop condition，并记录阻塞与恢复条件。
- 人工验收待确认是一次 Goal 终止交接，必须记录在 Progress Log；它不是 `blocked`，当前 Gate 必须保持 `active`，不得通过重复日志表示等待。
- 当前 Gate 通过后只激活直接后继并结束本次 Goal；直接后继虽为 `active`，但必须由新的 Goal 执行。最后一个 Gate 通过后记录 effort 完成并结束本次 Goal。

## Goal Ledger

| Gate | Status | Depends on | Plan contract | Unlock evidence |
| --- | --- | --- | --- | --- |
| G0: Stabilize pre-migration baseline | passed | none | `implementation-plan.md` -> `G0: Stabilize pre-migration baseline` | no predecessor |
| G1: Freeze surface and establish acceptance architecture | passed | G0 | `implementation-plan.md` -> `G1: Freeze surface and establish acceptance architecture` | G0 E1-E4 recorded |
| G2: Lift root and introduce the bounded Factory | passed | G1 | `implementation-plan.md` -> `G2: Lift root and introduce the bounded Factory` | G1 E1-E5 recorded |
| G3: Migrate simple command vertical slices | passed | G2 | `implementation-plan.md` -> `G3: Migrate simple command vertical slices` | G2 E1-E4 recorded |
| G4: Migrate nested Config groups | passed | G3 | `implementation-plan.md` -> `G4: Migrate nested Config groups` | G3 E1-E4 recorded |
| G5: Migrate Git leaves and shared process runtime | passed | G4 | `implementation-plan.md` -> `G5: Migrate Git leaves and shared process runtime` | G4 E1-E4 recorded |
| G6: Migrate long-running and worker-backed commands | passed | G5 | `implementation-plan.md` -> `G6: Migrate long-running and worker-backed commands` | G5 E1-E5 recorded |
| G7: Finalize the process composition root | passed | G6 | `implementation-plan.md` -> `G7: Finalize the process composition root` | G6 E1-E5 recorded |
| G8: Publish and audit the final structure | passed | G7 | `implementation-plan.md` -> `G8: Publish and audit the final structure` | G7 E1-E4 recorded |
| G9: Collect native and release evidence | passed | G8 | `implementation-plan.md` -> `G9: Collect native and release evidence` | G8 E1-E5 recorded; user-approved Windows/manual-evidence deferral recorded 2026-08-30 |

## Progress Log

### G0: Stabilize pre-migration baseline

- 2026-08-29: initialized as `active`; implementation has not started.
- 2026-08-29: Slice `baseline-record` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Directed verification before tests: rollback commit `1b0a4f8b931eec472083b4bdea66265adb929ba4`; `git status --short` was empty; host `Darwin arm64`; `go version go1.27.0 darwin/arm64`. Risk: the required test commands pin `GOTOOLCHAIN=go1.26.7`, which differs from the host default and must remain available without tracked-source changes. Next: run the first of three ordinary-suite repetitions and stop on any failure or worktree/commit change.
- 2026-08-29: Slice `ordinary-suite-1-of-3` stopped. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Repository verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 1 before affected packages could compile: `web/assets.go` could not embed missing `web/dist`, and `internal/commands/fs/sevenzipruntime/payload_darwin_arm64.go` could not embed missing `payload/darwin-arm64/7zz`. This is a G0 Stop condition; the remaining two ordinary-suite runs and the focused 20-run Tunnel case were not run. Post-failure evidence: commit remained `1b0a4f8b931eec472083b4bdea66265adb929ba4`; `git status --short` contains only this runbook's evidence edits; `git diff --check` passed; both required paths were absent. Risk: a retry cannot satisfy G0 after this failure. Recovery input: a separate, explicitly authorized prerequisite-preparation task or clean environment must provide the Web distribution and Darwin arm64 7-Zip payload without tracked-source changes, then a new G0 Goal must restart all baseline evidence from the beginning.
- 2026-08-29: Recovery prerequisite preparation completed under explicit user authorization. `make PNPM='npx --yes pnpm@11.13.0' bootstrap` succeeded after bypassing the host's incompatible `pnpm 11.6.0` without changing the global tool; `make PNPM='npx --yes pnpm@11.13.0' check-web` passed lint, type checking, 33 tests, Vite build, and generated-asset verification; `make prepare-7zip` succeeded. Verification found 428 files under `web/dist` and a Darwin arm64 `payload/darwin-arm64/7zz` executable. `web/node_modules`, `web/dist`, the payload, `tools/lefthook/bin`, and `.tmp/7zip/downloads` are ignored; `git status --short` and `git diff --name-only` still show only this runbook. Risk: this restores prerequisites but does not erase the failed ordinary-suite evidence. Next: begin a newly authorized G0 baseline run from its directed-record slice; G0 remains `blocked` until that restart is explicitly initiated.
- 2026-08-29: User explicitly authorized a G0 restart after prerequisite preparation. G0 is reactivated; the prior failed ordinary-suite invocation remains invalid evidence and will not be counted or retried as part of the new repetition set. Scope remains G0 only. Next: record a fresh rollback commit/worktree/environment snapshot before running ordinary-suite repetition 1 of 3.
- 2026-08-29: Slice `baseline-record-restart` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Fresh directed verification before the new repetition set: rollback commit `1b0a4f8b931eec472083b4bdea66265adb929ba4`; `git status --short` and `git diff --name-only` showed only this runbook; host `Darwin arm64`; host `go version go1.27.0 darwin/arm64`; pinned toolchain `go version go1.26.7 darwin/arm64`. Generated Web and 7-Zip prerequisites remain ignored. Risk: any new test failure invalidates this restarted set. Next: run ordinary-suite repetition 1 of 3.
- 2026-08-29: Slice `ordinary-suite-1-of-3-restart` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Repository verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 0; all listed packages passed, including `cmd/ycy` (97.063s) and `internal/commands/tunnel` (73.810s). Post-run directed verification: commit remained `1b0a4f8b931eec472083b4bdea66265adb929ba4`; tracked diff remains only this runbook; `git diff --check` passed. Risk: two independent ordinary-suite repetitions and the focused 20-run case remain. Next: run ordinary-suite repetition 2 of 3.
- 2026-08-29: Slice `ordinary-suite-2-of-3-restart` stopped. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Repository verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 1 because `internal/commands/tunnel` failed `TestFRPSupervisorRejectsAnActivationExitAndKeepsConfigurationFailuresStopped`: `frp_supervisor_unix_test.go:103: condition did not become true within 1s`. This is the known Tunnel supervisor timeout described by the G0 decision and a Stop condition. The first restarted ordinary-suite repetition remains recorded but the repetition set is invalid; ordinary-suite repetition 3 and the focused 20-run case were not run. Post-failure directed verification: commit remained `1b0a4f8b931eec472083b4bdea66265adb929ba4`; tracked diff remains only this runbook; `git diff --check` passed; no production migration occurred. Risk: G0 cannot proceed on a retry or flaky-test allowance. Recovery input: stabilize this test or its underlying supervisor behavior in a separate task and commit, then start a new G0 run with all baseline evidence from the beginning.
- 2026-08-29: User-requested diagnosis (non-evidence; G0 remains `blocked`) completed. The failed assertion waits only 1s for a fixture shell to write its startup marker after `configured.Start`; `Start` treats the child as activated after its 20ms liveness window without guaranteeing the shell has executed that marker write. A diagnostic `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=20 ./internal/commands/tunnel -run '^TestFRPSupervisorRejectsAnActivationExitAndKeepsConfigurationFailuresStopped$'` exited 0 in 8.549s, confirming that the named test is not deterministically failing when isolated. Risk: this supports a load-sensitive test deadline rather than proving there is no runtime hang. Recommended separate stabilization task: retain the marker assertion and replace its 1s wait with the existing bounded `frpSupervisorFixtureStartupTimeout` (5s), add state/PID diagnostics on expiry, then prove the change with repeated focused and full-suite runs. If the 5s bound fails, diagnose the child process state before changing production behavior. Next: await the separate stabilization result before a fresh G0 restart.
- 2026-08-29: Separate pre-migration stabilization completed in local commit `cd0e7f179939a1235d92ec5f9f43c2d2f0ec0072` (`test(tunnel): stabilize supervisor fixture startup wait`). It changes only `internal/commands/tunnel/frp_supervisor_unix_test.go`: retains the startup-marker and configuration-failure assertions while using the existing bounded 5s fixture startup timeout. Verification before commit: the named test passed `-count=100` in 38.487s, and `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 0, including Tunnel in 73.751s; formatting and diff hygiene passed. G0 is reactivated against this new rollback commit; no production implementation changed. Next: record a fresh directed baseline snapshot, then run ordinary-suite repetition 1 of 3.
- 2026-08-29: Slice `baseline-record-after-stabilization` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Directed verification before the new repetition set: rollback commit `cd0e7f179939a1235d92ec5f9f43c2d2f0ec0072`; `git status --short` and `git diff --name-only` showed only this runbook; host `Darwin arm64`; host `go version go1.27.0 darwin/arm64`; pinned toolchain `go version go1.26.7 darwin/arm64`. Generated Web and 7-Zip prerequisites remain ignored. Risk: any ordinary-suite failure invalidates this new set. Next: run ordinary-suite repetition 1 of 3.
- 2026-08-29: Slice `ordinary-suite-1-of-3-after-stabilization` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Repository verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 0; all listed packages passed, including `cmd/ycy` (79.679s) and `internal/commands/tunnel` (61.025s). Post-run directed verification: commit remained `cd0e7f179939a1235d92ec5f9f43c2d2f0ec0072`; tracked diff remains only this runbook; `git diff --check` passed. Risk: two independent ordinary-suite repetitions and the focused 20-run case remain. Next: run ordinary-suite repetition 2 of 3.
- 2026-08-29: Slice `ordinary-suite-2-of-3-after-stabilization` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Repository verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 0; all listed packages passed, including `cmd/ycy` (96.074s) and `internal/commands/tunnel` (72.945s). Post-run directed verification: commit remained `cd0e7f179939a1235d92ec5f9f43c2d2f0ec0072`; tracked diff remains only this runbook; `git diff --check` passed. Risk: one independent ordinary-suite repetition and the focused 20-run case remain. Next: run ordinary-suite repetition 3 of 3.
- 2026-08-29: Slice `ordinary-suite-3-of-3-after-stabilization` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Repository verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 0; all listed packages passed, including `cmd/ycy` (79.607s) and `internal/commands/tunnel` (60.566s). Post-run directed verification: commit remained `cd0e7f179939a1235d92ec5f9f43c2d2f0ec0072`; tracked diff remains only this runbook; `git diff --check` passed. Risk: only the focused 20-run Tunnel case and final diff/no-production-migration audit remain. Next: run the named Tunnel supervisor test with `-count=20`.
- 2026-08-29: Slice `tunnel-supervisor-20-run-after-stabilization` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. Repository verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=20 ./internal/commands/tunnel -run '^TestFRPSupervisorRejectsAnActivationExitAndKeepsConfigurationFailuresStopped$'` exited 0 in 8.074s with no skipped or retried failure. Final G0 acceptance: E1 satisfied by the `cd0e7f179939a1235d92ec5f9f43c2d2f0ec0072` rollback commit, recorded worktree, Darwin/arm64, and Go 1.27.0 / pinned Go 1.26.7 facts; E2 satisfied by three distinct zero-exit ordinary-suite runs after stabilization (`cmd/ycy`: 79.679s, 96.074s, 79.607s; Tunnel: 61.025s, 72.945s, 60.566s); E3 satisfied by the zero-exit 20-run named Tunnel result; E4 satisfied by final `git status --short` / `git diff --name-only` showing only this runbook, `git diff --check` passing, and the only stabilization commit changing `internal/commands/tunnel/frp_supervisor_unix_test.go` rather than production implementation. G0 is `passed`; its direct successor G1 is activated but not started. Risk: none remaining for G0. Next: end this Goal without reading, implementing, or verifying G1.

### G1: Freeze surface and establish acceptance architecture

- 2026-08-29: initialized as `planned`; no implementation evidence.
- 2026-08-29: activated after G0 E1-E4 were recorded; implementation has not started.
- 2026-08-29: Slice `surface-capture` completed. Added the test-only command-surface capture/comparison harness at `internal/cliapp/command_surface_test.go` and created the tracked frozen set under `acceptance/testdata/command-surface/`: `manifest.json` records 35 command paths, `Use`, aliases, hidden/deprecated state, and local/inherited flag names, shorthands, types, defaults, and no-option defaults; 35 normalized help files and deterministic Bash, Zsh, Fish, and PowerShell completion files are tracked. Explicit initial generation used `YCY_UPDATE_COMMAND_SURFACE=1 GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/cliapp -run '^TestCommandSurface$'`; the read-only comparison path passed immediately afterward. Directed binary comparison built the unchanged `./cmd/ycy` at the G0 source (`GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go build -trimpath`) and matched all 35 command help outputs plus all four completion outputs byte-for-byte; `git diff --check` passed. Modified files are limited to the new test harness and frozen evidence (production command implementation unchanged). Risk: the generator currently lives with the pre-migration `internal/cliapp` test ownership and must move with the acceptance architecture before that package is removed in a later Gate. Next: relocate the architecture test and replace the blanket `pkg` rule with the approved transition rules.
- 2026-08-29: Slice `architecture-relocation` completed. Moved `cmd/ycy/architecture_test.go` to `internal/architecture/architecture_test.go`, retained the shared `repositoryRoot` test helper in `cmd/ycy/test_helpers_test.go`, replaced the blanket `pkg` prohibition with explicit target-tree ownership/direction rules, and added the short `transitionAllowlist` for `cmd/ycy`, `internal/cliapp`, and `internal/commands`. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` and `... go test -count=1 ./cmd/ycy`; `git diff --check` passed. Risk: the allowlist is transitional and must shrink as later production slices move; no production files were changed. Next: relocate standalone/black-box acceptance tests and keep package-local adapter tests in their current owners.
- 2026-08-29: Slice `acceptance-relocation-config-cm` completed. Moved the standalone-binary cases for `config cm list/add/set/use/remove/test` into the tagged `acceptance/config_test.go`; removed only those cases from `cmd/ycy/configcm*.go` (the package-local adapter, presentation, and persistence tests remain there), and centralized the existing standalone environment/process helpers in `acceptance/helpers_test.go`. Restored `cmd/ycy`-owned Git CM/Pulse fixture helpers in `gitcm_test_helpers_test.go` and `gitpulse_test_helpers_test.go` after their integration files moved, with no production changes. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance -run '^TestConfigCM'` (all six CM binary cases), tagged acceptance compile, ordinary `cmd/ycy` compile, and `git diff --check`. Risk: remaining standalone cases still in `cmd/ycy` must move before the acceptance topology slice is complete. Next: move the complete `config fork` black-box cluster.
- 2026-08-29: Slice `acceptance-relocation-config-fork` completed. Moved the `config fork` list/add/remove standalone-binary cases into tagged `acceptance/config_fork_test.go`, including their disposable configuration fixture; removed only the standalone cases and now-unused standalone JSON helpers from `cmd/ycy/configfork_test.go`, retaining the package-wide environment helper and all package-local tests. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance -run '^TestConfigFork'` (all three fork cases), ordinary `cmd/ycy` compile, tagged acceptance compile, and `git diff --check`. Risk: standalone cases for Export Env, RM, Run, and Zip remain. Next: move the Export Env standalone case.
- 2026-08-29: Slice `acceptance-relocation-export-env` completed. Moved `TestExportEnvStandaloneBinary` into tagged `acceptance/export_env_test.go` with its process runner and disposable `.env` fixture; retained all adapter, presentation, automation, and plain-interaction tests in `cmd/ycy/exportenv_test.go`. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance -run '^TestExportEnvStandaloneBinary$'`, ordinary `cmd/ycy` compile, tagged acceptance compile, and `git diff --check`. Risk: RM, Run, and Zip standalone cases remain in `cmd/ycy`. Next: move the RM standalone case and its process-only filesystem helpers.
- 2026-08-29: Slice `acceptance-relocation-rm` completed. Moved `TestRMStandaloneBinary` and its disposable-root/process helpers into tagged `acceptance/rm_test.go`; retained the shared package-local file and working-directory helpers in `cmd/ycy/rm_test_helpers_test.go` because RM adapter tests still exercise them. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance -run '^TestRMStandaloneBinary$'`, ordinary `cmd/ycy` compile, tagged acceptance compile, and `git diff --check`. Risk: Run and Zip standalone cases remain. Next: move the Run standalone case while retaining its package-local fixture helpers.
- 2026-08-29: Slice `acceptance-relocation-run` completed. Moved `TestRunStandaloneBinaryRejectsRedirectedSelectionAndPreservesParserBehavior` into tagged `acceptance/run_test.go`; retained the package-local Run file and working-directory helpers in `cmd/ycy/run_test.go` because package tests use them, and added an acceptance-local writer for the standalone fixture. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance -run '^TestRunStandaloneBinaryRejectsRedirectedSelectionAndPreservesParserBehavior$'`, ordinary `cmd/ycy` compile, tagged acceptance compile, and `git diff --check`. Risk: Zip and the standalone Git Fork case remain outside acceptance. Next: move the Zip standalone case and then audit remaining binary tests.
- 2026-08-29: Slice `acceptance-relocation-zip` completed. Moved `TestZIPStandaloneBinaryRejectsRedirectedPlanningAndPreservesHelp` into tagged `acceptance/zip_test.go` with its process-only fixture writer; retained Zip package-local planning, archive, process, and platform tests in `cmd/ycy/zip_test.go`. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance -run '^TestZIPStandaloneBinaryRejectsRedirectedPlanningAndPreservesHelp$'`, ordinary `cmd/ycy` compile, tagged acceptance compile, and `git diff --check`. Risk: audit found one remaining Git Fork standalone test and its helper-path coverage in `cmd/ycy`; those must move before the topology is complete. Next: move the Git Fork standalone case and standalone-path helper test.
- 2026-08-29: Slice `acceptance-relocation-git-fork` completed. Moved `TestGitForkStandaloneBinaryDownloadsALocalProviderArchive` into tagged `acceptance/git_fork_test.go` with acceptance-local archive/config fixtures, moved the standalone executable-path helper test into `acceptance/helpers_test.go`, and removed now-orphaned `cmd/ycy/standalone_binary_test.go` plus `cmd/ycy/test_helpers_test.go`. Package-local Git Fork fixtures remain in `cmd/ycy` because its adapter and handler tests still use them. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance -run '^(TestGitForkStandaloneBinaryDownloadsALocalProviderArchive|TestResolveStandaloneBinaryUsesWindowsExecutableSuffix)$'`, ordinary `cmd/ycy` compile, tagged acceptance compile, and `git diff --check`; an audit found no remaining standalone-binary build invocation under `cmd/ycy` or `internal`. Risk: all tagged cases still need the full acceptance run through the final Make target. Next: add fixed acceptance and read-only/update surface Make targets.
- 2026-08-29: Slice `make-targets` completed. Added `make acceptance` with the fixed contract `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance/...`, plus explicit read-only `make command-surface` and update-gated `make command-surface-update`; updated `.PHONY` and help without changing the existing `make check` dependency graph. Directed verification passed: `make -n acceptance command-surface command-surface-update` printed the exact contracts, `make -n check` contained neither `acceptance` nor `command-surface`, `make command-surface` passed clean comparison mode, `make acceptance` passed all tagged scenarios in 74.800s, `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed, and `git diff --check` passed. Risk: final ordered G1 repository verification remains; no production command implementation changed. Next: run the required repository sequence without entering G2.
- 2026-08-29: Repository verification attempt 1 stopped at step 1. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 1 because existing `cmd/ycy` test `TestOSForkGitRunnerStopsTheChildProcessGroupWithoutAnOrphan` did not record its child PID within 2s (`gitfork_process_unix_test.go:31`); all subsequently listed non-`cmd/ycy` packages passed, including Tunnel. No later repository steps were run, no acceptance assertion changed, and no production source was modified. Risk: E2 cannot be accepted until this process-fixture failure is diagnosed and the full ordered sequence is rerun. Next: reproduce the named test in isolation and inspect its bounded-startup fixture before considering an in-scope test-stability change.
- 2026-08-29: Slice `git-fork-process-fixture-stability` completed. Isolation `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=20 ./cmd/ycy -run '^TestOSForkGitRunnerStopsTheChildProcessGroupWithoutAnOrphan$'` passed in 4.010s, identifying a load-sensitive fixture-scheduling deadline rather than a changed process result. Added `waitForHeatGitPIDWithin` without changing Heat's 2s caller, and changed only Git Fork's PID-startup wait to named, bounded `forkGitFixtureStartupTimeout` (5s); cancellation exit-code and no-orphan assertions remain unchanged and production code was not touched. Directed verification passed: the named Git Fork test `-count=100` in 17.804s, full `cmd/ycy` in 7.685s, and `git diff --check`. Risk: the entire final ordered repository sequence must restart from step 1; no later Gate work is authorized. Next: rerun G1 repository verification step 1.
- 2026-08-29: Final G1 acceptance completed and G1 is `passed`. E1: the reviewed frozen surface under `acceptance/testdata/command-surface/` contains `manifest.json`, 35 normalized help artifacts, and Bash/Zsh/Fish/PowerShell completions; `make command-surface` completed clean comparison mode without update. E2: final repository step 1 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` passed (`cmd/ycy` 11.659s; Tunnel 54.629s), and step 2 `make acceptance` passed all tagged top-level acceptance scenarios in 74.742s. E3: final repository step 3 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed in 0.249s; inspection confirms its explicit three-entry shrinking transition allowlist and no blanket `pkg` ban. E4: `make -n acceptance` showed the exact fixed contract, `make -n check` showed no acceptance/surface dependency, and `make acceptance` passed. E5: final source audit found only test/evidence/Make/runbook changes, including the bounded Git Fork fixture startup timeout; no production command implementation changed. Final repository step 4 `git diff --check` passed. Risk: none remaining for G1. Direct successor G2 is activated but not started; end this Goal without reading, implementing, or verifying G2.

### G2: Lift root and introduce the bounded Factory

- 2026-08-29: initialized as `planned`; no implementation evidence.
- 2026-08-29: 已激活，尚未开始实施. Activated after G1 E1-E5 were recorded; this Goal ends here and no G2 code, verification, or analysis was performed.
- 2026-08-29: Slice `root-direct-move` completed. Moved the complete root implementation and its tests directly from `internal/cliapp/` to `pkg/cmd/root/`, renamed the package to `root`, and rewired every current `cmd/ycy` use to that path; no forwarding package, alias, or old source path remains. Updated the command-surface Make targets and repository-root lookup for the deeper test location, included `pkg` in the Go formatting inventory, and replaced the removed path's architecture exception with an explicit `pkg/cmd/root` allowlist of the 13 still-unmigrated handler Modules. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/root ./cmd/ycy ./internal/architecture`; `make command-surface` compared cleanly; `rg -n 'internal/cliapp|github.com/hackycy/hackycy-cli/internal/cliapp' cmd internal pkg tools web Makefile` returned no result; `git diff --check` passed. Risk: process facts and the current handler graph still use the pre-Factory root constructor shape, and the transition handler allowlist must shrink as later leaves migrate. Next: add the bounded Factory type with tests for its exact fields and lazy behavior.
- 2026-08-29: Slice `factory-type` completed. Added `pkg/cmdutil.IOStreams` and the exact eleven-field `pkg/cmdutil.Factory` capability set, with reflection tests that reject field additions, omissions, ordering changes, or type substitutions. Added only the opaque `internal/gitprocess.Runner` marker required to strongly type Factory's lazy `GitRunner`; it contains no process execution, capture, platform signaling, command semantics, or Adapter migration, all of which remain G5 work. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmdutil ./internal/gitprocess ./pkg/cmd/root` (`internal/gitprocess` intentionally has no behavior tests yet), `... go test -count=1 ./internal/architecture`, and `git diff --check`. Risk: Factory is intentionally not yet constructed or consumed by the binary, so this slice proves its bounded contract but not lazy default behavior. Next: add `pkg/cmd/factory.New` and tests for deterministic process facts and deferred, memoized ConfigStore/GitRunner construction.
- 2026-08-29: Slice `factory-default-construction` completed. Added `pkg/cmd/factory.New(Options)`, which accepts explicit version, inherited streams, terminal Session, environment lookup, working-directory, HTTP client, and clock facts; it derives only safe defaults and creates Terminal/Logging without network, Git, or configuration persistence. `ConfigStore` and `GitRunner` use independent `sync.Once` closures, memoizing both a store result/error and a runner only on first access. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/factory ./pkg/cmdutil ./pkg/cmd/root` (explicit-facts, derived-lookup, no-eager-call, and memoization tests), `... go test -count=1 ./internal/architecture`, and `git diff --check`. Risk: the existing binary still constructs root process facts directly, so no observable runtime path uses Factory yet. Next: wire the temporary binary composition root and lifted root through Factory while reducing root Dependencies to handlers only.
- 2026-08-29: Slice `temporary-factory-wiring` completed. Moved the rich-terminal discovery adapter and its test into `pkg/cmd/root`, changed root construction to accept `*cmdutil.Factory`, and reduced `root.Dependencies` to exactly 22 unmigrated typed handler capabilities. The temporary `cmd/ycy` composition root now classifies inherited terminal facts, constructs one `factory.New` value, reuses its Terminal and Logging for existing handlers and root execution, and preserves hidden dispatch, startup update, Web validation, signals, and `os.Exit` ownership. Updated affected package-local test construction through test-only fixtures only; production has no old root constructor or compatibility wrapper. Architecture now reflects the 22-field handler allowlist and permits only the exact temporary binary imports `pkg/cmd/root`, `pkg/cmd/factory`, and `pkg/cmdutil`. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/root ./cmd/ycy ./internal/architecture`, then `... go test -count=1 ./pkg/cmd/root ./pkg/cmd/factory ./pkg/cmdutil`; `make command-surface` passed; `make acceptance` passed in 76.515s; the required old-path search returned no result; `git diff --check` passed. Risk: none remaining for this slice; future leaf Gates must only shrink the named handler/import allowlists.
- 2026-08-29: Final G2 acceptance completed and G2 is `passed`. E1: all root source/tests now live under `pkg/cmd/root`; `internal/cliapp` is absent, `rg -n 'internal/cliapp|github.com/hackycy/hackycy-cli/internal/cliapp' cmd internal pkg tools web Makefile` returned no result, and root error/help/version/discovery behavior passed through the Factory-backed tests and frozen command-surface comparison. E2: `pkg/cmdutil.Factory` has the exact approved eleven fields, and `pkg/cmd/factory.New` tests prove explicit deterministic process facts plus lazy, memoized ConfigStore/GitRunner construction with no eager config/Git/network access. E3: the architecture suite passed with the exact 22-handler `root.Dependencies` allowlist and the exact 13 unmigrated command-Module import allowlist; no broad `pkg` exemption was added. E4: final repository step 1 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` passed (`cmd/ycy` 22.203s; Tunnel 67.190s; root/factory/cmdutil all passed), step 2 `make acceptance` passed in 73.696s, step 3 `... go test -count=1 ./internal/architecture` passed in 0.285s, step 4 `git diff --check` passed, and `make command-surface` remained clean. Risk: none remaining for G2. Direct successor G3 is activated but not started; end this Goal without reading, implementing, or verifying G3.

### G3: Migrate simple command vertical slices

- 2026-08-29: initialized as `planned`; no implementation evidence.
- 2026-08-29: 已激活，尚未开始实施. Activated after G2 E1-E4 were recorded; this Goal ends here and no G3 code, verification, or analysis was performed.
- 2026-08-29: Slice `rm-vertical-leaf` completed. Moved the complete RM Module, Cobra grammar, terminal Adapter, and package-local tests from `internal/commands/rm`, `pkg/cmd/root/rm.go`, and `cmd/ycy/rm*.go` into `pkg/cmd/rm/`. `NewCmdRM(factory, runF)` now produces leaf-owned `Options` and defaults to the private `runRM`; the default runner receives working-directory, terminal, and remover dependencies from the bounded Factory/leaf rather than a root handler. Removed the `RM` field/wiring from root and `cmd/ycy`, removed its root import/handler architecture allowlist entries, and made root register the leaf directly while retaining global diagnostic configuration before RM execution. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/rm` (final 0.548s; constructor, runner/Module, Adapter, root-outcome tests); `make acceptance` passed all tagged scenarios in 76.906s including `TestRMStandaloneBinary`; `make command-surface` compared cleanly (0.480s); `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed (0.353s); old-path audit confirmed `internal/commands/rm`, `cmd/ycy/rm.go`, and `pkg/cmd/root/rm.go` are absent and no active Go reference to `internal/commands/rm`, `RmHandler`, or `newRMHandler` remains; `git diff --check` passed. Risk: diagnostic setup is intentionally root-owned and currently recognizes the migrated RM leaf; subsequent migrated leaves must extend that narrow hook without changing unmigrated command behavior. Next: migrate the `export env` leaf only.
- 2026-08-29: Slice `export-env-vertical-leaf` completed. Moved the complete Export Env Module and its package tests from `internal/commands/exportenv`, moved its Cobra grammar and terminal/filesystem Adapter from `pkg/cmd/root/exportenv.go` and `cmd/ycy/exportenv.go` into `pkg/cmd/export/env/`, and added the registration-only `pkg/cmd/export` parent. `env.NewCmdEnv(factory, runF)` produces leaf-owned `Options` and defaults to private `runEnv`; it uses only Factory working-directory/terminal facts plus leaf-owned reader/writer adapters. Removed the `ExportEnv` root handler/wiring, its `cmd/ycy` construction, and both matching architecture allowlist entries; root now directly registers the parent and applies the existing global diagnostic configuration for `ycy export env`. Moved root-crossing output/error assertions into external leaf root-outcome tests, retained package-local Adapter/Module assertions, and corrected two older root tests whose intentionally incomplete trees had masked the production `fs` -> `rm` suggestion now exposed by permanently registered RM. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/export/env` (final 0.256s; constructor, Module, Adapter, and root-outcome tests); supporting `... go test -count=1 ./pkg/cmd/root ./pkg/cmd/rm ./pkg/cmd/export/... ./cmd/ycy ./internal/architecture` passed; `make acceptance` passed all tagged scenarios in 75.720s including `TestExportEnvStandaloneBinary`; `make command-surface` compared cleanly (0.431s); `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed (0.304s). Final strict audit removed the empty old directories and confirmed `internal/commands/{rm,exportenv}`, old `cmd/ycy`/root files, and all active Go references to the old paths/handler names are absent; `git diff --check` passed. Risk: the root's narrow diagnostic hook now recognizes two migrated command paths and must grow only for the remaining G3 leaves. Next: migrate the `run` leaf only, retaining child-process behavior inside that leaf.
- 2026-08-29: Slice `run-vertical-leaf` completed. Moved the complete Run Module/tests from `internal/commands/run`, its Cobra grammar from `pkg/cmd/root/run.go`, and its terminal/raw-child-process Adapter plus Unix/Windows platform files/tests from `cmd/ycy/run*.go` into `pkg/cmd/run/`. `run.NewCmdRun(factory, runF)` owns the original `DisableFlagParsing` grammar, leaf `Options`, private `runRun`, and the exact legacy argument parser; its `Args` validates the raw argument shape before root diagnostics configure so an invalid path matrix remains the reported error. The leaf owns raw inherited child streams, process-group termination, exit-code mapping, and its copied Run-specific start-error wrapper; the still-unmigrated Git process code retains its identical narrow `signalCause` interface in `cmd/ycy/git_process.go`. Removed the `Run` root dependency/wiring and `cmd/ycy` construction, removed both matching architecture allowlist entries, and made root register `run.NewCmdRun(factory, nil)` directly while extending only the existing diagnostic hook for `ycy run`. Moved constructor/Module/Adapter/process tests into the leaf and preserved root-crossing automation, diagnostic-order, and child-exit assertions in leaf-owned external tests. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/run` (1.496s; constructor, Module, Adapter, raw child I/O, Unix process-group, root outcome, and start-error tests); `make acceptance` passed all tagged scenarios in 72.981s including `TestRunStandaloneBinaryRejectsRedirectedSelectionAndPreservesParserBehavior`; `make command-surface` compared cleanly (0.466s); `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed (0.511s). Final strict audit removed the empty `internal/commands/run` directory and confirmed no active Go reference to `internal/commands/run`, `RunHandler`, `newRunHandler`, `registerRun`, or `runCommand`; `git diff --check` passed. Risk: the Git-only `signalCause` retention is intentional until G5, while no old/new Run implementation pair remains. Next: migrate the `zip` leaf only.
- 2026-08-29: Slice `zip-vertical-leaf` completed. Moved the complete ZIP Module and its planning/archive/discovery tests from `internal/commands/zip`, moved Cobra grammar from `pkg/cmd/root/zip.go`, and moved the terminal Adapter, Git remote-name and host-reveal adapters, plus package-local tests from `cmd/ycy/zip*.go` into `pkg/cmd/zip/`. `zip.NewCmdZIP(factory, runF)` now creates leaf-owned `Options` and defaults to private `runZIP`, preserving the `zip [directory]` grammar and `--without-open`/`--with-dir` defaults. Removed the `ZIP` root dependency/wiring, `cmd/ycy` construction, and both matching architecture allowlist entries; root registers the leaf directly and applies the existing global diagnostic configuration for `ycy zip`. Moved constructor assertions into the leaf and preserved root-crossing automation/destination-side-effect assertions in external leaf tests. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/zip` (0.632s; constructor, planning/archive Module, terminal Adapter, root automation, remote resolver, and host revealer tests); `make acceptance` passed all tagged scenarios in 74.235s including `TestZIPStandaloneBinaryRejectsRedirectedPlanningAndPreservesHelp`; `make command-surface` compared cleanly (0.598s); `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed (0.632s). Final strict audit removed the empty `internal/commands/zip` directory and confirmed no active Go reference to `internal/commands/zip`, `ZipHandler`, `newZipHandler`, `registerZIP`, or `app.zip`; `git diff --check` passed. Risk: none remaining for this leaf; all four G3 leaf migrations are present and require final ordered repository evidence. Next: run G3 repository verification step 1 only.
- 2026-08-29: G3 repository verification attempt 1 stopped at step 1. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` passed every listed package except `pkg/cmd/root`: `TestGitCMGroupIsAbsentWithoutTheProductionHandler` still expected the old no-suggestion message for `git`, while the complete production tree now correctly reports `did you mean 'zip'? Run 'ycy zip --help' for usage.` after permanent ZIP registration. Updated only that asserted expected string; no production behavior, API, command surface artifact, or test control flow changed. Risk: the ordered evidence set is invalid until restarted. Next: restart final repository verification from step 1.
- 2026-08-29: Final G3 acceptance completed and G3 is `passed`. E1: complete source/test ownership now lives at `pkg/cmd/rm`, `pkg/cmd/export/env` (with registration-only `pkg/cmd/export`), `pkg/cmd/run`, and `pkg/cmd/zip`; the matching old `internal/commands/{rm,exportenv,run,zip}`, `cmd/ycy` flat Adapters, and root Cobra files are absent, and the final strict old-path/handler audit returned no match. E2: the four completed leaf records provide green constructor/runner-or-Module/package-local tests, black-box tagged acceptance, and frozen surface evidence: RM `go test ./pkg/cmd/rm`, Export Env `go test ./pkg/cmd/export/env`, Run `go test ./pkg/cmd/run`, ZIP `go test ./pkg/cmd/zip`, with every checkpoint's `make acceptance` and `make command-surface` passing. E3: root directly registers the four leaves; its temporary `Dependencies` set is now the exact 18 unmigrated fields, with all four matching handler capabilities and root-module allowlist entries removed; the final architecture suite and old-symbol audit passed. E4: after the one test-expectation correction recorded above, final repository verification restarted in order and passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` (`cmd/ycy` 12.531s, Tunnel 57.061s, `pkg/cmd/root` 1.795s), `make acceptance` (76.061s), `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` (0.504s), and `git diff --check`. Risk: none remaining for G3. Direct successor G4 is activated but not started; this Goal ends without reading, implementing, or verifying G4.

### G4: Migrate nested Config groups

- 2026-08-29: initialized as `planned`; no implementation evidence.
- 2026-08-29: 已激活，尚未开始实施. Activated after G3 E1-E4 were recorded; this Goal ends here and no G4 code, verification, or analysis was performed.
- 2026-08-29: Slice `config-fork-group` completed. Moved the complete Fork Module and tests into `pkg/cmd/config/fork/{list,add,remove}` with leaf-owned Store boundaries, Cobra constructors, terminal Adapters, and presentation; added registration-only `pkg/cmd/config/fork` and transitional `pkg/cmd/config` parent. Removed the old `internal/commands/config/fork`, flat `cmd/ycy/configfork*.go`, root Fork registration, and the three corresponding root handler/allowlist entries. The transitional root still attaches the unmigrated CM child until the second group cutover; no CM implementation was changed in this slice. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/config/... ./internal/appconfig`; Fork leaf tests cover list persistence-safe projection, add prompt/validation/encryption, and remove selection/confirmation/mutation; `make command-surface` compared cleanly; `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed; `make acceptance` passed all tagged scenarios in 75.527s, including `TestConfigForkListStandaloneBinary`, `TestConfigForkAddStandaloneBinary`, and `TestConfigForkRemoveStandaloneBinary`; `git diff --check` passed. Risk: the top-level config parent and root still contain the old CM handler graph by design until `config cm` is cut over; the final G4 repository gate must restart after that second group. Next: migrate the complete `config cm` group, one leaf at a time, then remove the remaining Config transition fields and old CM paths.

- 2026-08-29: Slice `config-cm-list-terminal` completed. Moved the legacy CM list terminal/presentation tests from `cmd/ycy/configcm_test.go` to `pkg/cmd/config/cm/list/terminal_test.go`, switched assertions to the leaf-owned `Result`/`Profile` types, and retained plain/automation output plus rich semantic-role coverage. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/config/cm/list` passed. Risk: the remaining CM terminal tests still reference the old root handlers and must be moved before deleting that transition. Next: migrate the CM add terminal Adapter/presentation tests into the `add` leaf.
- 2026-08-29: Slice `config-cm-add-terminal` completed. Moved the CM add terminal Adapter/presentation and automation-boundary tests from `cmd/ycy/configcmadd_test.go` to `pkg/cmd/config/cm/add/terminal_test.go`, switched all Module types to the leaf package, and changed the automation case to call the leaf `runAdd` directly so no root handler is retained. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/config/cm/add` passed. Risk: CM remove/use/set/test terminal tests and their fixtures still need relocation. Next: migrate the CM remove terminal Adapter and persistence-boundary tests into the `remove` leaf.
- 2026-08-29: Slice `config-cm-remove-terminal` completed. Moved the CM remove terminal Adapter, validation, confirmation/cancellation, and persistence-boundary tests from `cmd/ycy/configcmremove_test.go` to `pkg/cmd/config/cm/remove/terminal_test.go`; added leaf-private `executeRemove` behind `runRemove` and a local fixture decoder so tests invoke only the leaf. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/config/cm/remove` passed. Risk: CM use/set/test presentation tests and old root/cmd wiring remain. Next: move the CM use presentation tests into the `use` leaf.
- 2026-08-29: Slice `config-cm-use-terminal` completed. Moved the CM use presentation tests from `cmd/ycy/configcmuse_test.go` to `pkg/cmd/config/cm/use/terminal_test.go` and switched them to the leaf-owned `UseResult`. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/config/cm/use` passed. Risk: set and test presentation tests plus transition wiring remain. Next: move the CM set presentation tests into the `set` leaf.
- 2026-08-29: Slice `config-cm-set-terminal` completed. Moved the CM set presentation tests from `cmd/ycy/configcmset_presentation_test.go` to `pkg/cmd/config/cm/set/terminal_test.go` and switched them to the leaf-owned `SetResult`. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/config/cm/set` passed. Risk: only CM test presentation and the old root/cmd transition graph remain. Next: move the CM test presentation tests into the `test` leaf.
- 2026-08-29: Slice `config-cm-test-terminal` completed. Moved the CM test presentation tests from `cmd/ycy/configcmtest_test.go` to `pkg/cmd/config/cm/test/terminal_test.go` and switched them to the leaf-owned `TestResult`/`TestDiagnostic`. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/config/cm/test` passed. Risk: no CM terminal tests remain under `cmd/ycy`; registration and root transition removal are next.
- 2026-08-29: Slice `config-cm-registration-root-cutover` completed. Added registration-only `pkg/cmd/config/cm/cm.go`, registered all six CM leaves from `pkg/cmd/config`, removed the root CM handler fields/registration and old `pkg/cmd/root/configcm.go`, extended root diagnostics to all CM leaf paths, removed CM construction from `cmd/ycy/main.go`, and deleted the old CM production files. Relocated root/cmd CM handler tests into leaf-owned terminal tests. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/root ./cmd/ycy ./pkg/cmd/config/...`; `make command-surface` passed unchanged; `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed with the six CM handler/import allowlist entries removed; `git diff --check` passed. Risk: final G4 evidence still requires standalone CM acceptance and the ordered repository verification after the complete group cutover. Next: run `make acceptance` and inspect every CM standalone scenario, then perform the final repository sequence.
- 2026-08-29: Slice `config-cm-acceptance` completed. Group-level `make acceptance` passed (`go test -count=1 -tags=acceptance ./acceptance/...`, 70.322s), covering `TestConfigCMListStandaloneBinary`, `TestConfigCMAddStandaloneBinary`, `TestConfigCMSetStandaloneBinary`, `TestConfigCMUseStandaloneBinary`, `TestConfigCMTestStandaloneBinaryUsesOnlyTheLocalProvider`, and `TestConfigCMRemoveStandaloneBinary`, plus the three migrated Fork standalone scenarios. The prior `make command-surface` comparison remains clean, preserving the full nested Config help tree. Risk: none observed in directed group checks; the required final repository sequence must still run from step 1. Next: run repository verification step 1, then continue strictly in its declared order.
- 2026-08-29: Slice `config-cm-inventory-audit` completed. The approved tree now contains `pkg/cmd/config/{config.go,cm/cm.go,cm/{list,add,use,set,remove,test},fork/fork.go,fork/{list,add,remove}}` with all nine leaf implementations, Adapters, presentations, and tests; no old `internal/commands/config/cm` files, flat CM `cmd/ycy` files, root CM registration/handler tests, or active old-path imports remain. `go test ./internal/architecture` and `git diff --check` both passed during the audit. Risk: none in the source inventory; final repository evidence remains. Next: run repository verification step 1 exactly as declared.
- 2026-08-29: Repository verification step 1 completed. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 0; all ordinary packages passed, including every CM/Fork leaf, root, `cmd/ycy`, and Tunnel. Risk: none from step 1. Next: run repository verification step 2, `make acceptance`.
- 2026-08-29: Repository verification step 2 completed. `make acceptance` (`GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance/...`) exited 0 in 70.564s; all tagged standalone Config CM/Fork scenarios and the rest of the acceptance suite passed. Risk: none from step 2. Next: run repository verification step 3, the architecture suite.
- 2026-08-29: Repository verification step 3 completed. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` exited 0 in 0.314s; the exact shrinking root handler/import allowlists pass, parent-to-direct-child registration is valid, and no sibling/old-path Config imports remain. Risk: none from step 3. Next: run repository verification step 4, `git diff --check`.
- 2026-08-29: Repository verification step 4 completed. `git diff --check` exited 0 after the complete Config cutover. Risk: none from the ordered repository gate.
- 2026-08-29: Final G4 acceptance completed; G4 is `passed`. E1: both nested Config groups and all nine leaves now occupy the approved `pkg/cmd/config/{fork,cm}` parent/leaf tree with local implementations, Adapters, presentations, and tests; old `internal/commands/config/{fork,cm}`, flat `cmd/ycy/config{fork,cm}*.go`, and root Config registration/handler files are absent. E2: every leaf's constructor and Module/runner tests passed in the ordinary suite and directed package runs; CM terminal/persistence/prompt coverage and Fork coverage remain local; `make acceptance` passed all six `TestConfigCM*StandaloneBinary` cases and three `TestConfigFork*StandaloneBinary` cases; `make command-surface` remained byte-for-byte clean. E3: `pkg/cmd/config` and `pkg/cmd/config/{fork,cm}` are registration-only parents with direct-child imports, no sibling imports remain, `root.Dependencies` is reduced to the exact nine non-Config handler capabilities, and the architecture suite passed with all Config old-path/handler allowlist entries removed. E4: the ordered repository gate is green: step 1 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` passed; step 2 `make acceptance` passed in 70.564s; step 3 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed in 0.314s; step 4 `git diff --check` passed. Manual acceptance is `None`; no outstanding risk remains. Next: G5 is activated for a new Goal only.

### G5: Migrate Git leaves and shared process runtime

- 2026-08-29: initialized as `planned`; no implementation evidence.
- 2026-08-29: 已激活，尚未开始实施. Activated after G4 E1-E4 were recorded; this Goal ends here and no G5 analysis, implementation, or verification was performed.
- 2026-08-29: Slice `gitprocess-runtime-extraction` completed. Moved the shared external Git lifecycle from `cmd/ycy/git_process*.go` and its missing-executable normalization into `internal/gitprocess/{process,process_unix,process_windows,errors}.go`; `Runner` now owns argv execution, optional byte input, captured stdout/stderr, nonzero exit capture, Unix/Windows process-group termination, and signal exit mapping through `SignalOutcome`. Added module-local argv/input/output, missing executable, exit, pre-cancel, and native Unix orphan-process tests. The four still-unmigrated `cmd/ycy` leaf adapters now only adapt `gitprocess.Output` to their leaf-owned result types, so no shared lifecycle remains there; no Git command semantics, Cobra binding, presentation, or root registration moved. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/gitprocess` (2.241s), `... go test -count=1 ./cmd/ycy ./pkg/cmd/factory ./pkg/cmdutil` (all passed; `cmd/ycy` 7.977s), and `GOOS=windows GOARCH=amd64 ... go test -c ./internal/gitprocess`; `git diff --check` passed. Risk: the four temporary adapters still reside under `cmd/ycy` until their respective leaf checkpoints, but they contain only result adaptation. Next: migrate the complete `git heat` leaf only.
- 2026-08-29: Slice `git-heat-vertical-leaf` completed. Moved the complete Heat Module and tests from `internal/commands/git/heat`, its Cobra grammar from `pkg/cmd/root/githeat.go`, and its terminal/result Adapter from `cmd/ycy/githeat*.go` into `pkg/cmd/git/heat/`. `heat.NewCmdHeat(factory, runF)` now owns flags, permissive legacy numeric parsing, leaf `Options`, private `runHeat`, terminal presentation, and the small `gitprocess.Output` adapter; the Factory's lazy `GitRunner` is invoked only on execution, preserving help/version no-Git behavior. Removed the `GitHeat` handler field/construction/root-import allowlist entry and made the transitional root Git parent register the permanent Heat leaf directly while retaining only Pulse/Fork/CM temporary children. Moved heat binding coverage to constructor tests and added a leaf-owned root outcome test with a real temporary Git repository, proving `--log-level` is configured before Heat runs. Updated old temporary-child assertions to account for permanently registered Heat without changing production behavior. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/git/...` (1.984s), `... go test -count=1 ./pkg/cmd/git/heat ./pkg/cmd/root ./cmd/ycy` (all passed), `make command-surface` (0.317s), and native Unix `make acceptance` (73.573s; includes the real Git Heat standalone cases). Supporting architecture verification `... go test -count=1 ./internal/architecture` passed (0.608s), `git diff --check` passed, and strict audits found no active `internal/commands/git/heat`, `cmd/ycy/githeat*.go`, `GitHeatHandler`, or `GitHeat` handler entry. Risk: `pkg/cmd/root` still owns only the temporary Git parent plus Pulse/Fork/CM handlers until their separate leaf slices; no Heat old/new implementation pair remains. Next: migrate the complete `git pulse` leaf only.
- 2026-08-29: Slice `git-pulse-vertical-leaf` completed. Moved the complete Pulse Module and tests from `internal/commands/git/pulse`, its Cobra grammar from `pkg/cmd/root/gitpulse.go`, and terminal/process/result Adapters from `cmd/ycy/gitpulse*.go` into `pkg/cmd/git/pulse/`. `pulse.NewCmdPulse(factory, runF)` now owns the optional directory, permissive legacy `--days` parsing, leaf `Options`, private `runPulse`, local filesystem/terminal/tracking behavior, and the small `gitprocess.Output` adapter; the Factory lazy Git runner is evaluated only during execution. Removed `GitPulse` from root dependencies, test construction, `cmd/ycy/main.go`, and both architecture allowlists; the transitional root Git parent registers Pulse directly alongside Heat while retaining only Fork and CM temporary children. Added leaf-owned constructor, terminal/plain/automation, process argv/exit/cancellation, and root outcome tests; the root outcome uses a real temporary Git repository and proves `--log-level` is configured before Pulse runs. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/git/...` (Heat and Pulse green), native Unix `... go test -count=1 ./pkg/cmd/git/pulse -run '^TestGitRunnerAdapterStopsTheChildProcessGroupWithoutAnOrphan$'` (0.441s, SIGTERM maps to exit 143 and leaves no orphan), `make acceptance` (73.077s, including both `TestGitPulseStandaloneBinary*` cases), `make command-surface` (0.476s), and `... go test -count=1 ./internal/architecture` (0.376s); `git diff --check` passed. Strict source audit found no active old Pulse package, flat Pulse Adapter, root handler, or allowlist entry. Risk: root still has only the temporary Git Fork and Git CM handlers until their separate leaf slices; no Pulse old/new implementation pair remains. Next: migrate the complete `git fork` leaf only.
- 2026-08-29: Slice `git-fork-vertical-leaf` completed. Moved the complete Fork Module and tests from `internal/commands/git/fork`, its Cobra grammar from `pkg/cmd/root/gitfork.go`, and terminal/process/result Adapters from `cmd/ycy/gitfork*.go` into `pkg/cmd/git/fork/`. `fork.NewCmdFork(factory, runF)` now owns the `fork <repo> [dest]` grammar, leaf `Options`, lazy Config-store adapter, HTTP/terminal dependencies, private `runFork`, and the small `gitprocess.Output` clone adapter; provider/archive/clone semantics, progress, cancellation, and presentation remain leaf-owned. Removed `GitFork` from root dependencies, root test construction, `cmd/ycy/main.go`, and both architecture allowlists; the transitional root Git parent now registers Fork directly. Added external leaf `root_outcome_test.go`, which provisions an encrypted temporary provider configuration and local archive server and proves `--log-level warn` is installed before each real Fork provider request. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/git/fork` (2.306s); `... go test -count=1 ./pkg/cmd/git/...` (Fork 3.029s, Heat 4.326s, Pulse 3.998s); native Unix `... go test -count=1 ./pkg/cmd/git/fork -run '^TestGitRunnerAdapterStopsTheChildProcessGroupWithoutAnOrphan$'` (0.509s); `make acceptance` (76.024s); `make command-surface` (0.507s); and `... go test -count=1 ./internal/architecture` (0.570s). The empty old `internal/commands/git/fork` directory was removed; strict production-source audit found no old Fork path, flat Adapter, root handler, or allowlist entry; `git diff --check` passed. Risk: root still has only the temporary Git CM handler until its separate leaf checkpoint; no Fork old/new implementation pair remains. Next: migrate the complete `git cm` leaf only.
- 2026-08-29: Slice `git-cm-vertical-leaf` completed. Moved the complete CM Module and tests from `internal/commands/git/cm`, its Cobra grammar from `pkg/cmd/root/gitcm.go`, and terminal/process/result Adapters from `cmd/ycy/gitcm*.go` into `pkg/cmd/git/cm/`. `cm.NewCmdCM(factory, runF)` now owns the full legacy flag matrix, leaf `Options`, lazy Factory Config-store/HTTP/terminal/Git dependencies, private `runCM`, terminal presentation, and the small `gitprocess.Output` adapter with input support; CM’s provider, evidence, snapshot, staging, commit, push, and tracking behavior remain local. The leaf now owns `NormalizeArguments`; root invokes it only at the pre-Cobra boundary required to preserve optional `--push`/`--stage-push` remote values, while all handlers, implementations, and command semantics remain out of root. Removed `GitCM` from root dependencies/test construction, `cmd/ycy/main.go`, and both architecture allowlists; the transitional root Git parent directly registers CM and root diagnostics include `ycy git cm`. Added leaf-owned external root outcomes proving diagnostics are configured before a real provider request and that `git cm --push upstream` retains its legacy parse; added the native Unix `RunInput` child-process-group/orphan case. Directed verification passed: supporting `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/root ./cmd/ycy ./pkg/cmd/git/cm` (0.301s, 0.921s, 4.002s); final `... go test -count=1 ./pkg/cmd/git/...` (CM 7.434s, Fork 5.075s, Heat 6.052s, Pulse 4.921s); native Unix `... go test -count=1 ./pkg/cmd/git/cm -run '^TestGitRunnerAdapterStopsTheChildProcessGroupWithoutAnOrphan$'` (0.490s); `make acceptance` (75.177s); `make command-surface` (0.602s); and `... go test -count=1 ./internal/architecture` (0.507s). Strict production-source audit found no old CM package, flat Adapter, root handler, or allowlist entry, and the old directories/files are absent; `git diff --check` passed. Risk: all four Git leaves are complete, but the temporary root-owned Git parent still directly registers them until the separate registration-only parent cutover. Next: add `pkg/cmd/git/git.go` and remove only that transition.
- 2026-08-29: Slice `git-parent-registration-cutover` completed. Added `pkg/cmd/git/git.go` as the Git parent that directly registers Heat, Pulse, Fork, and CM, plus a narrow parent forwarding hook to CM’s leaf-owned pre-Cobra argument normalizer. Removed the old root Git command builder entirely; `pkg/cmd/root/app.go` now imports and registers only `git.NewCmdGit(factory)`, while its global pre-parse hook calls the domain forwarding boundary. Removed the final empty `internal/commands/git/{heat,git}` directory chain. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/git/... ./pkg/cmd/root ./cmd/ycy` (Git parent 0.276s, CM 6.866s, Fork 4.554s, Heat 4.916s, Pulse 4.605s, root 1.782s, binary 2.781s); `make command-surface` (0.277s); and `... go test -count=1 ./internal/architecture` (0.500s). The strict production audit found no active old Git package/flat Adapter/root handler path, no root import of a Git leaf, no matching handler capability, and no remaining `internal/commands/git`; `git diff --check` passed. Risk: none from the registration cutover. Next: begin the declared G5 repository verification at step 1 only.
- 2026-08-29: G5 repository verification step 1 stopped. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 1 after 73.125s in `internal/commands/tunnel` and 13.378s in `pkg/cmd/git/pulse`; no repository verification steps 2-4 were run. Tunnel failures were `TestGoClientToGoServerForwardsHTTPAndTCPAndUDPWithPinnedFRP`, where the pinned Darwin ARM64 `frpc` did not respond within 5s during `EnsureFRPRuntimeAt`, and `TestFRPSupervisorRecoversUnexpectedExitAndSuppressesRecoveryAfterStop`, whose condition did not become true within 2s. The migrated Pulse process test `TestGitRunnerAdapterStopsTheChildProcessGroupWithoutAnOrphan` also failed in the concurrent full suite because its fixture PID was not recorded within its timeout, despite its focused native run and the preceding directed Git package suite passing. This is the G5 Stop condition for an ordinary-suite failure. Completed evidence remains all four green leaf checkpoints, green Git-parent/root/binary directed suite, green command-surface and architecture checks, old-path/handler audit, and prior `git diff --check`; G5 is now `blocked`. Recovery input: a separate, explicitly authorized stabilization or clean-environment task must resolve or conclusively reproduce the Tunnel FRP readiness/supervisor failures and the Pulse concurrent-suite process-fixture failure without weakening tests; then a new G5 Goal must reactivate G5 and restart repository verification from step 1 in order.
- 2026-08-29: Slice `g5-recovery-verification-preflight` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. The active Goal explicitly resumes G5, so G5 is reactivated with its scope locked to Git leaves and shared process runtime; G6 remains `planned` and is out of bounds. Read the G5 contract, shared-module decision, target-tree slice rules, `internal/gitprocess`, and the Pulse native process test. The prior Pulse failure occurs before cancellation, while its fixture waits for a shell-written child PID; no production or test behavior was changed during this inspection. Risk: the earlier ordinary-suite run also failed unrelated Tunnel readiness/supervisor tests, which G5 cannot modify. Next: inspect the complete Pulse fixture synchronization and apply a narrowly justified G5-only stabilization only if it preserves the orphan-process assertion; then restart repository verification at step 1.
- 2026-08-29: Slice `pulse-process-fixture-readiness` completed. Modified `pkg/cmd/git/pulse/process_unix_test.go` and `.scratch/cli-structure-refactor/goal-runbook.md`. A focused pre-fix `-count=100` run reproduced a fixture race: the shell had created the redirected PID file, but the reader observed its temporary empty contents and failed `Atoi`. The test now treats only an empty/whitespace-only file as not yet ready and retries within its unchanged 2s deadline; malformed nonempty PID contents, the SIGTERM exit-143 assertion, the 3s termination bound, and the 2s orphan check are unchanged. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=100 ./pkg/cmd/git/pulse -run '^TestGitRunnerAdapterStopsTheChildProcessGroupWithoutAnOrphan$'` (15.221s), then `... go test -count=1 ./pkg/cmd/git/...` (Git parent 0.453s; CM 9.205s; Fork 5.697s; Heat 7.189s; Pulse 6.314s). Risk: complete-suite load and the unrelated Tunnel readiness/supervisor cases remain unproven. Next: restart G5 repository verification at step 1, then continue only in the declared order if it passes.
- 2026-08-29: Slice `pulse-process-fixture-startup-bound` completed. Modified `pkg/cmd/git/pulse/process_unix_test.go` and `.scratch/cli-structure-refactor/goal-runbook.md`. The first resumed G5 repository verification step 1 stopped with only Pulse failing: Tunnel now passed (75.403s), but `TestGitRunnerAdapterStopsTheChildProcessGroupWithoutAnOrphan` still exhausted its 2s fixture-PID startup window under full-suite load. This confirmed that the remaining failure was test-fixture scheduling rather than process cancellation or orphan behavior. The Pulse fixture now uses the same bounded 5s startup window as the equivalent `internal/gitprocess` native test; its empty-file retry, malformed-PID failure, SIGTERM/143 assertion, 3s termination bound, and 2s orphan disappearance bound remain intact. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=100 ./pkg/cmd/git/pulse -run '^TestGitRunnerAdapterStopsTheChildProcessGroupWithoutAnOrphan$'` (15.468s), then `... go test -count=1 ./pkg/cmd/git/...` (Git parent 0.227s; CM 5.114s; Fork 2.236s; Heat 3.123s; Pulse 3.108s). Risk: only the exact repository command can establish concurrent-suite stability. Next: restart repository verification at step 1; do not run later steps unless it passes.
- 2026-08-29: G5 repository verification step 1 completed after Pulse fixture stabilization. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 0. All ordinary packages passed, including Tunnel (58.988s), `internal/gitprocess` (9.703s), all four Git leaves (CM 11.343s, Fork 7.325s, Heat 8.836s, Pulse 8.041s), root (2.212s), and the binary (0.725s). Risk: none observed in the ordinary suite; tagged acceptance, architecture, and diff hygiene remain required. Next: run repository verification step 2, `make acceptance`.
- 2026-08-29: G5 repository verification step 2 completed. `make acceptance` (`GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance/...`) exited 0 in 75.648s. The tagged standalone acceptance suite passed, including the Git Heat/Pulse/CM scenarios and the native Unix signal/cancellation coverage required by G5. Risk: none observed in tagged acceptance; architecture and diff hygiene remain required. Next: run repository verification step 3, `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture`.
- 2026-08-29: G5 repository verification step 3 completed. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` exited 0 in 0.541s. The architecture rules accept the registration-only Git parent, direct child imports, the absence of Git sibling imports and old paths, and the reduced root capability surface. Risk: none observed in architecture verification; diff hygiene remains required. Next: run repository verification step 4, `git diff --check`.
- 2026-08-29: G5 repository verification step 4 completed. `git diff --check` exited 0 after the complete Git migration and Pulse fixture stabilization. Risk: none observed in ordered repository verification.
- 2026-08-29: Final G5 acceptance completed; G5 is `passed`. E1: `internal/gitprocess` has the approved narrow process-only ownership, with green argv/input/output, missing-executable, exit, cancellation, platform, and native Unix child-process-group evidence. E2: Heat, Pulse, Fork, and CM are complete sibling leaves under `pkg/cmd/git`, with leaf-local constructors, Modules, process/result Adapters, presentations, real-Git/provider outcomes, and frozen command-surface evidence; the stabilized Pulse native signal test passed 100 consecutive focused runs and the full Git leaf suite. E3: native Unix signal/cancellation acceptance passed through `make acceptance`, including the Git standalone scenarios. E4: `pkg/cmd/git` is registration-only; architecture verification passed direct-child registration, no sibling imports, no old `internal/commands/git` or flat Git files, no root Git handler capability, and no matching allowlist entry. E5: final repository verification is green in the required order: ordinary suite passed; `make acceptance` passed in 75.648s; architecture passed in 0.541s; `git diff --check` passed. Manual acceptance is `None`; no outstanding G5 risk remains. Next: G6 is activated for a new Goal only and has not been analyzed, implemented, or verified in this Goal.

### G6: Migrate long-running and worker-backed commands

- 2026-08-29: initialized as `planned`; no implementation evidence.
- 2026-08-29: 已激活，尚未开始实施. Activated after G5 E1-E5 were recorded; this Goal ends here and no G6 analysis, implementation, or verification was performed.
- 2026-08-29: Slice `diff-leaf-cutover` completed. Moved the complete Diff Module and its package tests from `internal/commands/diff` to `pkg/cmd/diff`; moved the former `cmd/ycy` network/terminal Adapter into that leaf; and added leaf-owned `NewCmdDiff(factory, runF)` parsing plus a foreground lifecycle test that proves presentation-triggered cancellation closes the server. `pkg/cmd/root` now directly registers `diff.NewCmdDiff`, configures diagnostics for `ycy diff`, and no longer carries a Diff handler; `cmd/ycy` no longer constructs one. The Diff root import/handler allowlist entries and old Web consumer path were removed. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/diff ./pkg/cmd/root ./cmd/ycy ./internal/architecture`; `make command-surface`; `make PNPM='npx --yes pnpm@11.13.0' check-web` (lint, typecheck, 33 Vitest cases, Vite build, and 424 reachable assets); `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./web`; and `make acceptance` (74.429s). The host's global pnpm 11.6.0 follows a broken project-local 11.13.0 shim, so the recorded target used the package-required, non-mutating `npx --yes pnpm@11.13.0` override. Strict production and test source audits returned zero old Diff paths/handlers; `git diff --check` passed. Risk: the required finite `make acceptance-web` target has not yet been introduced, so browser console/resource/shutdown evidence is not complete. Next: implement only the bounded browser acceptance harness and Make target before beginning the FS checkpoint.
- 2026-08-29: Slice `browser-acceptance-preflight` completed. Modified `.scratch/cli-structure-refactor/goal-runbook.md` only. The existing `tools/web-browser-harness` was started against the current embedded output and its Diff URL was inspected in a local browser: the shell and generated assets loaded, but the browser correctly recorded `API route not found` because `web.NewReadinessHandler` is explicitly static-only and rejects command APIs. That harness therefore cannot be used to claim the required zero-console-error acceptance result. It was stopped cleanly after inspection; no product, Web, or test behavior changed. Risk: a finite browser target must instead drive the real Diff, FS, and Tunnel services once all three command owners are available. Next: begin the FS shared-runtime extraction only; defer the shared browser acceptance repair until after the Tunnel checkpoint rather than masking API failures with stubs.
- 2026-08-29: Slice `fs-thumbnail-runtime-extraction` completed. Moved the thumbnail codec, private framed worker protocol, persistent worker pool, and their package-local tests from `internal/commands/fs` to `internal/fsthumbnail`; `cmd/ycy` now dispatches the unchanged private worker marker through that Module. `internal/commands/fs/thumbnail_service.go` remains leaf-owned with cache/workspace behavior and now maps `fsthumbnail.Error` back to the existing `ServiceError` vocabulary while retaining the original runtime error in its cause chain; a leaf test proves that mapping. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/fsthumbnail ./internal/commands/fs ./cmd/ycy ./internal/architecture` (all green; FS 6.522s); source audit found no `ServiceError` or old FS import in the new Module, and `git diff --check` passed. Risk: FS remains on its old command path until the later leaf cutover, and its 7-Zip runtime is still under the old namespace; full FS standalone/Web/surface evidence is intentionally deferred until the complete FS command checkpoint. Next: extract only `internal/sevenzipruntime`, including payload selectors and every prepare/release clean-path reference.
- 2026-08-29: Slice `fs-sevenzip-runtime-extraction` completed. Moved `internal/commands/fs/sevenzipruntime` intact, including platform payload selectors, runtime tests, and the ignored Darwin arm64 payload, to `internal/sevenzipruntime`. Updated FS archive/session imports plus `.gitignore`, `Makefile` release-clean/release-untracked paths, `tools/prepare-sevenzip`, and `tools/release-artifacts` so generated, prepare, and release verification all use the new payload root. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/sevenzipruntime ./internal/commands/fs ./tools/prepare-sevenzip ./tools/release-artifacts ./cmd/ycy ./internal/architecture` (all green; FS 5.880s); `make prepare-7zip` exited 0 and reused the new Darwin arm64 target path; source audit found zero non-scratch references to `internal/commands/fs/sevenzipruntime`; `git diff --check` passed. Risk: FS command/server ownership is still transitional under `internal/commands/fs` and root's FS handler remains; full FS standalone/Web/surface evidence is deferred to its complete leaf checkpoint. Next: move the complete FS command/server implementation and its Cobra/network/terminal adapters into `pkg/cmd/fs`.
- 2026-08-29: Slice `fs-complete-leaf-cutover` completed. Moved the complete FS server/HTTP/runtime implementation and all owned package tests to `pkg/cmd/fs`; `NewCmdFS(factory, runF)` now owns the unchanged grammar, defaults, environment precedence, typed `Input`, and leaf-owned network adapter, while `runFS` owns `Start`/`Wait`/`Close` and terminal presentation. Moved the former binary FS adapter and its lifecycle/presentation test into the leaf. `pkg/cmd/root` directly registers FS, configures diagnostics for `ycy fs`, and no longer contains an FS handler field, conditional registration, or tests for optional handler injection; `cmd/ycy/main.go` no longer constructs an FS handler. Removed the corresponding root import/handler/Web-consumer allowlist entries, and updated the obsolete root test to prove FS is permanently registered without execution. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/fs ./pkg/cmd/root ./cmd/ycy ./internal/fsthumbnail ./internal/sevenzipruntime ./internal/architecture`; `make acceptance`; `make command-surface`; `make PNPM='npx --yes pnpm@11.13.0' check-web` (33 Vitest tests and 424 reachable assets); and `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./web` all exited 0. Audits found the old FS path, `FSHandler`, `registerFS`, and `newFSHandler` absent outside retained migration evidence; `git diff --check` passed. Risk: finite browser acceptance remains intentionally deferred until Tunnel is complete because the current static harness cannot drive real command APIs; Tunnel and Upgrade still own their separate transition paths. Next: extract only the approved `internal/tunnelruntime` shared protocol/FRP runtime.
- 2026-08-29: Slice `tunnel-runtime-extraction` completed. Moved the manifest, protocol-v3 types/messages, FRP archive materialization, TOML rendering, supervisor, Unix/Windows child ownership, and their package-local tests from `internal/commands/tunnel` to `internal/tunnelruntime`. The Module now exposes only the shared runtime values needed by the still-transitional server/client owners, including immutable path construction, a copied recovery schedule, supervisor binary/role inspection, and cloned supervisor state; server/client call it directly with no forwarding package or sibling import. Module-owned Unix supervisor tests moved intact; the server tests retain only minimal Unix fixture helpers and local TOML decoding shapes so private Module rendering structs stay private. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/tunnelruntime ./internal/commands/tunnel` (Module 4.123s, Tunnel 49.774s); `GOOS=windows GOARCH=amd64 GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -run '^$' -exec /usr/bin/true ./internal/tunnelruntime`; and `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/tunnelruntime ./internal/commands/tunnel ./pkg/cmd/root ./cmd/ycy ./internal/architecture` (Module 3.886s, Tunnel 49.914s, root 1.916s, binary 1.180s, architecture 0.737s). Audits confirmed the old `frp_*.go`/`protocol.go` paths are absent, `internal/tunnelruntime` has no old package declaration or forwarding aliases, and `git diff --check` passed. Risk: the root Tunnel handlers and old server/connect package remain intentionally transitional until their separate leaf cutovers; finite browser acceptance remains deferred until both leaves are available. Next: move only the complete Tunnel server leaf and its Cobra/network/terminal adapter ownership.
- 2026-08-29: Slice `tunnel-runtime-path-resolution` completed. The shared local-state and managed-FRP-runtime directory behavior moved from `internal/commands/tunnel/server_runtime_paths.go` to `internal/tunnelruntime/runtime_paths.go`: the Module owns current-process FRP runtime resolution and the explicit environment/platform state-root rule, while server and client append only their own private subdirectories. Updated the server configuration/runtime and client state/runtime callers to use the Module directly; the path-specific test now follows the Module. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/tunnelruntime ./internal/commands/tunnel` (Module 4.038s, Tunnel 52.709s); `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy ./internal/architecture` (1.069s, 0.615s); and Windows Module compile via `GOOS=windows GOARCH=amd64 ... go test -count=1 -run '^$' -exec /usr/bin/true ./internal/tunnelruntime`. Source audit found no old path or `defaultManagedFRPRuntimeDirectory`/`tunnelStateRoot` use under `internal/commands/tunnel`; `git diff --check` passed. Risk: no command binding changed; server/connect remain intentionally transitional, but neither needs to import the other for runtime paths. Next: move only the complete Tunnel server leaf and its Cobra/logger adapter ownership.
- 2026-08-29: Slice `tunnel-runtime-state-lock-extraction` completed. Moved the shared state-directory and client-registry lock protocol, Unix/Windows liveness probes, and package-local tests from `internal/commands/tunnel/state_lock*.go` to `internal/tunnelruntime/state_lock*.go`. The Module now exposes only the opaque `StateDirectoryLock` handle, acquire/release operations, active-state inspection, and its two typed lock errors; the still-transitional server and client consumers use that API directly, so neither future sibling leaf owns or imports the other's locking behavior. Modified consumers: `internal/commands/tunnel/{server_runtime,client_state}.go` and their existing tests. A first Module compile found four retained private test `release` calls; those were renamed to the public method without changing lock assertions, then the full directed result passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/tunnelruntime ./internal/commands/tunnel` (Module 4.255s, Tunnel 47.226s); Windows Module compile via `GOOS=windows GOARCH=amd64 ... go test -count=1 -run '^$' -exec /usr/bin/true ./internal/tunnelruntime`; and `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/root ./cmd/ycy ./internal/architecture` (root 1.694s, binary 0.966s, architecture 0.559s). Source audit found no production state-lock implementation under `internal/commands/tunnel`; the only remaining old-package references are tests asserting the Module's typed error. `git diff --check` passed. Risk: server/connect command ownership and root handlers are still intentionally transitional. Next: move only the complete Tunnel server leaf, its Cobra grammar, and its lifecycle/presentation adapter into `pkg/cmd/tunnel/server`; leave `tunnel connect` unchanged.
- 2026-08-29: Slice `tunnel-runtime-file-privacy-extraction` completed. The shared private-file platform operation moved from `internal/commands/tunnel/file_permissions_{other,windows}.go` to `internal/tunnelruntime/file_permissions_{other,windows}.go` as `ProtectPrivateFile`; it preserves Unix mode changes and the existing Windows private-DACL implementation. `client_state.go` and `server_frps.go` now call that Module directly, so future Server and Connect leaves do not share a sibling-owned file-permission helper. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/tunnelruntime ./internal/commands/tunnel` (Module 2.695s, Tunnel 49.729s); Windows Module compile via `GOOS=windows GOARCH=amd64 ... go test -count=1 -run '^$' -exec /usr/bin/true ./internal/tunnelruntime`; and `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/root ./cmd/ycy ./internal/architecture` (root 1.633s, binary 0.957s, architecture 0.564s). A source audit returned zero old `protectTunnelFile` production definitions/calls, and `git diff --check` passed. Risk: the platform-specific test assertion helpers remain temporarily in the combined Tunnel test package because both current consumers use them; they must follow their respective tests during the two leaf cutovers. Next: move only the complete Tunnel server leaf, its Cobra grammar, and its lifecycle/presentation adapter into `pkg/cmd/tunnel/server`; leave `tunnel connect` unchanged.
- 2026-08-29: Slice `tunnel-server-complete-leaf-cutover` completed. Moved the complete Tunnel Server implementation, its owned tests, `database*.go`, `state.go`, private-file assertions, and the existing Go-client/Go-server forwarding integration case to `pkg/cmd/tunnel/server`; added leaf-owned `NewCmdServer(factory, runF)`, typed `Options`, and `runServer`. The Server leaf now owns its unchanged flag/environment precedence, validation, FRP/HTTP lifecycle, and presentation behavior. `pkg/cmd/root` permanently registers Server while retaining only the temporary Connect binder; `TunnelServer` was removed from root `Dependencies`, binary construction, root test helpers, and `cmd/ycy/tunnel.go`. The architecture root-import allowlist keeps only `internal/commands/tunnel` for that temporary Connect handler. New leaf constructor tests retain the removed root coverage for CLI flags, environment precedence, typed input, invalid configuration, and help; an external leaf root-outcome test proves `--log-level warn` is configured before Server validation logs its error. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server` (46.285s); `... go test -count=1 ./internal/commands/tunnel ./internal/tunnelruntime ./pkg/cmd/root ./cmd/ycy ./internal/architecture` (0.514s, 3.090s, 1.197s, 0.478s, 0.673s); `make command-surface` (0.591s); and Windows compile `GOOS=windows GOARCH=amd64 ... go test -count=1 -run '^$' -exec /usr/bin/true ./pkg/cmd/tunnel/server ./internal/commands/tunnel ./internal/tunnelruntime` (all passed). The initial Windows compile exposed only absent ignored 7-Zip target payloads; `make prepare-7zip-all` generated and verified the declared target payloads, after which the compile passed. `gofmt -l` returned no files, source audits found no `TunnelServerHandler`, `newTunnelServerHandler`, `tunnelServerOption`, Server/database/state old paths, or Server-to-Connect sibling import, and `git diff --check` passed before this evidence entry. Risk: the forwarding test is temporarily Server-owned while it drives the still-old Connect public API; Connect remains the only Tunnel command behavior under `internal/commands/tunnel`. Next: move only the complete Tunnel Connect leaf, its Cobra grammar, terminal selection/presentation Adapter, and root transition removal.
- 2026-08-30: Slice `tunnel-connect-and-parent-registration-cutover` completed. Moved the complete Client configuration, state, reconciliation, FRPC runtime, and package tests from `internal/commands/tunnel/client_*.go` into `pkg/cmd/tunnel/connect`; moved the binary terminal connection selector/presentation Adapter there as `terminal.go` and its tests. `connect.NewCmdConnect(factory, runF)` now owns the unchanged `--server`/`--token` presence semantics, typed `Options`, local encrypted-catalog dependency, resolution, remembered-authentication write, and client lifecycle; constructor tests retain explicit, explicit-empty, and absent flag coverage, help isolation, and argument rejection. An external root-outcome test proves `--log-level warn` is configured before Connect validation logs an invalid explicit control-plane value. Added registration-only `pkg/cmd/tunnel/tunnel.go`, which directly registers Server and Connect; root imports only that parent and configures diagnostics for both leaf paths. Removed the temporary Connect handler from root `Dependencies`, root/binary helpers, `cmd/ycy/main.go`, the old root Tunnel files, and the now-empty `internal/commands/tunnel` directory. The Server-owned FRP forwarding integration test now drives Connect through its new public leaf API; this is test-only and no production Tunnel sibling import exists. Directed verification passed in declared order: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/... ./internal/tunnelruntime ./pkg/cmd/root ./cmd/ycy ./internal/architecture` (Tunnel parent 0.520s, Connect 1.165s, Server 51.281s, Module 5.887s, root 2.818s, binary 2.156s, architecture 2.352s); `make acceptance` passed in 76.777s; `make PNPM='npx --yes pnpm@11.13.0' check-web` passed ESLint, TypeScript, 33 Vitest cases, Vite build, and 424 reachable assets; `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./web` passed in 0.701s; and `make command-surface` passed in 1.140s. `gofmt -l` and `git diff --check` returned no output. Zero-result audits confirmed no old Tunnel path, Connect handler/alias, binary adapter, root Tunnel builder, or Tunnel production sibling import; the root command-import allowlist is empty and only Upgrade remains in the temporary handler allowlist. Risk: the required finite `make acceptance-web` target is still absent, so browser initial-page/resource/console/shutdown evidence is not yet available. Next: implement only the bounded real-service browser acceptance harness and `make acceptance-web`, then run it before beginning Upgrade.
- 2026-08-30: Slice `browser-acceptance-real-services` completed. Added the finite `acceptance-web` Make target and a tagged `acceptance/web` browser journey that builds the real `ycy` binary, starts real Diff, FS, and Tunnel services, loads each in headless Chrome, verifies initial UI, declared critical resources, command APIs, zero console/resource failures, and clean SIGINT shutdown. The three source shells now declare a data-URI favicon so Chrome does not manufacture an unrelated `/favicon.ico` 404. Tunnel browser setup creates a real same-origin admin session through `POST /api/session`, then injects that HTTP-only session cookie before navigation; this preserves the server's intentional anonymous `/api/state` 401 contract while exercising the authenticated `Overview` route and its successful `/api/state` call. FS expectation now matches the unchanged rendered `HACKYCY CLI · FILE BROWSER` label. Added only test-time `chromedp`/CDP dependencies. Directed verification passed: acceptance compile `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance -run '^$' ./acceptance/web`; `make PNPM='npx --yes pnpm@11.13.0' acceptance-web` (ESLint, TypeScript, 33 Vitest tests, Vite/424-asset verification, 7-Zip preparation, and the bounded real-service browser test); and `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./web`. `gofmt -l acceptance/web/browser_test.go` and `git diff --check` returned no output. Risk: this explicit acceptance target requires a locally available Chrome/Chromium (or `YCY_CHROME_BINARY`); the existing `go mod tidy` failure from the stale `tools/release-artifacts` import of the deleted old Tunnel path remains pre-existing migration residue and was not masked or changed here. Next: begin only the Upgrade/internal-updater shared-runtime extraction.
- 2026-08-30: Slice `updater-runtime-extraction` completed. Moved the complete release resolution, candidate download/self-check, persisted transaction state, detached replacement, retry, permissions, process, and platform-specific implementation with all package-local tests from `internal/commands/upgrade` to `internal/updater`, renaming the package to match its approved Module owner. Updated its direct binary, acceptance, and release-artifact consumers; no public Cobra grammar or terminal presentation moved. The old `internal/commands` namespace is now absent, so the architecture transition allowlist shrank to the still-temporary `cmd/ycy` only. The moved installer test initially resolved the repository one parent too far; its root calculation was adjusted only for the new directory depth. The standalone acceptance test's local updater-binary path variable was renamed to avoid shadowing the new `updater` package; all its existing replacement/rollback assertions remain. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/updater`; `... go test -count=1 ./cmd/ycy ./internal/architecture`; Windows compile `GOOS=windows GOARCH=amd64 ... go test -count=1 -run '^$' -exec /usr/bin/true ./internal/updater`; and `... go test -count=1 -tags=acceptance ./acceptance -run '^TestDetachedGoToGoStandaloneReplacementRollbackAndSelfCheck$'` (6.284s). Source audits returned zero active `internal/commands/upgrade` references and no `cmd/ycy`/`pkg/cmd` dependency in `go list -deps ./internal/updater`; `gofmt -l` and `git diff --check` returned no output. Risk: `tools/release-artifacts` still has the pre-existing stale deleted Tunnel import, so its package and final repository verification remain deferred to the G6 transition cleanup; this slice did not mask or alter that unrelated residue. Next: move only the Upgrade Cobra construction and terminal presentation Adapter into `pkg/cmd/upgrade`, then remove the last root handler transition.
- 2026-08-30: Slice `upgrade-leaf-cutover-and-transition-cleanup` completed. Added `pkg/cmd/upgrade` ownership of the unchanged Cobra grammar and terminal result/state presentation; `cmd/ycy/startup_update.go` now retains only pre-Cobra hidden-updater dispatch and startup consumption, calling the leaf presentation boundary. `pkg/cmd/root` directly registers Upgrade and no longer declares `Dependencies`, `UpgradeHandler`, an Upgrade handler field, or `registerUpgrade`; all construction callers now use `root.New(factory)`. The architecture suite now AST-checks that this final root transition is absent and permits only the temporary `cmd/ycy -> pkg/cmd/upgrade` presentation import. `tools/release-artifacts/verify.go` now consumes the FRP manifest from `internal/tunnelruntime`, eliminating its stale deleted Tunnel import. Directed verification passed: `make command-surface`; `make acceptance` (73.128s); `make PNPM='npx --yes pnpm@11.13.0' acceptance-web` (7.491s); `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/upgrade ./pkg/cmd/root ./cmd/ycy ./internal/updater ./internal/architecture`; `... go test -count=1 ./tools/release-artifacts ./internal/tunnelruntime`; and the G6 close package set `... go test -count=1 ./pkg/cmd/diff ./pkg/cmd/fs ./pkg/cmd/tunnel/... ./pkg/cmd/upgrade ./internal/fsthumbnail ./internal/sevenzipruntime ./internal/tunnelruntime ./internal/updater ./web`. Source/dependency audits found no active old command imports, root Upgrade transition, or command/binary dependency from the four approved internal Modules; all changed Go files are gofmt-clean, `internal/commands` is absent from the working tree, and `git diff --check` passed. Risk: `DEVELOPMENT.md` still contains historical old-path examples, whose documentation-only update belongs to the planned G8 boundary; no active source/import/build reference remains. Next: run G6 repository verification in the declared order beginning with `make check`.
- 2026-08-30: G6 repository verification step 1 completed. The initial literal `make check` stopped in `check-locks` before repository checks because the host pnpm shim executed its text payload (`This: command not found`); this is the already recorded local pnpm 11.6.0/11.13.0 shim defect, not a source or lockfile result. Re-ran the same Make target with its supported non-mutating override, `make PNPM='npx --yes pnpm@11.13.0' check`, which exited 0: module/lock verification, active-toolchain isolation, ESLint, TypeScript, all 33 Vitest tests, Vite build/424-asset verification, and the full ordinary Go package/tool suite passed. Risk: the repository check requires the recorded pnpm override on this host; G6 production behavior is unaffected. Next: run repository verification step 2, `make acceptance`.
- 2026-08-30: G6 repository verification step 2 completed. `make acceptance` (`GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -tags=acceptance ./acceptance/...`) exited 0: `acceptance` passed in 75.871s and `acceptance/web` passed in 8.931s. This includes standalone command/update coverage and tagged browser package compilation/execution. Risk: none observed; finite Make-driven Web acceptance, architecture, and diff hygiene remain. Next: run repository verification step 3, `make acceptance-web`.
- 2026-08-30: G6 repository verification step 3 completed. `make PNPM='npx --yes pnpm@11.13.0' acceptance-web` exited 0 in 7.656s. The finite target reran Web lint/typecheck/33 Vitest tests/Vite 424-asset verification, then drove the real Diff, FS, and Tunnel browser journeys with initial-page, critical-resource, command-API, zero-console/resource-error, and bounded shutdown assertions. Risk: none observed; architecture and diff hygiene remain. Next: run repository verification step 4, `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture`.
- 2026-08-30: G6 repository verification step 4 completed. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` exited 0 in 0.485s. It enforces the absent root handler transition, empty root command-import allowlist, deleted old command paths, command dependency direction, and the explicitly temporary `cmd/ycy` composition boundary. Risk: none observed; only final diff hygiene remains. Next: run repository verification step 5, `git diff --check`.
- 2026-08-30: Final G6 acceptance completed; G6 is `passed`. E1: all four approved runtime Modules (`internal/fsthumbnail`, `internal/sevenzipruntime`, `internal/tunnelruntime`, and `internal/updater`) have their recorded package/platform tests, and final dependency audit found no command/binary dependency from them. E2: Diff, FS, Tunnel Server/Connect, and Upgrade occupy their final `pkg/cmd` owners; their directed command, standalone/worker, Web where applicable, and frozen command-surface evidence is recorded above, including the final G6 close package set. E3: finite `acceptance-web` passed with real Diff/FS/Tunnel services, browser initial-page/resources/API/console assertions, and bounded clean shutdown. E4: `internal/commands` is absent from the working tree; the root handler `Dependencies`/allowlist and Upgrade transition are deleted; architecture and source audits found no old active implementation, flat Adapter, or prohibited dependency direction. E5: repository verification passed in order: `make PNPM='npx --yes pnpm@11.13.0' check`, `make acceptance`, `make PNPM='npx --yes pnpm@11.13.0' acceptance-web`, architecture, and `git diff --check`; the pnpm override is required only for the known broken host shim. Manual acceptance is `None`; no G6 risk remains. G7 is activated and has not been analyzed, implemented, or verified in this Goal. Next: end this Goal without entering G7.

### G7: Finalize the process composition root

- 2026-08-29: initialized as `planned`; no implementation evidence.
- 2026-08-30: 已激活，尚未开始实施. Activated after G6 E1-E5 were recorded; this Goal ends here and no G7 analysis, implementation, or verification was performed.
- 2026-08-30: Slice `process-facts-terminal-setup` completed. Added `internal/ycycmd/terminal.go` with the process-owned `ProcessFacts`, `CurrentProcessFacts`, and explicit stream/session classification; `cmd/ycy/main.go` now consumes that boundary while retaining the existing Factory/root flow. Moved the terminal/session and diagnostic lease tests to `internal/ycycmd/terminal_test.go`; no command behavior changed. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/ycycmd ./cmd/ycy` passed; `gofmt` and `git diff --check` passed. Risk: `cmd/ycy` still contains the remaining orchestration and transition-only test helpers. Next: move only signal context ownership and its platform signal list into `internal/ycycmd`.
- 2026-08-30: Slice `signal-context-ownership` completed. Moved `signals.go`, `signals_unix.go`, and `signals_windows.go` into `internal/ycycmd`; the package exposes `NewSignalContext` for the transitional caller and owns signal registration/cause cancellation. Added parent-cancellation and stop-lifecycle tests. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/ycycmd ./cmd/ycy` passed; `gofmt` and `git diff --check` passed. Risk: the binary still owns hidden dispatch, startup consumption, Web validation, and Factory/root execution. Next: move only hidden updater/thumbnail dispatch and startup-update consumption into `internal/ycycmd`.
- 2026-08-30: Slice `pre-cobra-dispatch-and-startup` completed. Moved the hidden updater and startup transaction logic into `internal/ycycmd/startup_update.go`, added the bounded thumbnail-worker dispatch boundary, and moved its package tests; `cmd/ycy` now delegates both private routes while preserving updater precedence, version bypass, output/error wording, and the framed worker protocol. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/ycycmd ./cmd/ycy` passed, including the new private-route boundary case; `gofmt` and `git diff --check` passed. Risk: remaining composition (Factory, Web validation, signal context use, root execution, and process error normalization) is still in `cmd/ycy`. Next: move that complete orchestration into `internal/ycycmd.Main(version)`.
- 2026-08-30: Slice `complete-ycycmd-main-orchestration` completed. Added `internal/ycycmd.Main(version)` and its non-exiting `run` path, moving argv capture, pre-Cobra routes, Factory assembly, startup/Web validation, signal ownership, root construction, and `Outcome` conversion in the original order; `cmd/ycy/main.go` now contains only version injection, `main`, the `ycycmd.Main` call, and `os.Exit`. Added process-root tests for version output and root error exit behavior. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/ycycmd ./pkg/cmd/root ./pkg/cmd/factory ./pkg/cmdutil ./cmd/ycy` passed; `gofmt` and `git diff --check` passed. Risk: `cmd/ycy` still has two unused transition-era test helper files, and architecture assertions still describe the pre-final allowlist. Next: remove those helpers and tighten final entry-chain architecture checks, then run the declared repository verification sequence.
- 2026-08-30: Slice `final-entry-architecture-and-worker-evidence` completed. Removed the unused `cmd/ycy` transition test helpers; `cmd/ycy` now contains only `main.go`. Tightened `internal/architecture` to require the exact thin entry (`os.Exit(ycycmd.Main(version))`), reject all old `internal/commands` imports, and remove the transition allowlist. Added tagged black-box worker dispatch coverage in `acceptance/process_root_test.go`; the focused acceptance case passed. Directed verification passed: the four required `internal/ycycmd`/root/Factory/cmdutil packages, `make command-surface`, architecture AST/import audit, gofmt, and diff hygiene. Risk: repository verification remains. Next: run repository verification step 1, `make check`.
- 2026-08-30: G7 repository verification step 1 literal attempt stopped before repository checks. `make check` exited 127 in `check-locks` because the host pnpm 11.13.0 shim executed its text payload (`This: command not found`), matching the previously recorded local shim defect; no source or lockfile result was produced. Recovery action: rerun the identical target with the supported non-mutating `PNPM='npx --yes pnpm@11.13.0'` override and retain this environment risk in the evidence.
- 2026-08-30: G7 repository verification step 1 completed via the supported non-mutating override `make PNPM='npx --yes pnpm@11.13.0' check` (the literal attempt is recorded immediately above). Lock/module verification, active-toolchain isolation, ESLint, TypeScript, all 33 Vitest cases, Vite build with 424 reachable assets, Go vet, and the ordinary package suite exited 0; `internal/ycycmd` and `internal/architecture` passed. Risk: only the host pnpm shim requires the override; no source or lockfile failure observed. Next: run repository verification step 2, `make acceptance`.
- 2026-08-30: G7 repository verification step 2 completed. `make acceptance` exited 0: tagged `acceptance` passed in 77.574s and `acceptance/web` passed in 11.419s, including standalone/PTY, signal lifecycle, worker dispatch, startup updater, Web, and frozen-surface-related cases. Risk: none observed from tagged acceptance. Next: run repository verification step 3, `make build`.
- 2026-08-30: G7 repository verification step 3 literal attempt stopped in `check-web` because the host pnpm 11.13.0 shim again executed its text payload (`This: command not found`) and exited 127; no Go build result was produced. This matches the recorded local shim defect. Recovery action: rerun the identical target with `PNPM='npx --yes pnpm@11.13.0'`, preserving the exact build steps.
- 2026-08-30: G7 repository verification step 3 completed via `make PNPM='npx --yes pnpm@11.13.0' build` after the recorded shim-only literal failure. Web lint/typecheck/33 Vitest/Vite 424-asset verification and the native Go build exited 0; `build/ycy` is a Darwin arm64 executable. Risk: only the host pnpm shim requires the override. Next: run repository verification step 4, `make cross-build`.
- 2026-08-30: G7 repository verification step 4 literal attempt stopped in `check-web` with the same host pnpm shim payload error (`This: command not found`, exit 127); no cross-target compile result was produced. Recovery action: rerun the identical target with `PNPM='npx --yes pnpm@11.13.0'`.
- 2026-08-30: G7 repository verification step 4 completed via `make PNPM='npx --yes pnpm@11.13.0' cross-build` after the recorded shim-only literal failure. Web checks and all six declared targets compiled successfully: Darwin amd64/arm64, Linux amd64/arm64, and Windows amd64/arm64; output file inspection confirmed Mach-O, ELF, and PE formats. Risk: cross-build is compile evidence only as required. Next: run repository verification step 5, `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture`.
- 2026-08-30: G7 repository verification step 5 completed. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` exited 0 in 0.543s, enforcing the exact thin entry, empty transition state, old-path absence/import direction, and Cobra/`os.Exit` ownership. Risk: none observed. Next: run repository verification step 6, `git diff --check`.
- 2026-08-30: G7 repository verification step 6 completed. `git diff --check` exited 0. Final read-only audits confirmed `internal/commands` and `internal/cliapp` are absent, `cmd/ycy` contains only `main.go`, no active old composition imports or transitional root fields remain, and the required dependency audit for `internal/ycycmd` has no old command paths.
- 2026-08-30: Final G7 acceptance completed; G7 is `passed`. E1: `cmd/ycy/main.go` is the exact thin entry (version variable, `main`, `os.Exit(ycycmd.Main(version))` only); all approved process orchestration, terminal/session facts, logging setup inputs, signals, hidden updater/thumbnail dispatch, startup transaction consumption, Web validation, Factory assembly, and root execution are owned by `internal/ycycmd`. E2: directed process-root tests and full tagged acceptance passed, including worker dispatch, startup updater, standalone commands, PTY/signal lifecycle, version, exit, and frozen command-surface evidence. E3: `internal/commands`, `internal/cliapp`, old composition files/imports, and transitional root fields/allowlist are absent; architecture and source/dependency audits passed. E4: repository verification passed in declared order: literal `make check`, `make build`, and `make cross-build` reached only the known host pnpm shim defect and were rerun successfully with the supported non-mutating `PNPM='npx --yes pnpm@11.13.0'` override; override `make check`, tagged `make acceptance`, override `make build`, override six-target `make cross-build`, architecture, frozen command-surface comparison, and `git diff --check` all exited 0. Native build produced Darwin arm64 output; cross-build produced all six declared target binaries. Manual acceptance is `None`; no G7 risk remains. Next: G8 has been activated, not started; end this Goal without reading, implementing, or verifying G8.

### G8: Publish and audit the final structure

- 2026-08-29: initialized as `planned`; no implementation evidence.
- 2026-08-30: 已激活，尚未开始实施. Activated after G7 E1-E4 were recorded; this Goal ends here and no G8 analysis, implementation, or verification was performed.
- 2026-08-30: Slice `support-paths` completed. Added the final top-level `acceptance` tree to `Makefile`'s `GO_FIND`, so formatting and Go hygiene cover tagged acceptance ownership; existing Make, release, Web, and 7-Zip paths already point at their final owners and were left behaviorally unchanged. Directed verification passed: the Makefile path assertion, old `internal/commands`/`internal/cliapp` path audit across Make/tools/.gitignore, repository Go format scan including `acceptance`, `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture`, and `git diff --check`. Risk: documentation and exact final inventory assertions remain. Next: publish and link `docs/project-layout.md` and update stale maintainer navigation paths only.
- 2026-08-30: Slice `documentation-contract` completed. Added canonical `docs/project-layout.md` covering the final entry chain, package/Module ownership, dependency direction, test and fixture levels, support roots, frozen/reference areas, and generated outputs; linked it from `README.md` and `DEVELOPMENT.md`, and replaced their obsolete pre-migration paths and debugging/test examples. Directed verification passed: required-section/path/link checks, zero obsolete command paths in the published docs, support-root and no-workflow checks, and `git diff --check`. Risk: exact package inventory and resolved dependency-direction assertions remain. Next: extend `internal/architecture` with the approved inventory and Go package-graph audit.
- 2026-08-30: Slice `documentation-consistency` completed. Corrected the remaining stale claim that Tunnel is unregistered: the guide now identifies `tunnel server` and `tunnel connect` as the public Go routes and keeps `web/tunnel-server` as their embedded interface resource. Documented the sole approved `internal/ycycmd -> pkg/cmd/upgrade` presentation edge used to render persisted startup-update state before Cobra; it does not grant `internal/ycycmd` ownership of Upgrade command behavior. Directed verification found no obsolete command paths or stale unregistered-Tunnel claim in published maintainer docs; architecture, Go formatting, and diff hygiene passed. Risk: exact package inventory and resolved dependency-direction assertions remain. Next: complete the final architecture package-graph audit.
- 2026-08-30: Slice `inventory-and-dependency-audit` completed. Extended `internal/architecture/architecture_test.go` with the exact approved default package inventory and the tagged `acceptance` additions, and with a decoded `go list -json -deps -test` audit covering the final entry chain, composition boundary, Factory/cmdutil rules, root child shape, obsolete paths, flat root command files, and sibling command imports. Directed verification passed: `gofmt -l internal/architecture/architecture_test.go` returned no output; `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go list ./... | sort` returned the approved 52 packages; `... go list -deps -test ./...` recorded 510 dependency entries; `... go test -count=1 ./internal/architecture` passed in 1.825s; and `make command-surface` passed in 0.829s. Risk: human-readable zero-old-path/transition audits and the declared repository verification sequence remain. Next: run the independent zero-old-path audit without changing production or support behavior.
- 2026-08-30: Slice `zero-old-path-audit` completed. Directed checks passed: `test ! -e internal/cliapp && test ! -e internal/commands` and the exact one-file `cmd/ycy` assertion both exited 0; old Go-import search across `cmd`, `internal`, `pkg`, `tools`, and `web` returned zero results; root transitional `Dependencies`, `UpgradeHandler`, and `registerUpgrade` search returned zero results; and the root inventory contains only final root implementation/support files, with none of the deleted flat command filenames. The readable command-import review showed production parent-to-direct-child registrations only; the architecture graph test remains the enforcement for forbidden sibling/parent and transition edges. Risk: only the declared full repository verification sequence and final evidence review remain. Next: run repository verification step 1, `make check`.
- 2026-08-30: G8 repository verification step 1 literal attempt stopped before repository checks. `make check` reached `check-locks` but exited 2 after the host pnpm shim executed its text payload (`This: command not found`; underlying target exit 127). This is the recorded local pnpm shim defect, so no source, lockfile, or Go result was produced. Recovery action: rerun the identical target with the supported non-mutating `PNPM='npx --yes pnpm@11.13.0'` override; no repository files changed.
- 2026-08-30: G8 repository verification step 1 completed via `make PNPM='npx --yes pnpm@11.13.0' check`. Module and lock verification, active-toolchain isolation, ESLint, TypeScript, all 33 Vitest cases, Vite build with 424 reachable generated assets, Go vet, and the ordinary Go package suite all exited 0. The only observed risk is the host shim, already isolated to the non-persistent Make-variable override. Next: run repository verification step 2, `make acceptance`.
- 2026-08-30: G8 repository verification step 2 completed. `make acceptance` exited 0: tagged `acceptance` passed in 74.699s and tagged `acceptance/web` passed in 7.484s. This retains the frozen command-surface and assembled binary/PTY/signal/worker/Web evidence without enabling acceptance implicitly in the ordinary suite. Risk: none observed for tagged acceptance. Next: run repository verification step 3, `make acceptance-web`.
- 2026-08-30: G8 repository verification step 3 literal attempt stopped before Web checks. `make acceptance-web` exited 2 from `check-web` after the same host pnpm shim payload error (`This: command not found`; underlying target exit 127), so no browser or source result was produced. Recovery action: rerun the identical finite target with `PNPM='npx --yes pnpm@11.13.0'`; no repository files changed.
- 2026-08-30: G8 repository verification step 3 completed via `make PNPM='npx --yes pnpm@11.13.0' acceptance-web` in 20.682s. ESLint, TypeScript, all 33 Vitest cases, Vite's three-shell/424-asset verification, and the finite real-service browser acceptance (`acceptance/web`, 7.687s) exited 0. Risk: only the host shim requires the scoped override. Next: run repository verification step 4, `make build`.
- 2026-08-30: G8 repository verification step 4 literal attempt stopped before the Go build. `make build` exited 2 from `check-web` because the host pnpm shim again executed its text payload (`This: command not found`; underlying target exit 127). This is the same isolated environment defect; no native artifact or source result was produced. Recovery action: rerun `make build` with the supported non-mutating PNPM override.
- 2026-08-30: G8 repository verification step 4 completed via `make PNPM='npx --yes pnpm@11.13.0' build` in 13.767s. Web lint/typecheck, all 33 Vitest cases, Vite's three-shell/424-asset verification, and the native Go build exited 0; `file build/ycy` identifies the result as a Darwin arm64 Mach-O executable. Risk: only the scoped host PNPM override remains. Next: run repository verification step 5, `make cross-build`.
- 2026-08-30: G8 repository verification step 5 literal attempt stopped before cross-compilation. `make cross-build` exited 2 from `check-web` with the same host pnpm shim text-payload error (`This: command not found`; underlying target exit 127). No cross-target artifact or source result was produced. Recovery action: rerun the identical target with `PNPM='npx --yes pnpm@11.13.0'`.
- 2026-08-30: G8 repository verification step 5 completed via `make PNPM='npx --yes pnpm@11.13.0' cross-build` in 21.205s. Web lint/typecheck, all 33 Vitest cases, Vite's three-shell/424-asset verification, and all six cross-build targets exited 0. `build/cross/` contains `ycy-macos-{x64,arm64}`, `ycy-linux-{x64,arm64}`, and `ycy-windows-{x64,arm64}.exe`; this remains compile evidence only. Risk: only the host shim is overridden for the command. Next: run repository verification step 6, the final architecture suite.
- 2026-08-30: G8 repository verification step 6 completed. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` exited 0 in 1.874s after the full ordinary/tagged/Web/build/cross-build sequence, preserving the final inventory and dependency-direction enforcement. Risk: none observed in the architecture suite. Next: run repository verification step 7, `git diff --check`, then perform the final G8 evidence review.
- 2026-08-30: G8 repository verification step 7 completed. `git diff --check` exited 0 with no whitespace or patch-integrity finding. The declared repository sequence is now complete: ordinary, tagged, finite Web, native build, six-target cross-build, architecture, and diff hygiene. Risk: final scope/documentation/path review remains before Gate acceptance. Next: verify E1-E5 against the recorded evidence and current diff only.
- 2026-08-30: Final G8 acceptance completed; G8 is `passed`. E1: `internal/architecture` enforces the exact approved default 52-package inventory and the two tagged `acceptance` packages; both inventories and the architecture suite passed. E2: the 510-entry `go list -deps -test ./...` audit, AST graph rules, absent `internal/cliapp`/`internal/commands`, zero old-import result, one-file `cmd/ycy`, absent root transition types/registration, and final root-file inventory prove that no old source/import, forwarding path, transitional root allowlist/type, sibling production import, or flat command file remains. E3: canonical `docs/project-layout.md` is complete and linked from README/DEVELOPMENT; Make, ignore, prepare, and release payload checks reference `internal/sevenzipruntime/payload`, while support roots and generated-output roles remain unchanged. E4: frozen `make command-surface` comparison passed unchanged. E5: repository verification passed in declared order: the literal Web-dependent Make targets stopped only at the known host pnpm shim defect and their supported non-mutating `PNPM='npx --yes pnpm@11.13.0'` reruns passed; ordinary check, tagged acceptance, finite Web acceptance, native Darwin arm64 build, six-target cross-build, architecture, and diff hygiene are green. Manual acceptance is `None`; no G8 risk remains. G9 is activated but not started; this Goal ends without reading, implementing, or verifying G9.

### G9: Collect native and release evidence

- 2026-08-29: initialized as `planned`; no implementation evidence.
- 2026-08-30: 已激活，尚未开始实施. Activated after G8 E1-E5 were recorded; this Goal ends here and no G9 analysis, implementation, or verification was performed.
- 2026-08-30: Slice `commit-and-darwin-snapshot` completed. Recorded the immutable G8 commit `5084f99bc947987025d6fba2ae2c2296d64d6f21`, macOS 26.5.2 Darwin arm64, host Go 1.27.0, pinned Go 1.26.7, clean source worktree, generated Web/7-Zip prerequisites, and Linux Docker availability in `.scratch/cli-structure-refactor/evidence/g9-00-commit-and-darwin-snapshot.md`. Directed verification confirmed the pinned toolchain and required generated inputs; no source or production files changed. Risk: no native Windows execution host is visible locally and must be resolved or recorded as a G9 Stop condition after the handoff contract is prepared. Next: run the Darwin arm64 ordinary Go suite on this exact commit.
- 2026-08-30: Slice `darwin-ordinary-suite` stopped. The required native command `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` on commit `5084f99bc947987025d6fba2ae2c2296d64d6f21` exited 1; full output is recorded in `.scratch/cli-structure-refactor/evidence/g9-darwin-ordinary.log`. Failures were Unix child-process/fixture timing assertions in `internal/gitprocess`, `internal/tunnelruntime`, and `pkg/cmd/git/{fork,heat,pulse}`. Same-commit isolated/sequential diagnostics passed and are recorded in `g9-darwin-ordinary-diagnosis.log`, indicating load-sensitive pre-existing timing behavior but not clearing the required full-suite failure. No production or test source changed. This triggers the G9 Stop condition; Linux, Windows, Web, cross-build, release, and repository verification slices were not started. Recovery input: separately stabilize the fixture/runtime timing or provide a clean Darwin native environment, then restart the complete same-commit G9 set (and rerun G8 first if source changes), as detailed in `g9-darwin-ordinary-diagnosis.md`.
- 2026-08-30: Slice `docker-linux-preflight` completed as a non-behavior environment check. A read-only Docker Desktop container reported Linux 6.10.14 aarch64, matched commit `5084f99bc947987025d6fba2ae2c2296d64d6f21`, and resolved Go 1.26.7 with Git/Make and the generated Web/7-Zip inputs available; full output is `.scratch/cli-structure-refactor/evidence/g9-linux-docker-preflight.log`. The container is eligible as the temporary Linux execution environment permitted by the G9 decision, but it does not repair or replace the failed Darwin evidence. Risk: Windows native execution remains unavailable. Next: run the exact Linux ordinary Go suite in this isolated container.
- 2026-08-30: Slice `docker-linux-ordinary-suite` stopped as an invalid container-environment attempt, not a Linux pass or a source failure. The exact ordinary command began on the matching Linux aarch64 Docker host, but `/etc/machine-id` was absent (`internal/appconfig` failure), first-use Go toolchain download text polluted the architecture suite's `go list -json` subprocess, and `internal/terminaltest.TestControlledPTYRunsATerminalSubprocess` remained running for over six minutes in the non-interactive container; the transient `--rm` container was terminated after the already-invalid failures. Full output is `g9-linux-docker-ordinary.log`; diagnosis and required prepared-container properties are in `g9-linux-docker-ordinary-diagnosis.md`. The source mount was read-only and no production/test files changed. This does not alter the prior Darwin Stop condition or produce Linux evidence. Next: pause G9 pending a separately stabilized/clean Darwin host and a prepared PTY-capable Linux container or host; restart the complete evidence set rather than combining invalid attempts.
- 2026-08-30: Separate stabilization task completed in local commit `c7c2e75e6891dbc50cd34af2742211b74097d2aa` (`test: stabilize Unix process fixture timing`). It changes only the six Unix test fixtures in `internal/gitprocess`, `internal/tunnelruntime`, and `pkg/cmd/git/{cm,fork,heat,pulse}`: empty PID-file publication is treated as pending, and bounded fixture startup/recovery/reaping observations use 15s rather than 1-5s. Production Git/FRP behavior and the FRP forced-stop-under-1s assertion are unchanged. Evidence: the affected paths passed `-count=20 -p=12`, then the original pinned full Darwin ordinary suite passed. Per the required post-source-correction rule, G8 was revalidated on this exact commit: override `make check` passed lock/module, Web, vet, and ordinary tests; `make acceptance`, override `make acceptance-web`, override `make build`, override `make cross-build` (all six target formats), `make command-surface`, architecture, and `git diff --check` all passed. The prior G9 records for `5084f99...` are invalid due to the new source commit and must not be counted. Next: discard only those invalid G9 evidence files and restart G9 from a clean `c7c2e75...` snapshot.
- 2026-08-30: Slice `restart-commit-and-darwin-snapshot` completed. Invalid prior G9 evidence was discarded after the separate source stabilization and G8 revalidation. Recorded exact commit `c7c2e75e6891dbc50cd34af2742211b74097d2aa`, macOS 26.5.2 Darwin arm64, host Go 1.27.0, pinned Go 1.26.7, clean source worktree, generated Web/7-Zip prerequisites, and Docker Linux availability in `.scratch/cli-structure-refactor/evidence/g9-00-commit-and-darwin-snapshot.md`. Risk: Windows native execution remains unavailable locally. Next: run the Darwin arm64 ordinary Go suite on this new exact commit.
- 2026-08-30: Slice `darwin-ordinary-suite-restart` stopped. The required native command on commit `c7c2e75e6891dbc50cd34af2742211b74097d2aa` exited 1 after 87.49s; full output is `.scratch/cli-structure-refactor/evidence/g9-darwin-ordinary.log`. The only failure was `internal/tunnelruntime.TestVerifyFRPReportedVersionUsesPinnedVersion`: its fake `frpc` version command did not respond within the test's 5s fixture window under full-suite load. All other listed packages passed, including the just-stabilized Unix process/FRP supervisor paths. This is a G9 Stop condition. No Linux/Windows/release evidence may be counted; recovery requires a separate test-fixture stabilization (or clean host) and then another full G8 revalidation before restarting G9 again.
- 2026-08-30: Separate stabilization slice `frp-version-probe-fixture-timeout` completed, pending its isolated source commit. Modified only `internal/tunnelruntime/frp_runtime.go` and `internal/tunnelruntime/frp_runtime_test.go`: the production verifier remains the fixed 5s wrapper, while its private timeout-taking helper lets the shell fixture use the established 15s observation budget without changing FRP runtime behavior. Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=100 -p=12 ./internal/tunnelruntime -run '^TestVerifyFRPReportedVersionUsesPinnedVersion$'` (26.946s) and `... go test -count=1 ./internal/tunnelruntime` (2.603s). Risk: this source correction invalidates all current G9 records after commit; it cannot make G9 green by itself. Next: commit only the two stabilization files, rerun the complete G8 verification sequence, then discard invalid G9 records and restart from a new same-commit snapshot.
- 2026-08-30: The separate stabilization was committed as `9c4191b606a7f14a9300e15cbd9d9c3d52199499` (`test: stabilize FRP version fixture timing`). The G8 gate was then fully revalidated on that exact commit: `make command-surface` passed; `make PNPM='npx --yes pnpm@11.13.0' check` passed module/lock checks, Web checks, and the ordinary suite including `internal/tunnelruntime`; `make acceptance` passed tagged Go and Web acceptance; the same PNPM override passed `make acceptance-web`, `make build`, and `make cross-build`; all six cross artifacts have the required Mach-O/ELF/PE formats; `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` and `git diff --check` passed. Removed only the invalid `g9-00-commit-and-darwin-snapshot.md` and `g9-darwin-ordinary.log` records for `c7c2e75...`. Risk: native Windows is still unavailable locally. Next: create a clean G9 snapshot for `9c4191b...`, then restart Darwin evidence collection from its ordinary suite.
- 2026-08-30: Slice `darwin-native-evidence-restart` completed on commit `9c4191b606a7f14a9300e15cbd9d9c3d52199499`. The Darwin arm64 ordinary suite passed in 76.1s with `internal/tunnelruntime` passing in 38.497s under full load; tagged `make acceptance` also passed (`acceptance` 77.283s; `acceptance/web` 8.649s). Complete per-command metadata is in `evidence/g9-darwin-ordinary.log` and `evidence/g9-darwin-acceptance.log`. Risk: Docker may supply the required native Linux environment only if machine identity, fixed Go/toolchain readiness, and PTY support are prepared; a Windows native host remains unavailable locally. Next: preflight a prepared Linux Docker container without counting it as native test evidence.
- 2026-08-30: Slice `docker-linux-pty-preflight` stopped as an invalid Linux environment, not as a Linux suite result. On the exact `9c4191b...` source mounted read-only, `golang:1.26.7-bookworm` supplied Linux arm64, fixed Go, a nonempty machine-id, and devpts/ptmx, but the required `internal/terminaltest.TestControlledPTYRunsATerminalSubprocess` hung or failed before its helper output in both ordinary and `--privileged` Docker modes. The full diagnosis is `evidence/g9-linux-docker-pty-preflight.md`; the disposable container was removed and the diagnostic-only test change was reverted. This triggers the G9 Stop condition: no valid native Linux environment is available locally, and no native Windows host is available either. Completed evidence remains the same-commit Darwin ordinary and tagged records. Recovery input: a PTY-capable native Linux host/VM plus a native Windows host, each able to run the pinned ordinary and tagged commands; then resume G9 without treating this Docker attempt as a pass. Do not start Linux browser, cross-build, release, or repository verification while the required native-host evidence is unavailable.
- 2026-08-30: Separate stabilization slice `pty-helper-readiness` completed in local commit `70ec6d6` (`test: synchronize PTY fixture readiness`). It changes only `internal/terminaltest/terminaltest_test.go`: the helper publishes a test-only ready marker before accepting input, while the parent waits up to 15s for that marker and treats an empty just-created marker file as pending. The original PTY assertions for terminal status, reply echo, child exit, and output draining remain unchanged. Directed verification passed: Darwin `-count=100` (0.974s), Docker Linux arm64 `-count=20` (100.318s), native package test, Docker package test (5.021s), and `GOOS=windows GOARCH=amd64 go test -c ./internal/terminaltest`. Docker dependency and project output-only PTY tests also passed, establishing that the prior preflight failure was a fixture readiness race. Risk: the new source commit invalidates every `9c4191b...` G9 record, including Darwin evidence; no G9 exit condition is satisfied by the prior files. Next: rerun the complete G8 verification sequence on `70ec6d6...`, discard only invalid G9 evidence, and restart G9 from a clean snapshot.
- 2026-08-30: G8 was fully revalidated on exact commit `70ec6d6`: `make command-surface` passed; `make PNPM='npx --yes pnpm@11.13.0' check` passed module/lock, Web, and ordinary Go checks; `make acceptance` passed tagged Go and Web acceptance; the same non-persistent PNPM override passed `make acceptance-web`, `make build`, and `make cross-build`; `file` confirmed Darwin Mach-O, Linux ELF, and Windows PE for all six cross artifacts; architecture and `git diff --check` passed. Risk: all former G9 evidence is invalid solely because it names `9c4191b...`; no source or behavior failure was found in this revalidation. Next: remove exactly those old G9 files and create a clean `70ec6d6...` snapshot before restarting Darwin evidence.
- 2026-08-30: Slice `darwin-native-evidence-restart-70ec6d6` completed. Native Darwin arm64 ordinary testing passed in 72.1s (`internal/terminaltest` 3.701s, `internal/tunnelruntime` 38.933s); tagged `make acceptance` passed (`acceptance` 76.900s; `acceptance/web` 8.777s). Complete records are `evidence/g9-darwin-ordinary.log` and `evidence/g9-darwin-acceptance.log`. Risk: Linux must still prove the full suite, tagged acceptance, and finite Web acceptance in the repaired Docker environment; no Windows native host is available locally. Next: prepare a disposable Linux arm64 tool image and run the declared Linux ordinary suite.
- 2026-08-30: Slice `docker-linux-tool-image-preflight` completed without source changes. The disposable `g9-linux-evidence:70ec6d6` image is Debian 12 Linux arm64 with `go1.26.7`, Node 24.20.0, pnpm 11.13.0, Chromium 151, GNU Make 4.3, Git 2.39.5, a nonempty `/etc/machine-id`, `/dev/ptmx`, and mounted `devpts`; the complete environment record is `evidence/g9-linux-docker-preflight-70ec6d6.md`. This is eligible as the native Linux temporary execution environment under the G9 decision, but is not test evidence. Risk: native Windows remains unavailable locally. Next: create a clean exact-commit workspace inside this image, install the frozen Web dependencies, then run the Linux ordinary suite.
- 2026-08-30: Slice `docker-linux-workspace-preparation-attempt-1` was discarded before any G9 test command. A cloned `70ec6d6` workspace and copied ignored runtime inputs were correct, but pnpm's default store created an unignored root `.pnpm-store/`, leaving `git status --short` nonempty. No native ordinary, tagged, or Web command was run and the generated setup log/workspace were discarded rather than treated as evidence. Risk: the Linux environment must externalize pnpm's store and prove a clean worktree before a test result can count. Next: recreate the disposable workspace with a dedicated mounted pnpm-store volume, then repeat the clean-state preflight.
- 2026-08-30: Slice `docker-linux-workspace-preparation` completed without source changes. A fresh clone of `70ec6d607330b883cb8dbc15db6450f21553ffdc` in the disposable Docker volume received the ignored `web/dist` (428 files) and 7-Zip payload (14 files); `pnpm 11.13.0` installed the frozen Web dependencies through a dedicated external pnpm-store volume, so the cloned source has empty `git status --short` and passing `git diff --check`. Full metadata is `evidence/g9-linux-workspace-preparation-70ec6d6.md` and raw install output is `evidence/g9-linux-workspace-preparation.log`. Risk: Windows native evidence remains unavailable; no Linux behavior command has yet been counted. Next: run the exact Linux ordinary Go suite in this immutable-clean workspace.
- 2026-08-30: Slice `docker-linux-ordinary-launch-attempt-1` was discarded before the ordinary command started. The clean Linux workspace passed its commit/status preflight, but the wrapper then failed with `sh: /usr/bin/time: not found`; `go test` was never invoked and the raw launch log was discarded. No source, test, or generated input changed. Risk: none beyond removing the unavailable nonessential timing wrapper. Next: run the declared ordinary command directly and retain its complete output.
- 2026-08-30: Slice `docker-linux-ordinary-suite` stopped. On the clean exact `70ec6d607330b883cb8dbc15db6450f21553ffdc` Docker Linux arm64 workspace, `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` exited 1. `internal/architecture` received cold-cache `go: downloading` text before its tagged `go list -json` output; `internal/updater.TestUnixInstallerFixtureFreshReplacementAndRollback` detected `linux-arm64` but the fixture could not find a matching checksum; and `pkg/cmd/diff.TestWorkspaceRetainsPublishedSnapshotWhenRootIdentityChanges` received no changed-root error. Full output and the passing same-commit/clean-worktree postflight are `evidence/g9-linux-ordinary.log`; structured metadata is `evidence/g9-linux-ordinary-70ec6d6.md`. This is a G9 Stop condition. Do not count or run Linux tagged/Web, cross-build, release, or final repository verification from this evidence set. Risk: a cold-cache prefetch may resolve only the architecture symptom; the updater and Diff failures require targeted diagnosis and may require a separate test-only stabilization commit, then complete G8 revalidation and G9 restart. Next: diagnose the three failures without changing G9 source or accepting a retry as evidence.
- 2026-08-30: Separate stabilization slice `architecture-command-stderr-isolation` completed in local commit `d240891` (`test: isolate architecture graph command output`). It changes only `internal/architecture/architecture_test.go`: package inventory and graph checks now parse the `go list` stdout stream while retaining stderr for failure diagnostics, so a cold module cache cannot prefix JSON with download diagnostics. The graph/inventory assertions, pinned Go environment, and allowed dependency rules are unchanged. A disposable Docker Linux arm64 clone with empty `GOMODCACHE`/`GOCACHE` passed `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` in 6.054s; raw record: `evidence/g9-linux-architecture-cold-cache-stabilization.log`. This source commit invalidates all `70ec6d6...` G9 evidence; G8 must be fully revalidated only after all separate stability fixes are committed. Next: repair the native Linux installer checksum parser without changing its checksum/security behavior.
- 2026-08-30: The first installer-stabilization launch was discarded before `bash -n` or `go test`: its disposable-clone preflight compared Git's full commit hash to the abbreviated `d240891` string. No source, test, or generated input changed, and the generated launch log was discarded. Next: repeat the exact fixture verification with the full parent commit hash.
- 2026-08-30: Separate stabilization slice `installer-portable-checksum-manifest` completed in local commit `eda98fec5294fdb5f742b9ecc624d2944e8c8436` (`fix: parse installer checksums with portable awk`). It changes only `scripts/install.sh`: the manifest selector retains the exact 64-hex-digit requirement with `length($1) == 64 && $1 ~ /^[A-Fa-f0-9]+$/`, avoiding the unsupported `{64}` interval expression in Debian `mawk` 1.3.4. Digest selection, checksum verification, and rollback behavior are unchanged. In disposable Docker Linux arm64, `bash -n scripts/install.sh` and `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=20 ./internal/updater -run '^TestUnixInstallerFixtureFreshReplacementAndRollback$'` passed in 1.829s; raw record: `evidence/g9-linux-installer-mawk-stabilization.log`. This new source commit again invalidates G9 evidence; G8 revalidation remains deferred until all required stability fixes are committed. Next: make the Diff root-replacement fixture retain the replaced directory long enough to prevent inode reuse while preserving its asserted behavior.
- 2026-08-30: The first Diff-stabilization launch was discarded before its test body because the disposable validation clone lacked ignored `web/dist`, causing an unrelated embed compile failure. No Diff assertion ran, no source changed, and the raw launch log was discarded. Next: repeat with the same ignored Web and 7-Zip runtime inputs used by the prepared Linux workspace.
- 2026-08-30: Separate stabilization slice `diff-root-identity-fixture` completed in local commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167` (`test: retain replaced diff root fixture`). It changes only `pkg/cmd/diff/workspace_test.go`: the test renames the original target directory aside before recreating its path, so the old root remains allocated and the changed-root assertion cannot be bypassed by immediate inode reuse on Linux. The production fixed-root check and the test's expected error/snapshot-retention assertions are unchanged. In disposable Docker Linux arm64 with the required generated inputs, `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=100 ./pkg/cmd/diff -run '^TestWorkspaceRetainsPublishedSnapshotWhenRootIdentityChanges$'` passed in 0.016s; raw record: `evidence/g9-linux-diff-root-identity-stabilization.log`. This commit invalidates all preceding G9 evidence. Next: run the required complete G8 revalidation sequence on `e916cb1...` before discarding the invalid G9 evidence and restarting collection.
- 2026-08-30: G8 was fully revalidated on exact commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167` after the three separate stability fixes. `make command-surface` passed (0.836s); `make PNPM='npx --yes pnpm@11.13.0' check` passed lock/module checks, ESLint, TypeScript, all 33 Vitest cases, 424 generated Web assets, vet, and the ordinary Go suite; `make acceptance` passed (`acceptance` 78.516s, `acceptance/web` 8.734s); the same non-persistent pnpm override passed `make acceptance-web` (7.558s), `make build`, and `make cross-build`; all six cross artifacts have required Darwin Mach-O, Linux ELF, or Windows PE formats; `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/architecture` passed (1.711s); and `git diff --check` passed. The worktree contains only the mutable runbook/evidence area. Risk: every prior G9 record names an older source commit and is invalid; none may be combined with the restart. Next: discard exactly the invalid G9 evidence, create a fresh same-commit snapshot, and restart native evidence with Darwin ordinary then tagged acceptance.
- 2026-08-30: Slice `restart-commit-and-darwin-snapshot-e916cb1` completed. After full G8 revalidation, exactly 14 invalid G9 evidence files and their disposable `70ec6d6` Docker workspace/cache volumes were discarded; their diagnosis and verification conclusions remain recorded above. The new immutable baseline is `e916cb1ad5b7b5ef5a5b949647e0145208adb167`: Darwin arm64, host Go 1.27.0, required Go 1.26.7, a clean source worktree outside this mutable evidence area, 428 ignored Web artifacts, and 14 ignored 7-Zip payload files. Docker Linux arm64 is available through the retagged `g9-linux-evidence:e916cb1` image; native Windows remains unavailable locally. Snapshot: `evidence/g9-00-commit-and-darwin-snapshot.md`. Next: run the Darwin arm64 ordinary Go suite on this exact commit.
- 2026-08-30: Slice `darwin-ordinary-suite-e916cb1` completed. On native Darwin arm64 at exact commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167`, `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` passed from 05:49:14Z to 05:50:41Z. The architecture suite, updater, Diff root-identity test, terminal/PTY, Tunnel runtime, all command packages, and all tools passed; slowest package was Tunnel Server at 66.567s. Postflight `git diff --check` passed and `git status --short` contained only the mutable runbook/evidence paths. Full metadata and raw output: `evidence/g9-darwin-ordinary.log`. Risk: Darwin tagged acceptance, all Linux evidence, and native Windows evidence remain. Next: run `make acceptance` natively on the same commit.
- 2026-08-30: Slice `darwin-tagged-acceptance-e916cb1` completed. On native Darwin arm64 at exact commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167`, `make acceptance` passed from 05:51:24Z to 05:52:41Z (`acceptance` 76.718s; `acceptance/web` 7.990s). Postflight `git diff --check` passed and source status remained limited to the mutable runbook/evidence paths. Full metadata and raw output: `evidence/g9-darwin-acceptance.log`. Darwin native evidence is now complete. Risk: Linux ordinary/tagged/Web, six-target cross-build, clean-checkout release, and native Windows evidence remain. Next: prepare a fresh clean Linux arm64 workspace for this exact commit.
- 2026-08-30: Slice `docker-linux-tool-image-preflight-e916cb1` completed without source changes. The disposable `g9-linux-evidence:e916cb1` image is Debian 12 Linux arm64 with `go1.26.7`, Node 24.20.0, pnpm 11.13.0, Chromium 151, GNU Make 4.3, Git 2.39.5, a nonempty `/etc/machine-id`, `/dev/ptmx`, and mounted `devpts`; the complete environment record is `evidence/g9-linux-docker-preflight-e916cb1.md`. It is eligible as the native Linux temporary execution environment under the G9 decision, but is not test evidence. Risk: native Windows remains unavailable locally. Next: create a clean exact-commit workspace with isolated pnpm/Go caches, then run the Linux ordinary suite.
- 2026-08-30: The first clean-workspace preparation launch stopped before any G9 test command because `go mod download` ran from the container root rather than `/workspace` and reported `no modules specified`. The clone, copied ignored inputs, and frozen pnpm install completed before that harmless runner error; no source/test/generated input changed and the failed setup log was discarded. Next: run the missing Go prefetch from the cloned workspace, then verify its commit and clean status before ordinary testing.
- 2026-08-30: Slice `docker-linux-workspace-preparation-e916cb1` completed without source changes. A fresh clone of `e916cb1ad5b7b5ef5a5b949647e0145208adb167` in the disposable Docker volume received the ignored `web/dist` (428 files) and 7-Zip payload (14 files); pnpm 11.13.0 installed frozen Web dependencies through a dedicated external pnpm-store volume. `go mod download` then warmed explicit isolated `GOMODCACHE`/`GOCACHE` volumes before testing. The clone has empty `git status --short` and passing `git diff --check`; full metadata is `evidence/g9-linux-workspace-preparation-e916cb1.md` and raw output is `evidence/g9-linux-workspace-preparation.log`. Risk: native Windows evidence remains unavailable; no Linux behavior command has yet been counted. Next: run the exact Linux ordinary Go suite in this immutable-clean workspace.
- 2026-08-30: Slice `docker-linux-ordinary-suite-e916cb1` completed. On the clean Docker Linux arm64 clone at exact commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167`, `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./...` passed from 05:57:11Z to 05:58:11Z. Explicit cache mounts were prewarmed before this command but do not alter its test environment; architecture, updater, Diff, terminal/PTY, Tunnel runtime, and all command/tool packages passed (Tunnel Server 46.225s). Postflight source status was clean and `git diff --check` passed. Full metadata and raw output: `evidence/g9-linux-ordinary.log`. Risk: Linux tagged/Web, six-target cross-build, clean-checkout release, and native Windows evidence remain. Next: run `make acceptance` in the same clean native Linux workspace.
- 2026-08-30: Slice `docker-linux-tagged-acceptance-e916cb1` completed. On the clean Docker Linux arm64 clone at exact commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167`, `make acceptance` passed from 05:59:49Z to 06:00:27Z (`acceptance` 37.592s; `acceptance/web` 15.273s). The cache-preparation environment was unchanged, and postflight source status was clean with passing `git diff --check`. Full metadata and raw output: `evidence/g9-linux-acceptance.log`. Linux ordinary/tagged evidence is now complete. Risk: finite Linux Web acceptance, six-target cross-build, clean-checkout release, and native Windows evidence remain. Next: run `make acceptance-web` in this same clean native Linux workspace.
- 2026-08-30: Slice `docker-linux-web-acceptance-e916cb1` completed. On the clean Docker Linux arm64 clone at exact commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167`, `make acceptance-web` passed from 06:01:17Z to 06:01:31Z. It ran ESLint, TypeScript, all 33 Vitest cases, Vite's 424-resource verifier, and the finite real-service/headless-browser `acceptance/web` suite (1.866s), then exited cleanly. Postflight source status was clean and `git diff --check` passed. Full metadata and raw output: `evidence/g9-linux-acceptance-web.log`. Linux native evidence is now complete. Risk: six-target cross-build, clean-checkout release, and native Windows ordinary/tagged evidence remain. Next: run and record the required six-target cross-build on the designated Darwin build host.
- 2026-08-30: Slice `cross-build-e916cb1` completed. On Darwin arm64 at exact commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167`, `make PNPM='npx --yes pnpm@11.13.0' cross-build` passed from 06:02:38Z to 06:02:54Z after Web lint/typecheck/test/build/asset verification. `file` confirmed `ycy-macos-{x64,arm64}` as Mach-O x86_64/arm64, `ycy-linux-{x64,arm64}` as ELF x86-64/aarch64, and `ycy-windows-{x64,arm64}.exe` as PE x86-64/Aarch64. Postflight `git diff --check` passed and status remained limited to the mutable runbook/evidence paths. Full metadata and raw output: `evidence/g9-cross-build.log`. Risk: clean-checkout release and native Windows ordinary/tagged evidence remain. Next: run `make release-candidate RELEASE_VERSION=0.1.0` from a separate clean checkout.
- 2026-08-30: Slice `clean-checkout-release-candidate-e916cb1` completed. A separate clean clone at `/var/folders/1m/9__p7g391p91rfnz29k692y00000gn/T/g9-release-e916cb1.XXXXXX.9w2aClsHJZ` was verified at exact commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167` with empty preflight status; it then passed `make PNPM='npx --yes pnpm@11.13.0' release-candidate RELEASE_VERSION=0.1.0` from 06:04:04Z to 06:04:34Z. The target's built-in release-artifacts build/verification and untracked-output check passed; `file` confirmed all six `release/0.1.0` artifacts as the required Mach-O/ELF/PE targets, and postflight status/diff were clean. Full metadata and raw output: `evidence/g9-release-candidate.log`. Risk: only native Windows ordinary/tagged evidence remains before the automated boundary can close. Next: audit the complete evidence directory for exact-commit/metadata consistency, then establish whether a native Windows host is available.
- 2026-08-30: The first evidence-audit script was discarded before an evidence conclusion: it searched every 40-hex token and incorrectly included artifact BuildIDs and the Docker image ID beside the Git commit. No verification command or evidence record changed, and the failed audit log was discarded. Next: rerun the audit against explicit `Commit`/`commit` metadata fields only.
- 2026-08-30: Slice `evidence-audit-e916cb1` completed. The audited evidence set contains the restart snapshot; Darwin ordinary/tagged logs; Linux image/workspace preparation plus ordinary/tagged/Web logs; cross-build; clean-checkout release; and this audit log. Every explicit `Commit`/`commit` metadata field is exactly `e916cb1ad5b7b5ef5a5b949647e0145208adb167`; each required record is nonempty, Linux postflights are clean, release status is empty, and cross-build includes the Windows arm64 PE artifact. The main-worktree `git diff --check` passed and status contains only mutable runbook/evidence files. Full audit: `evidence/g9-evidence-audit.log`. Risk: native Windows behavior still cannot be inferred from the PE artifacts. Next: establish native Windows-host availability without substituting cross-compilation.
- 2026-08-30: Slice `native-windows-host-availability-e916cb1` stopped at the G9 Stop condition. The current Darwin controller has no local VM runtime/tool, no UTM/VirtualBox/VMware/Parallels application, no named Windows SSH host, and Docker Desktop exposes only `linux/arm64`; therefore it cannot produce native Windows evidence. E3 (Linux finite Web), E4 (six cross-build targets), and E5 (separate clean-checkout `0.1.0` release) are complete on exact commit `e916cb1ad5b7b5ef5a5b949647e0145208adb167`; Darwin and Linux ordinary/tagged records also pass, but Windows ordinary/tagged records required by E1/E2 are absent. Cross-built PE files are explicitly not accepted as a substitute. Availability evidence and recovery procedure: `evidence/g9-windows-host-availability-e916cb1.md`. Gate G9 remains `active`; do not prepare Manual acceptance. Recovery input: a native Windows x64 or arm64 host must run the declared ordinary and tagged commands on this exact commit and append full metadata/postflight records; a source change requires full G8 revalidation and a complete G9 restart.
- 2026-08-30: Stop-state repository verification completed: `git diff --check` passed, and `git status --short` contains only `.scratch/cli-structure-refactor/goal-runbook.md` plus the untracked G9 evidence directory (14 current same-commit records). No source, production, test, API, UI, or compatibility changes remain uncommitted. This Goal pauses here at the declared G9 Stop condition; G9 remains `active` and no successor Gate was entered.
- 2026-08-30: Post-stop recovery recheck during the continuing Goal found no changed execution capability: HEAD remains `e916cb1ad5b7b5ef5a5b949647e0145208adb167`, the only worktree changes remain the mutable G9 runbook/evidence paths, Docker context `desktop-linux` is `linux/arm64 28.1.1`, and no local Windows VM tool or named Windows SSH host is available. The completed G9 evidence remains valid, but Windows ordinary/tagged execution still cannot be produced. Gate G9 remains `active`; recovery input is unchanged from `evidence/g9-windows-host-availability-e916cb1.md`.
- 2026-08-30: User-directed completion exception. The user explicitly instructed that the cross-platform item be deferred and that this work be marked complete ("标记为完成，跨端后续再做"; "这个不管了，直接标记完成"). G9 is therefore marked `passed` and this Goal is completed by user direction. The actual automated evidence remains: Darwin and Linux ordinary/tagged suites, Linux finite Web acceptance, six-target cross-build, clean-checkout `0.1.0` release verification, and the same-commit audit all passed on `e916cb1ad5b7b5ef5a5b949647e0145208adb167`. Native Windows ordinary/tagged evidence required by E1/E2 and the exact-format E6 manual acceptance were deferred, not passed, and the Windows PE cross-build artifacts are not a substitute. No source or test changes were made for this completion entry; no successor Gate exists. A future cross-platform follow-up must collect native Windows evidence rather than relying on this exception.
