# CLI Terminal Experience Goal Runbook

## Source Baseline

| Path | SHA-256 |
| --- | --- |
| `.scratch/cli-terminal-experience/implementation-plan.md` | `3956cae169224c2e788dfec8cb6b4b014edafe50d3626167f15ca859cc24c4f8` |
| `.scratch/cli-terminal-experience/inventory/03-command-experience-output-contracts.md` | `81af47a95c4ec166aaa5bd6811fbb1fd0ca93572e0741c1d0c6be29855bd0edb` |
| `.scratch/cli-terminal-experience/issues/01-research-charmbracelet-terminal-runtime.md` | `5736a5752dc2e501fe9f73acdf9596e701f1c5d838f722ab3f17e27eb4f95486` |
| `.scratch/cli-terminal-experience/issues/02-define-terminal-capability-and-automation-contract.md` | `c3185b0aeac6b234434ca7eb29b7f2ebed78c218e35c6877224844fbdad239c5` |
| `.scratch/cli-terminal-experience/issues/03-inventory-command-experience-and-output-contracts.md` | `a0750641c2af9ac73804670751d538a769efa2e058dcbf047e89643bc54df741` |
| `.scratch/cli-terminal-experience/issues/04-prototype-the-terminal-visual-language.md` | `9adb84e17df9933a1f868266d3905fcb808825fb704b74d775880003acc26296` |
| `.scratch/cli-terminal-experience/issues/05-choose-the-terminal-experience-module-seam.md` | `bf9846c232ca9503d3a0dff1be29477356ec59bf7fd91c635e7e59d1734d5268` |
| `.scratch/cli-terminal-experience/issues/06-define-output-and-diagnostic-contract.md` | `db7d135c90b78faea10ed57679cccdc2ee623d1e95a818f2cff765f40b5e9aa2` |
| `.scratch/cli-terminal-experience/issues/07-choose-the-long-running-operation-model.md` | `ace2687573252e279c343ba245abb4cc4e007186ac344b4577fb8a16bc85c677` |
| `.scratch/cli-terminal-experience/issues/08-define-help-error-and-completion-experience.md` | `fec1816b30288c8ff503a69770183e3f684c35d644913d4dee64b3feccf45afb` |
| `.scratch/cli-terminal-experience/issues/09-approve-the-cli-experience-migration-and-acceptance-plan.md` | `5ed0a69cbf1b2c3438d1c25e5e6f6424d65b139841a127a9bcde4ee5b5d03d75` |
| `.scratch/cli-terminal-experience/issues/10-choose-automation-inputs-for-prompted-commands.md` | `2a650235be44ae248d6528ec44bfc7a6445b6d6cd5219b4a9eb5066cd84db538` |
| `.scratch/cli-terminal-experience/map.md` | `91b90b00fe5d3907a0d7c64bd43b027715626b81210615762523df47c15ea26e` |
| `.scratch/cli-terminal-experience/prototype/bun-baseline/README.md` | `f200c29877408e19bd22ca96d08b41a6bbab10de02c2226dd6f395b70513bfba` |
| `.scratch/cli-terminal-experience/research/01-charmbracelet-terminal-runtime.md` | `59a8e2e90eac1c6bf1c0801c42a78eeb13f19d5cdd8a17a66628ddd1aa7f7c6e` |
| `.scratch/go-migration/acceptance.md` | `61f90f051d982dfafe7b94aab7ac3e7b065c9190db88ddb0304b62a910b86667` |
| `.scratch/go-migration/map.md` | `d81d65e6fb5a31fa20291efbda5a79d6f389856f3d2ae56897d5f72b58802986` |
| `CONTEXT.md` | `2fd5522e5f4fbaf77271ef0b60411e472f35b3e9e1eac2918803c5cc15621cf3` |

## State Rules

- `implementation-plan.md` 是 Gate 合同的唯一来源；本账本只记录状态和证据。
- 一次只执行 Goal Ledger 中唯一 `active` 的 Gate。
- 每轮向对应 Gate 的 Progress Log 追加 slice、修改、验证结果、风险和下一动作。
- `passed` 需要计划中每条 Exit condition 的明确证据；普通实现或验证失败保持 `active`。
- `blocked` 只用于计划声明的 Stop condition，并记录阻塞与恢复条件。
- 人工验收待确认是一次 Goal 终止交接，必须记录在 Progress Log；它不是 `blocked`，当前 Gate 必须保持 `active`，不得通过重复日志表示等待。
- 当前 Gate 通过后只激活直接后继并结束本次 Goal；直接后继虽为 `active`，但必须由新的 Goal 执行。最后一个 Gate 通过后记录 effort 完成并结束本次 Goal。

## Goal Ledger

| Gate | Status | Depends on | Plan contract | Unlock evidence |
| --- | --- | --- | --- | --- |
| G0: Characterize Protected Behavior | `passed` | none | `implementation-plan.md` -> `G0: Characterize Protected Behavior` | no predecessor |
| G1: Establish Terminal Foundation | `passed` | G0 | `implementation-plan.md` -> `G1: Establish Terminal Foundation` | G0 passed 2026-08-27; G1 passed 2026-08-27 |
| G2: Wire Root and Diagnostics | `passed` | G1 | `implementation-plan.md` -> `G2: Wire Root and Diagnostics` | G1 passed 2026-08-27; G2 passed 2026-08-27 |
| G3: Migrate Static and Continuous Journeys | `passed` | G2 | `implementation-plan.md` -> `G3: Migrate Static and Continuous Journeys` | G2 passed 2026-08-27; G3 passed 2026-08-27 |
| G4: Migrate Ordinary Form Journeys | `passed` | G3 | `implementation-plan.md` -> `G4: Migrate Ordinary Form Journeys` | G3 passed 2026-08-27; G4 passed 2026-08-27 |
| G5: Migrate Tracked Git Journeys | `passed` | G4 | `implementation-plan.md` -> `G5: Migrate Tracked Git Journeys` | G4 passed 2026-08-27; G5 passed 2026-08-27 |
| G6: Audit Completion and Accept | `passed` | G5 | `implementation-plan.md` -> `G6: Audit Completion and Accept` | G5 passed 2026-08-27; G6 passed 2026-08-27 |

## Progress Log

### G0: Characterize Protected Behavior

- 2026-08-27: initialized as `active`; implementation has not started.
- 2026-08-27: Slice `G0.1 terminal test support` added `internal/terminaltest` with injected stream/environment facts, a recording semantic-run fixture, isolated redirected streams with terminal-control detection, and a controlled PTY subprocess fixture; added the test-only `github.com/creack/pty v1.1.24` dependency in `go.mod`/`go.sum`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminaltest` passed; the required directed `go test -count=1 ./cmd/ycy ./internal/cliapp ./internal/logging` passed (`cmd/ycy` 65.091s, `cliapp` 0.756s, `logging` 0.354s); `git diff --check` passed. Risk: the concrete PTY fixture is intentionally Unix-only and reports unsupported on Windows; native Windows console cleanup remains later acceptance evidence. Next: add focused Automation/stream/side-effect contracts where the existing suite does not yet exercise the approved fallback matrix.
- 2026-08-27: Slice `G0.2 discovery and parser stream contract` added `internal/cliapp/discovery_contract_test.go`. It executes help, version, Bash completion, and an unknown command through injected redirected streams and proves their stdout/stderr ownership, exit codes, and absence of terminal-control bytes. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/cliapp -run '^TestDiscoveryAndParserContractsKeepAutomationStreamsSeparated$'` passed; required directed suite passed (`cmd/ycy` 59.037s, `cliapp` 0.212s, `logging` 0.311s). Risk: the test protects Cobra's generated completion shape by its stable command function marker rather than a full generated-script golden. Next: add a focused command-result/error-stream contract for an existing deliberate mixed-output journey.
- 2026-08-27: Slice `G0.3 Upgrade mixed-stream contract` added a focused case in `internal/commands/upgrade/upgrade_test.go`. It proves ordinary and HTTP-classified failure both write only `Update aborted.` to stdout and one plain error to stderr, preserve their historical exit-code split (`1` versus `0`), and emit no terminal-control bytes. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/upgrade -run '^TestClassifyUpgradeErrorPreservesTheDeliberateMixedStreamContract$'` passed; required directed suite passed (`cmd/ycy` 63.861s, `cliapp` 0.564s, `logging` 0.926s). Risk: network/download and detached-updater behavior remains protected by its existing integration suite, not duplicated by this stream-only slice. Next: audit a destructive prompt path for direct cancellation-before-mutation and redirected-stream evidence.
- 2026-08-27: Slice `G0.4 rm redirected cancellation contract` added `TestRMRedirectedCancellationPreventsDeletionAndTerminalControl` in `cmd/ycy/rm_test.go`. It runs the current handwritten prompt adapter with an EOF redirected input, proves its existing cancellation output and empty stderr, verifies the target file remains present before any deletion, and rejects terminal-control bytes. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^TestRMRedirectedCancellationPreventsDeletionAndTerminalControl$'` passed; required directed suite passed (`cmd/ycy` 59.845s, `cliapp` 0.403s, `logging` 0.674s). Risk: this freezes the current cancellation baseline; the later Automation-failure behavior is a command migration concern and must replace this path only with separate before-side-effect evidence. Next: protect Raw Child I/O stream separation independently of prompt rendering.
- 2026-08-27: Slice `G0.5 run Raw Child I/O contract` updated `cmd/ycy/run_process_test.go` to run the existing real-child fixture through `terminaltest.RedirectedStreams`. It continues to prove exact inherited stdin, argv, cwd, stdout, and stderr separation, and additionally proves the fixture receives no ycy-injected terminal-control bytes. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^TestOSRunChildRunnerUsesArgvCWDAndInheritedStreams$'` passed; required directed suite passed (`cmd/ycy` 59.016s, `cliapp` 0.308s, `logging` 0.141s). Risk: Raw Child I/O may legitimately contain child-authored control bytes; the test constrains only ycy's plain fixture path. Next: protect the continuous diagnostic/redaction path independently from Command Result output.
- 2026-08-27: Slice `G0.6 continuous diagnostic stream contract` added `TestRuntimeRoutesRedactedDiagnosticsOnlyToInjectedStderr` in `internal/logging/logging_test.go`. It proves a Tunnel-scoped diagnostic writes only to injected stderr, redacts message and authorization values, retains structured fields, and emits no terminal-control bytes when color is off. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/logging -run '^TestRuntimeRoutesRedactedDiagnosticsOnlyToInjectedStderr$'` passed; required directed suite passed (`cmd/ycy` 64.549s, `cliapp` 0.334s, `logging` 0.162s). Risk: recursive redaction and NDJSON are later G2 output-contract implementation work; current G0 evidence freezes the existing top-level redaction and stream baseline. Next: compare the remaining acceptance matrix against existing focused command tests and add only missing direct contract coverage.
- 2026-08-27: Slice `G0.7 config cm secret prompt baseline` updated `TestTerminalCMAddPrompterReadsPipedPasswordWithoutWritingIt` in `cmd/ycy/configcmadd_test.go` to use `terminaltest.RedirectedStreams`. It protects the current piped-secret no-echo baseline, requires empty stderr and no terminal-control bytes, while the existing standalone case protects encrypted persistence and EOF cancellation before configuration creation. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^TestTerminalCMAddPrompterReadsPipedPasswordWithoutWritingIt$'` passed; required directed suite passed (`cmd/ycy` 59.032s, `cliapp` 0.398s, `logging` 0.496s). Risk: the approved future Automation contract rejects piped secret entry; G4 must replace this legacy behavior with a before-write failure test rather than preserve it. Next: confirm browser lifecycle stdout readiness and signal cancellation are already focused and record the applicable evidence.
- 2026-08-27: Slice `G0.8 browser lifecycle evidence` found no missing implementation: existing `TestDiffStandaloneBinaryPreservesCLIValidationAndLifecycle` and `TestFSStandaloneBinaryPreservesCLIHTTPAndSignalLifecycle` directly protect stdout readiness URLs while each service remains alive, CLI validation, and signal-driven cleanup. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^(TestDiffStandaloneBinaryPreservesCLIValidationAndLifecycle|TestFSStandaloneBinaryPreservesCLIHTTPAndSignalLifecycle)$'` passed; required directed suite passed (`cmd/ycy` 60.676s, `cliapp` 0.532s, `logging` 0.165s). Risk: these are native Unix-host lifecycle tests; native Windows terminal evidence remains a later Gate concern. Next: audit the remaining static and Git command journeys against their existing focused regression tests.
- 2026-08-27: Slice `G0.9 acceptance-matrix audit` confirmed existing focused module and standalone coverage for the remaining protected journeys: configuration list/add/remove/use/set/test, export, zip, Git Heat/Pulse/Fork/CM, Tunnel selection/server lifecycle, and their applicable result, error, mutation, cancellation, redaction, and signal contracts. The command-module suite contains 730 focused tests across the G0 command families; no equivalent test was duplicated. Cross-platform fixture check: `GOTOOLCHAIN=go1.26.7 GOWORK=off GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go test -c` passed for `./internal/terminaltest`, `./internal/cliapp`, `./internal/logging`, and `./internal/commands/upgrade`. Risk: Windows binaries were compiled but cannot execute on the macOS host; native console behavior remains later acceptance evidence. Next: run G0 Repository verification in the required `make check`, `make build`, `make cross-build` order.
- 2026-08-27: Repository verification diagnosis `G0.10 Tunnel fixture startup budget`: initial `make check` failed only at `TestFRPSupervisorStreamsOutputAndStopsItsUnixProcessGroup` while waiting one second for the fixture's grandchild PID publication; all other packages passed. The minimized default-parallel loop `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=10 ./... -run '^TestFRPSupervisorStreamsOutputAndStopsItsUnixProcessGroup$'` reproduced the failure, while the same loop with `-p 1` passed. The existing test was the regression seam. Updated only `internal/commands/tunnel/frp_supervisor_unix_test.go`: its fixture PID and initial stdout/stderr publication waits now use a five-second startup budget; process-state, log, process-group stop, and one-second cleanup assertions remain unchanged. Verification: the default-parallel ten-run loop passed after the change; `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/tunnel` passed in 52.313s; no debug instrumentation was introduced. Risk: startup timeout now reports a real fixture launch failure up to four seconds later under severe host load, but does not weaken any observable Tunnel lifecycle assertion. Next: rerun G0 Directed verification and then retry `make check`.
- 2026-08-27: Repository verification `G0.11 check`: required Directed verification passed after the Tunnel fixture repair (`cmd/ycy` 67.930s, `cliapp` 0.553s, `logging` 0.387s). The retry `make check` passed: module verification, no-Bun check, Web lint/typecheck/33 tests/Vite asset verification, Go vet, and the full Go suite all passed; Tunnel completed in 61.917s. The only build output note was the existing Vite chunk-size warning, which does not fail the target. Risk: repository-generated build output will be inspected after the next two required commands. Next: run `make build`.
- 2026-08-27: Repository verification `G0.12 build`: `make build` passed, including Web lint/typecheck/33 tests/Vite asset verification and the local `CGO_ENABLED=0` `build/ycy` compilation. The existing Vite chunk-size warning remained non-fatal. Risk: six-target compilation remains to be demonstrated. Next: run `make cross-build`.
- 2026-08-27: Final acceptance `G0.13`: all G0 Exit conditions are met. Reusable seams exist in `internal/terminaltest`: injected stream/environment facts, a recording semantic-run fixture, isolated redirected streams with terminal-control detection, and a controlled Unix PTY subprocess fixture with a Windows unsupported fallback. The acceptance-matrix audit and existing focused command tests protect all applicable discovery, configuration, export, local, Git, browser, Tunnel, and Upgrade stream/result/error/mutation/cancellation/redaction contracts; new focused tests add explicit redirected-stream evidence for discovery, Upgrade, rm, Raw Child I/O, diagnostics, and secret prompting. Prompt-cancellation module tests establish before-side-effect behavior; Automation-oriented stream assertions reject control bytes in the new seams. Directed evidence: the required three-package suite passed after every added slice and most recently with `cmd/ycy` 67.930s, `cliapp` 0.553s, `logging` 0.387s. Repository evidence: `make check`, then `make build`, then `make cross-build` all passed; artifacts verified as local arm64 Mach-O plus six CGO-free macOS x64/arm64, Linux x64/arm64, and Windows x64/arm64 targets. PTY helper tests passed natively; test binaries compile for Linux amd64 and Windows amd64. Risk: native Windows console execution remains intentionally deferred to later Gate acceptance; it is not claimed by cross-compilation. Next: G0 is `passed`; G1 is activated but has not started and must be handled by a new Goal.

### G1: Establish Terminal Foundation

- 2026-08-27: initialized as `planned`; no implementation evidence.
- 2026-08-27: activated after G0 final acceptance; implementation has not started.
- 2026-08-27: Slice `G1.1 session classifier` added `internal/terminal/session.go` and its injected-fact tests. `Classify` uses all three inherited stream facts, set-even-empty `CI`, `TERM`, and non-empty `NO_COLOR`; it selects Automation for any redirected stream or CI, Plain Interactive for absent/dumb/unknown terminal capability, and Rich Interactive only for an explicit recognized terminal family. `NO_COLOR` removes color without changing a Rich session. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./cmd/ycy` passed (`internal/terminal` 0.435s, `cmd/ycy` 67.850s); `git diff --check` passed. Risk: the recognized TERM set is intentionally conservative, so an unsupported-but-capable terminal remains Plain Interactive until its capability is explicitly approved. Next: add terminal-owned semantic requests, documents, and the narrow Experience/Experience Run interfaces without adapting any command.
- 2026-08-27: Slice `G1.2 semantic terminal seam` added typed `InteractionRequest`/`InteractionAnswer`, `PresentationDocument`, `TrackedOperation`, visual roles, and the narrow `Experience`/`ExperienceRun` interfaces in `internal/terminal/semantic.go`. The terminal-owned tracked contract receives command-owned phase updates and an optional cancellation request only; it cannot receive or perform business work. Added a typed `terminaltest.RecordingExperience` / `RecordingSemanticRun` so later `cmd/ycy` adapters can prove semantic translation without importing terminal implementation details into command Modules. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/terminaltest` passed (`internal/terminal` 0.405s, `terminaltest` 0.761s); required directed suite passed (`internal/terminal` 0.169s, `cmd/ycy` 65.012s); `git diff --check` passed. Risk: no command adapter consumes these types yet by design, so the public spelling remains private to `internal/terminal` and may be narrowed when the first adapter is implemented. Next: implement deterministic Plain/Automation presentation to stdout without Rich styling or terminal control.
- 2026-08-27: Slice `G1.3 plain document presentation` added `RenderPlain` / `WritePlain` in `internal/terminal/presentation.go`. It renders durable `PresentationDocument` blocks in order to the supplied stdout writer, ignores Rich-only `ClearBefore`, normalizes a final newline, and removes ESC/C1 control bytes from terminal-rendered text; all visual roles remain unstyled in Plain Interactive and Automation. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal` passed (`internal/terminal` 0.392s); required directed suite passed (`internal/terminal` 0.145s, `cmd/ycy` 62.898s); `git diff --check` passed. Risk: this foundation intentionally treats presentation text as human-facing and not a raw-byte transport; raw completion and Raw Child I/O remain outside this renderer. Next: add approved Lip Gloss visual roles and width-aware Rich document rendering.
- 2026-08-27: Slice `G1.4 Rich visual roles and width` added the approved direct `github.com/charmbracelet/lipgloss v1.1.0` dependency and local-renderer Rich document rendering. `WriteRich` maps title/active to cyan, success to green, warning to yellow, error to bold red, and muted text to faint gray; it never mutates Lip Gloss's global renderer. It wraps overlong text by display width without truncation, applies Rich-only clear-screen behavior, and keeps that layout while omitting color when `RichOptions.Color` is false. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off go mod tidy` and `go mod verify` passed; focused Rich PTY style, `NO_COLOR`, and narrow-width tests passed in `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal` (0.655s); required directed suite passed (`internal/terminal` 0.163s, `cmd/ycy` 65.516s); dependency inspection reports Lip Gloss `v1.1.0` and indirect termenv only, with no Huh, Bubble Tea, or Bubbles; `git diff --check` passed. Risk: terminal color selection remains session-owned and is not forced by an environment override; the PTY test strips ambient color controls to exercise the approved Rich case deterministically. Next: add Huh v1.0.0 interaction handling with explicit I/O and context ownership.
- 2026-08-27: Slice `G1.5 interaction wrapper` added approved direct `github.com/charmbracelet/huh v1.0.0` plus `InteractionHandler`. It rejects Automation before reading or writing, uses deterministic line prompts and terminal-only echo-disabled secret input for Plain Interactive, and constructs Rich Huh forms only with the supplied input, diagnostic writer, and command Context. It maps Huh user abort to terminal cancellation and gives `ctx.Err()` precedence over Huh's cancellation-as-timeout behavior. The controlled PTY suite emulates the upstream background/cursor capability handshake, proves explicit Rich input and stderr output, and proves both successful Enter and Ctrl-C returns restore cursor and disable bracketed paste. Verification: `go mod tidy` and `go mod verify` passed; focused interaction suite passed in 0.606s; the earlier required directed suite passed (`internal/terminal` 5.194s, `cmd/ycy` 63.783s); `go.mod` directly pins only Huh/Lip Gloss while Huh's approved upstream graph supplies Bubble Tea `v1.3.6` and Bubbles `v0.21.1-...` as indirect modules, with no direct terminal-package import of either. Risk: Rich capability probing is isolated to the Rich Huh path; Plain and Automation never construct a Huh form or use a controlling terminal. Next: add concrete Experience lifecycle, renderer leasing, and lease-aware diagnostic writing.
- 2026-08-27: Slice `G1.6 lifecycle and lease coordination` added concrete `Runtime` / `ExperienceRun`, `RendererLease`, and `LeaseAwareDiagnosticWriter`. `Present` writes only the configured stdout; `Ask` and `Track` acquire the exclusive diagnostic lease, write renderer-owned material directly to stderr, and flush deferred normal diagnostics in order after cleanup. `Track` consumes only command-owned phase updates and can request cooperative cancellation; it does not run business work or add a Bubble Tea view. `Close` serializes with active operations and rejects subsequent use. Added typed recording Experience support in `internal/terminaltest`. Verification: focused lifecycle/lease suite passed in 0.495s; `internal/terminaltest` passed in 0.903s; the full `internal/terminal` suite passed in 5.300s; `CGO_ENABLED=0 go test -c ./internal/terminal` passed for Darwin arm64, Linux amd64, and Windows amd64; `git diff --check` passed. Risk: no command has been wired to this foundation yet by design, so current public command stream behavior remains solely under its G0 protection. Next: run final G1 Directed verification, then `make check`, `make build`, and `make cross-build` in order.
- 2026-08-27: Repository verification `G1.7 check`: final Directed verification passed (`internal/terminal` 5.316s, `cmd/ycy` 64.170s). `make check` then passed: module verification, no-Bun isolation, Web lint/typecheck/33 tests/Vite asset verification, Go vet, and the full Go suite including `internal/terminal` (6.506s) and `internal/terminaltest` (1.517s). The existing Vite chunk-size warning remained non-fatal. Risk: local and six-target build evidence remains required. Next: run `make build`.
- 2026-08-27: Repository verification `G1.8 build`: `make build` passed, including Web lint/typecheck/33 tests/Vite asset verification and the local CGO-free `build/ycy` compilation. The existing Vite chunk-size warning remained non-fatal. Risk: six-target compilation remains required. Next: run `make cross-build`.
- 2026-08-27: Final acceptance `G1.9`: all G1 Exit conditions are met. `internal/terminal` owns the injected Session classifier, semantic requests/documents/operations, Rich visual roles, display-width wrapping, explicit Huh interaction, concrete Experience Run lifecycle, controlled PTY cleanup, exclusive Renderer Lease, and lease-aware diagnostic writer. Focused tests prove Rich, Plain Interactive, and Automation classification; non-empty `NO_COLOR` style suppression without session demotion; deterministic stdout Command Results; diagnostic stderr routing and lease deferral; Rich PTY successful and Ctrl-C cleanup; and no terminal control in Plain/Automation rendering. Direct requirements are Huh `v1.0.0` and Lip Gloss `v1.1.0`; `go mod tidy` and `go mod verify` passed, Bubble Tea/Bubbles remain upstream indirect-only modules with no direct terminal-package import, and `internal/terminal` compiled CGO-free for Darwin arm64, Linux amd64, and Windows amd64. Final Directed evidence: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./cmd/ycy` passed (`internal/terminal` 5.316s, `cmd/ycy` 64.170s). Repository evidence in required order: `make check`, `make build`, then `make cross-build` all passed; artifacts are local arm64 Mach-O plus six CGO-free macOS x64/arm64, Linux x64/arm64, and Windows x64/arm64 binaries. Final audit: module verification and `git diff --check` passed; no Cobra, command-business, Bubble Tea, or Bubbles direct import exists in `internal/terminal`. Risk: the G1 foundation is intentionally not wired into Cobra or existing command adapters; current command behavior remains unchanged until later Gates. Next: G1 is `passed`; G2 is activated but has not started and must be handled by a new Goal.

### G2: Wire Root and Diagnostics

- 2026-08-27: initialized as `planned`; no implementation evidence.
- 2026-08-27: activated after G1 final acceptance; implementation has not started.
- 2026-08-27: Slice `G2.1 root Experience construction` added the private `rootTerminal` composition object in `cmd/ycy/main.go`. The normal CLI path now constructs exactly one `internal/terminal` Experience from the inherited stdin/stdout/stderr files, `os.LookupEnv`, and the root terminal detector; all existing command modules, Cobra streams, and Raw Child I/O still receive those same unwrapped file identities. Added `cmd/ycy/main_test.go` to prove root stream identity and Rich-session classification from injected terminal facts. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^TestNewRootTerminalConstructsOneExperienceFromInheritedFacts$'` passed in 1.526s; required Directed verification passed (`internal/terminal` 5.463s, `internal/cliapp` 0.479s, `internal/logging` 0.802s, and separately re-run `cmd/ycy` 62.232s); `git diff --check` passed. Risk: the root retains the Experience but normal diagnostics still write directly to the raw stderr until the final lease-aware-writer slice; no command-specific adapter was changed. Next: add the Cobra-owned discovery document and a thin root terminal adapter while preserving exact Plain/Automation Help, version, and raw completion bytes.
- 2026-08-27: Slice `G2.2 discovery document translation` added `internal/cliapp/discovery.go` with a Cobra-owned `DiscoveryDocument` (command path, summary, usage, direct available descendants, flags, and declared examples) and a terminal-library-free presenter interface. `cmd/ycy/discovery.go` translates only that document into a terminal semantic presentation and closes its Experience Run. The root installs the adapter only for a Rich Interactive Session; Plain Interactive and Automation retain Cobra's normal Help serializer, while version and generated completion bypass the presenter in every session. Verification: focused `TestDiscoveryPresenterReceivesHelpButNotVersionOrRawCompletion` and `TestTerminalDiscoveryPresenterTranslatesAndClosesOneExperienceRun` passed; required Directed verification passed (`internal/terminal` 5.493s, `internal/cliapp` 0.797s, `internal/logging` 0.630s, `cmd/ycy` 69.663s); `git diff --check` passed. Risk: Rich Help now has a deliberate compact layout while Cobra remains the source of grammar and command topology; no static command report or raw completion generator changed. Next: normalize parser-recovery errors into the approved single-line actionable form with direct-candidate guidance, preserving exit codes and existing command validation messages.
- 2026-08-27: Slice `G2.3 parser-recovery error normalization` added `internal/cliapp/errors.go`. Cobra remains the parser and direct-candidate owner: its returned unknown-command parent path and `SuggestionsFor` result drive guidance, while the current command's actual flags are used only for a unique high-confidence unknown-flag candidate. Parser errors now render once as `error: ...`; a unique candidate names its exact `--help` invocation and no candidate directs callers to the nearest valid parent. Existing business/validation errors are untouched. Updated protected cliapp parser assertions to the approved one-line form without changing their no-handler, stdout, or exit-code checks. Verification: focused parser and Automation discovery cases passed; required Directed verification passed (`internal/terminal` 5.262s, `internal/cliapp` 0.208s, `internal/logging` 0.324s, `cmd/ycy` 61.862s); `git diff --check` passed. Risk: candidate guidance is intentionally conservative and omits ambiguous matches; special `run` parsing is not rewritten. Next: parse Diagnostic Configuration as an independent immutable contract (CLI/environment precedence, aliases, conflicts, and discovery bypass) before wiring it into Cobra handlers.
- 2026-08-27: Slice `G2.4 diagnostic configuration parsing` added immutable `logging.Configuration` parsing for text/json format, CLI-over-environment precedence, `-v`/`--verbose`, `-q`/`--quiet`, repeated controls, and conflicts. `internal/cliapp` now accepts the approved global grammar, validates it in each executing leaf's pre-run path before handler effects, and bypasses it for root/help/version/raw completion discovery. The frozen `run` parser consumes the same controls after its own grammar parse and before its handler. Because `git heat` already owns `-q <query>`, the root normalizes a global `-q` to `--quiet` except on that proven legacy Heat query path; `--quiet` remains unambiguous. Focused tests prove precedence, conflicts, invalid format, discovery bypass, run before-handler failure, and Heat shorthand coexistence. Verification: required Directed verification passed (`internal/terminal` 5.463s, `internal/cliapp` 0.365s, `internal/logging` 0.475s, `cmd/ycy` 67.434s); `git diff --check` passed. Risk: format selection is parsed but not yet rendered by Runtime until the next formatter slice, so no existing Diagnostic Record shape changes here. Next: implement text and NDJSON Diagnostic Record formatting with session-aware color policy, preserving User-Actionable Error output outside the logger.
- 2026-08-27: Slice `G2.5 diagnostic record formatter` updated `internal/logging/logging.go` so Runtime selects text or NDJSON Diagnostic Records through immutable invocation configuration. Text records now use UTC millisecond timestamps, padded uppercase levels, bracketed scopes, one-line redacted messages, compact context, and Rich-only semantic color roles; JSON records use the approved stable NDJSON schema with lowercase levels and omit absent scope/context fields. `cmd/ycy/main.go` derives log color from the immutable Rich Session and its NO_COLOR style fact, while `internal/cliapp/app.go` applies the resolved record format without changing User-Actionable Error output. Focused tests in `internal/logging/logging_test.go` and `internal/cliapp/app_test.go` prove text/JSON bytes, optional NDJSON field omission, all Rich style roles, unstyled Plain/NO_COLOR/JSON output, and format configuration reaching Runtime. Verification: focused formatter/configuration tests passed (`internal/logging` 0.363s; `internal/cliapp` 0.414s); required Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./internal/logging ./cmd/ycy` passed (`internal/terminal` 5.551s, `internal/cliapp` 0.904s, `internal/logging` 0.335s, `cmd/ycy` 70.586s); `git diff --check` passed. Risk: credential redaction is still the preceding shallow implementation and must be made recursive before records can claim the full diagnostic safety contract. Next: implement recursive structured and credential-shaped text redaction without changing formatter schema or command results.
- 2026-08-27: Slice `G2.6 recursive diagnostic redaction` added `internal/logging/redaction.go` and routes merged logger context through it before either formatter. Credential field matching now ignores case and separators for authorization, cookies, passwords, secrets, tokens, API keys, credentials, private keys, and request bodies; nested maps, arrays, strings, and errors receive the same safe transformation. Credential-shaped text covers Bearer values and key/value forms, while a single unencodable leaf becomes deterministic `[UNENCODABLE]` without discarding safe sibling context. User-Actionable Errors retain their one-line renderer and now share the stronger text redaction only. Focused text and NDJSON tests prove no direct or nested credential/request-body/bearer value leaks, JSON stays parseable, separator variants are redacted, ordinary `project=demo` text remains visible, and invalid nested values preserve the rest of context. Verification: focused redaction suite passed (`internal/logging` 0.444s); required Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./internal/logging ./cmd/ycy` passed (`internal/terminal` 5.497s, `internal/cliapp` 0.568s, `internal/logging` 0.151s, `cmd/ycy` 71.163s); `git diff --check` passed. Risk: normal logger output still reaches raw stderr at the composition root, so it cannot yet defer behind an active Renderer Lease. Next: supply `Experience.DiagnosticWriter()` to normal diagnostic paths and prove lease deferral at root integration.
- 2026-08-27: Slice `G2.7 lease-aware diagnostic writer integration` changed `cmd/ycy/main.go` so the root builds one normal diagnostic writer from `Experience.DiagnosticWriter()` and supplies it to the logging Runtime, Cobra's `Err` stream, Upgrade error output, and root initialization errors. `run` alone retains the original inherited stderr file for its Raw Child I/O contract. Added `newRootLoggingRuntime` as the small composition seam and `TestRootDiagnosticsDeferNormalWritesUntilRendererLeaseCloses`: while a Plain tracked view owns the Renderer Lease, both a normal diagnostic write and a logger record remain absent from stderr; after cleanup they flush in write order after the renderer line. Verification: focused root construction/lease test passed (`cmd/ycy` 0.483s); required Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./internal/logging ./cmd/ycy` passed (`internal/terminal` 5.491s, `internal/cliapp` 0.997s, `internal/logging` 0.599s, `cmd/ycy` 63.516s); `git diff --check` passed. Risk: no remaining G2 implementation risk is known; full repository verification remains required. Next: run `make check`, then `make build`, then `make cross-build` without changing Gate scope.
- 2026-08-27: Repository verification `G2.8 check`: `make check` passed. It verified modules and active-toolchain isolation, completed Web ESLint/typecheck/33 tests/Vite asset verification, ran Go vet, and passed the full Go suite including `cmd/ycy` (68.798s), `internal/terminal`, `internal/cliapp` (0.844s), `internal/logging` (0.425s), and Tunnel (58.362s). The existing Vite chunk-size warning remained non-fatal. Risk: local and cross-target build evidence remains required. Next: run `make build`.
- 2026-08-27: Repository verification `G2.9 build`: `make build` passed, including Web ESLint/typecheck/33 tests/Vite asset verification and the local CGO-free CLI build. The existing Vite chunk-size warning remained non-fatal. Risk: cross-target compilation remains required. Next: run `make cross-build`.
- 2026-08-27: Repository verification diagnosis `G2.10 Web proxy socket anomaly`: the first `make cross-build` stopped in `check-web` before any target compilation because Vitest reported one unhandled Undici `setTypeOfService EINVAL` during `development-proxy.test.ts`; all 9 files and 33 assertions had passed. The smallest real-path loop `pnpm --dir web exec vitest run development-proxy.test.ts` then passed once and 10 consecutive times (3/3 each, about 3 seconds each) with no socket anomaly. The repository's earlier Web acceptance history records the same transient host-socket symptom after otherwise-passing assertions and repeated complete/focused reruns without recurrence. No source or test was weakened because there is no deterministic red-capable defect to repair. Risk: the host-level socket anomaly may recur on a retry, but no G2 change reaches the Web proxy. Next: rerun the original required `make cross-build` to reverify the complete build path.
- 2026-08-27: Repository verification `G2.11 cross-build`: the retry `make cross-build` passed after its full Web lint/typecheck/33-test/Vite-asset phase. It produced the six required CGO-free artifacts: macOS x64 and arm64 Mach-O executables, Linux x64 and arm64 static ELF executables, and Windows x64 and arm64 PE console executables. The existing Vite chunk-size warning remained non-fatal. Risk: none remaining for G2. Next: record final Gate acceptance.
- 2026-08-27: Final acceptance `G2.12`: all G2 Exit conditions are met. `cmd/ycy` constructs exactly one terminal Experience from inherited streams and environment facts; normal logger, Cobra, Upgrade, and initialization diagnostics use its lease-aware writer while `run` retains raw child stderr. Cobra remains owner of discovery, Help/version/completion stream semantics, parser recovery, and command grammar; Rich discovery is a thin document adapter only. Executing handlers enforce the approved diagnostic Configuration precedence and conflicts while discovery bypasses invalid controls. Focused tests prove approved text/NDJSON schemas, UTC milliseconds, semantic color roles, NO_COLOR/Plain/JSON no-ANSI behavior, recursive redaction, unencodable-value containment, and lease deferral/ordering. Final Directed evidence: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./internal/logging ./cmd/ycy` passed (`internal/terminal` 5.491s, `internal/cliapp` 0.997s, `internal/logging` 0.599s, `cmd/ycy` 63.516s). Repository evidence in required order: `make check`, then `make build`, then retry `make cross-build` all passed; `git diff --check` passed. G2 is `passed`; G3 is activated but has not started and must be handled only by a new Goal.

### G3: Migrate Static and Continuous Journeys

- 2026-08-27: initialized as `planned`; no implementation evidence.
- 2026-08-27: activated after G2 final acceptance; implementation has not started.
- 2026-08-27: Slice `G3.1 Tunnel server continuous diagnostics` added focused root and foreground-lifecycle evidence without changing the service implementation. `cmd/ycy/main_test.go` now proves the root-owned `tunnel.server` logger uses only stderr for Rich text, `NO_COLOR` text, and redirected NDJSON; Rich emits semantic style, `NO_COLOR` and redirection emit no terminal control, both formats recursively redact credentials, and a synthetic Renderer Lease defers normal Tunnel diagnostics until it closes. `internal/commands/tunnel/server_command_test.go` now proves the real foreground lifecycle emits its start, stopping, and stopped Diagnostic Records before returning from context cancellation. Tunnel server has no handwritten presenter to remove: its existing command adapter already owns only the continuous logger path. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy ./internal/commands/tunnel -run '^(TestRootTunnelServerDiagnosticsPreserveSessionStreamContracts|TestRootTunnelServerDiagnosticsDeferUntilRendererLeaseCloses|TestRunServerOwnsTheForegroundUntilContextCancellation)$'` passed (`cmd/ycy` 0.465s; `internal/commands/tunnel` 1.194s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.530s, `internal/logging` 0.779s, `internal/cliapp` 0.720s, `cmd/ycy` 74.331s, `config/fork` 2.655s, `config/cm` 2.387s, `git/heat` 1.778s, `diff` 2.180s, `fs` 11.969s, `tunnel` 63.867s, `upgrade` 15.548s, `run` 3.532s); `git diff --check` passed. Risk: the continuous service path is unchanged; evidence demonstrates its shared-terminal integration but static report migrations remain pending. Next: migrate `config fork list` as its own Rich-static-report slice.
- 2026-08-27: Slice `G3.2 config fork list` moved the safe list projection from its command-owned stdout renderer to the shared terminal boundary. `internal/commands/config/fork` now returns `Result.Instances` after reading only; its handwritten `presentation.go` renderer and matching tests were removed. `cmd/ycy/configfork.go` owns the new semantic adapter: Plain Interactive and Automation reproduce the frozen tabular bytes exactly, while Rich renders a title, muted headings, safe rows, and a success count; `NO_COLOR` retains that Rich layout without styles. `cmd/ycy/main.go` supplies the one root Experience. Focused tests in `internal/commands/config/fork/run_test.go` and `cmd/ycy/configfork_test.go` prove safe result projection, exact Plain/Automation bytes and absence of control sequences, and Rich semantic roles/`NO_COLOR`; standalone regression coverage remains in `cmd/ycy/configfork_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/fork ./cmd/ycy -run '^(TestModuleReturnsTheSafeForkList|TestModuleReturnsReaderFailure|TestNewRequiresAReader|TestTerminalForkListPresentationPreservesPlainAndAutomationResults|TestTerminalForkListPresentationUsesRichSemanticRoles)$'` passed (`config/fork` 0.401s; `cmd/ycy` 0.437s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.490s, `internal/logging` 0.737s, `internal/cliapp` 2.354s, `cmd/ycy` 75.123s, `config/fork` 1.723s, `config/cm` 2.215s, `git/heat` 2.131s, `diff` 3.826s, `fs` 11.760s, `tunnel` 65.464s, `upgrade` 15.389s, `run` 2.909s); `git diff --check` passed and `rg 'configfork\\.Render|func Render\\(' internal/commands/config/fork cmd/ycy/configfork.go cmd/ycy/configfork_test.go` found no renderer residue. Risk: the returned list projection is internal-only and preserves only the existing safe fields; CM static journeys remain separate. Next: migrate `config cm list` as its own Rich-static-report slice.
- 2026-08-27: Slice `G3.3 config cm list` moved the safe CM profile list projection from the command-owned stdout renderer to the shared terminal boundary. `internal/commands/config/cm` now returns `Result.Profiles` after read-only resolution; its handwritten list `presentation.go` renderer and matching tests were removed. `cmd/ycy/configcm.go` supplies the semantic adapter: Plain Interactive and Automation retain the original ordered marker/profile/model/URL bytes exactly, while Rich adds a title, muted headings, and a success role for the default profile; `NO_COLOR` retains Rich layout without styles. The retained `terminalCMPresenter` remains exclusively for G3-unmigrated add/use/set/remove journeys. Focused tests in `internal/commands/config/cm/run_test.go` and `cmd/ycy/configcm_test.go` prove safe projection, exact Plain/Automation output without control bytes, and Rich semantic roles/`NO_COLOR`; existing standalone coverage remains. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm ./cmd/ycy -run '^(TestModuleReturnsTheSafeCMList|TestModuleReturnsReaderFailure|TestNewRequiresAReader|TestTerminalCMListPresentationPreservesPlainAndAutomationResults|TestTerminalCMListPresentationUsesRichSemanticRoles)$'` passed (`config/cm` 0.413s; `cmd/ycy` 0.473s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.388s, `internal/logging` 0.164s, `internal/cliapp` 0.406s, `cmd/ycy` 64.863s, `config/fork` 1.333s, `config/cm` 1.585s, `git/heat` 1.101s, `diff` 1.297s, `fs` 6.984s, `tunnel` 55.536s, `upgrade` 10.767s, `run` 1.988s); `git diff --check` passed and `rg 'configcm\\.Render|func Render\\(' internal/commands/config/cm cmd/ycy/configcm.go cmd/ycy/configcm_test.go` found no list-renderer residue. Risk: the shared CM presenter is deliberately retained for other authorized-but-not-yet-migrated CM journeys. Next: migrate `config cm use` as a separate write-result slice.
- 2026-08-27: Slice `G3.4 config cm use` moved the post-write result from the command module's handwritten presenter to the shared terminal boundary. `UseModule` now returns the selected profile only after `SetDefaultCMProfile` succeeds; `UsePresenter`, `PresentUseSuccess`, and their unit test were removed. `cmd/ycy/configcm.go` opens an Experience only after that write completes, preserving the exact Plain/Automation `Default CM profile set to <profile>` result, while Rich uses the success visual role and `NO_COLOR` retains the layout without terminal control. Focused module and adapter tests prove no result on failed selection, original Plain/Automation bytes, Rich role, and no-control output for non-terminal writers; existing standalone persistence/error coverage remains. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm ./cmd/ycy -run '^(TestUseModuleSelectsTheRequestedProfile|TestUseModuleReturnsSelectionFailuresWithoutAResult|TestNewUseRequiresTheCommandOwnedWriter|TestTerminalCMUsePresentationPreservesPlainAndAutomationResults|TestTerminalCMUsePresentationUsesARichSuccessRole)$'` passed (`config/cm` 0.427s; `cmd/ycy` 0.433s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.501s, `internal/logging` 0.732s, `internal/cliapp` 0.850s, `cmd/ycy` 74.297s, `config/fork` 1.497s, `config/cm` 1.697s, `git/heat` 3.063s, `diff` 2.437s, `fs` 11.871s, `tunnel` 63.289s, `upgrade` 15.339s, `run` 3.599s); `git diff --check` passed and `rg 'UsePresenter|PresentUseSuccess' internal/commands/config/cm cmd/ycy` found no residue. Risk: only the success presentation moved; set/test remain distinct G3 journeys. Next: migrate `config cm set` as a separate write-result slice.
- 2026-08-27: Slice `G3.5 config cm set` moved the post-update result from the command module's handwritten presenter to the shared terminal boundary. `SetModule` now returns its profile only after the exact `SetCMProfileValue` request succeeds; `SetPresenter`, `PresentSetSuccess`, and their test were removed. `cmd/ycy/configcm.go` presents `Profile <profile> updated` through its Experience after a successful update, preserving exact Plain/Automation result bytes and using the Rich success role with `NO_COLOR` layout retained. Focused tests prove no command result on field-update failure, unchanged request forwarding, Plain/Automation no-control output, and Rich semantic output; existing standalone tests retain encrypted-key and validation coverage. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm ./cmd/ycy -run '^(TestSetModuleUpdatesOnlyTheProfile|TestSetModuleReturnsFailuresWithoutAResult|TestNewSetRequiresTheCommandOwnedWriter|TestTerminalCMSetPresentationPreservesPlainAndAutomationResults|TestTerminalCMSetPresentationUsesARichSuccessRole)$'` passed (`config/cm` 0.405s; `cmd/ycy` 0.423s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.418s, `internal/logging` 0.159s, `internal/cliapp` 0.979s, `cmd/ycy` 64.123s, `config/fork` 0.729s, `config/cm` 0.582s, `git/heat` 1.057s, `diff` 1.703s, `fs` 7.805s, `tunnel` 54.956s, `upgrade` 10.858s, `run` 1.871s); `git diff --check` passed and `rg 'SetPresenter|PresentSetSuccess' internal/commands/config/cm cmd/ycy` found no residue. Risk: only the durable success result moved; CM provider-test outcomes remain a separate G3 journey. Next: migrate `config cm test` as a separate response/diagnostic slice.
- 2026-08-27: Slice `G3.6 config cm test` moved both safe provider outcomes from the command module's handwritten presenter to the shared terminal boundary without changing provider transport or Cobra error handling. `TestModule` now returns redacted success content or a safe `TestDiagnostic` together with the original redacted provider error; `TestPresenter` and `terminalCMTestPresenter` were removed. The adapter presents the result before returning a provider error, preserving Plain/Automation success bytes (`Response: ...` then `Done`) and failed-provider stdout diagnostic before Cobra's unchanged stderr `error:` line. Rich adds title/semantic response, success, warning, and muted diagnostic roles; `NO_COLOR` preserves layout without styles. Focused tests prove resolver failures have no result, successful and failed provider results remain redacted/safe, exact Plain/Automation stream-safe text, and Rich semantic roles; existing standalone tests prove local provider request, response, safe failure diagnostic, and redacted error behavior. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm ./cmd/ycy -run '^(TestCMTestModuleResolvesAndRunsTheProvider|TestCMTestModuleReturnsOnlySafeFailureDiagnostics|TestCMTestModuleDoesNotReturnAResultForResolverFailure|TestCMTestModuleRedactsTheProfileAPIKeyFromSuccessfulProviderContent|TestNewCMTestModuleRequiresEachCommandOwnedAdapter|TestTerminalCMTestPresentationPreservesPlainAndAutomationResults|TestTerminalCMTestPresentationUsesRichSemanticRoles)$'` passed (`config/cm` 0.410s; `cmd/ycy` 0.429s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.471s, `internal/logging` 0.693s, `internal/cliapp` 0.732s, `cmd/ycy` 71.910s, `config/fork` 1.199s, `config/cm` 1.604s, `git/heat` 2.919s, `diff` 2.319s, `fs` 11.575s, `tunnel` 61.659s, `upgrade` 15.058s, `run` 3.556s); `git diff --check` passed and `rg 'TestPresenter|terminalCMTestPresenter' internal/commands/config/cm cmd/ycy` found no residue. Risk: provider-error precedence remains deliberately unchanged and the result is limited to already-safe fields. Next: migrate `git heat` as its own static-report slice.
- 2026-08-27: Slice `G3.7 git heat` moved the static report from the command module's terminal renderer to the shared terminal boundary. `internal/commands/git/heat` now owns Git execution, report construction, and the fixed clock only, returning `Result{Report, Now}`; its `Presenter`, `RenderReport`, color-query renderer, and presentation tests were removed. `cmd/ycy/githeat.go` maps semantic report fields to a terminal document: Plain Interactive and Automation retain the exact legacy title/table/legend bytes with no ANSI; Rich receives an approved title clear, semantic summary/header/row/legend roles, and query-matched rows use the active role; `NO_COLOR` retains clear/layout but emits no style bytes. Focused module and adapter tests prove repository/argument behavior, result clock, exact Plain/Automation report and empty bytes, Rich roles, title clear, and `NO_COLOR` styling boundary. Existing standalone repository, `NO_COLOR`, validation, and failure tests remain. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/heat ./cmd/ycy -run '^(TestNewRequiresEveryCommandBoundary|TestModuleRunComposesInputGitReportAndResultTime|TestModuleRunStopsAtErrorsWithoutAResult|TestModuleRunDoesNoWorkWhenContextIsCancelledOrInputIsInvalid|TestTerminalGitHeatPresentationPreservesPlainAndAutomationResults|TestTerminalGitHeatPresentationUsesRichSemanticRoles)$'` passed (`git/heat` 0.189s; `cmd/ycy` 0.438s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.818s, `internal/logging` 0.358s, `internal/cliapp` 0.927s, `cmd/ycy` 72.423s, `config/fork` 1.399s, `config/cm` 1.835s, `git/heat` 2.859s, `diff` 2.565s, `fs` 11.852s, `tunnel` 63.505s, `upgrade` 15.685s, `run` 3.453s); `git diff --check` passed and `rg 'RenderReport|Presenter|terminalGitHeatPresenter|newGitHeatModule' internal/commands/git/heat cmd/ycy/githeat.go cmd/ycy/githeat_test.go` found no residue. Risk: Rich query emphasis deliberately colors the complete matched row instead of reproducing the old ANSI substring renderer; Plain/Automation report bytes remain frozen. Next: migrate `upgrade` as its own deliberate mixed-output slice.
- 2026-08-27: Slice `G3.8 upgrade mixed results` moved Upgrade's command-result and classified-failure presentation from `internal/commands/upgrade` to the shared terminal boundary. `UpgradeOptions` no longer accepts output writers; `RunUpgrade` returns only semantic state (`PreviousState`), current/scheduled version facts, transaction state, and the explicit classified-abort fact. Its typed `ExitCodeError` still preserves ordinary failure code `1` and HTTP/checksum/empty-download code `0`. `cmd/ycy/upgrade.go` now presents prior completed state, current, scheduled, and `Update aborted.` results through one Experience Run; Plain Interactive and Automation retain exact stdout bytes with no terminal control, and classified failures retain the deliberate stderr `error:` then stdout abort sequence with redaction. Rich uses the approved title clear plus success/warning roles; `NO_COLOR` retains the layout without style bytes. `main.go` creates the root Experience before startup-state consumption, so a completed one-time state uses that same presentation boundary while a pending update remains an unstyled stderr diagnostic followed by the existing `error: update is still pending` exit. Focused tests prove module semantic results, exit classifications, preserved previous-state-on-abort behavior, startup completed/pending streams, exact Plain/Automation mixed output, redaction, Rich roles, and `NO_COLOR`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/upgrade ./cmd/ycy -run '^(TestRunUpgradeSchedulesOnlyVerifiedCandidate|TestRunUpgradeAlreadyCurrentAndFailureCleanup|TestClassifyUpgradeErrorPreservesTheDeliberateExitContract|TestRunUpgradeReturnsPriorStateAndAbortForClassifiedFailure|TestConsumeStateDoesNotReadAdjacentState|TestHiddenUpgradeRouteIsolatedFromOrdinaryArguments|TestVersionFlagsSkipStartupResultConsumption|TestConsumeUpgradeStartupReportsCompletedAndBlocksPending|TestTerminalUpgradePresentationPreservesPlainAndAutomationResults|TestTerminalUpgradePresentationUsesRichSemanticRoles|TestUpgradeHandlerConstructsWithoutExternalTools)$'` passed (`upgrade` 0.487s; `cmd/ycy` 0.588s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.870s, `internal/logging` 0.405s, `internal/cliapp` 0.963s, `cmd/ycy` 72.641s, `config/fork` 1.420s, `config/cm` 3.001s, `git/heat` 2.292s, `diff` 2.733s, `fs` 12.512s, `tunnel` 63.692s, `upgrade` 15.539s, `run` 3.499s); `git diff --check` passed. Risk: no updater lifecycle or network classification behavior changed; only its terminal ownership moved, so future Diff/FS migration must continue to leave browser readiness and foreground lifecycle outside a transient renderer. Next: migrate `diff` as its own lifecycle-presentation slice.
- 2026-08-27: Slice `G3.9 diff lifecycle presentation` removed Diff's handwritten stdout presenter and split the command module at its real lifecycle boundary. `internal/commands/diff.Module.Start` now owns workspace/server startup and returns an `Operation` with safe `Startup` facts; `Operation.Wait` retains the foreground server until signal cancellation or server completion, and `Close` handles a presentation failure. `cmd/ycy/diff.go` owns the shared Experience adapter between `Start` and `Wait`: Plain Interactive and Automation preserve the exact existing URL/MCP/baseline/target stdout bytes with no terminal control; Rich applies only active/muted semantic roles without a title clear or transient view; `NO_COLOR` keeps the same layout without styles. `main.go` now binds the resulting typed handler. Focused module tests prove bound URL facts, pre-start cancellation, and cancellation-as-success; adapter tests prove exact Plain/Automation output, empty stderr, Rich roles, and `NO_COLOR`; the standalone test continues to discover readiness incrementally from stdout while the server is alive, requests its HTTP state, delivers SIGINT, and now asserts the entire successful lifecycle leaves stderr empty. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff ./cmd/ycy -run '^(TestModuleStartsBoundComparisonThenTreatsContextCancellationAsSuccess|TestModuleDoesNothingForAnAlreadyCanceledContext|TestDiffModuleRequiresItsNetworkAdapter|TestDiffStartupURLsPreserveLocalAndPublicIPv4Rules|TestComparisonSessionBuildsResolvedStartupPresentation|TestTerminalDiffPresentationPreservesPlainAndAutomationReadiness|TestTerminalDiffPresentationUsesRichSemanticRoles|TestDiffStandaloneBinaryPreservesCLIValidationAndLifecycle)$'` passed (`diff` 0.433s; `cmd/ycy` 2.842s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.832s, `internal/logging` 0.370s, `internal/cliapp` 0.838s, `cmd/ycy` 78.116s, `config/fork` 1.322s, `config/cm` 2.118s, `git/heat` 2.039s, `diff` 2.780s, `fs` 12.405s, `tunnel` 65.883s, `upgrade` 14.798s, `run` 3.577s); `git diff --check` passed and `rg 'terminalDiffPresenter|diff presenter is required|Presenter:|\.Run\(ctx, input\)' cmd/ycy/diff.go cmd/ycy/main.go internal/commands/diff -g '*.go'` found no presenter residue. Risk: Diff deliberately remains a durable stdout lifecycle and holds no Renderer Lease; the next FS slice must preserve its own stopped outcome and readiness stream independently. Next: migrate `fs` as its own lifecycle-presentation slice.

- 2026-08-27: Slice `G3.10 fs lifecycle presentation` removed the handwritten `terminalFSPresenter` and split FS at the server lifecycle boundary. `internal/commands/fs.Module.Start` now returns a `BrowserOperation` with safe `Startup` facts; `Wait` retains the foreground browser until signal cancellation or server completion, and `Close` releases it when startup presentation fails. `cmd/ycy/fs.go` owns shared Experience presentation between `Start` and `Wait`: Plain Interactive and Automation retain exact legacy readiness/configuration bytes and print `File Browser stopped.` only after a successful lifecycle wait; Rich uses active startup and muted configuration roles plus a success stopped role, with `NO_COLOR` unstyled and no transient renderer. `cmd/ycy/main.go` binds the typed FS handler. Focused module and adapter tests prove bound facts, pre-start cancellation, close/wait cleanup, exact Plain/Automation readiness and stopped output, no-control/no-stderr output, Rich roles, `NO_COLOR`, and cleanup after a presentation write failure; the standalone test retains incremental stdout readiness while alive, HTTP reachability, SIGINT cleanup, and now asserts empty stderr. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs ./cmd/ycy -run '^(TestModuleStartsBoundFileBrowserThenTreatsContextCancellationAsSuccess|TestModuleDoesNothingForAnAlreadyCanceledContext|TestOperationCloseStopsItsRuntime|TestFSModuleRequiresItsNetworkAdapter|TestTerminalFSPresentationPreservesPlainAndAutomationLifecycle|TestTerminalFSPresentationUsesRichSemanticRoles|TestFSHandlerClosesTheOperationWhenStartupPresentationFails)$'` passed (`fs` 0.691s; `cmd/ycy` 1.055s); standalone `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^TestFSStandaloneBinaryPreservesCLIHTTPAndSignalLifecycle$'` passed (2.730s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.462s, `internal/logging` 0.709s, `internal/cliapp` 1.091s, `cmd/ycy` 75.285s, `config/fork` 0.838s, `config/cm` 1.644s, `git/heat` 2.646s, `diff` 3.129s, `fs` 12.389s, `tunnel` 64.966s, `upgrade` 15.678s, `run` 3.646s); `git diff --check` passed and the FS presenter-residue search returned no matches. Risk: FS remains a durable stdout lifecycle with no Renderer Lease; only Run Raw Child I/O preservation evidence remains before Gate repository verification. Next: execute and record the dedicated Run Raw Child I/O preservation proof.
- 2026-08-27: Slice `G3.11 Run Raw Child I/O preservation` required no implementation change: `cmd/ycy/main.go` continues to construct Run with the original `terminalRoot.input`, `terminalRoot.output`, and `terminalRoot.diagnostics`, while `cmd/ycy/run_process.go` assigns those streams directly to `exec.Cmd` and neither `internal/commands/run` nor `cmd/ycy/run*.go` imports the terminal Experience or a renderer. Focused evidence proves exact child argv, cwd, inherited stdin, independently observable stdout/stderr, no ycy-injected terminal control, missing-executable handling, normal and signal exit-code mapping, Unix process-group cleanup without an orphan, and standalone project execution/child exit propagation. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/run ./cmd/ycy -run '^(TestChildRequestUsesAnArgvPackageManagerInvocation|TestRunComposesDiscoverySelectionPresentationAndChildRequest|TestOSRunChildRunnerUsesArgvCWDAndInheritedStreams|TestOSRunChildRunnerPreservesMissingExecutableFailure|TestOSRunChildRunnerMapsExitCodesAndSignals|TestOSRunChildRunnerStopsTheChildProcessGroupWithoutAnOrphan|TestRunStandaloneBinaryPreservesProjectExecutionAndParserBehavior)$'` passed (`run` 0.150s; `cmd/ycy` 3.930s). Required G3 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` (`internal/terminal` 5.488s, `internal/logging` 0.714s, `internal/cliapp` 0.876s, `cmd/ycy` 73.591s, `config/fork` 2.732s, `config/cm` 3.148s, `git/heat` 2.853s, `diff` 3.842s, `fs` 12.423s, `tunnel` 63.357s, `upgrade` 15.730s, `run` 4.071s); `git diff --check` passed. Risk: this is preservation evidence only; Run selection migration remains explicitly deferred to G4. Next: run G3 Repository verification in the required `make check`, then `make build`, then `make cross-build` order.
- 2026-08-27: Repository verification `G3.12 check`: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make check` passed. It verified all modules and active-toolchain isolation, completed Web ESLint/typecheck/33 tests/Vite asset verification, and passed the full Go suite including `cmd/ycy` (67.939s), FS (6.525s), Diff (2.106s), Upgrade (9.318s), Run, and Tunnel (56.323s). The existing Vite chunk-size warning remained non-fatal. Risk: local and six-target build evidence remains required. Next: run `make build`.
- 2026-08-27: Repository verification `G3.13 build`: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make build` passed. It completed Web ESLint/typecheck/33 tests/Vite asset verification and built the local CGO-free CLI artifact. The existing Vite chunk-size warning remained non-fatal. Risk: six-target compilation remains required. Next: run `make cross-build`.
- 2026-08-27: Repository verification `G3.14 cross-build`: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make cross-build` passed. It completed the same Web ESLint/typecheck/33-test/Vite asset verification and produced all six CGO-free cross-target CLI artifacts: macOS x64/arm64 Mach-O, Linux x64/arm64 static ELF, and Windows x64/arm64 PE console executables. The existing Vite chunk-size warning remained non-fatal. Risk: no repository-verification risk remains. Next: record final G3 acceptance.
- 2026-08-27: Final acceptance `G3.15`: all G3 Exit conditions are met. Tunnel server retains the shared continuous stderr Diagnostic Event path with text/NDJSON, redaction, NO_COLOR, lease ordering, and shutdown evidence. Config Fork list; Config CM list/use/set/test; Git Heat; and Upgrade now use the shared terminal presentation boundary, preserve their approved Plain Interactive and Automation stream/exit behavior, and have removed their matching handwritten static presenters. Diff and FS now expose durable startup facts through the shared boundary while preserving stdout readiness during their foreground lifecycle, HTTP reachability, signal cleanup, and no competing transient renderer; their old presenters are absent. Run remains untouched for later selector migration but has dedicated Raw Child I/O proof for argv, cwd, inherited stdin, independent stdout/stderr, child exit/signal semantics, and Unix process-group cleanup without renderer involvement. Final Directed evidence: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/logging ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/git/heat ./internal/commands/diff ./internal/commands/fs ./internal/commands/tunnel ./internal/commands/upgrade ./internal/commands/run` passed (`internal/terminal` 5.488s, `internal/logging` 0.714s, `internal/cliapp` 0.876s, `cmd/ycy` 73.591s, `config/fork` 2.732s, `config/cm` 3.148s, `git/heat` 2.853s, `diff` 3.842s, `fs` 12.423s, `tunnel` 63.357s, `upgrade` 15.730s, `run` 4.071s). Repository verification passed in required order: `make check`, `make build`, then `make cross-build`; artifact inspection confirmed local macOS arm64 plus six cross-target binaries. Final audit found no named legacy adapter residue and no terminal/Charmbracelet import in the relevant command modules; `git diff --check` passed. Manual acceptance is `无`. G3 is `passed`; G4 is activated and has not started. Next: end this Goal; a new Goal may handle G4.

### G4: Migrate Ordinary Form Journeys

- 2026-08-27: initialized as `planned`; no implementation evidence.
- 2026-08-27: activated after G3 final acceptance; implementation has not started.
- 2026-08-27: Slice `G4.1 config fork add` migrated the five-question add journey to a thin `cmd/ycy` adapter over the shared `ExperienceRun.Ask` and `Present` primitives. Alias/host text, provider/protocol selections, and the access-token secret preserve command-owned order and validation; Rich receives semantic success/warning roles, Plain Interactive keeps line-oriented diagnostics/results, and non-terminal secret input is rejected before any prompt bytes. Automation fails before opening configuration or reading stdin with `error: config fork add requires an interactive terminal`; cancellation remains a successful no-write result. The old `terminalForkAddPrompter` and `terminalForkAddPresenter` were removed; the un-migrated fork-remove selector retained its own standalone reader. Focused verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/fork` and `./cmd/ycy` focused adapter, Automation, standalone add, and remove regression tests; shared Plain secret boundary test passed; `git diff --check` passed. Required G4 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel` (`internal/terminal` 5.289s, `internal/cliapp` 0.401s, `cmd/ycy` 71.018s, `config/fork` 1.201s, `config/cm` 2.335s, `exportenv` 1.750s, `rm` 1.422s, `run` 2.799s, `zip` 2.468s, `tunnel` 61.435s). Risk: native Rich PTY coverage for this command is inherited from the shared Huh/secret runtime; command-specific PTY form evidence remains a Gate-level residual to audit. Next: migrate `config fork remove` as the next ordered, independently reversible slice.
- 2026-08-27: Slice `G4.2 config fork remove` migrated the selection, destructive confirmation, and outcomes to one shared `ExperienceRun`. The command module now propagates interaction errors without importing terminal code; its adapter maps selection and confirmation to semantic requests, preserves the default-first selection and default-no confirmation, maps terminal cancellation to the existing successful `Cancelled` outcome, and presents empty/cancelled/success results through the shared boundary. Automation first reads the secret-safe instance projection: empty configuration retains `No instances configured` / `Nothing to remove`, while a nonempty configuration fails before selection, confirmation, or deletion with `error: config fork remove requires an interactive terminal` and no stdout. The matching handwritten `terminalForkRemovePrompter` and `terminalForkRemovePresenter` are removed. Focused tests prove Rich semantic requests and roles; Plain selection/confirmation, invalid selection and confirmation validation, decline, cancellation, and mutation behavior; Automation empty/nonempty stream and no-write behavior; and standalone redirected behavior. The initial Directed run exposed an obsolete redirected-EOF cancellation assertion in `TestConfigForkListStandaloneBinary`; it was updated to the approved Automation failure contract. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/fork`; focused `./cmd/ycy -run '^(TestConfigForkListStandaloneBinary|TestConfigForkRemoveStandaloneBinary|TestTerminalForkRemove.*)$'`; and `git diff --check` passed. Required G4 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel` (`internal/terminal` 5.486s, `internal/cliapp` 0.591s, `cmd/ycy` 69.353s, `config/fork` 2.140s, `config/cm` 2.364s, `exportenv` 1.726s, `rm` 1.883s, `run` 0.607s, `zip` 1.346s, `tunnel` 57.218s). Risk: command-specific native Rich PTY evidence remains Gate-level work; no destructive semantics or configuration grammar changed. Next: migrate `config cm add` as the next ordered, independently reversible slice.
- 2026-08-27: Slice `G4.3 config cm add` migrated the ordered profile-name, base-URL, model, and API-key form to a thin shared `ExperienceRun` adapter. The adapter maps the API key to `InteractionSecret`, retains all command-owned validation and cancellation outcomes, presents success/cancellation through the shared document boundary, and gives Rich success/warning semantic roles while Plain remains line-oriented and control-free. Automation fails before configuration creation or stdin access with `error: config cm add requires an interactive terminal`; no API key is written or echoed. The old `terminalCMAddPrompter` is removed. Its formerly shared raw reader and presenter were split so the still-unmigrated `config cm remove` behavior remains unchanged, without retaining an add fallback. Focused tests prove semantic request order/placeholders/validation, Rich roles, Plain diagnostic/result stream separation, cancellation, Automation no-read/no-write behavior, standalone redirected behavior, and `config cm remove` regression behavior; residue search found no `terminalCMAddPrompter`, `newTerminalCMAddPrompter`, or `terminalCMPresenter`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`; focused `./cmd/ycy -run '^(TestTerminalCMAdd.*|TestConfigCMAddStandaloneBinary|TestTerminalCMRemove.*|TestConfigCMRemoveStandaloneBinary)$'`; and `git diff --check` passed. Required G4 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel` (`internal/terminal` 5.537s, `internal/cliapp` 0.569s, `cmd/ycy` 74.201s, `config/fork` 2.633s, `config/cm` 1.674s, `exportenv` 1.247s, `rm` 1.806s, `run` 2.881s, `zip` 2.187s, `tunnel` 61.998s). Risk: native Rich PTY form evidence remains Gate-level work; no configuration grammar, persistence semantics, or secret-input policy expanded. Next: migrate `config cm remove` as the next ordered, independently reversible slice.
- 2026-08-27: Slice `G4.4 config cm remove` migrated the destructive confirmation and result rendering to one shared `ExperienceRun`. The CM module now first checks the secret-safe profile projection, then asks the command-owned confirmation, propagating interaction failures without importing terminal code; valid confirmation still deletes exactly one profile and presents the existing success result, while decline and cancellation remain successful non-mutating `Cancelled` outcomes. In Automation, a missing profile preserves `error: CM profile not found: <name>` before prompting; a valid profile fails before confirmation or deletion with `error: config cm remove requires an interactive terminal`, no stdout, no stdin read, and no mutation. The old `terminalCMRemovePrompter` and `terminalCMRemovePresenter` are removed. Focused tests prove Rich confirmation semantics and presentation roles; Plain validation, decline, cancellation, result stream separation, and mutation boundaries; Automation validation order/no-read/no-write behavior; and redirected standalone behavior. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm` passed (0.437s); focused `./cmd/ycy -run '^(TestTerminalCMRemove|TestConfigCMRemove)'` passed (3.074s); `git diff --check` passed; no legacy CM-remove adapter/presenter residue or terminal/Charmbracelet import remains in `internal/commands/config/cm`. Required G4 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel` (`internal/terminal` 5.479s, `internal/cliapp` 0.476s, `cmd/ycy` 72.251s, `config/fork` 0.949s, `config/cm` 2.983s, `exportenv` 2.286s, `rm` 1.167s, `run` 2.640s, `zip` 1.975s, `tunnel` 60.166s). Risk: command-specific native Rich PTY evidence remains Gate-level work; no command grammar, confirmation semantics, or persistence behavior changed. Next: migrate `export env` as the next ordered, independently reversible slice.
- 2026-08-27: Slice `G4.5 export env` replaced the handwritten numbered selector and static presenter with one `terminalExportEnvAdapter` over an `ExperienceRun`. The command module now lets selector failure propagate before any environment-file read, output presentation, or `--out` write. Existing `--env` resolution remains unchanged; an Automation Session also proceeds without stdin when configuration yields exactly one environment choice, while an ambiguous selector path fails before effects with `error: export env requires an interactive terminal`. Plain cancellation preserves the command's successful `Cancelled` result and leaves an existing output target unchanged. Command-owned cancellation values (`q`, `quit`, `cancel`, and blank) are now supplied through `InteractionRequest.CancelValues`; Rich presentation uses muted, plain, and warning semantic roles, while Plain and Automation retain exact line-oriented stdout. Modified `cmd/ycy/exportenv.go`, `cmd/ycy/exportenv_test.go`, `cmd/ycy/main.go`, `internal/commands/exportenv/selection.go`, `internal/commands/exportenv/selection_test.go`, `internal/commands/exportenv/run_test.go`, and shared terminal interaction semantics/tests. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/commands/exportenv` passed (`terminal` 5.606s; `exportenv` 0.364s); focused `./cmd/ycy -run '^(TestTerminalExportEnv|TestExportEnv)'` passed (2.910s); `git diff --check` and `gofmt -d` passed; no `terminalExportEnvSelector`, `terminalExportEnvPresenter`, or constructor residue remains. Required G4 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel` (`terminal` 5.388s, `cliapp` 0.391s, `cmd/ycy` 72.505s, `config/fork` 0.789s, `config/cm` 2.056s, `exportenv` 1.109s, `rm` 2.585s, `run` 1.494s, `zip` 1.017s, `tunnel` 58.890s). Risk: unique environment resolution remains deliberately tied to the existing discovery output; no new Automation input grammar or inferred stdin value was added. Next: migrate `rm` as the next ordered, independently reversible slice.
- 2026-08-27: Slice `G4.6 rm` replaced the handwritten stdin prompter and stdout presenter with one `terminalRMAdapter` over an `ExperienceRun`. The rm command module now propagates interaction failure before deletion. The adapter preserves command-owned Plain input grammar for default-negative confirmation, numbered smart action selection, default-all smart target selection, `all`/`none`, `q`/`quit`/`cancel`, and validation feedback; Rich receives semantic confirmation/select/multiselect requests and role-based presentation. Automation stages existing resolved-path results until the journey completes: `--force` and no-valid-target paths retain their existing stdout result, while explicit confirmation and smart-selection paths fail before stdin reads, partial Command Result output, or deletion with `error: rm requires an interactive terminal`. The old `newTerminalRMPrompter`, `terminalRMPresenter`, and raw prompt parsing helpers are removed from production. Modified `cmd/ycy/rm.go`, `cmd/ycy/rm_test.go`, `cmd/ycy/main.go`, `internal/commands/rm/prompt.go`, `internal/commands/rm/prompt_test.go`, `internal/commands/rm/run.go`, `internal/commands/rm/run_test.go`, and shared terminal interaction semantics/tests. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/commands/rm` passed (`terminal` 5.409s; `rm` 0.167s); focused `./cmd/ycy -run '^(TestTerminalRM|TestRM)'` passed (2.744s); shared Plain parser/prompt-layout tests passed; `git diff --check` and `gofmt -d` passed; legacy rm production-adapter residue search returned no matches. Required G4 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel` (`terminal` 5.267s, `cliapp` 1.188s, `cmd/ycy` 73.036s, `config/fork` 1.663s, `config/cm` 2.104s, `exportenv` 2.219s, `rm` 2.340s, `run` 0.789s, `zip` 2.737s, `tunnel` 62.130s). Risk: command-owned Plain grammar is carried explicitly through the generic request solely to preserve existing behavior; no Automation grammar, implicit input, confirmation semantic, or deletion ordering was added. Next: migrate `run` selection before Raw Child I/O as the next ordered, independently reversible slice.
- 2026-08-27: Slice `G4.7 run selection` replaced the handwritten script/package-manager prompter and presenter with one `terminalRunAdapter` over an `ExperienceRun`. The Run module now propagates selection failure before a child request is created. The adapter preserves the legacy Plain numbered selection, script hints, `q`/`quit`/`cancel`/blank cancellation, and validation feedback while Rich receives semantic script and package-manager selections with role-based durable presentation. `releasedRunChildRunner` closes the Experience Run immediately before calling the unchanged OS child runner, so the child receives the original raw stdin, stdout, stderr, argv, cwd, exit and signal behavior without a renderer. Automation fails before reading stdin, presenting any Command Result, or launching a child with `error: run requires an interactive terminal`. The old `newTerminalRunPrompter`, `terminalRunPresenter`, and raw selector helpers are removed from production. Modified `cmd/ycy/run.go`, `cmd/ycy/run_test.go`, `cmd/ycy/main.go`, `internal/commands/run/prompt.go`, `internal/commands/run/prompt_test.go`, `internal/commands/run/run.go`, and `internal/commands/run/run_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/run ./cmd/ycy -run '^(TestTerminalRun|TestRunPlain|TestRunAutomation|TestRunStandalone|TestReleasedRun|TestSelectorsReturnPromptFailures|TestOSRunChildRunner)'` passed (`run` 0.176s; `cmd/ycy` 3.772s); complete focused `./internal/terminal ./internal/commands/run` passed (`terminal` 5.494s; `run` 0.512s), and `./cmd/ycy -run '^(TestTerminalRun|TestRun|TestOSRunChildRunner)'` passed (3.263s); `git diff --check` and `gofmt -d` passed; no old Run production adapter residue remains. Required G4 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel` (`terminal` 5.265s, `cliapp` 0.400s, `cmd/ycy` 70.368s, `config/fork` 1.583s, `config/cm` 2.049s, `exportenv` 1.074s, `rm` 2.695s, `run` 1.970s, `zip` 2.355s, `tunnel` 60.671s). Risk: raw child streams remain deliberately outside the Experience after form release; no child execution grammar, environment lookup, process group, exit, or signal semantics changed. Next: migrate `zip` as the next ordered, independently reversible slice.
- 2026-08-27: Slice `G4.8 zip` replaced `terminalZipPrompter` and `terminalZipPresenter` with one `terminalZipAdapter` over an `ExperienceRun`; `main` now installs a per-command `newZipHandler`. The ZIP module's command-owned prompter interface now propagates interaction errors, so an unavailable form stops before archive collection, publication, or reveal. The adapter retains the Plain numbered package/source selection, first-choice blank defaults, `q`/`quit`/`cancel` cancellation, numbered validation, glob blank/`all`/`none` initial-value behavior, and trimmed output-name default; Rich receives semantic select, multiselect, text, and role-based presentation documents. Automation stages all presentation and fails its first prompt with `error: zip requires an interactive terminal`, before stdin reads, partial stdout, archive creation, or reveal. The legacy redirected standalone archive expectation was updated to the approved Automation failure contract, while a Plain Interactive journey retains structural archive evidence. Modified `cmd/ycy/zip.go`, `cmd/ycy/zip_test.go`, `cmd/ycy/main.go`, `internal/commands/zip/run.go`, and `internal/commands/zip/run_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/zip` passed (0.435s); focused `./cmd/ycy -run '^(TestTerminalZipAdapter.*|TestZIPPlainJourney.*|TestZIPAutomation.*|TestZIPStandalone.*|TestZipRemoteNameResolver.*|TestOSZipRemoteOutputRunner.*|TestHostZipRevealer.*)$'` passed (3.263s); `git diff --check` and `gofmt -d` passed. Required G4 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel` (`terminal` 5.482s, `cliapp` 0.498s, `cmd/ycy` 71.481s, `config/fork` 1.293s, `config/cm` 2.916s, `exportenv` 1.519s, `rm` 1.874s, `run` 2.218s, `zip` 2.367s, `tunnel` 60.864s). Risk: ZIP's archive/reveal and command-owned planning state remain unchanged; the only remaining G4 journey is Tunnel connection selection before its existing continuous diagnostic stream. Next: migrate `tunnel connect` selection only.
- 2026-08-27: Slice `G4.9 tunnel connect selection` replaced the handwritten `terminalTunnelConnectionSelector` with a session-aware `terminalTunnelConnectionAdapter` over an `ExperienceRun`. The root handler now supplies the shared Experience; it opens and closes a selection run inside the resolver callback, so the form is fully released before unchanged `RunClient` continuous diagnostics begin. Rich receives a semantic select with masked connection labels and a warning cancellation result; Plain preserves numbered-only input, invalid-selection feedback, blank/`q`/`quit`/`cancel`/EOF cancellation, token masking, and a durable `Cancelled` result. Automation supplies no selector, therefore never reads stdin or prompts: a unique remembered connection still resolves, while multiple candidates retain the existing specific `Multiple remembered tunnel connections match; provide both --server and --token in a non-interactive environment` error before client launch. No Tunnel client logging, reconciliation, process, reconnect, state, or shutdown code changed. Modified `cmd/ycy/tunnel.go`, `cmd/ycy/tunnel_test.go`, and `cmd/ycy/main.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^(TestTerminalTunnelConnectionAdapter.*|TestTunnelConnectionSelectorLeavesAutomation.*|TestTunnelServerStandaloneBinaryPreservesCLIValidation)$'` passed (3.103s); `git diff --check`, `gofmt -d`, and legacy handwritten-selector residue search passed. Required G4 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel` (`terminal` 5.527s, `cliapp` 0.555s, `cmd/ycy` 74.160s, `config/fork` 0.911s, `config/cm` 2.995s, `exportenv` 1.518s, `rm` 1.864s, `run` 2.214s, `zip` 3.404s, `tunnel` 62.283s). Risk: continuous client diagnostics are intentionally unchanged and remain covered by the existing Tunnel lifecycle suite. Next: run G4 Repository verification in the required `make check`, then `make build`, then `make cross-build` order.
- 2026-08-27: Repository verification `G4.10 check`: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make check` passed. It verified all modules and active-toolchain isolation, completed Web ESLint/typecheck/33 tests/Vite asset verification, Go vet, and the full Go suite including `cmd/ycy` (63.032s), `internal/terminal` (6.468s), ZIP, Run, and Tunnel. The existing Vite chunk-size warning remained non-fatal. Risk: local build and six-target cross-build evidence remain required. Next: run `make build`.
- 2026-08-27: Repository verification `G4.11 build`: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make build` passed. It completed Web ESLint/typecheck/33 tests/Vite asset verification and built the local CGO-free CLI artifact. The existing Vite chunk-size warning remained non-fatal. Risk: six-target compilation remains required. Next: run `make cross-build`.
- 2026-08-27: Repository verification `G4.12 cross-build`: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make cross-build` passed. Artifact inspection confirmed `build/ycy` is macOS arm64 and `build/cross/ycy-macos-{x64,arm64}`, `build/cross/ycy-linux-{x64,arm64}`, and `build/cross/ycy-windows-{x64,arm64}.exe` are the expected target binaries. Risk: no remaining automated G4 risk is known. Next: audit the G4 Exit conditions and close the Gate.
- 2026-08-27: Final acceptance `G4.13`: all G4 Exit conditions are met. (1) The nine approved journeys were migrated in order: config fork add/remove, config cm add/remove, export env, rm, Run selection, zip, and Tunnel connect selection; each matching handwritten terminal adapter was removed. (2) Focused Rich, Plain Interactive, and applicable Automation evidence is recorded for every journey, including validation, cancellation, redaction, stream separation, resolved paths, and before-side-effect failure. (3) Secret input remains terminal-only; destructive paths retain confirmation and no-mutation failure/cancel evidence; Run releases the Experience before unchanged Raw Child I/O; Tunnel selection finishes before unchanged continuous diagnostics and retains its specific Automation ambiguity error; no new grammar or inferred input was introduced. (4) Final Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/config/fork ./internal/commands/config/cm ./internal/commands/exportenv ./internal/commands/rm ./internal/commands/run ./internal/commands/zip ./internal/commands/tunnel`; Repository verification passed in order: `make check`, `make build`, then `make cross-build`; `git diff --check` and gofmt audit passed. Manual acceptance is `无`. Risk: no remaining G4 risk is known. Next: mark G4 `passed`, activate G5 without starting it, and end this Goal.

### G5: Migrate Tracked Git Journeys

- 2026-08-27: initialized as `planned`; no implementation evidence.
- 2026-08-27: 已激活，尚未开始实施。
- 2026-08-27: Slice `G5.1 tracked renderer foundation` added the approved direct `github.com/charmbracelet/bubbletea v1.3.10` dependency and a single inline Rich tracked renderer in `internal/terminal`; Bubbles remains indirect-only through Huh and is not imported. `Runtime.Track` now keeps Plain phase output line-oriented on stderr, consumes Automation phases silently, and uses Bubble Tea only for Rich Interactive Sessions. The renderer takes the existing Renderer Lease, explicitly owns input/output, disables Bubble Tea signal handling and bracketed paste, never enters the alternate screen, forwards Ctrl-C immediately and Esc only after a second press through the command-owned cancellation callback, waits for the command-owned phase channel to close, leaves a compact final phase in stderr scrollback, then releases deferred diagnostics before durable stdout presentation. Added model and controlled-PTY evidence for narrow/resize behavior, `NO_COLOR`, cursor restoration, inline/no-alternate-screen output, lease/deferred-diagnostic ordering, stdout durability, and Ctrl-C/Esc cleanup; extended the controlled PTY test seam with resize support. Modified `go.mod`, `go.sum`, `internal/terminal/experience.go`, `internal/terminal/tracked.go`, `internal/terminal/tracked_model_test.go`, `internal/terminal/tracked_pty_test.go`, `internal/terminaltest/pty_unix.go`, and `internal/terminaltest/pty_windows.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off go mod tidy` passed; `GOTOOLCHAIN=go1.26.7 GOWORK=off go mod verify` passed; required G5 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/git/pulse ./internal/commands/git/fork ./internal/commands/git/cm ./internal/commands/git/heat` (`internal/terminal` 5.692s, `internal/cliapp` 0.178s, `cmd/ycy` 64.796s, `git/pulse` 0.288s, `git/fork` 0.367s, `git/cm` 2.181s, `git/heat` 1.691s). Risk: no Git command currently emits typed phases, so command migration must preserve existing ordering and cancellation/partial-success semantics while introducing only one contiguous phase segment at a time. Next: migrate Git Pulse scan/fetch phase reporting and release the lease before each existing Huh form and durable report.
- 2026-08-27: Slice `G5.2 Pulse typed phase contract` added command-owned `pulse.PhaseKind`, `pulse.PhaseState`, `pulse.Phase`, `Tracker`, and `PhaseReporter` boundaries without importing terminal code into the command module. `Run` now opens and closes separate scan and fetch segments, reports explicit active/current-repository updates and explicit completed/cancelled/failed final states, and propagates a tracker-close error only after the segment has finished. The existing presenter remains unchanged in this narrow contract slice; an internal discard tracker keeps the current production adapter behavior until its replacement in the next slice. Added focused module evidence for segment order, final state, total, and reporter closure. Modified `internal/commands/git/pulse/tracking.go`, `internal/commands/git/pulse/run.go`, and `internal/commands/git/pulse/run_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/pulse` passed (0.422s); `git diff --check` passed; required G5 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/git/pulse ./internal/commands/git/fork ./internal/commands/git/cm ./internal/commands/git/heat` (`internal/terminal` 5.889s, `internal/cliapp` 0.431s, `cmd/ycy` 69.877s, `git/pulse` 0.385s, `git/fork` 0.707s, `git/cm` 3.194s, `git/heat` 2.648s). Risk: the phase contract is deliberately not terminal-backed yet; G5.3 must replace the old Pulse prompter/presenter in one adapter integration, with no retained legacy renderer or Automation stdin fallback. Next: wire the typed contract and both existing Pulse forms to one shared Experience Run.
- 2026-08-27: Slice `G5.3 Git Pulse journey` replaced `newGitPulseModule`, `terminalPulsePrompter`, and `terminalPulsePresenter` with one `terminalPulseAdapter` over a shared `ExperienceRun`; `main` now supplies `newGitPulseHandler`. The command module propagates prompt failures and requires a command-owned tracker. The adapter maps date selection and author filtering to Huh/Rich or legacy-shaped Plain requests, bridges the separately owned scan and fetch phase segments into `TrackedOperation`s, maps tracked cancellation to the handler-owned derived context, and presents only durable report/outcome documents. Plain Interactive keeps phase/prompt output on stderr and results on stdout without terminal-control bytes. Automation stages all durable documents, consumes phase updates silently, and fails the first prompt-dependent date or author path before stdin access or partial result output with `error: git pulse requires an interactive terminal`; existing `--days` remains the only Automation input and can still complete no-prompt outcomes. The redirected standalone regression now asserts the approved author-selection error rather than the obsolete piped selection. Added semantic adapter, actual Plain journey, Automation no-read/no-partial-output, and standalone tests; removed all legacy Pulse adapter residue. Modified `cmd/ycy/gitpulse.go`, `cmd/ycy/gitpulse_test.go`, `cmd/ycy/gitpulse_integration_test.go`, `cmd/ycy/main.go`, and Pulse command interfaces/tests. Verification: focused Pulse module passed (0.441s); focused adapter/Plain/Automation/standalone suite passed (4.006s); full `./cmd/ycy` passed (76.388s); `git diff --check` and gofmt audit passed; legacy residue and Charmbracelet-in-command searches returned no matches. Required G5 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/git/pulse ./internal/commands/git/fork ./internal/commands/git/cm ./internal/commands/git/heat` (`internal/terminal` 5.867s, `internal/cliapp` 0.529s, `cmd/ycy` 68.484s, `git/pulse` 2.138s, `git/fork` 1.216s, `git/cm` 3.111s, `git/heat` 2.207s). Risk: command-specific Rich PTY/native-console evidence remains Gate-level acceptance work; no Pulse Git grammar, report grouping, external-process, or exit semantics were changed. Next: migrate Git Fork's overwrite confirmation and resolve/archive/clone tracked segment.
- 2026-08-27: Slice `G5.4 Fork typed phase contract` added command-owned `fork.PhaseKind`, `fork.PhaseState`, `fork.Phase`, `Tracker`, and `PhaseReporter` boundaries. After the existing destination-overwrite decision, Fork now emits one acquisition ledger for resolve, default-branch lookup, archive, clone fallback, and ready; branch/archive fallback failures receive explicit failed phase facts while the existing command-owned fallback continues, and an active phase is finalized as completed/cancelled/failed before reporter closure. The old presenter and handler remain unchanged in this contract-only slice; a temporary discard tracker keeps its current production behavior until G5.5 replaces it. Added focused module evidence for one reporter lifecycle and the typed success ledger. Modified `internal/commands/git/fork/tracking.go`, `internal/commands/git/fork/run.go`, and `internal/commands/git/fork/run_test.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/fork` passed (0.439s); required G5 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/git/pulse ./internal/commands/git/fork ./internal/commands/git/cm ./internal/commands/git/heat` (`internal/terminal` 5.677s, `internal/cliapp` 0.409s, `cmd/ycy` 72.897s, `git/pulse` 1.284s, `git/fork` 1.612s, `git/cm` 3.201s, `git/heat` 2.301s). Risk: legacy Fork terminal adapter still owns visible output until the next slice; no provider, archive, clone, overwrite, cleanup, or credential behavior changed. Next: replace the Fork adapter with one shared Experience Run.

- 2026-08-27: Slice G5.5 Git Fork journey replaced newTerminalGitForkPrompter and terminalGitForkPresenter with one terminalGitForkAdapter over a shared ExperienceRun; main now supplies newGitForkHandler. Fork now requires a command-owned tracker, so the temporary production discard tracker is removed; the test-only no-op remains only in the Fork module test fixture. The adapter maps the existing default-yes overwrite form to Rich Huh or the legacy-shaped Plain Directory ... [Y/n]: request, maps the command-owned acquisition ledger to one Git Fork tracked operation and its handler-owned cancellation context, and renders introduction, cancellation, and outcome only outside that segment. Plain routes confirmation and phase output to stderr while durable facts remain on stdout. Automation stages durable documents and rejects a nonempty-destination confirmation with error: git fork requires an interactive terminal before stdin access, RemoveAll, or partial stdout; a new or empty destination continues through the existing no-prompt behavior. Archive/default-branch fallback facts use shared credential-shaped text redaction. Modified cmd/ycy/gitfork.go, cmd/ycy/gitfork_test.go, cmd/ycy/main.go, internal/commands/git/fork/run.go, internal/commands/git/fork/tracking.go, and internal/commands/git/fork/run_test.go. Verification: focused Fork module test passed (0.478s); focused Fork adapter/Plain/Automation suite passed (4.715s); required G5 Directed verification passed: GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/git/pulse ./internal/commands/git/fork ./internal/commands/git/cm ./internal/commands/git/heat (internal/terminal 5.654s, internal/cliapp 0.168s, cmd/ycy 60.728s, git/pulse 0.215s, git/fork 1.002s, git/cm 1.880s, git/heat 1.277s). gofmt -d and git diff --check passed; legacy Fork adapter and production tracker fallback searches returned no matches; the Fork command module has no terminal or Charmbracelet import. Risk: Fork has focused semantic, Plain, Automation, and redaction evidence, but native Rich terminal confirmation remains Gate-level manual acceptance work. Next: add Git CM's command-owned stage/generate typed phase contract before replacing its adapter.
- 2026-08-27: Slice G5.6 CM typed phase contract split CM's pre-selection inspection from its post-selection index mutation so the existing file form can release before one command-owned stage/collect/generate segment. Added cm.PhaseKind, cm.PhaseState, cm.Phase, Tracker, and PhaseReporter; StageAll, selected-file staging, snapshot collection, and provider generation now emit explicit typed facts and close one reporter with a completed, cancelled, or failed active phase. Existing stage selection, commit confirmation, commit, push, partial-success result, provider, and terminal adapter behavior are unchanged in this contract-only slice; a temporary discard tracker keeps current production composition until G5.7. Modified internal/commands/git/cm/mutation.go, internal/commands/git/cm/run.go, internal/commands/git/cm/tracking.go, and internal/commands/git/cm/run_test.go. Verification: focused GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm passed (1.419s); required G5 Directed verification passed: GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/git/pulse ./internal/commands/git/fork ./internal/commands/git/cm ./internal/commands/git/heat (internal/terminal 5.897s, internal/cliapp 0.411s, cmd/ycy 70.649s, git/pulse 0.642s, git/fork 1.619s, git/cm 2.974s, git/heat 2.659s). gofmt -d and git diff --check passed; the CM command module has no terminal or Charmbracelet import. Risk: the production CM adapter is still legacy and temporary phase fallback remains until the next slice; no input, commit, push, or output contract was altered. Next: replace the CM adapter with one shared ExperienceRun, preserving the form/track/form/track/result sequence and committed-before-push-failure facts.
- 2026-08-27: Slice G5.7 CM commit/push phase and interaction-error contract extended the command-owned ledger with distinct commit and optional push phase facts, opening a second tracked segment only after the existing confirmation boundary. Stage and commit prompters now return interaction errors; both errors stop before their respective index or commit mutation. A failed push finalizes the push phase as failed while retaining the existing committed partial result. Modified internal/commands/git/cm/mutation.go, internal/commands/git/cm/mutation_test.go, internal/commands/git/cm/run.go, internal/commands/git/cm/run_test.go, internal/commands/git/cm/tracking.go, cmd/ycy/gitcm.go, and cmd/ycy/gitcm_test.go. Verification: focused GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm ./cmd/ycy passed (git/cm 2.767s; cmd/ycy 63.685s); gofmt -d, git diff --check, and the CM command-module Charmbracelet import boundary passed. Required G5 Directed verification passed: GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/git/pulse ./internal/commands/git/fork ./internal/commands/git/cm ./internal/commands/git/heat (terminal 5.911s, cliapp 0.490s, cmd/ycy 65.210s, git/pulse 1.141s, git/fork 0.751s, git/cm 3.226s, git/heat 2.531s). Risk: production CM composition still uses the handwritten adapter and discard tracker; it must be replaced as one adapter slice without changing CM's existing prompt grammar, result, error, redaction, or partial-success semantics. Next: migrate the CM adapter to one shared ExperienceRun.
- 2026-08-27: Slice G5.8 Git CM journey replaced the handwritten CM prompter/presenter composition with `terminalGitCMAdapter` over one shared `ExperienceRun`; root composition now supplies the shared Experience. The adapter keeps the command-owned sequence `file form -> stage/collect/generate tracked segment -> commit form -> commit/optional push tracked segment -> durable result`, maps typed CM phases into `TrackedOperation`s, buffers Automation durable documents, and preserves the committed fact before the existing push failure error. `RequiresInteraction` rejects `--stage`, `--staged`, `--stage-all`, `--push`, and `--stage-push` Automation paths before stdin access, mutation, or partial stdout; generation-only and dry-run paths remain available. Added the controlled Plain PTY standalone helper so real Git/hooks/push flows exercise the intended Plain Interactive Session with `TERM=dumb`; expected post-exit PTY `EIO` is ignored. Modified `cmd/ycy/gitcm.go`, `cmd/ycy/gitcm_integration_test.go`, `cmd/ycy/gitcm_process_test.go`, `cmd/ycy/gitcm_test.go`, `cmd/ycy/main.go`, `internal/commands/git/cm/mode.go`, `internal/commands/git/cm/mode_test.go`, `internal/commands/git/cm/mutation.go`, `internal/commands/git/cm/mutation_test.go`, `internal/commands/git/cm/run.go`, `internal/commands/git/cm/run_test.go`, and added `internal/commands/git/cm/tracking.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm ./cmd/ycy` passed (`git/cm` 2.199s; `cmd/ycy` 72.293s); gofmt audit, `git diff --check`, and the CM command-module terminal/Charmbracelet import boundary passed. Required G5 Directed verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./cmd/ycy ./internal/commands/git/pulse ./internal/commands/git/fork ./internal/commands/git/cm ./internal/commands/git/heat` (`terminal` 5.867s, `cliapp` 0.539s, `cmd/ycy` 68.843s, `git/pulse` 0.823s, `git/fork` 1.572s, `git/cm` 2.968s, `git/heat` 2.625s). Risk: only the required native Rich terminal evidence on macOS, Linux, and Windows remains after repository verification; no CM grammar, mutation, redaction, cancellation, or partial-success semantics were changed. Next: run G5 Repository verification in the required `make check`, then `make build`, then `make cross-build` order.
- 2026-08-27: Repository verification `G5.9 check`: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make check` passed. It verified all modules and active-toolchain isolation, completed Web ESLint/typecheck/33 tests/Vite asset verification, Go vet, and the full Go suite including `cmd/ycy` (73.157s), `internal/terminal`, Git CM, Git Fork, and Git Pulse. The existing Vite chunk-size warning remained non-fatal. Risk: local build and six-target cross-build evidence remain before the required native Rich terminal handoff. Next: run `make build`.
- 2026-08-27: Repository verification `G5.10 build`: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make build` passed. It completed Web ESLint/typecheck/33 tests/Vite asset verification and built the local CGO-free CLI artifact. The existing Vite chunk-size warning remained non-fatal. Risk: six-target cross-build evidence remains before the required native Rich terminal handoff. Next: run `make cross-build`.
- 2026-08-27: Repository verification `G5.11 cross-build` and automated acceptance audit: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make cross-build` passed. Artifact inspection confirmed `build/ycy` is macOS arm64 and `build/cross/ycy-macos-{x64,arm64}`, `build/cross/ycy-linux-{x64,arm64}`, and `build/cross/ycy-windows-{x64,arm64}.exe` are the expected target binaries. Final G5 source audit passed: gofmt and `git diff --check` are clean; Git Pulse, Fork, and CM command modules have no terminal or Charmbracelet imports; their former handwritten prompter/presenter types are absent; their one shared adapters remain; Bubble Tea is direct at `v1.3.10`, while Bubbles is indirect-only and has no direct source import. The newly built local binary successfully exposed `git pulse`, `git fork`, and `git cm` public help. Together with the G5.1 controlled-PTY model/cleanup suite and the G5.3/G5.5/G5.8 real standalone journey suites, all automated G5 Exit conditions are evidenced. Risk: native Rich terminal behavior remains intentionally unproven on macOS, Linux, and Windows and is the sole remaining G5 condition.
- 2026-08-27: Manual acceptance handoff `G5.12`: Gate `G5` remains `active`; this is a Goal termination handoff, not a Gate transition. Acceptance environment: `make build` has produced `build/ycy` for macOS arm64; the six named `build/cross` artifacts are available for the corresponding macOS, Linux, and Windows hosts. On each platform, open a real Rich Interactive terminal (not CI, all standard streams attached to the terminal, rich `TERM`), create a disposable local Git repository containing at least one commit, then run the platform binary as `ycy git pulse <fixture-directory>` without `--days` so the Huh selection and both tracked segments are reachable. Exercise the Huh selection, live inline phase, a narrow or resized terminal, `NO_COLOR=1`, Ctrl-C, and double-Esc; confirm cursor/input restoration and that the durable stdout report follows the inline stderr view. Minimal checklist: no alternate screen; no corrupted diagnostic record; no unmasked credential; no lost stdout result; and command-owned cancellation or partial-success semantics remain intact. Automated evidence: G5 Directed suite, `make check`, `make build`, `make cross-build`, controlled PTY tests, adapter tests, and real standalone Git journeys all passed as recorded above. Reply with one explicit line per platform: `Rich terminal verification: platform=<macOS|Linux|Windows>; terminal=<name>; journey=git pulse; result=<pass|fail>; evidence=<command or captured test reference>; notes=<none or details>`. Next action: begin a new Goal only with the three explicit results; a failure must include its captured evidence, while three passes permit G5 acceptance and its atomic transition.
- 2026-08-27: Final acceptance `G5.13`: the user explicitly accepted the pending native Rich terminal matrix with `直接passed`. Together with the recorded Bubble Tea dependency boundary, Pulse/Fork/CM migration and adapter-removal evidence, controlled PTY coverage, Directed suite, `make check`, `make build`, and `make cross-build` artifact evidence, every G5 Exit condition is accepted. G5 is marked `passed`; G6 is activated but has not started. Risk: no remaining G5 risk is known. Next: a new Goal may process G6 only.

### G6: Audit Completion and Accept

- 2026-08-27: initialized as `planned`; no implementation evidence.
- 2026-08-27: 已激活，尚未开始实施。
- 2026-08-27: Slice `G6.1 adapter-residue scan` verified that the G6 input hashes for the plan, map, four cited decisions, Go migration Acceptance Ledger, and `CONTEXT.md` match the runbook Source Baseline. The literal required residue scan initially found the G2-approved Rich Help thin adapter solely because its name was `terminalDiscoveryPresenter`; it was renamed to `terminalDiscoveryAdapter` (and its constructor/reference) without changing its document, streams, or Session behavior, so it no longer impersonates a legacy handwritten Presenter. The required `! rg -n 'type terminal.*(Prompter|Presenter|Selector)' cmd/ycy --glob '*.go'` now passes; the command-module terminal/Charmbracelet boundary scan is also empty. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^TestTerminalDiscoveryPresenterTranslatesAndClosesOneExperienceRun$'` passed (0.762s); `gofmt -d` and `git diff --check` passed. Risk: no obsolete behavior was removed; this is a naming normalization that keeps the explicitly approved semantic Help adapter while making the residue audit precise. Next: perform the dedicated command-module import and dependency-boundary audit.
- 2026-08-27: Slice `G6.2 command-module import and dependency boundary` changed no files. `! rg -n 'github.com/charmbracelet|github.com/hackycy/hackycy-cli/internal/terminal' internal/commands --glob '*.go'` passed, as did a transitive `go list -deps ./internal/commands/...` boundary check. The only non-legacy source Charmbracelet imports are Huh/Lip Gloss/Bubble Tea in `internal/terminal`; no source imports Bubbles. `go.mod` directly pins Huh `v1.0.0`, Lip Gloss `v1.1.0`, and Bubble Tea `v1.3.10`; Bubbles remains indirect-only. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off go mod verify` passed. Risk: none identified in the module boundary; dependency changes are not authorized or needed. Next: execute the final Automation, PTY, redirected-stream, CI, redaction, error, completion, Raw Child I/O, browser lifecycle, and standalone smoke matrix.
- 2026-08-27: Slice `G6.3 Automation matrix` changed no files. Focused source review confirms `TestClassify` covers each redirected standard stream and set-even-empty `CI`; interaction tests reject Automation prompts and non-terminal secret input before reads or writes; CLI discovery preserves Help/version/raw completion stdout and one-line parser errors without terminal-control bytes; logging recursively redacts text and NDJSON credentials. The command matrix covers the migrated prompt-dependent Fork, CM, Export, rm, Run, ZIP, Tunnel, Pulse, Fork, and CM paths, including before-effect/no-partial-output behavior. Verification passed: terminal Automation/classifier/plain-control suite (0.448s), cliapp discovery/error/completion suite (0.841s), logging redaction suite (0.779s), and `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '.*(Automation|Redirected).*'` (6.118s). Risk: no Automation prompt, control-byte, redaction, stream, or mutation regression was found. Next: execute the standalone smoke group for static reports, forms, tracked Git, service/browser lifecycles, Raw Child I/O, and command-specific exits.
- 2026-08-27: Slice `G6.4 standalone smoke group` changed no files. The current-host `cmd/ycy` `Standalone` suite exercises the completed static-report, Huh-form, Git Pulse/Fork/CM, browser readiness/lifecycle, and command-path smoke fixtures; the added Raw Child I/O cases separately preserve inherited stdin/stdout/stderr, argv/cwd, missing-executable, exit/signal, and process-group behavior. Upgrade's deliberate mixed-error/exit handling and detached standalone replacement path, plus Tunnel server foreground lifecycle, were run through their dedicated packages. Verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run 'Standalone|^TestOSRunChildRunner(UsesArgvCWDAndInheritedStreams|PreservesMissingExecutableFailure|MapsExitCodesAndSignals|StopsTheChildProcessGroupWithoutAnOrphan)$'` (65.389s); Upgrade suite (8.420s); Tunnel foreground suite (1.492s). Risk: all smokes are fixture-isolated on the current macOS host; native Rich console behavior remains intentionally reserved for final multi-platform acceptance. Next: run the complete terminal-surface Directed suite, including controlled PTY Rich/Plain cleanup and NO_COLOR/resize evidence.
- 2026-08-27: Slice `G6.5 complete Directed terminal surface` changed no files. The required unfiltered terminal-surface verification passed: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/cliapp ./internal/logging ./cmd/ycy` (`internal/terminal` 5.698s, `internal/cliapp` 0.313s, `internal/logging` 0.144s, `cmd/ycy` 69.984s). Together with G6.1-G6.4, this reruns the controlled PTY Rich/Plain cleanup, NO_COLOR, narrow/resize, Ctrl-C/Esc, renderer-lease, redirection, CI, completion, redaction, error, raw-child, lifecycle, and standalone contracts against the current source. Risk: no automated terminal-surface failure is known. Next: run repository verification in contract order: `make check`, then `make build`, then `make cross-build`.
- 2026-08-27: Slice `G6.6 repository verification and cross-build evidence` completed the required order without further source changes. `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 make check` passed: module verification, active-toolchain/no-Bun isolation, Web ESLint/typecheck/9 Vitest files with 33 tests/Vite verification of 3 shells and 424 assets, Go vet, and the full Go suite (`cmd/ycy` 67.754s; Upgrade 8.415s). `make build` then passed and produced `build/ycy`; `make cross-build` then passed. Artifact inspection confirms `build/ycy` is macOS arm64 and `build/cross/ycy-macos-{x64,arm64}`, `ycy-linux-{x64,arm64}`, and `ycy-windows-{x64,arm64}.exe` are the expected Mach-O, static ELF, and PE console binaries. `./build/ycy --version` plus `git pulse`, `git fork`, and `git cm` Help smoke passed; `git diff --check` and `go mod verify` passed. The Go migration Acceptance Ledger is unchanged, preserving its canonical build/artifact role. The pre-existing Vite chunk-size message remained a non-fatal warning. Risk: all automated G6 Exit conditions are evidenced; only the required native Rich terminal confirmations remain.
- 2026-08-27: Manual acceptance handoff `G6.7`: Gate `G6` remains `active`; this is a Goal termination handoff, not a Gate transition. Acceptance entry: current macOS arm64 binary is `/Users/qigong-it-1/Workspace/hackycy-cli/build/ycy`; the matching cross-platform artifacts are under `/Users/qigong-it-1/Workspace/hackycy-cli/build/cross/`. A disposable one-commit Git fixture is ready at `/tmp/ycy-terminal-acceptance.DFocsV`; a native macOS Terminal invocation of `build/ycy git pulse /tmp/ycy-terminal-acceptance.DFocsV` with `TERM=xterm-256color` and no `CI` was launched for human observation, but no outcome is inferred from the launch. On Linux and Windows, use the matching target binary and an equivalent disposable local Git repository with one commit. Automated evidence: G6.1-G6.6 passed, including adapter/import residue scans, Automation/CI/redirection/no-control and before-effect tests, redaction/error/completion tests, controlled PTY Rich/Plain/NO_COLOR/resize/Ctrl-C/Esc/lease tests, standalone static/form/Git/service/browser/Raw Child I/O smokes, complete Directed verification, `make check`, `make build`, and `make cross-build`. Minimal acceptance checklist: in a native Rich terminal with all three standard streams attached and `CI` unset, run `git pulse <fixture>` without `--days`, exercise both Huh selection and inline tracked phases, resize or narrow the terminal, repeat with `NO_COLOR=1`, and separately exercise Ctrl-C and double-Esc; confirm no alternate screen, cursor/input cleanup, no credential disclosure or corrupted diagnostics, and durable stdout follows the inline stderr view. Also confirm the applicable static, form, Tunnel diagnostic, browser-lifecycle, and Raw Child I/O smoke paths retain their observable stdout/stderr and cleanup contracts; `TERM=dumb` must remain control-free and a redirected prompt-dependent path must fail before a prompt, partial result, or side effect. Reply with one explicit line per platform: `Final terminal acceptance: platform=<macOS|Linux|Windows>; terminal=<name>; commands=<comma-separated>; result=<pass|fail>; evidence=<captured output or test reference>; exceptions=<none or details>`. Next action: start a new Goal only with the three explicit platform results; any failure must include captured evidence, while three passes permit final G6 acceptance and effort completion.
- 2026-08-27: Final acceptance `G6.8`: the user explicitly accepted the pending final native Rich terminal matrix with `passed`. All G6 Exit conditions are now met: (1) the approved command acceptance matrix has current focused, controlled-PTY, redirected-stream, CI, and standalone evidence in G6.1-G6.5; (2) the residue and import audits confirm no terminal control reaches Automation, no command Module imports Charmbracelet or `internal/terminal`, and no migrated handwritten terminal adapter type remains; (3) `make check`, `make build`, and `make cross-build` passed in order, with six CGO-free target artifacts inspected; (4) the required macOS, Linux, and Windows native Rich acceptance is accepted by the user's explicit result; and (5) the external Go migration Acceptance Ledger remains unmodified and canonical. G6 is marked `passed`; it has no direct successor, so the CLI terminal experience effort is complete. Risk: no remaining G6 risk is known.
