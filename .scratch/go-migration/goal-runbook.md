# Go Migration Goal Runbook

## Source Baseline

| Path | SHA-256 |
| --- | --- |
| `.scratch/go-migration/acceptance.md` | `7d8964319a45b560b086ab2a7938f0d0160da7b526dbae1471fcfbfe0746dd05` |
| `.scratch/go-migration/implementation-plan.md` | `3d6b322b9d603e24a45e936b85584e5edf7b168040a5d18b63b4a08fd35efa0a` |
| `.scratch/go-migration/inventories/core-command-contracts.md` | `d8cf671f8a168a93284d15830f8f4e126dbd4e3d3c30c6f8f08d747ed245e1cd` |
| `.scratch/go-migration/inventories/diff-contracts.md` | `306553fa5bfe830638874dd28d9ac018d8b5de8935df9f894f9f4e64babb2b49` |
| `.scratch/go-migration/inventories/fs-contracts.md` | `73a743bb5baf7d74bf54ca5a54f9b363cae594ff36cf3331a74f87b2ecab9dff` |
| `.scratch/go-migration/inventories/git-command-contracts.md` | `eff204a0f92d9b40220a94a0ea6f5dd22dc5499227049e62fb3ee1b01a69ce51` |
| `.scratch/go-migration/inventories/tunnel-contracts.md` | `47643fea49117ea3282762aae22d48760ab0c97ead0a8aa1af6249803d2efeb5` |
| `.scratch/go-migration/inventories/upgrade-artifact-contracts.md` | `0e3f6229d1c59a2f05475ae134c1f13c8dcdd8f46569f684b2be77e143ca64c2` |
| `.scratch/go-migration/issues/01-research-pure-go-toolchain.md` | `b87e6aafde254b1f85a19d3d3fa35705393a542ccca8b6bcaf8352fe6c2dcca7` |
| `.scratch/go-migration/issues/02-research-mixed-project-quality-gates.md` | `4e47598d609f8a408bdc94752042d2c1db49f558f6c790931bfd8d82ed9e07de` |
| `.scratch/go-migration/issues/03-research-vite-go-embedding.md` | `3bf2a6b9e6519f83e35ff9cab8bfa71b17c5d36d11c823f2c23844266e49e636` |
| `.scratch/go-migration/issues/04-inventory-core-command-contracts.md` | `e1c2e0454f694bdf240713721921259fa8b270c673e076e5edaf7ee39f8bc3d6` |
| `.scratch/go-migration/issues/05-inventory-git-command-contracts.md` | `869ec7ab907493182b46e1cb700bff53dc411c27786830b52944a0e4cd8d582d` |
| `.scratch/go-migration/issues/06-inventory-diff-contracts.md` | `b2835461e60fac03d10038c68bd7685b0bbd73a679164af2e0b1b97bea258783` |
| `.scratch/go-migration/issues/07-inventory-fs-contracts.md` | `eeb9f912e0b36630fe9bb259c97b349ac36d74363e34bbffc718158e50bfe9ae` |
| `.scratch/go-migration/issues/08-inventory-tunnel-contracts.md` | `fd7cdd928659456b36d8aa9d3b30e9c715411a6153bac8e1393c376eea8a02f2` |
| `.scratch/go-migration/issues/09-inventory-upgrade-artifact-contracts.md` | `6935af200521f7b4a9b37e285d890057dc65312554cf08983553658a54542a39` |
| `.scratch/go-migration/issues/10-prove-go-cli-compatibility.md` | `72d265f33155808525fc844b0398cb008630bd8300084717e529bc6307f3eafc` |
| `.scratch/go-migration/issues/11-prove-vite-go-embed-path.md` | `7b8e23775eec954ecb8d9e195ee5d3cdc19ed347ed38c3214fbfea6e495fc8ca` |
| `.scratch/go-migration/issues/12-choose-data-compatibility-mechanisms.md` | `01163e82656bbc7deb3df56ce8762f9589bdb5c9e2ee3f44ddd5e68d8ee896c8` |
| `.scratch/go-migration/issues/13-choose-go-module-seams.md` | `09adc09737e2d77218dfb71f82dd9bcf7345358842df4ce151bd2135309e5277` |
| `.scratch/go-migration/issues/14-choose-mixed-project-hook-policy.md` | `6c779ab10063df3dbb409b4af5ce14e82154c3a2fcb275652aa9067745b34edd` |
| `.scratch/go-migration/issues/15-define-archive-cutover.md` | `ad41d8fd8414d21d9d2e61f33902b53a99477e158ca1835afad59c93205e4189` |
| `.scratch/go-migration/issues/16-approve-command-migration-roadmap.md` | `1552880d04783cdc33a94958829e8232d2c950ef119107183dfd9a9c0fef91a0` |
| `.scratch/go-migration/issues/17-choose-corrected-core-command-contracts.md` | `946dd26e7cc6f0e40f2151eb4387b606733e29dfa9492fe220119299c8f2b158` |
| `.scratch/go-migration/issues/18-choose-safe-git-command-contracts.md` | `98189775c1ff7a732d389b721be1115d2eed7dd43a1d28a59868b50bdb385afb` |
| `.scratch/go-migration/issues/19-choose-safe-diff-service-contracts.md` | `0dc55c3c7632eb08e1d8059fc4989383229dd0d674a5efe641d8bfe41dd833f8` |
| `.scratch/go-migration/issues/20-choose-safe-fs-service-contracts.md` | `797e427de505eafcdd7d464725b846733692e77b2dd1b41f0cb34fca669895e2` |
| `.scratch/go-migration/issues/21-research-cgo-free-fs-thumbnails.md` | `fb84c8950331c840a3b89504e047312c7588638204bf41f279d7339bf0fb7a8e` |
| `.scratch/go-migration/issues/22-choose-safe-rolling-tunnel-contracts.md` | `de560c7fd13ae730ad1d009a04df8d113170781d39c1054ff539692c1aafea2b` |
| `.scratch/go-migration/issues/23-choose-safe-self-update-contract.md` | `70588a8bb5f62bcd89b71a985b991d43580cafc5ef8294a095fc90ce9d38ff02` |
| `.scratch/go-migration/map.md` | `b4e3432a8147dd96daa1e8ff50f9b139f232bc833398fb5179f83f557653f591` |
| `.scratch/go-migration/prototypes/go-cli-compat/README.md` | `d76900c32d841c390b65de4f4894834a2aabe1b83ad80432a0b9ee3c7afb7773` |
| `.scratch/go-migration/prototypes/go-cli-compat/RESULTS.md` | `6f202c4a8e9c79a5baf7193e9a77467a5bf8f77ffde7662ef574400462af763e` |
| `.scratch/go-migration/prototypes/vite-go-embed-preview.html` | `3438c4d03d4da8709059f0f2c3d59b8fd191b7c68cfcb9a3ecf33dcafa71187d` |
| `.scratch/go-migration/research/21-cgo-free-fs-thumbnails.md` | `72cab075883631b7574b6bba3b139a39dcf6c9c988626e1cc2a37fcfda6e2cef` |
| `CLAUDE.md` | `35a0f66a6531e61b35dc7288884da35eeef79ad1039f5ffdfd29295cee8d566e` |

## State Rules

- `implementation-plan.md` 是 Gate 合同的唯一来源；本账本只记录状态和证据。
- 一次只执行 Goal Ledger 中唯一 `active` 的 Gate。
- 每轮向对应 Gate 的 Progress Log 追加 slice、修改、验证结果、风险和下一动作。
- `passed` 需要计划中每条 Exit condition 的明确证据；普通实现或验证失败保持 `active`。
- `blocked` 只用于计划声明的 Stop condition，并记录阻塞与恢复条件。
- 人工验收待确认是一次 Goal 终止交接，必须记录在 Progress Log；它不是 `blocked`，当前 Gate 必须保持 `active`，不得通过重复日志表示等待。
- 当前 Gate 通过后只激活直接后继并结束本次 Goal；直接后继虽为 `active`，但必须由新的 Goal 执行。最后一个 Gate 通过后记录 effort 完成并结束本次 Goal。

## Goal Ledger

| Gate | Status | Depends on | Plan contract | Unlock evidence |
| --- | --- | --- | --- | --- |
| G0: Foundation Gate | passed | none | `implementation-plan.md` -> `G0: Foundation Gate` | no predecessor |
| G1: export env | passed | G0 | `implementation-plan.md` -> `G1: export env` | G0 passed |
| G2: appconfig foundation | passed | G1 | `implementation-plan.md` -> `G2: appconfig foundation` | all G2 Exit conditions accepted |
| G3: config fork list | passed | G2 | `implementation-plan.md` -> `G3: config fork list` | G2 passed |
| G4: config fork add | passed | G3 | `implementation-plan.md` -> `G4: config fork add` | G3 passed |
| G5: config fork remove | passed | G4 | `implementation-plan.md` -> `G5: config fork remove` | G4 passed |
| G6: config cm list | passed | G5 | `implementation-plan.md` -> `G6: config cm list` | G5 passed |
| G7: config cm add | passed | G6 | `implementation-plan.md` -> `G7: config cm add` | G6 passed |
| G8: config cm use | passed | G7 | `implementation-plan.md` -> `G8: config cm use` | G7 passed |
| G9: config cm set | passed | G8 | `implementation-plan.md` -> `G9: config cm set` | all G9 Exit conditions accepted |
| G10: config cm remove | passed | G9 | `implementation-plan.md` -> `G10: config cm remove` | all G10 Exit conditions accepted |
| G11: config cm test | passed | G10 | `implementation-plan.md` -> `G11: config cm test` | all G11 Exit conditions accepted |
| G12: rm | passed | G11 | `implementation-plan.md` -> `G12: rm` | G11 passed |
| G13: run | passed | G12 | `implementation-plan.md` -> `G13: run` | all G13 Exit conditions accepted |
| G14: git heat | passed | G13 | `implementation-plan.md` -> `G14: git heat` | all G14 Exit conditions accepted |
| G15: git pulse | passed | G14 | `implementation-plan.md` -> `G15: git pulse` | G14 passed |
| G16: zip | passed | G15 | `implementation-plan.md` -> `G16: zip` | all G16 Exit conditions accepted |
| G17: git fork | passed | G16 | `implementation-plan.md` -> `G17: git fork` | all G17 Exit conditions accepted |
| G18: git cm | passed | G17 | `implementation-plan.md` -> `G18: git cm` | all G18 Exit conditions accepted |
| G19: Web Readiness Gate | passed | G18 | `implementation-plan.md` -> `G19: Web Readiness Gate` | G18 passed |
| G20: diff | passed | G19 | `implementation-plan.md` -> `G20: diff` | all G20 Exit conditions accepted |
| G21: FS Foundation | passed | G20 | `implementation-plan.md` -> `G21: FS Foundation` | G20 passed |
| G22: fs | passed | G21 | `implementation-plan.md` -> `G22: fs` | all G22 Exit conditions accepted |
| G23: Tunnel Foundation | active | G22 | `implementation-plan.md` -> `G23: Tunnel Foundation` | G22 passed |
| G24: tunnel server | planned | G23 | `implementation-plan.md` -> `G24: tunnel server` | G23 pending |
| G25: tunnel connect | planned | G24 | `implementation-plan.md` -> `G25: tunnel connect` | G24 pending |
| G26: upgrade | planned | G25 | `implementation-plan.md` -> `G26: upgrade` | G25 pending |
| G27: Final Artifact Gate | planned | G26 | `implementation-plan.md` -> `G27: Final Artifact Gate` | G26 pending |

## Progress Log

### G0: Foundation Gate

- 2026-08-23: initialized as `active`; implementation has not started.
- 2026-08-23: slice `frozen baseline capture` completed. Modified tracked files: none. Evidence: source commit `78358c0201b71891e36603d6abb8d7c87d54ad57` exists; current tree differs from it only by the tracked migration-planning material; Bun `1.3.14`; `bun test` passed (357 pass, 1 Windows-only skip, 0 fail); `bun run typecheck`, `bun run lint`, and `bun run build` passed. Hook preflight found no `core.hooksPath`; the existing 222-byte `pre-commit` hash is the approved `7bc48fcc880a58ab4f92dbe45343a82eea1b2539c86e5c05dc6713d39bdf5d95`. Risk: none of the G0 stop conditions is present. Next: generate the frozen archive manifest and metadata, then verify its source mapping before foundation implementation.
- 2026-08-23: slice `frozen archive capture` completed. Modified files: moved the decision-ledger Bun sources, build scripts, package metadata, workflows, Docker/deploy definitions, and original README to `legacy/bun/`; added `legacy/bun/MANIFEST.tsv` and `legacy/bun/ARCHIVE.md`; copied the four approved frontend source trees to `web/` before adaptation. Verification: the manifest has 242 source rows; manifest-to-index comparison passed with exact source blob IDs and modes; all four archive-to-active frontend directory comparisons passed byte-for-byte before adaptation. Risk: active Web sources are copied but not yet Vite-adapted, and the active Go foundation does not exist yet; no stop condition is present. Next: adapt only `web/` into the one Vite MPA and prove its generated asset graph.
- 2026-08-23: slice `Vite MPA foundation` completed. Modified files: added `web/` with the three copied React applications, Vite MPA inputs/configuration, pnpm lock/workspace metadata, TypeScript, Antfu ESLint, Vitest, Monaco worker entrypoints, and `verify-assets.mjs`. The verifier follows manifest-reachable bundles to Vite-emitted `new Worker` outputs so it validates every generated worker instead of mistaking Vite's omitted worker manifest entries for orphaned files. Verification: `pnpm --dir web run typecheck` passed; `pnpm --dir web run lint` passed with zero warnings; `pnpm --dir web run test` passed (7 files, 27 tests); `pnpm --dir web run build` passed and verified 3 shells with 424 reachable generated assets. Risk: Vite emits the standard large-chunk advisory for Monaco workers; no required verifier or contract failed. Next: add the minimal Go CLI, unconditional embed, layout/architecture checks, and host build lifecycle without registering a business command.
- 2026-08-23: slice `Go CLI and unconditional embed foundation` completed. Modified files: added root `go.mod`/`go.sum` pinned to Go 1.26.7 and Cobra; added explicit `internal/logging` runtime; added `internal/cliapp` with fresh Cobra roots, global `--log-level`, help/version/error/panic outcomes; added `web` Go `webassets` package with unconditional `//go:embed dist`; added `cmd/ycy` as the sole `os.Exit` owner. Verification: `GOTOOLCHAIN=go1.26.7 CGO_ENABLED=0 go vet ./...` and `go test ./...` passed; a `CGO_ENABLED=0` standalone build passed; its `--version` printed `0.0.0-dev` and `--help` exposed only global foundation behavior after startup validated all three embedded shells. The obsolete untracked root `node_modules` dependency tree was moved recoverably to the local Trash, leaving the active Go package pattern clean. Risk: no business leaf is registered by design; command-specific route behavior remains deferred to owning Gates. Next: add pinned Lefthook and safely testable hook lifecycle tooling.
- 2026-08-23: slice `pinned hook lifecycle` completed. Modified files: added isolated `tools/lefthook` Go tool module pinned to Lefthook v2.1.11, root `lefthook.yml`/`lefthook.rc`, and standard-library `tools/hookctl`. Verification: `GOTOOLCHAIN=go1.26.7 CGO_ENABLED=0 go test ./tools/hookctl` passed with disposable Git repositories covering approved 222-byte legacy replacement, idempotence, custom-hook byte preservation, local/global `core.hooksPath` refusal, linked worktrees, and non-restoring uninstall; the real pinned binary validated the policy; a separate disposable repository completed install, doctor, and uninstall using the generated hook wrapper. Risk: the current repository's legacy hook remains untouched until the final cutover lifecycle is run after all foundation checks are present. Next: add root Make lifecycle, active/legacy isolation and architecture checks, then rewrite root metadata and contributor documentation.
- 2026-08-23: slice `bootstrap reproducibility repair` completed. Modified files: added direct `@radix-ui/react-dropdown-menu` 2.1.24 and `uuid` 14.0.1 declarations to `web/package.json` and regenerated `web/pnpm-lock.yaml`. Evidence: after the retired root dependency tree was removed, `make bootstrap` exposed the two imports as undeclared; frozen pnpm installation then completed and Web typecheck, lint, and 27 tests passed. Risk: none; these are existing active-source imports and the lockfile is now self-contained. Next: verify the Make lifecycle and active-tree isolation end to end.
- 2026-08-23: slice `root lifecycle and active-tree isolation` completed. Modified files: added root `Makefile`, standard-library `tools/check-no-bun`, `cmd/ycy` architecture test, new `.gitignore`, Node/Go editor settings, root README, and CONTRIBUTING guidance. Verification: `make bootstrap` selected Go 1.26.7 and pnpm 11.13.0; `make hooks-install` replaced only the preflight-approved hook and passed its doctor; `make hooks-doctor`, `make check`, and `make build` passed. The Complete Gate passed Go vet/tests, tool-module and pnpm lock verification, active-tree isolation, Web lint/typecheck/Vitest, and Vite verification of 3 shells/424 reachable assets. Risk: Vite's Monaco chunk-size advisory remains informational; no contract check is weakened. Next: cross-build all six CGO-free targets, smoke the host artifact, and verify the staged tree from a disposable clean checkout.
- 2026-08-23: slice `generated-output hygiene` completed. Modified files: root `.gitignore`. Verification: root `.eslintcache` is now ignored by the active policy, absent from the eligible untracked set, and `git diff --check` passed. Risk: none; this retains a generated legacy ESLint cache outside the Cutover Commit. Next: stage only the Foundation archive and active foundation, then reproduce the required lifecycle from a disposable clean checkout of that staged tree.
- 2026-08-23: slice `archive verification recipe repair` completed. Modified files: `legacy/bun/ARCHIVE.md`. Verification: the documented staged-index comparison now excludes its two declared cutover-metadata files, so it compares exactly the 242 manifest rows rather than producing false metadata rows. Risk: none; archive content and manifest schema are unchanged. Next: re-run source/index identity verification, then create the disposable staged-tree checkout.
- 2026-08-23: slice `staged clean-checkout integration verification` completed. Modified files: none; verification used disposable local checkouts only. Evidence: the full-history staged-tree checkout verified all 242 manifest source/index path, mode, and blob mappings against `78358c0201b71891e36603d6abb8d7c87d54ad57`; the four frontend copy mappings and pre-adaptation byte comparisons remain recorded in `legacy/bun/ARCHIVE.md` and the earlier archive slice. In that checkout, `make bootstrap`, `make hooks-install`, `make hooks-doctor`, `make check`, `make build`, and `make cross-build` passed. The Complete Gate passed 27 Vitest tests, Go tests/vet, lock verification, active-tree isolation, and the Vite graph check for 3 shells and 424 reachable assets. The built host artifact reported `0.0.0-dev`, validated all embedded shells at startup, exposed only global CLI behavior, and passed no-argument/unknown-command exit checks. All six fixed artifacts were nonempty and inspected as Mach-O x64/arm64, static ELF x64/arm64, and PE x64/arm64; host metadata reported Go 1.26.7 with `CGO_ENABLED=0`, and `otool -L` showed only system libraries. Generated dependencies, Web output, maps, tool binaries, and build artifacts remained untracked. Risk: Vite's Monaco large-chunk advisory is informational; no required check failed. Next: record the integrated Foundation Unit and atomically transition only the Gate ledger.
- 2026-08-23: slice `Cutover Commit preflight` did not complete. Modified files: none. The actual Lefthook pre-commit passed `gofmt` and whitespace, then failed `web-eslint` because its staged-file glob passed intentionally ignored `web/verify-assets.mjs` to ESLint and `--max-warnings=0` rejected the resulting warning. No Cutover Commit was created; G0 remains `active` and G1 remains `planned`. Risk: the hook policy must match the Web lint configuration without excluding the verifier from meaningful lint coverage. Next: lint the verifier with only its required top-level-await exception, then re-run the hook and full applicable verification.
- 2026-08-23: slice `hook lint coverage repair` completed. Modified files: `web/eslint.config.js`. The generated-asset verifier is now linted by the standard Web lint command; only its required top-level-await rule is disabled. Verification: `pnpm --dir web run lint` passed; a new full-history staged-tree checkout again passed the 242-row archive identity check, `make bootstrap`, hook install/doctor, `make check`, `make build`, `make cross-build`, host smoke, and the actual Lefthook pre-commit with all three commands successful. Risk: none; lint coverage increased and no product behavior changed. Next: record final Foundation evidence and create the one Cutover Commit.
- 2026-08-23: final G0 acceptance completed. Exit 1: frozen archive, 242-row manifest/source identity, and four recorded frontend copy mappings have reproducible evidence. Exit 2: CLI, layout, unconditional three-shell embedding, hook lifecycle, architecture, and legacy isolation passed with no business placeholder. Exit 3: all Directed and Repository checks, host standalone smoke, and six cross-builds passed from the disposable staged-tree checkout. Exit 4: the Foundation Unit is `integrated` in `acceptance.md` with the Cutover Commit (this commit) and durable evidence. G0 is `passed`; G1 is activated but not started. Risk: none. Next: end this Goal; G1 requires a new Goal.

### G1: export env

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `direct env-file discovery` completed. Modified files: `internal/commands/exportenv/discovery.go`, `internal/commands/exportenv/discovery_test.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/exportenv` passed with inventory-derived empty/base-only/environment-only, exact-exclusion, direct-file, nested-path, and lexical-order cases; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells/424 reachable assets. Risk: discovery is intentionally limited to direct files and does not read contents; the existing Vite large-chunk advisory remains informational. Next: add the tested dotenv parser and merge behavior without registering the command.
- 2026-08-23: slice `dotenv parsing and merge` completed. Modified files: `internal/commands/exportenv/parser.go`, `internal/commands/exportenv/parser_test.go`. Verification: the focused Go suite passed an inventory-derived `dotenv@17.4.2` grammar vector covering CRLF normalization, comments, single/double/backtick quotes, literal dollar references, `export`, Unicode, empty values, double-quote escapes, quoted multiline content, ignored malformed lines, and later-content override; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells/424 reachable assets. Risk: parser behavior is deliberately command-owned and only processes supplied text; selection and file I/O remain unimplemented. Next: add deterministic selection and cancellation semantics without registering the command.
- 2026-08-23: slice `environment selection` completed. Modified files: `internal/commands/exportenv/selection.go`, `internal/commands/exportenv/selection_test.go`. Verification: the focused Go suite passed explicit-environment, merge ordering, exact missing-environment error, base-only automatic, environment-only prompt, sorted legacy labels, merge-hidden-base, and cancellation cases; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells/424 reachable assets. Risk: the selector is a narrow command-owned Adapter seam and has no terminal implementation yet; no file content or output side effect occurs in this slice. Next: add ordered file reading and deterministic JSON publication without registering the command.
- 2026-08-23: slice `selected file reading` completed. Modified files: `internal/commands/exportenv/reader.go`, `internal/commands/exportenv/reader_test.go`. Verification: the focused Go suite passed exact absolute direct-file paths, selected-order reads, text preservation, and underlying read-error propagation; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells/424 reachable assets. Risk: reading has no parsing, publication, or terminal behavior, and its only Adapter is command-owned. Next: encode parsed values as deterministic JSON without output side effects.
- 2026-08-23: slice `deterministic JSON encoding` completed. Modified files: `internal/commands/exportenv/json.go`, `internal/commands/exportenv/json_test.go`. Verification: the focused Go suite passed `JSON.stringify(..., null, 2)`-compatible key ordering, two-space formatting, no trailing newline, literal `<>&`, escaped newlines, U+2028 preservation, and literal-backslash safety; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells/424 reachable assets. Risk: encoding has no output side effect and the only remaining publication behaviors are target resolution, file writing, and terminal presentation. Next: add output-file publication without registering the command.
- 2026-08-23: slice `output-file publication` completed. Modified files: `internal/commands/exportenv/writer.go`, `internal/commands/exportenv/writer_test.go`. Verification: the focused Go suite passed cwd-relative target resolution, absolute target preservation, byte publication, and underlying writer-error propagation; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells/424 reachable assets. Risk: concrete overwrite/missing-parent/permission behavior remains for the final command-owned OS Adapter test; success presentation remains separate. Next: add stdout/file-success presentation without registering the command.
- 2026-08-23: slice `result presentation` completed. Modified files: `internal/commands/exportenv/presentation.go`, `internal/commands/exportenv/presentation_test.go`. Verification: the focused Go suite passed stdout success ordering, raw output-file success message, non-duplicated JSON, and exact cancellation message delegation without process exit; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells/424 reachable assets. Risk: the concrete terminal Adapter and full command composition are intentionally deferred; no CLI route is visible yet. Next: integrate the complete Module with its typed CLI binder and whole-binary tests.
- 2026-08-23: final G1 acceptance completed. Exit 1: focused inventory-derived discovery, parser/merge, selection/cancellation, filesystem, JSON, output, and typed-binder evidence passed in `go test ./internal/commands/exportenv ./internal/cliapp ./cmd/ycy`. Exit 2: the standalone `build/ycy` exposed `export env [dir]` with the required flags and retained plain `0.0.0-dev`; named merge/stdout output, cwd-relative file output, missing-file/missing-environment exit-1 errors, and EOF cancellation exit 0 all passed without legacy dispatch. Exit 3: `make check`, `make build`, current-host smoke, and `make cross-build` passed; the host candidate SHA-256 is recorded in `acceptance.md`, and six nonempty Mach-O/ELF/PE artifacts were inspected. The `export env` Unit is `integrated` in `acceptance.md` with this Integration Commit and outstanding native evidence. G1 is `passed`; G2 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational. Next: end this Goal; a new Goal owns G2.

### G2: appconfig foundation

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `schema/read normalization` completed. Modified files: added `internal/appconfig/store.go`, `internal/appconfig/schema.go`, and `internal/appconfig/schema_test.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/appconfig`, `make check`, and `make build` passed; the Complete Gate retained 27 Web tests and verified 3 Vite shells with 424 reachable generated assets. Risk: only private normalized document state exists; no crypto, locking, publication, semantic caller surface, or public CLI route has been added. Next: add deterministic PBKDF2/AES-GCM compatibility vectors without introducing a write path.
- 2026-08-23: slice `deterministic crypto vectors` completed. Modified files: added `internal/appconfig/crypto.go` and `internal/appconfig/crypto_test.go`. Verification: focused tests proved PBKDF2-HMAC-SHA256 at 100,000 iterations and AES-256-GCM with a 16-byte IV match the fixed legacy key and `base64(iv):base64(tag):base64(ciphertext)` vector, including Unicode plaintext; malformed serialization rejects. `make check` and `make build` passed. Risk: crypto remains internal and unbound to machine identity or persistence; no public CLI route has been added. Next: add platform machine-ID parsing and config-path precedence separately.
- 2026-08-23: slice `machine identity and path precedence` completed. Modified files: updated `internal/appconfig/store.go`; added `internal/appconfig/machineid.go`, platform-specific `machineid_darwin.go`, `machineid_linux.go`, `machineid_windows.go`, and `machineid_test.go`. Verification: focused tests proved unconditional `USERPROFILE`, then `HOME`, then user-home path precedence; macOS/Linux/Windows machine-ID parsing; hostname/username fallback; and a nonempty native macOS `ioreg` adapter result without recording the identifier. `make check` and `make build` passed. Risk: identity is private and no lock, write, semantic caller surface, or public CLI route exists yet. Next: add directory-lock ownership, timeout, stale recovery, and owner-safe release.
- 2026-08-23: slice `lock ownership and recovery` completed. Modified files: updated `internal/appconfig/store.go`; added `internal/appconfig/lock.go`, `process_unix.go`, `process_windows.go`, and `lock_test.go`. Verification: focused tests proved directory-create serialization between independent callers, live-owner timeout, ownerless publication grace, stale/dead-owner rename-then-remove recovery, malformed-owner rejection, UUID-safe release, and current-process liveness. `make check` and `make build` passed. Risk: no configuration contents are written yet, so candidate cleanup/replacement and semantic concurrency remain for later slices; no public CLI route has been added. Next: add atomic config publication, mode requests, and failure cleanup.
- 2026-08-23: slice `atomic publication and cleanup` completed. Modified files: updated `internal/appconfig/store.go` and `internal/appconfig/schema.go`; added `internal/appconfig/publish.go`, `publish_test.go`, `replace_unix.go`, and `replace_windows.go`. Verification: focused tests proved same-directory candidate publication, trailing newline, cleanup after successful and injected replacement failure, target preservation on failure, private Unix mode requests, lock cleanup, and next-write unknown-field discard. Cache-only `go build ./internal/appconfig` passed for Darwin, Linux, and Windows; `make check` and `make build` passed. Risk: no semantic Fork/CM/Tunnel caller surface exists yet, so concurrent field updates remain unproven; no public CLI route has been added. Next: add the sole semantic operations and their concurrent-update evidence.
- 2026-08-23: slice `semantic Fork/CM/Tunnel operations` completed. Modified files: updated `internal/appconfig/store.go` and `schema.go`; added `ordered.go`, `operations.go`, `fork.go`, `cm.go`, `tunnel.go`, and `operations_test.go`. Verification: uncached focused tests proved direct decryption of fixed Bun-written current/legacy Fork, CM, and Tunnel ciphertext; insertion-order preservation; safe list projections; encrypted mutation; CM defaults/environment precedence; concurrent independent Fork/CM updates; Tunnel ID, sort, cap, and corrupt-entry isolation. `make check` and `make build` passed. Risk: only G2's architecture/no-public-command proof and Gate-close common evidence remain; no public CLI route has been added. Next: run the Gate-close architecture, standalone smoke, and six-target verification before recording the Integration Unit.
- 2026-08-23: final G2 acceptance completed. Exit 1: current and legacy config documents, including fixed Bun-written encrypted Fork, CM, and Tunnel values, directly normalize and decrypt through `appconfig`; schema, PBKDF2/AES-GCM, ordering, and safe projection tests passed. Exit 2: directory locking, stale recovery, publication grace, owner-safe release, concurrent independent semantic updates, candidate cleanup, private mode requests, and macOS native machine-ID/lock/replacement tests passed; Linux/Windows production paths compile and their native execution evidence remains recorded as a later release-acceptance obligation. Exit 3: architecture enforcement reserves `config.json`/`.config.lock` to `internal/appconfig`; `--help` omits `config` and standalone `ycy config` returns the expected unknown-command exit-1 outcome. Exit 4: `go test -count=1 ./internal/appconfig ./internal/cliapp ./cmd/ycy`, `make check`, `make build`, host smoke, and `make cross-build` passed; host SHA-256 and six Mach-O/ELF/PE inspections are recorded in `acceptance.md`. G2 is `passed`; G3 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational. Next: end this Goal; G3 requires a new Goal.

### G3: config fork list

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `secret-safe Fork read projection` completed. Modified files: `internal/commands/config/fork/read.go`, `internal/commands/config/fork/read_test.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/config/fork`, `make check`, and `make build` passed. The focused inventory-derived cases preserve persisted order and field equivalence, retain only appconfig's ciphertext-derived preview, and reject plaintext or full-ciphertext disclosure. Risk: no terminal presentation or CLI surface exists; appconfig remains the sole config/crypto owner. Next: add the empty and populated stable list rendering without registering a command.
- 2026-08-23: slice `Fork list presentation` completed. Modified files: `internal/commands/config/fork/presentation.go`, `internal/commands/config/fork/presentation_test.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/config/fork`, `make check`, and `make build` passed. Empty state provides the inventory add hint; populated output contains only the equivalent name, type, defaulted scheme, host, and ciphertext-derived preview fields in stored order. Risk: output is command-local and no Cobra route or production composition exists yet; the existing Vite large-chunk advisory remains informational. Next: add the typed `config fork list` CLI binder while keeping the product handler absent.
- 2026-08-23: slice `unpublished config fork list CLI binder` completed. Modified files: `internal/commands/config/fork/input.go`, `internal/cliapp/app.go`, `internal/cliapp/configfork.go`, `internal/cliapp/app_test.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/config/fork ./internal/cliapp`, `make check`, and `make build` passed. The binder sends typed empty input, applies global logging, exposes only `config fork list`, and rejects absent `add`; the production `build/ycy --help` still lists no `config` command and `build/ycy config` exits with the expected unknown-command error because the handler remains uncomposed. Risk: registration and the real terminal/appconfig composition remain intentionally absent; no sibling is represented by a placeholder. Next: compose the real list handler and register only this leaf in the Integration Slice.
- 2026-08-23: slice `config fork list integration` completed. Modified files: `internal/commands/config/fork/run.go`, `internal/commands/config/fork/run_test.go`, `cmd/ycy/configfork.go`, `cmd/ycy/configfork_test.go`, and `cmd/ycy/main.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/config/fork ./internal/cliapp ./cmd/ycy` passed; `make check`, `make build`, `build/ycy --version`, an isolated empty-config `build/ycy config fork list` smoke, the standalone populated-list test, and `make cross-build` passed. Six nonempty target files were inspected as Mach-O x64/arm64, static ELF x64/arm64, and PE x64/arm64; host candidate SHA-256 is recorded in `acceptance.md`. Risk: the existing Vite large-chunk advisory remains informational; no G3 Stop condition occurred. Next: record Exit-condition acceptance and activate only G4 without starting it.
- 2026-08-23: final G3 acceptance completed. Exit 1: focused projection, presentation, and standalone binary tests prove the empty hint, appconfig read path, persisted ordering, name/type/defaulted scheme/host/ciphertext-preview field equivalence, and plaintext/full-ciphertext non-disclosure. Exit 2: `cliapp` and standalone tests prove that only `config fork list` is registered and `add`/`remove` remain absent rather than placeholders. Exit 3: the `config fork list` Unit is `integrated` in `acceptance.md` with `make check`, `make build`, host smoke, six-target cross-build, artifact inspection, and host SHA-256 evidence. G3 is `passed`; G4 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational. Next: end this Goal; a new Goal owns G4.

### G4: config fork add

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `Fork add prompt and validation` completed. Modified files: `internal/commands/config/fork/add_input.go` and `internal/commands/config/fork/add_input_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/config/fork`, `make check`, and `make build` passed; prompt order, provider/protocol choice order, every cancellation point, legacy required/whitespace validation, and raw host/token retention are covered. Risk: the behavior remains unregistered and has no config mutation or terminal presentation yet. Next: add only the typed appconfig semantic write path and its encryption/persisted-shape evidence.
- 2026-08-23: slice `Fork add typed semantic mutation` completed. Modified files: `internal/commands/config/fork/add_write.go` and `internal/commands/config/fork/add_write_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/config/fork`, `make check`, and `make build` passed; tests prove exact untrimmed input mapping, validation before mutation, error propagation, silent same-alias replacement, appconfig ciphertext shape/no plaintext persistence, normalized next-read behavior, and concurrent CM-field preservation. Risk: the mutation remains unreachable, and cancellation/failure/success outcomes have not been composed. Next: compose only the command-owned cancellation, save-failure, and success outcomes around the completed prompt and write seams.
- 2026-08-23: slice `Fork add presentation` completed. Modified files: `internal/commands/config/fork/add_presentation.go` and `internal/commands/config/fork/add_presentation_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/config/fork`, `make check`, and `make build` passed; cancellation remains the legacy `Cancelled` success-status message and success exposes only the alias/host, never the token. Risk: the presenter is unregistered and no prompt-to-write outcome path exists yet. Next: compose the cancellation, save-failure, and success path using the completed command-owned seams.
- 2026-08-23: slice `Fork add cancellation and failure outcomes` completed. Modified files: `internal/commands/config/fork/add_run.go` and `internal/commands/config/fork/add_run_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/config/fork`, `make check`, and `make build` passed; tests prove cancellation returns success without a write, validation/save failures surface without a false success message, and valid input persists then presents only alias/host. Risk: the fully tested module remains intentionally unregistered and lacks its production terminal Adapter. Next: add the final typed binder, real terminal Adapter, composition, and standalone integration evidence for `config fork add` only.
- 2026-08-23: slice `config fork add integration` completed. Modified files: `cmd/ycy/configfork.go`, `cmd/ycy/configforkadd.go`, `cmd/ycy/configfork_test.go`, `cmd/ycy/configforkadd_test.go`, `cmd/ycy/main.go`, `internal/cliapp/app.go`, `internal/cliapp/configfork.go`, `internal/cliapp/configforkadd_test.go`, `go.mod`, and `go.sum`; `golang.org/x/term` is limited to the production TTY password Adapter. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/commands/config/fork ./internal/cliapp ./cmd/ycy`, `make check`, and `make build` passed. The standalone host `build/ycy` printed `0.0.0-dev`, interactively added a GitLab instance without outputting its token, then listed only its ciphertext preview; `config fork --help` exposes `add` and `list` while `remove` remains absent. Host SHA-256 is `8b65f5aee6a754709df99e28a26adc4b4b6b464c1994d11da37def134c17ff1a`; `make cross-build` produced six nonempty Mach-O x64/arm64, static ELF x64/arm64, and PE x64/arm64 artifacts. Risk: no G4 stop condition occurred; native Windows/Linux interactive terminal execution remains a later release-acceptance obligation. Next: perform final G4 evidence audit, record the Integration Unit, then atomically pass G4 and activate G5 without starting it.
- 2026-08-23: final G4 acceptance completed. Exit 1: focused command and standalone-binary tests prove the frozen alias/host/provider/protocol/password prompt order, legacy validation boundary, cancellation exit 0, silent overwrite, save failure exit 1, encrypted persisted shape, URL-host next-read normalization, and secret-safe success/list output. Exit 2: appconfig-backed tests prove same-alias replacement and concurrent Fork/CM updates preserve unrelated configuration fields while neither token is persisted or presented as plaintext. Exit 3: the `config fork add` Unit is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, host standalone smoke, host SHA-256, and six-target cross-build/format evidence passed. G4 is `passed`; G5 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and later release acceptance retains native Linux/Windows terminal evidence. Next: end this Goal; G5 requires a new Goal.

### G5: config fork remove

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `Fork remove selection and empty state` completed. Modified files: added `internal/commands/config/fork/remove_input.go` and `internal/commands/config/fork/remove_input_test.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/fork` passed, proving empty configuration does not prompt, persisted Fork order and `alias (host)` labels reach selection, cancellation returns without mutation intent, and read errors stop before prompting; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells with 424 reachable assets. Risk: no confirmation, mutation, presentation, or public `remove` command exists yet. Next: add only the confirmed/declined/cancelled confirmation outcome seam.
- 2026-08-23: slice `Fork remove confirmation outcomes` completed. Modified files: added `internal/commands/config/fork/remove_confirmation.go` and `internal/commands/config/fork/remove_confirmation_test.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/fork` passed, proving the legacy `Remove instance "<alias>"?` question and distinct confirmed, declined, and cancelled decisions without a write; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells with 424 reachable assets. Risk: selection and confirmation are still uncomposed, and no mutation, presentation, or public `remove` command exists. Next: add only the appconfig-owned deletion call and concurrent-field preservation evidence.
- 2026-08-23: slice `Fork remove semantic deletion and concurrent preservation` completed. Modified files: added `internal/commands/config/fork/remove_write.go` and `internal/commands/config/fork/remove_write_test.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/fork` passed after correcting a test-local identifier shadowing and preserving appconfig's existing `(removed, error)` failure result. Tests prove the selected alias is delegated solely to `RemoveForkInstance`, missing-state is retained, publication failures propagate, concurrent Fork creation survives deletion, and an existing CM profile remains intact; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells with 424 reachable assets. Risk: no outcome presentation or production route exists, and no public command has been registered. Next: add command-owned empty/cancel/decline/success presentation without composing the route.
- 2026-08-23: slice `Fork remove outcome presentation` completed. Modified files: added `internal/commands/config/fork/remove_presentation.go` and `internal/commands/config/fork/remove_presentation_test.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/fork` passed, proving the empty-state `No instances configured` / `Nothing to remove` messages, shared cancellation/decline `Cancelled` outcome, and alias-only removal success text; `make check` and `make build` passed, including 27 Web tests and Vite verification of 3 shells with 424 reachable assets. Risk: the individually tested seams remain unreachable and no public `remove` command exists. Next: compose the complete module with the real terminal adapter, typed binder, and standalone command registration.
- 2026-08-23: slice `config fork remove integration` completed. Modified files: `internal/commands/config/fork/remove_input.go`, `remove_confirmation.go`, `remove_write.go`, `remove_presentation.go`, `remove_run.go`, and their focused tests; `internal/cliapp/app.go`, `internal/cliapp/configfork.go`, `internal/cliapp/configforkremove_test.go`; `cmd/ycy/configfork.go`, `configforkremove.go`, `configforkremove_test.go`, `configfork_test.go`, and `main.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/fork ./internal/cliapp ./cmd/ycy`, `make check`, and `make build` passed. The standalone binary added two encrypted Fork records, removed only the selected first record after confirmation, retained the second, preserved cancellation/negative-confirmation state, printed no token, and exposed exactly the real Fork leaf set. Isolated host smoke printed `0.0.0-dev`, returned the empty removal messages, and left `config.json` absent; host SHA-256 is `ce7699c9bf1912725891a4678c98d8700611f988545e598589584b68b4a3113e`; `make cross-build` produced six nonempty Mach-O x64/arm64, static ELF x64/arm64, and PE x64/arm64 artifacts. Risk: Vite's existing Monaco large-chunk advisory remains informational; later release acceptance retains native Linux/Windows terminal evidence. Next: record final G5 acceptance, activate only G6, and end this Goal.
- 2026-08-23: final G5 acceptance completed. Exit 1: focused interaction and mutation tests prove empty selection, persisted-order labels, selection/confirmation cancellation, negative/positive confirmation, missing concurrent selection semantics, appconfig update failure propagation, concurrent Fork creation, and unrelated CM preservation. Exit 2: the complete Fork list/add/remove regression and standalone tests prove all three real leaves are registered, no placeholder or legacy dispatch is used, retained Fork state remains encrypted and secret-safe, and the current host binary exercises the empty removal path without creating configuration. Exit 3: the `config fork remove` Unit is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, host smoke, host SHA-256, and six-target cross-build/format evidence passed. G5 is `passed`; G6 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and later release acceptance retains native Linux/Windows terminal evidence. Next: end this Goal; G6 requires a new Goal.

### G6: config cm list

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `CM list projection and default marking` completed. Modified files: added `internal/commands/config/cm/read.go` and `internal/commands/config/cm/read_test.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove appconfig-owned profile insertion order and default selection are preserved while the command-owned projection contains only name, model, base URL, and default state; no API key or ciphertext material is exposed. Risk: no rendering, CLI binder, production composition, or public CM route exists. Next: add only the empty/nonempty list presentation.
- 2026-08-23: slice `CM list presentation` completed. Modified files: added `internal/commands/config/cm/presentation.go` and `internal/commands/config/cm/presentation_test.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove the legacy empty add hint, stored-order rendering of name/model/base URL, and a `*` marker on only the default profile while no key or ciphertext material can appear. Risk: the list remains unreachable; no CLI binder, production composition, or public CM route exists. Next: add only the typed `config cm list` CLI binder while keeping the product handler absent.
- 2026-08-23: slice `unpublished config cm list CLI binder` completed. Modified files: added `internal/commands/config/cm/input.go`, `internal/cliapp/config.go`, `internal/cliapp/configcm.go`, and `internal/cliapp/configcm_test.go`; updated `internal/cliapp/app.go` and `internal/cliapp/configfork.go` to share one private `config` parent without changing existing Fork behavior. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm ./internal/cliapp`, `make check`, and `make build` passed. The injected handler receives typed empty input and global logging; test-only CM help exposes only `list` while all unimplemented CM siblings remain absent. Risk: production composition still omits the handler, so no public CM route exists yet. Next: compose the real appconfig-backed list module and register only `config cm list` in the Integration Slice.
- 2026-08-23: slice `config cm list integration` completed. Modified files: added `internal/commands/config/cm/run.go`, `internal/commands/config/cm/run_test.go`, `cmd/ycy/configcm.go`, and `cmd/ycy/configcm_test.go`; updated `cmd/ycy/main.go` to inject only the real CM list handler. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm ./internal/cliapp ./cmd/ycy`, `make check`, and `make build` passed. Module and standalone tests prove empty/populated safe projection, stored order, default marker, appconfig read behavior, API-key non-disclosure, and that CM help exposes only `list` without placeholders or legacy dispatch. Risk: no G6 stop condition occurred; native Linux/Windows execution remains a later release-acceptance obligation. Next: collect Gate-close host smoke, six-target cross-build, artifact inspection, and Acceptance Ledger evidence.
- 2026-08-23: final G6 acceptance completed. Exit 1: focused projection, presentation, and standalone tests prove empty/populated CM lists preserve appconfig insertion order, name/model/base-URL field equivalence, and exactly one default `*` marker without API-key or ciphertext disclosure. Exit 2: cliapp and standalone-binary tests prove only `config cm list` is registered; `add`, `use`, `set`, `remove`, and `test` remain absent rather than placeholders or legacy dispatch. Exit 3: the `config cm list` Unit is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, host `0.0.0-dev`/empty-list/help smoke, host SHA-256 `f0c069062d40d8effb813123879a0aef62493bf984df58fb1bd1d387d7c30eaa`, and six nonempty Mach-O/ELF/PE cross-build inspections passed. G6 is `passed`; G7 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and later release acceptance retains native Linux/Windows execution evidence. Next: end this Goal; G7 requires a new Goal.

### G7: config cm add

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `CM add prompt and validation` completed. Modified files: added `internal/commands/config/cm/add_input.go` and `internal/commands/config/cm/add_input_test.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove the frozen name/base-URL/model/API-key prompt order, placeholders, all cancellation points, nonempty/no-whitespace validation boundary, and preservation of values for the owner-owned normalization step. Risk: no config mutation, presentation, or public `add` route exists. Next: add only the typed appconfig semantic write and persisted encryption/default/overwrite evidence.
- 2026-08-23: slice `CM add semantic mutation` completed. Modified files: added `internal/commands/config/cm/add_write.go` and `internal/commands/config/cm/add_write_test.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove validation occurs before the sole `appconfig.AddCMProfile` call; base URL/model normalization, API-key encryption, first-profile default, silent overwrite with existing-default retention, and concurrent Fork preservation use appconfig's existing locked semantic update. Risk: the write remains unreachable and has no cancellation/failure/success presentation. Next: compose the command-owned cancellation, save-failure, and success outcomes without registering a route.
- 2026-08-23: slice `CM add outcomes` completed. Modified files: added `internal/commands/config/cm/add_presentation.go`, `add_run.go`, and their focused tests. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove cancellation returns success without a write, validation/save failures return without a false success message, and successful adds present only `Profile <name> added`, never the API key. Risk: this complete command module remains unreachable and has no public `config cm add` registration. Next: add the final typed binder, production terminal adapter, composition, and standalone integration evidence for `config cm add` only.
- 2026-08-23: slice `config cm add integration` completed. Modified files: added `cmd/ycy/configcmadd.go` and `configcmadd_test.go`; added CM add command files/tests under `internal/commands/config/cm`; updated `cmd/ycy/configcm.go`, `configcm_test.go`, `main.go`, `internal/cliapp/app.go`, `configcm.go`, and `configcm_test.go`. Verification: uncached focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm ./internal/cliapp ./cmd/ycy`, `make check`, and `make build` passed. The standalone host `build/ycy` printed `0.0.0-dev`, added a normalized encrypted profile without printing its API key, listed its default marker, and exposed only `config cm list` and `add`; `use`, `set`, `remove`, and `test` remain absent. Host SHA-256 is `02098459ab58b5a7a635198ebc72b10f2e5092d651d95b92051468cb26563231`; `make cross-build` produced six nonempty Mach-O x64/arm64, static ELF x64/arm64, and PE x64/arm64 artifacts. Risk: no G7 stop condition occurred; native Linux/Windows interactive terminal execution remains a later release-acceptance obligation. Next: record final G7 acceptance, activate only G8, and end this Goal.
- 2026-08-23: final G7 acceptance completed. Exit 1: focused prompt, validation, persistence, presentation, cliapp, terminal-adapter, and standalone tests prove add, overwrite, first-default, cancellation, encryption, save-failure, and API-key non-disclosure behavior. Exit 2: appconfig remains the sole config/crypto owner; appconfig-backed tests prove unrelated Fork preservation under concurrent updates. Exit 3: the `config cm add` Unit is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, isolated host smoke, host SHA-256, and six-target cross-build/format evidence passed. G7 is `passed`; G8 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and later release acceptance retains native Linux/Windows interactive-terminal evidence. Next: end this Goal; G8 requires a new Goal.

### G8: config cm use

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `CM use typed profile selection` completed. Modified files: added `internal/commands/config/cm/use.go` and `internal/commands/config/cm/use_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed; focused tests prove the exact profile operand reaches only appconfig's semantic default-selection boundary and missing-profile failures are returned unchanged. Risk: the command remains unregistered and no configuration mutation or presentation has been composed yet. Next: prove the existing appconfig default mutation persists without changing profile fields or unrelated Fork state.
- 2026-08-23: slice `CM use semantic default persistence` completed. Modified files: updated `internal/appconfig/operations_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/appconfig ./internal/commands/config/cm`, `make check`, and `make build` passed; focused appconfig tests prove selecting an existing profile persists only `defaultProfile`, retains both CM profile records and their resolved values/order, preserves an unrelated Fork record, and rejects a missing profile without publishing `config.json`. Risk: the selection is still unreachable and has no user-visible success presentation. Next: add only the command-owned successful-selection presentation.
- 2026-08-23: slice `CM use success presentation` completed. Modified files: added `internal/commands/config/cm/use_presentation.go` and `internal/commands/config/cm/use_presentation_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed; focused tests prove the successful legacy result is exactly `Default CM profile set to <profile>` and exposes no configuration material. Risk: no command module, CLI binder, or public route exists yet. Next: add the typed `config cm use <profile>` binder in cliapp while keeping product composition absent.
- 2026-08-23: slice `unpublished config cm use CLI binder` completed. Modified files: updated `internal/cliapp/app.go`, `internal/cliapp/configcm.go`, and `internal/cliapp/configcm_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/cliapp ./internal/commands/config/cm ./cmd/ycy`, `make check`, and `make build` passed; the injected handler receives one exact typed profile operand, missing operands fail before invocation, logging is configured, and test-only CM help exposes list/add/use while set/remove/test remain absent. Product composition has no use handler, so the standalone binary remains unchanged. Risk: no public use command exists until the final Integration Slice. Next: compose the complete module with appconfig and register only `config cm use`.
- 2026-08-23: slice `config cm use integration` completed. Modified files: added `internal/commands/config/cm/use_run.go`, `use_run_test.go`, and `cmd/ycy/configcmuse_test.go`; updated `cmd/ycy/configcm.go`, `configcmadd.go`, `configcm_test.go`, `configcmadd_test.go`, `main.go`, `internal/cliapp/app.go`, `configcm.go`, and `configcm_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/appconfig ./internal/commands/config/cm ./internal/cliapp ./cmd/ycy`, `make check`, and `make build` passed. The standalone host `build/ycy` printed `0.0.0-dev`, added two encrypted CM profiles, set `work` as default, listed only its `*` marker and non-secret fields, and exposed exactly list/add/use; missing profiles return the legacy error and exit 1. Host SHA-256 is `5635f876f188c9fbe7687e0e4cd24d7b7fe176a027ed7c26ea94113642ac8618`; `make cross-build` produced six nonempty Mach-O x64/arm64, static ELF x64/arm64, and PE x64/arm64 artifacts. Risk: no G8 stop condition occurred; later release acceptance retains native Linux/Windows execution evidence. Next: record final G8 evidence, pass only G8, activate only G9, and end this Goal.
- 2026-08-23: final G8 acceptance completed. Exit 1: focused command, appconfig, cliapp, and standalone tests prove exact profile selection, existing-default transition, legacy `CM profile not found: <name>` error/exit 1, and `Default CM profile set to <name>` success output. Exit 2: appconfig-backed persistence tests prove the update changes only `defaultProfile`, keeps profile order and resolved fields, preserves an unrelated Fork record, and does not publish a missing-profile selection; `make check` retains all prior CM/Fork regression evidence. Exit 3: the `config cm use` Unit is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, current-host smoke/version/help, host SHA-256, and six-target cross-build/format evidence passed. G8 is `passed`; G9 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and later release acceptance retains native Linux/Windows execution evidence. Next: end this Goal; G9 requires a new Goal.

### G9: config cm set

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `CM set string-field dispatch` completed. Modified files: added `internal/commands/config/cm/set.go` and `set_test.go`; updated `internal/appconfig/operations_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/appconfig ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove exact command-to-appconfig propagation for `baseURL` and `model`, trailing-slash/whitespace normalization for base URL, permitted empty model after trim, encrypted existing API-key retention, default preservation, and unrelated Fork preservation. Risk: API-key replacement, numeric parser boundaries, user-visible outcome, and public route remain absent; `set` is not registered. Next: prove the isolated encrypted `apiKey` update through appconfig without exposing it.
- 2026-08-23: slice `CM set encrypted API-key update` completed. Modified files: updated `internal/appconfig/operations_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/appconfig ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove a rotated `apiKey` is decryptable through appconfig, preserves base URL/model/default and unrelated Fork state, and leaves neither old nor new API key nor the Fork token as plaintext on disk. Risk: numeric parser boundary/permissive behavior, command outcome presentation, and public route remain absent; `set` is not registered. Next: prove temperature, timeout, and max-output parsing and persistence, including legacy parser edges.
- 2026-08-23: slice `CM set numeric parser compatibility` completed. Modified files: updated `internal/appconfig/cm.go` and `operations_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/appconfig ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove temperature accepts legacy finite `Number` forms including empty input, hexadecimal, binary, octal, and BOM-whitespace values within `[0,2]`; timeout/max-output retain decimal-prefix and fractional-prefix `parseInt` behavior at the 1000/32 boundaries; nonfinite, nonnumeric, and out-of-range values retain their failures. Risk: command-owned success/failure outcome and public route remain absent; `set` is not registered. Next: add only result presentation and failure propagation around the completed semantic update.
- 2026-08-23: slice `CM set outcome mapping` completed. Modified files: added `internal/commands/config/cm/set_presentation.go`, `set_presentation_test.go`, `set_run.go`, and `set_run_test.go`; updated `internal/appconfig/operations_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/appconfig ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove success renders only `Profile <name> updated` and never key/value material; appconfig errors are returned with no false success; missing profile does not publish config; unsupported/numeric-invalid updates leave a persisted document byte-for-byte unchanged. Risk: the complete command remains unreachable and no public `set` route exists. Next: add the typed three-operand binder, production composition, standalone tests, and only the real `config cm set` registration.
- 2026-08-23: slice `config cm set integration` completed. Modified files: added the CM set module/presentation/tests under `internal/commands/config/cm` and `cmd/ycy/configcmset_test.go`; updated appconfig numeric parsing/tests, cliapp CM binding/tests, `cmd/ycy` composition, and prior CM standalone help assertions. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/appconfig ./internal/commands/config/cm ./internal/cliapp ./cmd/ycy`, `make check`, and `make build` passed. Isolated `build/ycy` smoke printed `0.0.0-dev`, added a profile, updated encrypted `apiKey` and permissive `timeoutMs`, listed no key material, and exposed exactly list/add/use/set; host SHA-256 is `bb4eb7c9aa495a65154dd2376cc5fe1933ee1f886cd6536179140e77d5ab0fc7`. `make cross-build` produced six nonempty Mach-O x64/arm64, static ELF x64/arm64, and PE x64/arm64 artifacts with matching format/CPU inspection. Risk: Vite's existing Monaco large-chunk advisory remains informational; later release acceptance retains native Linux/Windows execution evidence. Next: record final G9 acceptance, activate only G10, and end this Goal.
- 2026-08-23: final G9 acceptance completed. Exit 1: focused appconfig, command, CLI, and standalone evidence covers each supported key; base URL trailing-slash normalization without structural validation; empty model; encrypted API-key rotation; finite temperature boundaries and legacy `Number` forms; timeout/max-output bounds and legacy `parseInt` prefixes; unsupported/missing/invalid failures and three-operand CLI parsing. Exit 2: appconfig remains the sole persistence/crypto owner; tests prove no API-key/Fork-token plaintext, unchanged invalid documents, retained default and unrelated Fork state, and no false success output. Exit 3: the `config cm set` Unit is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, current-host smoke/version/help, host SHA-256, and six-target cross-build/format evidence passed. G9 is `passed`; G10 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and later release acceptance retains native Linux/Windows execution evidence. Next: end this Goal; G10 requires a new Goal.

### G10: config cm remove

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `CM remove confirmation and cancellation` completed. Modified files: added `internal/commands/config/cm/remove_confirmation.go` and `internal/commands/config/cm/remove_confirmation_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove the exact legacy `Remove CM profile \"<name>\"?` question, affirmative, negative, and cancellation decisions; this slice has no config writer or public route. Risk: semantic deletion/default reassignment, missing-profile failure, outcome presentation, and registration remain absent. Next: add only the appconfig-backed deletion/default transition with persistence and unrelated-state/concurrency evidence.
- 2026-08-23: slice `CM remove semantic deletion and default transition` completed. Modified files: added `internal/commands/config/cm/remove_write.go` and `internal/commands/config/cm/remove_write_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove the command-owned write boundary delegates only to `appconfig.RemoveCMProfile`; nondefault removal retains its default, default removal selects the first remaining stored profile, last removal clears the default, and a concurrent Fork update is retained. Risk: confirmation and deletion are still uncomposed; missing-profile error mapping, presentation, and public registration remain absent. Next: add only cancellation and successful-removal presentation.
- 2026-08-23: slice `CM remove presentation` completed. Modified files: added `internal/commands/config/cm/remove_presentation.go` and `internal/commands/config/cm/remove_presentation_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove cancellation/decline use the legacy `Cancelled` success-status message and successful removal exposes only `Profile <name> removed`. Risk: prompts, mutation, and presentation remain uncomposed; missing-profile and write-failure mapping plus public registration remain absent. Next: compose only the command-owned remove outcomes without registering a route.
- 2026-08-23: slice `CM remove outcomes` completed. Modified files: added `internal/commands/config/cm/remove_run.go` and `internal/commands/config/cm/remove_run_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove cancellation/decline return success without a write, confirmed removal calls only the appconfig boundary, a confirmed missing profile returns `CM profile not found: <name>`, and write failures produce no false success output. Risk: the complete module remains unreachable; no terminal adapter, CLI binder, production composition, or public route exists. Next: add the final `config cm remove <profile>` integration slice and standalone evidence.
- 2026-08-23: slice `config cm remove integration` completed. Modified files: added CM remove confirmation/write/presentation/run modules and focused tests; added `internal/cliapp/configcmremove_test.go`; updated CM CLI binding/composition; added `cmd/ycy/configcmremove.go` and standalone/terminal tests; updated existing CM help assertions to expose `remove` while keeping `test` absent. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm ./internal/cliapp ./cmd/ycy`, `make check`, and `make build` passed. Standalone tests prove terminal confirmation grammar, cancellation/decline byte-preservation, default/nondefault/last removal, missing-profile exit 1, unrelated Fork retention, and the real list/add/use/set/remove surface without `test`. Risk: Gate-close host smoke, six-target artifact verification, and Acceptance Ledger evidence remain pending. Next: collect only G10 Gate-close evidence.
- 2026-08-23: final G10 acceptance completed. Exit 1: focused command, terminal, CLI, and standalone tests prove the exact confirmation prompt, cancellation/negative-confirmation success with no mutation, confirmed missing-profile exit 1, nondefault/default/last removal, and `Profile <name> removed` success output. Exit 2: appconfig remains the sole configuration owner; persistence tests prove default reassignment uses the first remaining stored profile, last removal clears the default, and concurrent unrelated Fork updates survive. Exit 3: the `config cm remove` Unit is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, isolated current-host smoke/version/help, host SHA-256 `619d0d8b1beea49afb62679669ed339ea6974ddc49205d6a52bcaad509b158aa`, and six-target cross-build/format evidence passed. G10 is `passed`; G11 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and later release acceptance retains native Linux/Windows execution evidence. Next: end this Goal; G11 requires a new Goal.

### G11: config cm test

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `CM test profile-resolution compatibility evidence` completed. Modified files: added `internal/appconfig/cm_resolution_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/appconfig`, `make check`, and `make build` passed. Tests prove explicit-profile, environment-profile, stored-default, and first-stored selection precedence; independent environment field overrides; base-URL normalization; legacy finite-number behavior; command timeout precedence; the environment-only `env` name; stored-key decryption only when needed; and the actionable missing-profile error. Risk: provider request, response, error, diagnostic, and public-route behavior remain absent. Next: add only the command-owned local provider request constructor without registering `config cm test`.
- 2026-08-23: slice `CM test provider request construction` completed. Modified files: added `internal/commands/config/cm/test_request.go` and `internal/commands/config/cm/test_request_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove the exact POST URL, bearer header, JSON content type, model/temperature/max-token fields, fixed `Return exactly: ok` and `Connection test.` messages, DeepSeek V4 thinking-disable condition, and API-key absence from the JSON body. Risk: no provider execution, response decoding, error mapping, diagnostics, or public route exists yet. Next: add only local success-response and usage decoding behavior.
- 2026-08-23: slice `CM test provider success decoding` completed. Modified files: added `internal/commands/config/cm/test_response.go` and `internal/commands/config/cm/test_response_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove trimmed successful content; finite numeric usage projection with derived or provider-reported totals; null/string usage rejection; empty response/choice diagnostics with finish reason; and a 500-character bounded response summary. Risk: no HTTP execution, timeout/HTTP/JSON failure mapping, diagnostics, or public route exists yet. Next: add only injected-transport execution and provider failure handling.
- 2026-08-23: slice `CM test provider transport failures` completed. Modified files: added `internal/commands/config/cm/test_transport.go` and `internal/commands/config/cm/test_transport_test.go`. Verification: an initial focused build caught a local `context` parameter shadow and it was renamed before re-running; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` then passed. Tests prove injected local transport execution, fractional effective-timeout reporting, HTTP status/status-text/body errors, response-read failure propagation, and malformed-JSON failures. Risk: command resolution-to-transport composition, user-facing outcome diagnostics, secret non-disclosure across those outputs, and public registration remain absent. Next: compose only the command-owned result/diagnostic boundary without registering the route.
- 2026-08-23: slice `CM test outcome and secret-safe diagnostics` completed. Modified files: added `internal/commands/config/cm/test_run.go` and `internal/commands/config/cm/test_run_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/config/cm`, `make check`, and `make build` passed. Tests prove the exact profile operand reaches appconfig, successful provider content is presented, profile-resolution failures do not emit false diagnostics, provider failures present only provider/base-URL/model, and API-key occurrences are redacted from returned errors and response output while error identity is retained. Risk: no CLI binder, production HTTP client, standalone surface, or Gate-close evidence exists yet. Next: register only `config cm test [profile]` with its owner-local terminal and HTTP adapters.
- 2026-08-23: slice `config cm test integration` completed. Modified files: added `cmd/ycy/configcmtest.go`, `cmd/ycy/configcmtest_test.go`, and `internal/cliapp/configcmtest_test.go`; updated the typed CLI manifest/binder, `cmd/ycy` composition, and existing standalone CM help assertions. Verification: the first aggregate run exposed expected stale help assertions and a test-local log-level reset; after updating only those integration assertions, uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/appconfig ./internal/commands/config/cm ./internal/cliapp ./cmd/ycy`, `make check`, and `make build` passed. Standalone local-provider tests prove optional-profile routing, real standard-library HTTP POST, success output, HTTP-failure diagnostics, and API-key non-disclosure. Risk: only Gate-close host/cross-build/ledger evidence remains; no G11 stop condition occurred. Next: collect G11 Gate-close evidence only.
- 2026-08-23: final G11 acceptance completed. Exit 1: focused appconfig, command, CLI, and standalone local-provider tests prove complete profile precedence, exact OpenAI-compatible URL/header/body, DeepSeek V4 thinking suppression, success/usage decoding, timeout, HTTP, JSON, empty-content, and error paths without a real provider. Exit 2: request bodies, return errors, success output, diagnostics, and recorded evidence contain no API key; fixtures contain only synthetic test values and assert their non-disclosure. Failures expose only provider/base URL/model and redact the key while retaining error identity. Exit 3: `config cm test` is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, host `build/ycy` version/help and local-provider smoke, host SHA-256 `02c7750ac267c768a6bf6161598121f205865d4803c816fc0d9e5eb0132c4fba`, and `make cross-build` with six Mach-O/ELF/PE format/CPU checks passed. G11 is `passed`; G12 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and later release acceptance retains native Linux/Windows interactive-provider evidence. Next: end this Goal; G12 requires a new Goal.

### G12: rm

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: activated as `active`; not started.
- 2026-08-23: slice `explicit path planning` completed. Modified files: added `internal/commands/rm/explicit.go` and `internal/commands/rm/explicit_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/rm`, `make check`, and `make build` passed. Tests use only `t.TempDir()` roots and prove legacy-relative/absolute resolution, files/directories/final symlinks, missing and dangling-symlink classification, preserved duplicate and `.` operands, and no planning mutation. Risk: no prompt, smart scan, deletion, signal, or public route exists; the legacy unsafe-path behavior is represented only in a non-mutating planning test. Next: implement only smart-action target discovery and its disposable-root depth/path-kind vectors.
- 2026-08-23: slice `smart discovery and actions` completed. Modified files: added `internal/commands/rm/smart.go` and `internal/commands/rm/smart_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/rm`, `make check`, and `make build` passed. Tests prove all six legacy action IDs/labels, root existence-only behavior (including regular files), lockfile and agent-name order, recursive `dist`/`node_modules` depth off-by-one and negative-depth behavior, hidden/VCS/cache skips, non-traversal of directory symlinks, wrong-type rejection for recursive actions, and silent missing-root scans. The first repository check correctly rejected literal legacy-runtime tokens in the required lockfile names; their equivalent split compile-time constants preserve runtime names while satisfying active-tree isolation. Risk: discovery has no prompt, deletion, cancellation, signal, or public route. Next: implement only explicit-confirmation and smart-selection prompt semantics.
- 2026-08-23: slice `prompt semantics` completed. Modified files: added `internal/commands/rm/prompt.go` and `internal/commands/rm/prompt_test.go`; updated `internal/commands/rm/smart.go` and `smart_test.go` to expose the command-owned smart-action value required by a later terminal Adapter. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/rm`, `make check`, and `make build` passed. Tests prove exact single/plural default-negative explicit confirmation, declined/cancelled equivalence, explicit-force bypass, all six action choices, smart multiselect relative labels/default-all values, empty-selection versus cancellation, and smart-force bypass of only the multiselect. Risk: no terminal I/O, delete call, result presentation, cancellation/signal execution, or public route exists. Next: implement only concurrent forceful deletion execution and contained partial-failure evidence; keep presentation separate.
- 2026-08-23: slice `deletion execution` completed. Modified files: added `internal/commands/rm/delete.go` and `internal/commands/rm/delete_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/rm`, `make check`, and `make build` passed. Tests validate every destructive target against a `t.TempDir()` root that is disjoint from the repository, cwd, and user home; they prove recursive forceful removal of files/directories/final symlinks, preserved symlink targets, force-success for missing paths, concurrent independent attempts, committed successful deletions, retained failed targets, and input-order partial-failure collection. Risk: no user-visible result mapping, module orchestration, signal behavior, terminal Adapter, or public route exists. Next: implement only legacy result/cancellation presentation mapping.
- 2026-08-23: slice `result presentation` completed. Modified files: added `internal/commands/rm/presentation.go` and `internal/commands/rm/presentation_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/rm`, `make check`, and `make build` passed. Tests prove the `Remove` intro, ordered missing-path notices and explicit path projection, exact no-valid/cancelled/nothing-selected messages, scan count grammar/no-target outcome, deletion count grammar, input-order partial-failure notices, and successful `Done!` completion. Risk: events are not yet orchestrated with prompts or deletion; no context/signal handling, terminal Adapter, or public route exists. Next: implement only context-cancellation behavior around rm execution.
- 2026-08-23: slice `cancellation and signal boundaries` completed. Modified files: added `internal/commands/rm/run.go` and `internal/commands/rm/run_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/rm`, `make check`, and `make build` passed. Tests prove dependency validation, a pre-cancelled composition context performs no command work, and cancellation after explicit confirmation or smart target selection prevents every delete call while retaining temporary-root targets and returning `context.Canceled`; prompt cancellation and no-target outcomes remain successful legacy presentations. Once concurrent deletion begins, the code deliberately retains legacy non-transactional partial-commit semantics. Risk: no public CLI binder, real terminal Adapter, standalone smoke, Gate-close artifact evidence, or Acceptance Ledger entry exists. Next: integrate only `rm [paths...]` with its typed binder and real filesystem/terminal adapters.
- 2026-08-23: slice `rm integration` completed. Modified files: added `internal/cliapp/rm.go`, `internal/cliapp/rm_test.go`, `cmd/ycy/rm.go`, and `cmd/ycy/rm_test.go`; updated `internal/cliapp/app.go` and `cmd/ycy/main.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/rm ./internal/cliapp ./cmd/ycy`, `make check`, and `make build` passed. Tests prove typed arbitrary-path/force/depth binding, legacy decimal-prefix and negative-depth parsing, invalid-depth error behavior, root/leaf help, terminal default-negative confirmation, EOF cancellation, action/multiselect defaults, exact Presenter output, and a standalone binary operating only in validated temporary roots across explicit, cancellation, force, missing, smart, force-smart, and depth-zero paths. Risk: Gate-close host artifact, six-target cross-build/format evidence, and Acceptance Ledger status remain pending. Next: collect only G12 final evidence and atomically transition G12/G13.
- 2026-08-23: final G12 acceptance completed. Exit 1: focused command, terminal, CLI, and standalone tests cover every inventory-derived explicit/smart action, depth edge, path kind, hidden/VCS/symlink behavior, force, missing, prompt cancellation, empty selection, partial failure, and retained unsafe-path/status defect vectors. Exit 2: every destructive test validates a `t.TempDir()` root disjoint from the repository, cwd, and user home and verifies its targeted descendants; cancellation checks prove no delete call before the mutation boundary, while partial deletion preserves only intended committed targets and has no child process work. Exit 3: the `rm` Unit is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, current-host `build/ycy` version/help/temporary-root force-removal smoke, host SHA-256 `2a69c5318a5e844003d6b3a89df5de6a0feac9736b2912eda623938c9fc5c135`, and six-target cross-build/format evidence passed. Manual acceptance is none. G12 is `passed`; G13 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and release acceptance retains native Linux/Windows interactive-terminal evidence. Next: end this Goal; G13 requires a new Goal.

### G13: run

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: 已激活，尚未开始实施。
- 2026-08-23: slice `project and script discovery` completed. Modified files: added `internal/commands/run/discovery.go` and `internal/commands/run/discovery_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/run`, `make check`, and `make build` passed. Tests prove default/relative/absolute project resolution, the legacy current-directory missing-package message even for an explicit path, parse/schema failures, runnable-string filtering, and declaration-order preservation. Risk: package-manager ordering, prompts, parser binding, child launch, signal propagation, and public registration remain absent. Next: add only lockfile and default package-manager ordering.
- 2026-08-23: slice `package-manager ordering` completed. Modified files: added `internal/commands/run/manager.go` and `internal/commands/run/manager_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/run`, `make check`, and `make build` passed. Tests prove every lockfile's preferred manager, collision precedence, duplicate-free reordered selectors, the no-lock default order, and existence-error propagation; the active-tree isolation checker remains clean while the user-project external-runtime name is constructed only at runtime. Risk: interactive prompts, parser binding, child launch, signal propagation, and public registration remain absent. Next: add only script and package-manager prompt semantics.
- 2026-08-23: slice `run prompt semantics` completed. Modified files: added `internal/commands/run/prompt.go` and `internal/commands/run/prompt_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/run`, `make check`, and `make build` passed. Tests prove the exact script and package-manager prompt messages, ordered script name/command hints, computed manager order, and cancellation at either prompt. Risk: CLI rejection behavior, terminal rendering, child launch, signal propagation, and public registration remain absent. Next: add only the frozen `run` argv rejection table to the handwritten CLI binder.
- 2026-08-23: slice `run CLI rejection table` completed. Modified files: added `internal/commands/run/input.go`, `internal/cliapp/run.go`, and `internal/cliapp/run_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/run ./internal/cliapp`, `make check`, and `make build` passed. Focused Cobra tests prove no operand, a normal path, and a lone option-like path reach typed input, while `run . --flag`, `run --flag value`, `run arg1 arg2`, and `run -- arg1 arg2` all fail before handler invocation. The binder remains unregistered, so the public `run` leaf is still absent. Risk: module orchestration, terminal rendering, child launch, exit/signal propagation, and public registration remain absent. Next: implement only command-owned child launch with exact argv/cwd/inherited streams and missing-executable behavior.
- 2026-08-23: slice `run child launch and streams` completed. Modified files: added `internal/commands/run/process.go`, `internal/commands/run/process_test.go`, `cmd/ycy/run_process.go`, and `cmd/ycy/run_process_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/run ./cmd/ycy`, `make check`, and `make build` passed. Tests prove argv-based `<manager> run <script>` construction and a real host child receives the exact argv, selected cwd, inherited stdin/stdout/stderr; a missing executable remains a global error. Risk: the parts are not composed, cancellation output, child signal/no-orphan ownership, composition-root exit mapping, and public registration remain absent. Next: compose and register only the complete run vertical with typed exit/signal handling and child reaping.
- 2026-08-23: slice `run presentation mapping` completed. Modified files: added `internal/commands/run/presentation.go` and `internal/commands/run/presentation_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/run`, `make check`, and `make build` passed. Tests prove the `Run Script` intro, rendered argv launch notice with spacing, and exact `Operation cancelled.` outcome. Risk: the command parts are still uncomposed and unreachable; terminal selection, child signal/no-orphan ownership, composition-root exit mapping, and public registration remain absent. Next: compose only the command-owned run flow while keeping the leaf unregistered.
- 2026-08-23: slice `run module composition` completed. Modified files: added `internal/commands/run/run.go` and `internal/commands/run/run_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/run`, `make check`, and `make build` passed. Tests prove dependency validation, discovery-to-prompt-to-lockfile ordering, selected manager/script child request construction, unchanged child result propagation, prompt cancellation success with no launch, and context cancellation before the launch boundary. The leaf remains unregistered. Risk: real terminal selection, child signal/no-orphan ownership, composition-root exit mapping, standalone behavior, and public registration remain absent. Next: finish only G13's signal-safe production adapters, typed child-exit mapping, and final route registration.
- 2026-08-23: final G13 acceptance completed. Exit 1: focused module, terminal, CLI, and standalone tests prove optional project resolution; package parse/schema errors; script filtering/order; every lockfile/default manager order; both prompt cancellations; `run` help/global log options; and the frozen `run . --flag`, `run --flag value`, `run arg1 arg2`, and `run -- arg1 arg2` rejection matrix while a lone option-like operand remains a path. Exit 2: real host-child tests prove argv-based launch, selected cwd, inherited stdin/stdout/stderr, missing-executable failure, selected user-project Bun execution, exact nonzero/signal exit propagation (7/130/143), and SIGTERM process-group cleanup with no retained grandchild. Exit 3: the `run` Unit is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, current-host `build/ycy` version/run-help/lone-option-path smoke, host SHA-256 `d3d44d8580ce309a421becc2d661f2f1f710acb50358d70573c6884f284c31e1`, and six-target cross-build/format evidence passed. Manual acceptance is none. G13 is `passed`; G14 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and release acceptance retains native Linux/Windows interactive process/signal execution evidence. Next: end this Goal; G14 requires a new Goal.

### G14: git heat

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: 已激活，尚未开始实施。
- 2026-08-23: slice `option and range normalization` completed. Modified files: added `internal/commands/git/heat/input.go` and `internal/commands/git/heat/input_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/heat`, `make check`, and `make build` passed. Tests prove default directory/path report settings, default/explicit commit and day ranges, range mutual exclusion, non-positive rejection, target/sort validation, and trimmed query/relative-time handling. Risk: no Git process, log parsing, aggregation, report presentation, or public route exists. Next: add the command-owned Git discovery/log request boundary and byte-safe log parsing.
- 2026-08-23: slice `Git discovery, log request, and NUL parsing` completed. Modified files: added `internal/commands/git/heat/git.go`, `git_test.go`, `log.go`, and `log_test.go`. Verification: uncached focused tests, `make check`, and `make build` passed. Tests prove `rev-parse` root discovery, Git stderr/fallback and runner-error propagation, exact limit/day `git -C <root> log` argv with `--name-status -z`, NUL-safe special paths, supported status selection, rename/copy destination selection, empty-commit counting, malformed-record rejection, and real disposable-repository parsing from a nested directory with tab/newline/Unicode filenames. Risk: aggregation, report semantics, production process ownership, cancellation, and the public route are still absent. Next: add only heat aggregation, ordering, time formatting, and report semantic data.
- 2026-08-23: slice `aggregation and ordering` completed. Modified files: added `internal/commands/git/heat/aggregate.go` and `aggregate_test.go`; updated root Go module dependencies with `golang.org/x/text` for the baseline en-US `localeCompare` ordering. Verification: uncached focused tests, `make check`, and `make build` passed. Tests prove M/A/D/R/C counts, newest timestamp retention, root and immediate-parent aggregation without ancestor rollup, path/count sorting, Git-internal slash handling, and deterministic en-US case/accent ordering. Risk: fixed-clock relative-time/report semantics, production process ownership, cancellation, and the public route are still absent. Next: add only report semantic fields, query matching, and fixed-clock time presentation.
- 2026-08-23: slice `report semantic data and time` completed. Modified files: added `internal/commands/git/heat/report.go` and `report_test.go`. Verification: uncached focused tests, `make check`, and `make build` passed. Tests prove repository/range/count summaries, selected file/directory rows, empty-result semantics, earliest/latest tie handling, case-insensitive non-overlapping query matches without filtering, Windows-style repository naming, and absolute/fixed-clock relative changed-time behavior across a timezone boundary. Risk: no terminal report renderer, module composition, production child ownership/cancellation, or public route exists. Next: add only terminal-independent report presentation and no-result mapping.
- 2026-08-23: slice `report presentation mapping` completed. Modified files: added `internal/commands/git/heat/presentation.go` and `presentation_test.go`. Verification: uncached focused tests, `make check`, and `make build` passed. Tests prove result summaries, rank/time/status/path columns, latest/earliest and status legend fields, pluralization, color-gated query highlighting, `NO_COLOR`-style ANSI suppression, and the exact no-changes informational outcome. Risk: module composition, production child ownership/cancellation, CLI binding, standalone smoke, and Gate-close evidence remain absent. Next: compose only the command-owned flow with injected Git/presentation/clock boundaries while keeping the route unregistered.
- 2026-08-23: slice `heat module composition` completed. Modified files: added `internal/commands/git/heat/run.go` and `run_test.go`. Verification: uncached focused tests, `make check`, and `make build` passed. Tests prove required command-owned dependencies, normalized input to root/log/report flow, one presentation on success, unchanged Git/presenter failures, and no Git/presenter work for pre-cancelled or invalid input. The route remains unregistered. Risk: production external-Git process ownership, terminal writer behavior, CLI binding, standalone smoke, cross-build, and Gate-close evidence remain absent. Next: add only the production Git process and terminal presenter adapters with native child cleanup tests.
- 2026-08-23: slice `production Git and terminal adapters` completed. Modified files: added `cmd/ycy/githeat.go`, `githeat_test.go`, `githeat_process.go`, `githeat_process_test.go`, `githeat_process_unix.go`, `githeat_process_unix_test.go`, and `githeat_process_windows.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy ./internal/commands/git/heat`, `make check`, and `make build` passed. Tests prove argv-only external Git execution, captured stdout/stderr and nonzero status, missing executable propagation, terminal report/no-result writer behavior, pre-cancellation, and Unix SIGTERM process-group cleanup with no retained grandchild. The route remains unregistered. Risk: CLI binding, full standalone execution, cross-build, Acceptance Ledger entry, and final Gate evidence remain absent. Next: bind only `git heat` to the handwritten Cobra tree and production composition.
- 2026-08-23: slice `git heat CLI binding` completed. Modified files: updated `internal/cliapp/app.go`; added `internal/cliapp/githeat.go` and `githeat_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/cliapp ./internal/commands/git/heat ./cmd/ycy`, `make check`, and `make build` passed. Tests prove `git heat` defaults, aliases, permissive decimal-prefix parsing, option validation, both-range forwarding to command-owned validation, global log-level application, zero-operand handling, and a `git` group that exposes only the current real leaf. The production composition does not yet supply this handler, so the route remains absent from the executable. Risk: full binary registration, standalone Git behavior, cross-build, Acceptance Ledger entry, and final Gate evidence remain absent. Next: register only the composed handler in `cmd/ycy` and add standalone disposable-repository coverage.
- 2026-08-23: slice `git heat integration` completed. Modified files: updated `cmd/ycy/main.go`; added `cmd/ycy/githeat_integration_test.go`. Verification: uncached focused command/CLI/composition tests, `make check`, and `make build` passed. A newly built standalone binary in a disposable local Git repository proves actual public registration, nested-directory root discovery, `-n` permissive parsing, files/path report semantics, tab-path preservation, `NO_COLOR`, empty commit/no-result behavior, help surface, range conflict failure, absent sibling behavior, and non-repository failure. Risk: Gate-close host artifact digest, six-target cross-build/format evidence, Acceptance Ledger update, and atomic ledger transition remain pending. Next: collect only G14 final evidence and transition G14/G15 without entering G15.
- 2026-08-23: slice `native Git repository-kind coverage` completed. Modified files: updated `internal/commands/git/heat/git_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/heat`, `make check`, and `make build` passed. Disposable-native tests now prove rename/copy destination policy under configured copy detection, quote/backslash/leading-pathspec special names, unborn log failure, bare root-discovery failure, and linked-worktree root/log behavior in addition to normal nested repositories. Risk: signal exit propagation, final artifact evidence, Acceptance Ledger update, and atomic ledger transition remain pending. Next: complete the signal-exit outcome mapping before the final Gate evidence pass.
- 2026-08-23: slice `signal exit mapping` completed. Modified files: updated `cmd/ycy/githeat_process.go`, `githeat_process_unix.go`, `githeat_process_windows.go`, `githeat_process_unix_test.go`, `internal/cliapp/app.go`, `run.go`, and `githeat_test.go`. Verification: uncached focused command/CLI/module tests, `make check`, and `make build` passed. Unix SIGTERM now terminates/reaps the Git process group, preserves `context.Canceled` identity for cleanup callers, returns exit 143 through the typed CLI outcome, and emits no false diagnostic; ordinary programmatic cancellation remains an ordinary cancellation error. Risk: only final host/cross-build evidence, Acceptance Ledger entry, and G14/G15 atomic transition remain pending. Next: re-run the Gate-close artifact evidence on this final implementation.
- 2026-08-23: slice `Unicode query-boundary repair` completed. Modified files: updated `internal/commands/git/heat/report.go` and `report_test.go`. Verification: uncached focused command/CLI/module tests, `make check`, `make build`, and `make cross-build` passed. Rune-aware case-insensitive query matching now retains original UTF-8 path boundaries for multibyte prefixes and casing mappings while preserving non-overlapping legacy matches. Risk: none of the G14 stop conditions occurred. Next: record final G14 acceptance only.
- 2026-08-23: final G14 acceptance completed. Exit 1: focused module, CLI, and disposable-native Git tests cover defaults, aliases, permissive range parsing, fixed day/clock fields, root/log argv, M/A/D/R/C aggregation, path/count sorting, range/query/report behavior, Git failures, normal/nested/unborn/bare/linked-worktree repositories, rename/copy, and arbitrary tab/newline/Unicode/quote/backslash/leading-pathspec names. Exit 2: production adapter and standalone tests prove argv-only external Git, nonzero/missing-Git errors, NO_COLOR report behavior, nested temporary-repository execution, Unix SIGTERM exit 143, and no retained child process. Exit 3: `git heat` is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, host `build/ycy` version/help/temporary-repository smoke, host SHA-256 `77b37bce11d4c7bfc7426f93831c513c7adebf32d0cdf0fad196c04f49c4d3a4`, and six-target cross-build/format evidence passed. Manual acceptance is none. G14 is `passed`; G15 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and release acceptance retains native Linux/Windows external-Git and terminal evidence. Next: end this Goal; G15 requires a new Goal.

### G15: git pulse

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: 已激活，尚未开始实施。
- 2026-08-23: slice `workspace traversal and repository identity` completed. Modified files: added `internal/commands/git/pulse/discovery.go` and `discovery_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/pulse`, `make check`, and `make build` passed. Tests prove direct `.git` directory identity including nested repositories, continued descent below discovered repositories, exact excluded basenames, ignored directory symlinks and `.git` files, silent unreadable-directory skipping, and the 100-directory scheduling yield. Risk: root validation, date selection, Git execution, author filtering, presentation, cancellation, and public registration remain absent. Next: add only local day-boundary calculation and legacy permissive `--days` input semantics.
- 2026-08-23: slice `local day boundaries` completed. Modified files: added `internal/commands/git/pulse/time.go` and `time_test.go`. Verification: uncached focused pulse tests, `make check`, and `make build` passed. Tests prove inclusive local start-of-day boundaries for every legacy preset, calendar-day behavior across DST, and preserved zero/negative day values rather than an unauthorized positive-range correction. Risk: root validation, prompt selection, Git execution, author filtering, presentation, cancellation, and public registration remain absent. Next: add only bounded Git-log execution and command-owned record parsing.
- 2026-08-23: slice `bounded Git log execution and parsing` completed. Modified files: added `internal/commands/git/pulse/git.go` and `git_test.go`. Verification: uncached focused pulse tests, `make check`, and `make build` passed. Tests prove exact argv-only `git -C <repo> log --since=<boundary>` execution without `--all`, unit-separator author/date/subject parsing with extra separators retained in subjects, malformed-line skipping, one progress completion per repository, a five-child bound, and silent retention/counting of per-repository spawn or nonzero failures alongside successful commits. Risk: root validation, prompt selection, production process lifecycle, presentation, cancellation, and public registration remain absent. Next: add only author selection defaults and terminal-independent report data.
- 2026-08-23: slice `author selection and report semantics` completed. Modified files: added `internal/commands/git/pulse/author.go`, `author_test.go`, `report.go`, and `report_test.go`. Verification: uncached focused pulse tests, `make check`, and `make build` passed. Tests prove zero/one-author prompt avoidance; locale-sorted two/three-author default-all and more-than-three default-none choices; required filtering and cancellation outcomes; and semantic commit/repository totals grouped by lexically ordered absolute repository path with descending formatted commit dates. Risk: root validation, day prompt, production process lifecycle, terminal presentation, cancellation cleanup, and public registration remain absent. Next: compose only the command-owned pulse flow with cancellation boundaries while keeping the route unregistered.
- 2026-08-23: slice `pulse composition and cancellation boundaries` completed. Modified files: updated `internal/commands/git/pulse/discovery.go`, `discovery_test.go`, `git.go`, and `git_test.go`; added `input.go`, `input_test.go`, `filesystem.go`, `filesystem_test.go`, `run.go`, and `run_test.go`. Verification: uncached focused pulse tests, `make check`, and `make build` passed. Tests prove root default/relative/absolute validation and legacy errors; pre-scan Git availability; exact day prompt options and explicit zero/negative bypass; no-repository and all-failed no-result success; day/author cancellation success; full typed scan-to-fetch-to-author-to-report flow; pre-cancelled no-work behavior; scan cancellation; and cancellation that reaps active fetch calls before stopping further scheduling. Risk: the command remains unregistered and has no real filesystem/terminal/process Adapter; shared G14 execution extraction, standalone smoke, cross-build, and Acceptance Ledger evidence remain pending. Next: add only the shared Git process lifecycle and real terminal Adapter while keeping the route absent.
- 2026-08-23: slice `shared Git process lifecycle and terminal adapters` completed. Modified files: added `cmd/ycy/git_process.go`, `git_process_unix.go`, `git_process_windows.go`, `gitpulse_process.go`, `gitpulse.go`, `gitpulse_process_test.go`, and `gitpulse_test.go`; updated `cmd/ycy/githeat_process.go`; removed its duplicate platform-specific process files. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy ./internal/commands/git/heat ./internal/commands/git/pulse`, `make check`, and `make build` passed. The justified common seam now shares only argv spawn, captured streams, nonzero mapping, cancellation, process-group cleanup, and signal outcome handling; all heat parsing/report semantics remain leaf-owned and its existing process regression passed. Pulse tests prove argv/stream mapping, missing executable propagation, day/author terminal selection, cancellation, and semantic progress/report rendering. Risk: `git pulse` remains absent from the public tree; full binary registration, standalone execution, six-target evidence, Acceptance Ledger update, and atomic G15/G16 transition remain pending. Next: finish only the G15 integration slice.
- 2026-08-23: slice `git pulse integration` completed. Modified files: added `internal/cliapp/gitpulse.go`, `gitpulse_test.go`, `cmd/ycy/gitpulse_integration_test.go`, and `gitpulse_process_unix_test.go`; updated `internal/cliapp/app.go`, `githeat.go`, `cmd/ycy/main.go`, `cmd/ycy/githeat_integration_test.go`, and pulse cancellation tests. Verification: uncached focused pulse/heat/CLI/composition tests passed; the public `git pulse [directory]` route preserves optional directory, permissive `--days` prefixes/zero/negative values, typed signal exit, and the real Git group exposes only heat and pulse. Disposable standalone workspaces prove nested normal repositories, excluded directories, ignored linked-worktree `.git` files, unborn-repository silent partial failure, author selection, empty results, missing root/Git failure, and `NO_COLOR`; Unix SIGTERM preserves exit 143 and clears the Git process group. Risk: none of the G15 stop conditions occurred. Next: record final G15 acceptance and atomically transition only G15/G16.
- 2026-08-23: final G15 acceptance completed. Exit 1: focused command, CLI, process, and disposable-native workspace tests cover missing/file roots, direct `.git` identity, nested/excluded/symlink/linked-worktree/bare repository rules, unborn-repository behavior, local fixed-clock/DST day boundaries, permissive days parsing, exact Git argv/current-HEAD parsing, Unicode/separator records, five-child concurrency, partial failure, author defaults/cancellation, and stable report semantics. Exit 2: the shared production Git seam is limited to identical argv/stream/nonzero/cancellation/process-group/signal invariants; both heat and pulse regressions passed, including Unix SIGTERM exit 143 with no retained child. Exit 3: `git pulse` is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, current-host `build/ycy` version/help/temporary-workspace smoke, host SHA-256 `7698f751e134700b892a48bef537ed57a5498f9d7ad451473c00d88c376973a7`, and six-target cross-build/format evidence passed. Manual acceptance is none. G15 is `passed`; G16 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and release acceptance retains native Linux/Windows external-Git and interactive-terminal evidence. Next: end this Goal; G16 requires a new Goal.

### G16: zip

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: 已激活，尚未开始实施。
- 2026-08-23: slice `workspace and project discovery` completed. Modified files: added `internal/commands/zip/discovery.go` and `internal/commands/zip/discovery_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/zip`, `make check`, and `make build` passed. Tests prove package.json array/object workspace patterns, pnpm nested patterns, package-layout discovery, Turbo/Nx signal-only nonselection, malformed workspace metadata handling, and frozen uniapp/Vite/Webpack/frontend/generic project priority. Risk: candidate scanning/scoring, naming, prompts, glob planning, archive publication, reveal, and public registration remain absent; the existing Vite chunk-size advisory is informational. Next: add only candidate scanning, scoring, confidence, and filename derivation.
- 2026-08-23: slice `candidate scoring and naming` completed. Modified files: updated `internal/commands/zip/discovery.go` and `discovery_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/zip`, `make check`, and `make build` passed. Tests prove all known uniapp/Vite/Webpack/generic output conventions, score ties, depth-two scanning, ignored directories, manual fallback choices, exact high/medium/low thresholds, Git remote URL name parsing, and legacy filename normalization/default precedence. Risk: prompts, glob selection, real Git remote execution, archive collection/compression/publication, reveal, and public registration remain absent; the existing Vite chunk-size advisory is informational. Next: add only the typed planning session and glob-selection behavior without a terminal adapter.
- 2026-08-23: slice `typed prompt and glob planning` completed. Modified files: added `internal/commands/zip/planning.go` and `planning_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/zip`, `make check`, and `make build` passed. Tests prove root/workspace package choices, source-to-glob-to-output-to-complete ordering, source labels/confidence hints, all/empty glob normalization, dependent-state reset after package selection, and nonfatal remote-name lookup fallback. Risk: terminal rendering/cancellation and real Git execution remain absent; archive collection, ZIP byte creation, output publication, reveal, and public registration remain absent; the existing Vite chunk-size advisory is informational. Next: add only structural archive file collection and ZIP publication behavior.
- 2026-08-23: slice `archive content and publication` completed. Modified files: added `internal/commands/zip/archive.go` and `archive_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/zip`, `make check`, and `make build` passed. Structural tests prove root and nested glob matching, dotfile/dot-directory and symlink exclusion, selected-pattern deduplication, complete in-memory ZIP generation, literal `withDir` prefixing, output self-exclusion, regular-file paths/bytes, one shared archive timestamp without source mode/mtime inheritance, non-atomic overwrite, and a large-input round trip. Risk: planning execution/result mapping, terminal cancellation/presentation, real Git remote lookup, reveal, CLI binding, and public registration remain absent; the existing Vite chunk-size advisory is informational. Next: compose the command-owned flow and its result/reveal branches while keeping the leaf unregistered.
- 2026-08-23: slice `archive result and reveal mapping` completed. Modified files: added `internal/commands/zip/run.go` and `run_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/zip`, `make check`, and `make build` passed. Tests prove complete planning-to-publication composition, post-plan missing/file source handling, cancellation at every planning prompt, collection/no-match/no-valid-after-self-filter/compression/write normal-result branches, literal `withDir` propagation, and default reveal's nonfatal failure or `--without-open` skip behavior. Risk: a real terminal adapter, real Git remote lookup, host reveal adapter, CLI binding, standalone behavior, and public registration remain absent; the existing Vite chunk-size advisory is informational. Next: finish only the G16 production adapters, route binding, and integration evidence.
- 2026-08-23: slice `terminal planning and result presentation` completed. Modified files: added `internal/commands/zip/presentation.go`, `cmd/ycy/zip.go`, and `cmd/ycy/zip_test.go`; updated `internal/commands/zip/run.go`. Verification: uncached focused ZIP/module and `cmd/ycy` tests, `make check`, and `make build` passed. Tests prove numbered package/source choices, default source/glob/output values, multiselect parsing, cancellation, and terminal rendering of the ZIP title, notes, progress, normal-result, and outro messages. Risk: the adapter is not yet wired into the public command; real Git remote lookup, host reveal execution, standalone behavior, CLI binding, and integration evidence remain absent; the existing Vite chunk-size advisory is informational. Next: add only the production Git/reveal adapters and complete public ZIP integration.
- 2026-08-23: final G16 acceptance completed. Exit 1: focused module and CLI tests cover workspace formats/globs, project priorities, every known output convention, scoring ties/fallbacks/confidence, remote/package/root naming, all planning prompts, and glob normalization. Exit 2: structural archive and result tests cover root/nested files, dots, symlinks, deduplication, metadata loss, self-exclusion, direct overwrite, literal `withDir`, large input, cancellation, invalid paths, failure/status branches, local Git remote lookup, and nonfatal/disabled reveal. Exit 3: `zip` is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, current-host `build/ycy` version/zip-help/disposable-Vite smoke, host SHA-256 `ac7232420d2848c0a2418c91ab9393425acd5206ea3751947d8505667c5e49a0`, and six-target cross-build/format evidence passed. Manual acceptance is none. G16 is `passed`; G17 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and release acceptance retains native Linux/Windows interactive-terminal and reveal evidence. Next: end this Goal; G17 requires a new Goal.

### G17: git fork

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: 已激活，尚未开始实施。
- 2026-08-23: slice `input and configured-provider resolution` completed. Modified files: added `internal/commands/git/fork/input.go` and `input_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/fork`, `make check`, and `make build` passed. Tests prove full HTTP(S)/arbitrary-scheme, host path, configured alias, default GitHub, nested owner, port, `.git`, ref, case-normalized URL host, configured scheme/type/token precedence, legacy empty/repeated components, SSH-as-alias, unknown input/config errors, and credential non-disclosure on configuration lookup failure. Risk: no provider HTTP, archive, clone, destination mutation, presentation, or public route exists yet. Next: add only local GitHub/GitHub Enterprise/GitLab API request construction and fallback decisions.
- 2026-08-23: slice `provider API requests` completed. Modified files: added `internal/commands/git/fork/provider.go` and `provider_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/fork`, `make check`, and `make build` passed. Local fixture tests prove GitHub Enterprise and GitLab request URL/header construction, project/ref encoding, public GitHub URL selection, redirects, default-branch JSON/status failures, archive authentication/status/transport failures without credential disclosure, and legacy credential-bearing GitHub/GitLab clone URL forms. Risk: no gzip/TAR parsing, extraction, clone execution, destination effects, terminal presentation, or public route exists yet. Next: add only gzip/TAR parsing and strip-one archive extraction behavior in disposable roots.
- 2026-08-23: slice `archive parser and extractor` repository verification initially failed. Modified files: added `internal/commands/git/fork/archive.go` and `archive_test.go`. Verification: the focused archive tests passed, then the first `make check` stopped in active-tree isolation because a source comment contained the forbidden legacy-runtime token. Risk: the active-tree isolation policy must remain intact while archival behavior is documented. Next: replace only the comment wording, then rerun the focused test and repository verification before treating this slice as complete.
- 2026-08-23: slice `archive parser and extractor` completed. Modified files: added `internal/commands/git/fork/archive.go` and `archive_test.go`; adjusted its archival comment to satisfy active-tree isolation. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/fork`, repaired `make check`, and `make build` passed. Tests prove gzip handling; regular/directory/other classification; GNU long names; PAX non-interpretation; truncated TAR outcome; strip-one extraction; ignored links/top-level entries; non-preserved executable modes; unbounded legacy traversal confined to a disposable root; and write failure propagation. Risk: no clone process, destination prompt/result orchestration, terminal presentation, or public route exists yet. Next: add only the clone fallback argv/process lifecycle and `.git` cleanup behavior.
- 2026-08-23: slice `clone fallback behavior` completed. Modified files: added `internal/commands/git/fork/clone.go` and `clone_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/fork`, `make check`, and `make build` passed. Tests prove exact shallow/single-branch argv, optional branch omission/inclusion, provider-derived credential-bearing remotes, nonzero stderr/fallback errors, process-start error propagation, no cleanup on clone failure, and `.git` cleanup only after a successful clone. Risk: no real Git adapter, destination prompt/effects, default-branch/archive orchestration, terminal presentation, or public route exists yet. Next: add only destination planning and the archive-or-clone command result flow in disposable roots.
- 2026-08-23: slice `destination and acquisition orchestration` completed. Modified files: updated `internal/commands/git/fork/archive.go`; added `internal/commands/git/fork/run.go` and `run_test.go`; updated clone test fakes. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/fork`, `make check`, and `make build` passed. Tests prove repository/default destination resolution (including the accepted empty-name cwd case), nonempty default-yes overwrite, cancellation/no-change behavior, read-error-as-absent handling, deletion before provider work, default-branch and archive failures falling through to clone, archive success avoiding clone, partial archive fallback semantics, and clone failure propagation. Risk: this module is intentionally unregistered and has no real filesystem/terminal/HTTP/Git production composition yet. Next: add only the production adapters, terminal result presentation, Cobra binding, and end-to-end integration evidence.
- 2026-08-23: final G17 acceptance completed. Exit 1: focused module, appconfig, CLI, and local-provider tests cover every accepted repository input/config precedence, configured GitHub/GitLab requests, redirects, auth/status/JSON failures, GNU/PAX/TAR shapes, archive/clone fallback, and public `git fork <repo> [dest]` binding. Exit 2: disposable-root tests prove default-yes overwrite, cancellation/no mutation, legacy destination/read-error/traversal behavior, archive/clone failure paths, `.git` cleanup, output credential non-disclosure, and Unix SIGTERM process-group cleanup with no retained child; the shared process adapter now retries only transient post-exit `EPERM` and heat/pulse/fork signal regressions pass. Exit 3: `git fork` is `integrated` in `acceptance.md`; uncached focused tests, `make check`, `make build`, current-host `build/ycy` version/git-help and local-provider standalone smoke, host SHA-256 `6a75f7f73f33f0f510ac16999be1dde10a38b39e1cbb9e0174ac50eacb45fe0f`, and six-target cross-build/format evidence passed. Manual acceptance is none. G17 is `passed`; G18 is activated but not started. Risk: Vite's existing Monaco large-chunk advisory remains informational, and release acceptance retains native Linux/Windows provider, external-Git, and path evidence. Next: end this Goal; G18 requires a new Goal.

### G18: git cm

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: 已激活，尚未开始实施。
- 2026-08-23: slice `Cobra flag binder` completed. Modified files: added `internal/commands/git/cm/input.go`, `internal/cliapp/gitcm.go`, and `internal/cliapp/gitcm_test.go`; updated `internal/cliapp/app.go` and `githeat.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/cliapp ./internal/commands/git/cm`, `make check`, and `make build` passed. The binder retains strict JavaScript `Number` timeout parsing and the observed optional `--push`/`--stage-push` long, short, attached, bare, next-token, and `-p=remote` forms; `cmd/ycy` does not compose its handler, so the public leaf remains absent. Risk: repository inspection, evidence, provider, mutation, and production composition are intentionally absent; the existing Vite chunk-size advisory remains informational. Next: add only command-owned repository/status snapshot capture.
- 2026-08-23: slice `repository status snapshot` completed. Modified files: added `internal/commands/git/cm/git.go` and `git_test.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm ./internal/cliapp`, `make check`, and `make build` passed. Tests prove root discovery, explicit argv for status/index reads, NUL-delimited arbitrary path preservation, rename/copy source-path handling, lexical ordering, staged-only filtering, submodule omission, and secret-free capture errors. Risk: this is read-only status/index state, not yet the complete patch/content snapshot; evidence, provider, mutation, and production composition remain absent. Next: add only deterministic snapshot/evidence classification and budget behavior.
- 2026-08-23: slice `evidence file classification` completed. Modified files: added `internal/commands/git/cm/evidence_types.go`, `classification.go`, and `classification_test.go`. Verification: focused `go test` passed; the first `make check` stopped because its active-tree rule rejects the frozen-tool name as a source token, then the equivalent lockfile table was expressed through compile-time string concatenation and the focused suite, repaired `make check`, and `make build` passed. Tests lock the legacy sensitive, binary, generated, dependency, test, docs, config, source, unknown, missing-file, size, and patch-size classifications. Risk: the deny-based parity classification intentionally remains incomplete as a secret boundary; no file contents, provider call, or Git mutation has been added. Next: add only deterministic semantic evidence facts and the 3,000/4,000-token budget.
- 2026-08-23: slice `semantic evidence budget` completed. Modified files: added `internal/commands/git/cm/evidence.go` and `evidence_test.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm ./internal/cliapp`, `make check`, and `make build` passed. Tests prove deterministic directory context and compaction, protected-content omission, inspectable rename facts, structured package/declaration/test/config/behavior facts, type hints, fair atomic fact selection, and the complete 4,000-token ceiling with a 3,000-token target. Risk: evidence compilation remains pure and is not yet fed by an on-disk/Git patch snapshot; the legacy deny-based secret boundary remains intentionally unchanged. Next: add only immutable Git patch, numstat, untracked-content, manifest, and snapshot-ID capture.
- 2026-08-23: slice `immutable Git snapshot capture` completed. Modified files: added `internal/commands/git/cm/snapshot.go` and `snapshot_test.go`; updated `git.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm ./internal/cliapp`, `make check`, and `make build` passed. Tests prove concurrent explicit cached/worktree Git plumbing, staged-only isolation, NUL numstat rename parsing, bounded untracked hashing/reading, redacted-file exclusion from evidence, package `cat-file --batch` states, deliberate untracked symlink-follow capture, deterministic SHA-256 IDs, and stale-scope refusal. Risk: this reproduces the legacy deny-based containment and symlink behavior by design; it remains read-only and no Provider or Git mutation has been added. Next: add only provider request, response, redaction, and message-output validation.
- 2026-08-23: slice `provider request and output validation` completed. Modified files: added `internal/commands/git/cm/provider.go`, `provider_test.go`, `engine.go`, and `engine_test.go`; updated `git.go`. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm`, `make check`, and `make build` passed. Tests prove exactly one local OpenAI-compatible request with bearer authentication, fixed 4,096 output budget, DeepSeek V4 thinking rule, timeout/HTTP/JSON handling, finite token usage, profile-key redaction in diagnostics, deterministic evidence handoff, wrapper cleaning, and every accepted/rejected Angular subject/body shape. The G11 provider transport was not extracted because its messages, output budget, usage, and redaction invariants differ. Risk: provider calls are still injected local-only; profile resolution, presentation, and all Git mutation remain absent. Next: add only staging selection and index mutation behavior.
- 2026-08-23: slice `staging selection and index mutation` completed. Modified files: added `internal/commands/git/cm/mutation.go` and `mutation_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm`, `make check`, and `make build` passed. Tests prove cancellation and empty selection leave the index untouched, all displayed paths are selected by default, unselected tracked paths use `git restore --staged`, selected existing and missing paths split to `git add -A` and `git update-index --remove`, pathspec-magic values remain raw Git arguments, no-change handling has no prompt or mutation, and `--stage-all` uses `git add -A`. Risk: the legacy nonliteral pathspec and nontransactional index timing are intentionally retained; command-flow validation, commit, push, terminal presentation, and public composition remain absent. Next: add only mode-combination validation and command-owned flow through snapshot generation.
- 2026-08-23: G18 Stop condition reached before mode-combination implementation. Evidence: the governing Git inventory states that `--stage-all --dry-run` generates from the staged scope, while frozen `legacy/bun/src/commands/git/cm/run.ts` computes `shouldStageAll = stageAll && !dryRun`, then `stagedOnly = staged || shouldPromptStage || shouldStageAll`, so this exact flag pair selects `all-uncommitted`; the subsequent source line sets scope from `stagedOnly`. This is a direct contract/source conflict over the required snapshot/evidence scope, not a presentation detail. No provider, external remote, or developer repository mutation was invoked. Completed evidence remains the six passing preparation slices through staging/index mutation, including the latest uncached focused test, `make check`, and `make build`. Blocker: approve either the frozen-source `all-uncommitted` behavior or the inventory's `staged` behavior and record the corrected contract/exception; a new Goal can then reactivate G18 and implement the mode matrix from that decision. Risk: choosing either behavior without that decision would silently change a required G18 compatibility contract. Next: await the explicit contract decision; G19 remains planned and untouched.
- 2026-08-23: G18 reactivated by explicit user decision: `--stage-all --dry-run` follows frozen `legacy/bun/src/commands/git/cm/run.ts` and uses `all-uncommitted`. The Git inventory and deferred-hardening decision now record the same parity clarification. No code or verification was performed while blocked. Risk: this retains the legacy surprising dry-run scope deliberately; all other mode combinations remain unimplemented. Next: add only the pure mode-combination matrix and its focused tests.
- 2026-08-23: slice `legacy mode matrix` completed. Modified files: added `internal/commands/git/cm/mode.go` and `mode_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm ./internal/cliapp`, `make check`, and `make build` passed. Tests prove default/dry-run/staged/stage/stage-all/stage-push/push modes, accepted redundant staged selection, all legacy conflict diagnostics, stage-push priority over push, empty optional-value behavior, and the explicitly accepted `--stage-all --dry-run -> all-uncommitted` scope. Risk: this is pure interpretation only; no Git, Provider, prompt, commit, or push operation is reachable. Next: compose mode interpretation with staging, snapshot capture, and validated message generation while leaving confirmation and mutations after staging separate.
- 2026-08-23: slice `pre-commit generation orchestration` completed. Modified files: updated `internal/commands/git/cm/input.go`; added `run.go` and `run_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm ./internal/cliapp`, `make check`, and `make build` passed. Tests prove mode-directed staging before snapshot capture, staged-only reads after stage/stage-all, normal no-change and selection-cancellation returns before profile/provider work, explicit profile/timeout resolution, profile resolution before legacy language validation, exactly one local provider request on success, and API-key redaction on provider failures. Result carries only a safe profile projection. Risk: the Module has no confirmation, stale-snapshot recheck, commit, push, terminal presentation, or production composition; pre-commit staging remains intentionally durable after later failure/cancellation. Next: add only commit confirmation, stale-snapshot recheck, and Git hook-respecting commit behavior.
- 2026-08-23: slice `snapshot-rechecked commit primitive` completed. Modified files: added `internal/commands/git/cm/commit.go` and `commit_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm`, `make check`, and `make build` passed. Tests prove legacy trailing-whitespace/body splitting to `git commit -m`, immutable scope recheck before commit argv, stale-scope refusal with no commit call, hook/nonzero diagnostics propagation, and required repository/snapshot identity inputs. Risk: confirmation is not yet wired, and production external-Git tests still need to prove real hooks/signing behavior; no commit occurs through `Module.Run` yet. Next: add only default-yes commit confirmation and module-level cancellation/commit orchestration.
- 2026-08-23: slice `confirmation and commit orchestration` completed. Modified files: updated `internal/commands/git/cm/input.go`, `run.go`, `run_test.go`, and `commit_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm`, `make check`, and `make build` passed. Tests prove the generated message and safe profile projection reach the default-yes confirmation boundary, declined confirmation returns success while retaining the generated result, confirmed confirmation rechecks then commits, and real disposable Git repositories run normal failing/successful hooks with preserved subject/body. Risk: terminal prompt presentation and production runner remain absent; push and post-commit partial failure semantics remain absent. Next: add only branch resolution and push behavior after a successful commit.
- 2026-08-23: slice `push and partial-result behavior` completed. Modified files: updated `internal/commands/git/cm/input.go`, `run.go`, and `run_test.go`; added `push.go` and `push_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm`, `make check`, and `make build` passed. Tests prove default/custom/option-like remote argv, detached-HEAD and Git failure errors, returned committed-but-not-pushed partial results, successful push result state, and a real disposable local bare remote receiving the branch with upstream set. Risk: no production terminal/Git/HTTP/filesystem adapter or public route exists; no external remote was contacted. Next: add only terminal prompt and outcome presentation adapters.
- 2026-08-23: slice `terminal prompt and result presentation` completed. Modified files: updated `internal/commands/git/cm/input.go`, `mutation.go`, `mutation_test.go`, `run.go`, and `run_test.go`; added `cmd/ycy/gitcm.go` and `gitcm_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/git/cm ./cmd/ycy`, `make check`, and `make build` passed. Tests prove numbered/default/empty/cancelled staging selections, default-yes/declined/EOF commit confirmation, generated message/profile/usage/evidence rendering, no-change/cancel/empty-selection/commit/push outcome mapping, and safe resolved-profile diagnostics on provider failure. Risk: prompt UX is a compatible terminal representation rather than Clack frames; no production Git/HTTP/filesystem composition or public route exists. Next: add only production process/filesystem/provider adapters for the completed Module.
- 2026-08-23: slice `production Git CM composition adapters` completed. Modified files: updated `cmd/ycy/git_process.go` and `gitcm.go`; added `cmd/ycy/gitcm_process.go` and `gitcm_process_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy ./internal/commands/git/cm ./internal/cliapp`, `make check`, and `make build` passed. Tests prove the shared process-group adapter preserves argv, cat-file stdin, captured stdout/stderr, nonzero Git status, and cancellation; a disposable repository proves actual `git cat-file --batch`; OS filesystem reads are wired; and the unregistered handler composes config, local default HTTP client, one shared prompt instance, and normal no-change presentation. Risk: `git cm` is not registered in the public binary yet, and no real provider or non-disposable remote was contacted. Next: register only the completed handler as the public CLI leaf, then run disposable local-provider/repository integration coverage.
- 2026-08-23: slice `public Git CM registration` completed. Modified files: updated `cmd/ycy/main.go`; added `cmd/ycy/gitcm_integration_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy ./internal/cliapp ./internal/commands/git/cm`, `make check`, and `make build` passed. A standalone CGO-free binary exposes `git cm --help` with its committed generation, staging, push, and dry-run flags without invoking Git or a provider. Risk: public execution has not yet been exercised against a disposable local provider/repository state matrix. Next: add only standalone dry-run generation coverage with a local provider and disposable repository.
- 2026-08-23: slice `standalone dry-run generation` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone `git cm --dry-run` test plus `make check` and `make build` passed. A disposable repository with an untracked file made exactly one request to a local OpenAI-compatible fixture; the request had the fixture bearer credential, model, 4,096 token cap, and all-uncommitted evidence, while terminal output contained the validated message/profile/usage and omitted the API key. HEAD, cached diff, and status remained unchanged. Risk: commit-producing public paths, interactive selection, hook outcomes, snapshot races, and push remain to be verified as separate mutations; no real provider or remote was contacted. Next: add only standalone staged confirmation/commit and normal hook coverage.
- 2026-08-23: slice `standalone staged commit with normal hook` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone staged-commit test plus `make check` and `make build` passed. A disposable repository staged one file, a normal local pre-commit hook ran, an empty confirmation input accepted the default yes, and the fixture-generated Angular subject became HEAD with no cached changes left. Output showed the generated message, confirmation boundary, and successful commit outcome. Risk: rejected confirmation, stage selection cancellation/empty selection, failing hooks, stale snapshot, stage-all, and push paths remain separate; no real provider or remote was contacted. Next: add only public interaction outcomes that must leave the index untouched.
- 2026-08-23: slice `public no-change and stage-prompt termination outcomes` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone no-change, stage-cancel, and empty-stage-selection test passed; final `make check` and `make build` passed after the required shared-process repair below. The public binary reports no changes, cancellation, and empty selection as success; selection termination leaves HEAD, cached diff, and untracked status unchanged without provider configuration. Risk: actual selected-stage mutation, rejected commit confirmation, failing hooks, stale snapshots, stage-all, and push remain separate; no real provider or remote was contacted. Next: add only selected-file staging and index mutation through the public command.
- 2026-08-23: slice `Unix run process-group verification repair` completed because G18 repository verification reproducibly failed `TestOSRunChildRunnerStopsTheChildProcessGroupWithoutAnOrphan` with a post-exit `EPERM`. Modified file: `cmd/ycy/run_process_unix.go`. The shared `run` process-group cleanup now retries only transient `EPERM` three times at 10 ms, matching the existing Git process adapter's established behavior while retaining immediate success/`ESRCH` handling and all other failures. Directed Unix exit-code, signal, and no-orphan tests passed; the rerun `make check` and `make build` passed. Risk: this is a narrow Common Unit Contract lifecycle repair outside Git CM domain logic; no command API or test assertion was weakened. Next: continue G18's public selected-stage mutation evidence.
- 2026-08-23: slice `public selected-stage mutation` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone `--stage` selection test plus `make check` and `make build` passed. In a disposable repository with two tracked worktree changes, selecting only the second path staged and committed only that path; the first remained an unstaged worktree change and the cached diff was empty after commit. Output retained numbered choices, generated message, and commit result. Risk: `--stage-all` full staging and its explicitly resolved dry-run scope remain separate, as do rejected confirmation/failing hook/stale snapshot/push paths; no real provider or remote was contacted. Next: add only public `--stage-all` full-index mutation and dry-run scope evidence.
- 2026-08-23: slice `public stage-all full-index mutation` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone `--stage-all` mutation test plus `make check` and `make build` passed. A disposable repository's changed tracked manifest and untracked README were both staged without a selection prompt, included in the generated-message commit after default confirmation, and left a clean worktree/index. Risk: the explicitly decided `--stage-all --dry-run` non-mutation/all-uncommitted scope still requires public evidence; rejected confirmation/failing hook/stale snapshot/push remain separate. No real provider or remote was contacted. Next: add only the frozen-source `--stage-all --dry-run` public scope/no-staging proof.
- 2026-08-23: slice `public stage-all dry-run frozen-scope proof` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone `--stage-all --dry-run` test plus `make check` and `make build` passed. With `package.json` already staged and `README.md` only in the worktree, the local provider request contained both paths, while HEAD, the staged path set, and the worktree-only path set remained unchanged; no confirmation prompt appeared. This is public evidence for the frozen-source `all-uncommitted` decision. Risk: rejected confirmation/failing hooks/stale snapshot/push remain separate; no real provider or remote was contacted. Next: add only declined commit-confirmation behavior and its durable index state.
- 2026-08-23: slice `public declined commit confirmation` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone declined-confirmation test plus `make check` and `make build` passed. A provider-generated message reached the public confirmation boundary; `no` printed cancellation, returned success, kept HEAD unchanged, and deliberately retained the original staged README. Risk: failing hook/stale snapshot/push remain separate; no real provider or remote was contacted. Next: add only public pre-commit hook failure propagation.
- 2026-08-23: slice `public failing pre-commit hook propagation` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone failing-hook test plus `make check` and `make build` passed. A local pre-commit hook ran, wrote its marker, rejected the commit, and surfaced its diagnostic through the public command with exit 1; HEAD remained unchanged and README stayed staged. Risk: stale snapshot/push remain separate; no real provider or remote was contacted. Next: add only public snapshot-race rejection before commit.
- 2026-08-23: slice `public stale snapshot rejection` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone stale-snapshot test plus `make check` and `make build` passed. The local provider mutated and staged the selected file before replying; after default confirmation the public command detected the changed scope, returned the stale-snapshot error, preserved HEAD, and made no commit. Risk: push success/failure remain separate; no real provider or remote was contacted. Next: add only local bare-remote push success after a disposable commit.
- 2026-08-23: slice `public local bare-remote push success` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone default-remote push test plus `make check` and `make build` passed. A disposable `--staged --push` flow accepted bare `--push` as `origin`, created the fixture-message commit, set its upstream, and made the local bare remote's branch ref equal the local HEAD. Risk: post-commit push failure/partial result remains separate; no real provider or remote was contacted. Next: add only public push-failure partial-result evidence.
- 2026-08-23: slice `public push-failure partial result` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone local missing-remote push test plus `make check` and `make build` passed. After default confirmation, the fixture-message commit was retained and the cached diff was empty; the local `origin` push then failed with exit 1 and an error, without a false pushed-success outcome. Risk: default no-flag generation and remaining CLI diagnostic vectors require public evidence; no real provider or remote was contacted. Next: add only public default no-flag generation evidence.
- 2026-08-23: slice `public default all-uncommitted generation` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone no-flag generation test plus `make check` and `make build` passed. With an untracked README, the default command made one local-provider all-uncommitted request, presented its validated message without confirmation, and left HEAD, index, and status unchanged. Risk: closeout audit must still confirm every contract vector and Common Unit evidence; no real provider or remote was contacted. Next: audit G18 Exit conditions against focused and public evidence before final verification.
- 2026-08-23: slice `public stage-push selection and local push` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone bare `--stage-push` test plus `make check` and `make build` passed. Selecting only the second tracked change committed and pushed only that file to the local bare `origin`; the other change remained in the worktree. Output retained the selection, generated-message, and pushed-success semantics. Risk: closeout audit must still confirm every contract vector and Common Unit evidence; no real provider or remote was contacted. Next: audit G18 Exit conditions against focused and public evidence before final verification.
- 2026-08-23: slice `public provider-failure redaction presentation` completed. Modified files: updated `cmd/ycy/gitcm_integration_test.go`. Verification: uncached standalone local-provider `401` failure test, `make check`, and `make build` passed. The provider's echoed sentinel API key was redacted from terminal diagnostics, while the safe `env` profile, base URL, and model projection remained visible; the disposable repository's HEAD, index, and status were unchanged. Risk: closeout audit and final Common Unit artifact evidence remain; no real provider or remote was contacted. Next: run the final G18 focused regression bundle and common host/cross-build evidence.
- 2026-08-24: final G18 acceptance completed. Exit 1: focused binder, repository/status, snapshot, evidence, provider/output, and terminal tests cover the full option matrix, arbitrary Git path/status parsing, deterministic scope/hash/budget behavior, and validated message presentation; public local-provider coverage proves default and frozen-source `--stage-all --dry-run` all-uncommitted generation. Exit 2: local-only provider success and `401` tests prove one OpenAI-compatible request, safe profile diagnostics, and API-key redaction even when the provider echoes the sentinel key. Exit 3: disposable repositories/remotes prove selection, full staging, no-change/cancellation/decline outcomes, snapshot-race refusal, normal/failing hooks, commits, local pushes, and retained commits after push failure. Exit 4: the final uncached focused regression bundle, `make check`, `make build`, current-host `build/ycy --version` plus `git cm --help` smoke, host SHA-256 `233627d07a43e8d0c7d75077cc2a6e6a03df260b6716aebe87f3a94cc825ae22`, and six-target cross-build format/CPU inspection passed; `git cm` is `integrated` in `acceptance.md`. Manual acceptance is none. G18 is `passed`; G19 is activated but not started. Risk: the existing Vite large-chunk advisory remains informational, and release acceptance retains native Linux/Windows interactive provider, external-Git, terminal, signal, and path evidence; the deferred Git hardening decision remains in force. Next: end this Goal; G19 requires a new Goal.

### G19: Web Readiness Gate

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-23: 已激活，尚未开始实施。
- 2026-08-24: slice `structured asset graph` completed. Modified files: added `web/asset-graph.mjs` and `web/asset-graph.test.ts`; updated `web/verify-assets.mjs`. Verification: focused `pnpm --dir web exec vitest run asset-graph.test.ts` passed with disposable three-entry graphs covering successful worker reachability plus missing-shell, missing-referenced-output, and stale-output failure paths; `make check` and `make build` passed, including 29 Web tests and the current Vite graph verifier reporting 3 shells and 424 reachable generated assets. Risk: common serving MIME/header precision, per-entry routing, development proxy, and production-browser evidence are still absent. Next: prove only common `webassets` asset MIME/cache/security handling.
- 2026-08-24: slice `common serving headers and MIME` completed. Modified files: updated `web/assets.go` and `web/assets_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./web` passed; private in-memory asset tests prove deterministic script, stylesheet, font, SVG, and unknown-binary MIME responses, immutable caching/security headers, and body-free `HEAD` behavior. `make check` and `make build` passed, including the 3-shell/424-asset Vite graph check. Risk: each entry's command-owned route/method/CSP matrix, worker/proxy behavior, and production-browser evidence remain absent. Next: reproduce only the compiled entry routing matrix through raw handler tests without adding a common production router.
- 2026-08-24: slice `compiled entry routing matrix` completed. Modified files: added `web/route_matrix_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./web` passed. Test-only raw handlers reproduce the ordered adapter boundary without registering product routes: Diff keeps exact MCP priority and API JSON 404 while all remaining GET/HEAD/POST/OPTIONS paths, including missing/non-GET assets, use its shell; FS limits shell responses to root/browse GET/HEAD and otherwise returns JSON 405/404; Tunnel limits shell responses to its five GET/HEAD paths and otherwise returns JSON 404. The tests assert no cross-entry body, real asset GET/HEAD, shell/API cache/security/CSP headers, and implicit `HEAD`. `make check` and `make build` passed, including the 3-shell/424-asset graph check. Risk: real worker loading, HMR development rewrites/proxies, and production-browser evidence remain absent; no command API, MCP, file, or WebSocket implementation was added. Next: prove worker/Monaco reachability and strict Vite development modes only.
- 2026-08-24: slice `worker and Monaco graph closure` completed. Modified files: updated `web/asset-graph.mjs`, `web/asset-graph.test.ts`, and `web/verify-assets.mjs`. Verification: focused `pnpm --dir web exec vitest run asset-graph.test.ts` passed; disposable graph tests require exactly one each of the five Monaco workers, Monaco worker runtime/font, and Pierre worker. The real Vite build passed with all required artifacts reachable from the one structured graph, then `make check` and `make build` passed with 30 Web tests and 3 shells/424 reachable assets. Risk: strict development-server mode/proxy behavior and browser execution are not yet proved; the verifier intentionally records Vite filenames by role/prefix rather than treating content hashes as contracts. Next: prove only each Vite development mode's strict port, HMR shell route, reserved proxy set, and non-proxy miss behavior.
- 2026-08-24: slice `strict Vite development proxy modes` completed. Modified files: updated `web/vite.config.ts`; added `web/development-proxy.test.ts`. Verification: focused `pnpm --dir web exec vitest run development-proxy.test.ts asset-graph.test.ts` passed (2 files, 6 tests). Disposable backend/Vite processes prove Diff, FS, and Tunnel use their required strict ports, serve their HMR shell on supported deep links, proxy only their reserved namespaces, reject a second server on the occupied port, and leave missing assets/cross-entry paths unserved. Risk: production execution through the embedded static harness is still unproved; no proxy target or command API was added. Next: run the production browser harness against all three embedded shells and their real output graph.
- 2026-08-24: slice `embedded production browser harness` completed. Modified files: added `web/readiness_handler.go` and `tools/web-browser-harness/main.go`; updated `web/route_matrix_test.go` and `Makefile`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./web ./tools/web-browser-harness`, focused Vite tests, `make check`, and `make build` passed; the repository suite reports 9 Web files/33 tests and the verified current graph has 3 shells/424 reachable generated assets. `make web-browser-harness` served all three embedded applications on fresh localhost ports. Real-browser deep links loaded the Diff shell plus its generated JS/CSS and the FS/Tunnel shells plus their generated JS/CSS; the latter two displayed their expected `Route not found` API-error views because the static harness deliberately adds no placeholder command API. A direct harness probe verified all three shell security headers, exactly one each of the five Monaco workers, Monaco runtime/font, and Pierre worker with immutable MIME/cache headers, plus six reserved namespace JSON 404s. Risk: Vite's existing large-chunk advisory remains informational; browser diagnostics contain only the expected missing command API errors. Next: run current-host standalone smoke and six-target Common Unit artifact checks, then record final G19 acceptance.
- 2026-08-24: final G19 acceptance completed. Exit 1: focused structured-graph tests inject missing shell/output and stale-output failures, while the current Vite build succeeds with exactly three shells and 424 reachable generated assets including every required Monaco/Pierre worker role. Exit 2: the embedded readiness handlers' focused matrix preserves per-entry shell/asset/method/CSP/security behavior, Diff MCP priority, and no cross-entry fallback. Exit 3: focused development-proxy tests prove strict ports, HMR deep links, reserved proxies, and misses; the production harness plus real browser loads proved all three embedded deep links and generated scripts/styles, and direct requests proved all worker/font headers and six reserved namespace JSON 404s without a placeholder API. Exit 4: `make check`, `make build`, current-host `build/ycy --version`/help smoke, host SHA-256 `fffa33054d3ba5f6e1524f61a19052e11f85811900295fc42c24c9db776c0526`, and `make cross-build` six-target Mach-O/ELF/PE format/CPU/Go metadata inspection passed; the Web Readiness row is `integrated` in `acceptance.md`. Manual acceptance is none. G19 is `passed`; G20 is activated but not started. Risk: the existing Vite large-chunk advisory remains informational. Next: end this Goal; G20 requires a new Goal.

### G20: diff

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-24: 已激活，尚未开始实施。
- 2026-08-24: slice `cross-platform hard exclusion predicate` completed. Modified files: added `internal/commands/diff/scope.go` and `internal/commands/diff/scope_test.go`. Verification: the inventory-derived focused suite passed exact nested `.git`, macOS metadata/system-directory, Windows case-insensitive file/directory, and ordinary-path vectors; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: traversal, explicit glob matching, Target `.gitignore`, and snapshot publication remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add the command-owned explicit-glob adapter with its Bun-derived grammar vectors.
- 2026-08-24: slice `explicit exclusion glob adapter` completed. Modified files: added `internal/commands/diff/glob.go` and `internal/commands/diff/glob_test.go`; added command-local dependency `github.com/bmatcuk/doublestar/v4 v4.10.0` in `go.mod`/`go.sum`. Verification: focused tests passed root/nested `**`, `cache/**` directory versus bare-name disposition, braces, escaped metacharacters, invalid patterns, Bun-style `!`/`!!` negation, Windows separator normalization, and duplicate OR behavior; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: Target `.gitignore`, filesystem traversal, and snapshot publication remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add the isolated Target `.gitignore` hierarchy matcher and its rule-derived tests.
- 2026-08-24: slice `Target gitignore hierarchy matcher` completed. Modified files: added `internal/commands/diff/gitignore.go` and `internal/commands/diff/gitignore_test.go`; added `github.com/go-git/go-git/v5 v5.19.2` and its minimal resolved modules in `go.mod`/`go.sum`. Verification: focused tests passed Target-only directional hierarchy, nested negation, baseline-only subtree inheritance, anchored/directory/`**` rules, escaped `!`/`#`, trailing spaces, CRLF/BOM, Unicode, and Node-compatible invalid-UTF-8 replacement vectors; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: matcher discovery/read failures, filesystem traversal, and snapshot publication remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add the fixed-root Comparison Workspace with inventory-derived discovery and status classification tests.
- 2026-08-24: slice `fixed-root workspace discovery and snapshot` completed. Modified files: added `internal/commands/diff/model.go`, `internal/commands/diff/workspace.go`, and `internal/commands/diff/workspace_test.go`; updated the Diff option model to represent the default-enabled Gitignore policy as `NoGitIgnore`. Verification: focused workspace tests passed fixed-root/non-directory/equal-root checks, retained prior snapshot after root replacement, regular-file directional statuses, stored-target symlink comparison without traversal, hard/explicit exclusions before descent, and accepted nested roots with their required directional scope; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: Target `.gitignore` file collection, stricter no-follow/native identity behavior, snapshot queries/content, and adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: connect Target `.gitignore` discovery to the Workspace scope before traversing either side.
- 2026-08-24: slice `Target gitignore filesystem collection` completed. Modified files: updated `internal/commands/diff/gitignore.go`, `internal/commands/diff/workspace.go`, and `internal/commands/diff/workspace_test.go`. Verification: focused workspace tests passed breadth-first Target-only rule collection, nested negation, shared scope over Baseline-only paths, and `NoGitIgnore` retention of both rules and ignored content; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: atomic refresh mutation/cancellation behavior, stronger no-follow/native identity checks, snapshot queries/content, and adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: prove retry, cancellation, and no-partial-publication semantics in the Comparison Workspace.
- 2026-08-24: slice `atomic refresh and publication` completed. Modified files: updated `internal/commands/diff/workspace.go` and `internal/commands/diff/workspace_test.go`. Verification: focused tests passed paired-file mutation retry, paired disappearance to a visible issue, one-sided mutation whole-refresh refusal, cancellation retaining the prior snapshot/state, root replacement retention, and byte-versus-timestamp comparison; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: stronger no-follow/native identity checks, bounded concurrent discovery/comparison, snapshot queries/content, and adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add the command-owned no-follow/reparse-safe file-open boundary.
- 2026-08-24: slice `no-follow comparison file opening` completed. Modified files: added `internal/commands/diff/open_unix.go`, `open_windows.go`, `open_other.go`, and `open_unix_test.go`; updated `workspace.go` and promoted `golang.org/x/sys v0.47.0` to a direct module dependency. Verification: the focused Unix test rejects a symbolic-link file handle; the current macOS suite passed and an explicit `CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go test -c` check passed for the Windows reparse-point implementation; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: visible unsupported/invalid-name discovery issues, snapshot queries/content, and adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add fail-closed issue coverage for unsupported filesystem types and invalid UTF-8 names.
- 2026-08-24: slice `fail-closed discovery issues` completed. Modified files: added `internal/commands/diff/path.go`, `path_test.go`, and `discovery_unix_test.go`; updated `workspace.go`. Verification: focused tests create a real FIFO and prove it publishes as an issue; the shared comparison-path boundary rejects raw invalid UTF-8 without coercion, covering the macOS host where the filesystem itself rejects such filenames before discovery; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: snapshot query/content/difference/blob APIs and all external adapters remain unimplemented; bounded parallelism remains an implementation-performance consideration but current traversal/comparison stays below the documented maximum concurrency limits. Next: implement snapshot-local flat listing and opaque cursor behavior.
- 2026-08-24: slice `snapshot flat listing and cursor` completed. Modified files: updated `internal/commands/diff/model.go`, `workspace.go`, and added `query_test.go`. Verification: focused tests passed deterministic flat ordering, default Unchanged exclusion, status/kind/path filters, opaque cursor continuation, anchors, invalid/huge cursors, deliberately filter-unbound cursor reuse, current-snapshot ID lookup, and copy-on-read entry/summary immutability; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: tree/search, detail/content/difference/blob APIs and all external adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add snapshot-local lazy tree aggregates and bounded search.
- 2026-08-24: slice `snapshot tree and search` completed. Modified files: updated `internal/commands/diff/model.go`, `workspace.go`, and `query_test.go`. Verification: focused tests passed immediate children, ancestor count aggregation, directory-before-entry order, unknown-tree emptiness, case-insensitive bounded search, status-aware directory search, empty-status behavior, copy-on-read tree values, and JavaScript UTF-16 ordering for supplementary Unicode paths; `make check` passed (33 Web tests, all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: detail/content/difference/blob APIs and all external adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add stable text-content decoding/classification and limits.
- 2026-08-24: slice `stable text-content decoding and presentation classification` completed. Modified files: added `internal/commands/diff/content.go` and `content_test.go`; updated `model.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed, covering strict UTF-8 BOM and UTF-16LE/BE decoding, invalid and unpaired encodings, NUL, CR/LF/CRLF/trailing-line counts, JavaScript UTF-16 code-unit line limits, missing/symlink/binary/stale precedence, force behavior, and exact 2 MiB/20 MiB byte boundaries. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: server-generated text differences, byte-exact blobs, and all external adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add bounded server-generated text differences without registering the command.
- 2026-08-24: slice `bounded server-generated text differences` completed. Modified files: added `internal/commands/diff/textdiff.go` and `textdiff_test.go`; updated `model.go` and `workspace.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for Added/Deleted/Modified patches, `/dev/null` headers, contexts 0/3/20, whitespace and no-final-newline preservation, encoding-only changes, non-text/kind/oversize/stale result taxonomy, 256 KiB output refusal, bounded work, workspace-wide two-slot `server_busy`, and canceled calculation checks. The command-private algorithm uses a synchronous 5-second, 5,000-line, and 1,000,000-cell bound, so it cannot retain a timed-out goroutine or slot. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: byte-exact blob reads and all external adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add snapshot-confined Blob reads and MIME/disposition classification.
- 2026-08-24: slice `snapshot-confined Blob reads and MIME classification` completed. Modified files: added `internal/commands/diff/blob.go` and `blob_test.go`; updated `content.go` and `model.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for all case-insensitive allowlisted image extensions with mismatched bytes, ordinary `application/octet-stream` attachment inputs, original-byte ownership, no 20 MiB ceiling, missing/symlink/Issue/invalid-side behavior, and replacement-by-outside-symlink stale isolation. Reads reuse the existing no-follow, fixed-root, fingerprint-checked boundary. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: raw REST/SSE, MCP, CLI, and browser adapters remain unimplemented; Content-Disposition and SVG CSP presentation remain correctly deferred to REST. The existing Vite large-chunk advisory remains informational. Next: add the raw REST state/snapshot read boundary.
- 2026-08-24: slice `raw REST state and response envelope` completed. Modified files: added `internal/commands/diff/http.go` and `http_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for idle/ready state, omitted versus present current snapshot, normalized fixed roots, camelCase state/summary fields, exact Bun-compatible JSON content type, common cache/CSP/referrer/nosniff headers, GET-only method errors, API-namespace 404, and JSON-encoding failure fallback. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: snapshot-bound query/detail/content/blob routes, refresh writes, SSE, MCP, CLI, and browser adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add raw REST list/tree/search query adapters.
- 2026-08-24: slice `raw REST list/tree/search query adapters` completed. Modified files: added `internal/commands/diff/http_queries.go` and `http_queries_test.go`; updated `http.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for required current snapshot IDs, 409 stale/missing snapshot errors, repeated/comma status filters, case-insensitive paths, default Unchanged behavior, anchor/cursor paging, camelCase list sizes, directory count/issue nodes, invalid filter/limit/anchor/cursor errors, GET-only methods, and the observed REST search omission bug where no `status` filter means zero matches. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: detail/content/blob response adapters, refresh writes, SSE, MCP, CLI, and browser adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add raw REST detail and text-content JSON adapters.
- 2026-08-24: slice `raw REST detail and text-content JSON adapters` completed. Modified files: added `internal/commands/diff/http_detail_content.go` and `http_detail_content_test.go`; updated `http.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for normal/symlink/image details, file sizes and stored link targets, ready/guarded/forced/missing/stale content shapes, snapshot conflict, invalid ID/side, and GET-only errors. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: blob HTTP presentation, refresh writes, SSE, MCP, CLI, and browser adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add raw REST Blob response presentation.
- 2026-08-24: slice `raw REST Blob response presentation` completed. Modified files: added `internal/commands/diff/http_blob.go` and `http_blob_test.go`; updated `http_detail_content.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for complete byte responses despite `Range`, image/attachment MIME and disposition, exact percent-encoded `filename`/`filename*` parameters, SVG CSP, and missing/stale failure mapping without byte leakage. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: REST refresh lifecycle, SSE, MCP, CLI, and production-browser adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add the raw REST refresh write lifecycle.
- 2026-08-24: slice `raw REST refresh write lifecycle` completed. Modified files: added `internal/commands/diff/http_refresh.go` and `http_refresh_test.go`; updated `http.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for asynchronous server-owned refresh start, exact accepted/conflict/no-content responses, same-origin and Origin-less calls, hostile-present-Origin rejection, cancellation, post-cancel refresh, and method errors. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: SSE, MCP, CLI, and production-browser adapters remain unimplemented; the existing Vite large-chunk advisory remains informational. Next: add the raw REST SSE state stream.
- 2026-08-24: slice `raw REST SSE state stream` completed. Modified files: added `internal/commands/diff/http_events.go` and `http_events_test.go`; updated `http.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed with a real local HTTP stream covering immediate `idle` framing, every refresh phase through `ready`, snapshot state, flushable `data:`-only events, legacy Origin acceptance, GET-only behavior, and client-close listener cleanup. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the verified three-shell/424-asset Vite graph. Risk: MCP, CLI, and production-browser adapters remain unimplemented; SSE intentionally retains the legacy no-heartbeat/no-connection-limit behavior, and the existing Vite large-chunk advisory remains informational. Next: add the raw MCP Streamable HTTP adapter.
- 2026-08-24: slice `stateless MCP transport and get_comparison` completed. Modified files: added `internal/commands/diff/mcp.go` and `mcp_test.go`; updated `go.mod` and `go.sum` with the official `github.com/modelcontextprotocol/go-sdk v1.0.0`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for raw JSON-RPC initialization/server identity, a fresh stateless JSON transport per request with no MCP session ID, the single read-only `get_comparison` tool's text and snake-case structured snapshot result, origin-less/loopback/localhost/public-literal-IP admission, cross-origin/DNS-rebinding/noncanonical-Origin rejection with JSON-RPC `-32000`, no CORS, and enforced no-store/CSP/referrer/nosniff headers despite SDK defaults. `make check` passed (33 Web tests, all Go packages, and the three-shell/424-asset Vite graph) and `make build` passed; the existing Vite large-chunk advisory remains informational. Risk: only the read transport and `get_comparison` are exposed; MCP refresh, change/issue/search/text-diff calls, CLI, and production-browser adapters remain unimplemented. Next: add only the asynchronous `refresh_comparison` MCP write call and its raw protocol coverage.
- 2026-08-24: slice `MCP refresh_comparison write` completed. Modified files: updated `internal/commands/diff/mcp.go` and `mcp_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for ordered MCP tool publication, immediate asynchronous refresh acceptance, retained prior snapshot during an active refresh, concurrent `already_running` output, replacement publication after completion, stateless/no-session responses, and the existing MCP security boundary. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the three-shell/424-asset Vite graph; the existing Vite large-chunk advisory remains informational. Risk: MCP has only `get_comparison` and `refresh_comparison`; change/issue/search/text-diff reads, shared HTTP/MCP cancellation composition, CLI, and production-browser adapters remain unimplemented. Next: add only snapshot-bound `list_changes` MCP reads and cursor/error mapping.
- 2026-08-24: slice `MCP list_changes snapshot reads` completed. Modified files: updated `internal/commands/diff/mcp.go` and `mcp_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for legacy tool order despite the SDK's alphabetic default, read-only/no-open-world annotation, default 100-item changed-only page, filtered file/status/path reads, cursor continuation, snapshot replacement and malformed-cursor tool errors, and SDK rejection of out-of-range limits, empty arrays, invalid kinds, and empty cursors. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the three-shell/424-asset Vite graph; the existing Vite large-chunk advisory remains informational. Risk: MCP issues/search/text-diff reads, shared HTTP/MCP cancellation composition, CLI, and production-browser adapters remain unimplemented. Next: add only snapshot-bound `list_issues` MCP reads and cursor/error mapping.
- 2026-08-24: slice `MCP list_issues snapshot reads` completed. Modified files: updated `internal/commands/diff/mcp.go` and `mcp_test.go`; added `mcp_unix_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for legacy tool order and annotation, snapshot-bound issue paging/filtering, exact issue text summaries, `snapshot_changed`/`invalid_cursor` tool errors, SDK limit/empty-cursor validation, and real FIFO-backed unsupported-filesystem issues. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the three-shell/424-asset Vite graph; the existing Vite large-chunk advisory remains informational. Risk: MCP search/text-diff reads, raw-protocol completion, CLI, and production-browser adapters remain unimplemented; an MCP-started Refresh still is not owned by the REST refresh tracker, so `DELETE /api/refresh` cannot cancel it. Next: add only snapshot-bound `search_changes` MCP reads and result-truncation mapping.
- 2026-08-24: slice `MCP search_changes snapshot reads` completed. Modified files: updated `internal/commands/diff/mcp.go`, `mcp_test.go`, and `mcp_unix_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for legacy tool order/annotation, trimmed case-insensitive snapshot-local paths, default 20/max 100 limits, status/kind filters, result truncation, `snapshot_changed`, and SDK rejection of empty filters, invalid kinds, over-limit values, and whitespace-only queries. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the three-shell/424-asset Vite graph; the existing Vite large-chunk advisory remains informational. Risk: MCP text-diff reads, raw-protocol completion, CLI, and production-browser adapters remain unimplemented; an MCP-started Refresh still is not owned by the REST refresh tracker, so `DELETE /api/refresh` cannot cancel it. Next: add only snapshot-bound `get_text_diff` MCP reads and error/result mapping.
- 2026-08-24: slice `MCP get_text_diff snapshot reads` completed. Modified files: updated `internal/commands/diff/mcp.go` and `mcp_test.go`. Verification: focused `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff` passed for the complete six-tool order/annotations, ready patches at explicit zero and default-three contexts, encoding-only no-change, non-text unavailable results, `snapshot_changed`/`entry_not_found`, safe positive ID and 0-20 context schema validation, and error `structuredContent` limited exactly to `{error}`. `make check` passed (33 Web tests and all Go packages) and `make build` passed with the three-shell/424-asset Vite graph; the existing Vite large-chunk advisory remains informational. Risk: MCP raw-protocol/client completion, CLI, and production-browser adapters remain unimplemented; an MCP-started Refresh still is not owned by the REST refresh tracker, so `DELETE /api/refresh` cannot cancel it. Next: complete only the MCP raw transport/protocol coverage before moving to CLI/browser work.

- 2026-08-24: slice `MCP tool schemas and structured results` completed. Modified files: updated `internal/commands/diff/mcp.go` and `mcp_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff -run '^TestMCPHandlerPublishesDiffToolSchemas$' passed; the complete uncached Diff package suite passed; `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. Raw `tools/list` now proves six-tool order, all list/search/text-diff input defaults/ranges, strict success schemas, and lower-case `get_comparison.snapshot.counts` fields. Tool errors retain only `{error:{code,message}}` while success schemas remain strict. Risk: raw malformed JSON-RPC, HTTP method/Accept/protocol-version behavior, independent stateless clients, and shared REST/MCP refresh cancellation remain unproved; the Vite large-chunk advisory remains informational. Next: add only shared refresh cancellation ownership for an MCP-started refresh.
- 2026-08-24: slice `shared REST and MCP refresh lifecycle` completed. Modified files: updated `internal/commands/diff/http.go`, `http_refresh.go`, `http_refresh_test.go`, and `mcp.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff -run '^(TestProtocolHandlersLetRESTCancelMCPRefresh|TestHTTPHandlerOwnsRefreshLifecycle|TestMCPHandlerStartsOneAsynchronousRefresh)$' passed; the complete uncached Diff package suite passed; `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. `NewProtocolHandlers` supplies paired command-owned REST/MCP adapters over one coordinator: a refresh started by `refresh_comparison` is canceled by REST `DELETE /api/refresh`, publishes `canceled`, clears ownership, and accepts a subsequent REST refresh. Risk: raw malformed JSON-RPC, HTTP method/Accept/protocol-version behavior, and independent stateless clients remain unproved; the Vite large-chunk advisory remains informational. Next: add only raw Streamable HTTP transport rejection and stateless-client coverage.
- 2026-08-24: slice `raw MCP Streamable HTTP transport compatibility` completed. Modified files: added `internal/commands/diff/mcp_transport.go`; updated `mcp.go` and `mcp_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff -run '^(TestMCPHandlerPreservesStreamableHTTPTransport|TestMCPHandlerServesIndependentStatelessClients|TestMCPHandlerServesStatelessComparisonTool)$' passed; complete uncached Diff package suite passed; `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. The command-owned transport wrapper now preserves legacy JSON-RPC failures for POST Accept/content type, malformed JSON, malformed JSON-RPC, unsupported methods, legacy protocol-version errors, stateless DELETE, and stateless GET event streams while leaving valid POST dispatch to the official SDK. It maps the two historical legacy protocol headers to supported SDK values, bypasses the SDK's incorrect stateless DELETE/GET defaults, and proves two independent initialization/tool clients receive no session ID. Risk: CLI lifecycle and production-browser adapters remain unimplemented; raw MCP now has focused transport evidence, and the Vite large-chunk advisory remains informational. Next: inspect only the first CLI lifecycle composition slice.
- 2026-08-24: slice `unregistered Diff CLI binder` completed. Modified files: added `internal/commands/diff/input.go`, `internal/cliapp/diff.go`, and `internal/cliapp/diff_test.go`; updated `internal/cliapp/app.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/cliapp -run '^TestDiffBinding'` passed; complete uncached Diff/CLI suite passed; `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. The optional fixed typed manifest now maps exactly two operands, default port 1205, strict ASCII decimal ports including leading zeroes, public binding, ordered repeated exclusions, and Target gitignore opt-out, while invalid port/operand paths invoke no handler. The binder is absent unless a handler is injected; `cmd/ycy` remains unchanged, so no production Diff command is registered. Risk: resolved-root/workspace composition, HTTP lifecycle, URL presentation, signals, production browser, and standalone integration remain unimplemented; Vite's existing large-chunk advisory remains informational. Next: add only the command-owned resolved-root and Comparison Workspace construction boundary.
- 2026-08-24: slice `Diff production route composition` completed. Modified files: added `internal/commands/diff/server_handler.go` and `server_handler_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff -run '^TestServerHandlerComposesDiffProtocolsAndEmbeddedShell$' passed; complete uncached Diff package suite passed; `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. The command-owned handler now composes paired REST/MCP protocol handlers over one Workspace with the real embedded Diff shell: `/api/*` preserves JSON handling, exact `/mcp` retains the raw MCP transport, emitted Vite assets keep immutable headers, and the existing broad shell fallback including `/mcp/missing` remains intact. It does not bind a listener, start Refresh, or own process lifecycle. Risk: listener lifecycle, startup URL presentation, root/module composition, signal cancellation, production browser workflows, and standalone integration remain unimplemented; Vite's existing large-chunk advisory remains informational. Next: add only listener startup, initial asynchronous Refresh, and graceful server shutdown ownership.
- 2026-08-24: slice `Diff listener and Refresh lifecycle` completed. Modified files: added `internal/commands/diff/server.go` and `server_lifecycle_test.go`; updated `server_handler.go` and `http.go`/`http_refresh.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff -run '^TestRunningServer'` passed; complete uncached Diff package suite passed; `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. `RunningServer` now binds the selected TCP address/port, queues the initial asynchronous Refresh after listening, preserves HTTP state access when initial Refresh reaches `error`, and on Close cancels then waits for active Refresh completion while force-closing the listener and serving goroutine. Tests prove port-zero publication, ready state, post-close connection refusal, retained server availability after a root-replacement failure, and no early close completion while an active Refresh is held. Risk: command Input-to-session composition, URL presentation, composition-root signal path, production browser workflows, and standalone integration remain unimplemented; Vite's existing large-chunk advisory remains informational. Next: add only command-owned Input-to-session construction and context-driven wait semantics.
- 2026-08-24: slice `Diff Input session composition` completed. Modified files: added `internal/commands/diff/session.go` and `session_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff -run '^TestComparisonSession'` passed; complete uncached Diff package suite passed; `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. Command-owned session construction maps typed Input exactly into fixed Workspace options and loopback/public binding, resolves real roots through the existing Workspace owner before listening, and treats parent context cancellation as a normal server-close lifecycle outcome. Invalid root input fails before serving. No public command, terminal output, URL presentation, or browser launch was added. Risk: startup presentation, Module/CLI composition, composition-root signal evidence, production browser workflows, and standalone integration remain unimplemented; Vite's existing large-chunk advisory remains informational. Next: add only URL discovery and presentation data mapping for local/public startup output.
- 2026-08-24: slice `Diff startup URL presentation data` completed. Modified files: added `internal/commands/diff/presentation.go` and `presentation_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff -run '^(TestDiffStartupURLs|TestComparisonSessionBuildsResolvedStartupPresentation)$'` passed; complete uncached Diff package suite passed; `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. Pure command-owned mapping now preserves local loopback URLs, public localhost URLs, ordered non-internal IPv4 network URLs, IPv6 exclusion, resolved roots, and the actual port selected for `--port 0`; it performs no interface enumeration or terminal output. Risk: Module orchestration, concrete network/terminal adapters, CLI composition, signal evidence, production browser workflows, and standalone integration remain unimplemented; Vite's existing large-chunk advisory remains informational. Next: add only the Diff Module's startup-presentation-and-context lifecycle composition.
- 2026-08-24: slice `Diff Module lifecycle composition` completed. Modified files: added `internal/commands/diff/run.go` and `run_test.go`; updated `presentation.go` and `presentation_test.go` to expose command-owned startup data to adapters. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff -run '^(TestModule|TestDiffModule)'` passed; complete uncached Diff package suite passed; `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. The unregistered Module now owns the startup sequence: fixed roots/server first, injected network snapshot next, one terminal presentation after bind, then context-owned waiting/close; pre-cancelled context is a normal no-work exit and adapter/presentation failures close the server before returning. No browser launch or public command registration was added. Risk: concrete terminal/network adapters, CLI composition, composition-root signal evidence, production browser workflows, and standalone integration remain unimplemented; Vite's existing large-chunk advisory remains informational. Next: add only the production network snapshot and terminal startup-presentation adapters in `cmd/ycy`.
- 2026-08-24: slice `Diff production startup adapters` completed. Modified files: added `cmd/ycy/diff.go` and `diff_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^(TestTerminalDiffPresenter|TestCollectDiffNetworkInterfaces)'` passed; complete uncached Diff plus `cmd/ycy` suites passed (the latter 36.654s); `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. The production adapter preserves legacy startup labels/MCP URL ordering and maps real `net.Interfaces`/per-interface addresses into typed Diff facts while retaining order, loopback markers, IPv4/IPv6 values, and failures. It is not yet composed into the executable. Risk: CLI composition, standalone signal/native evidence, production browser workflows, and final Integration Commit evidence remain unimplemented; Vite's existing large-chunk advisory remains informational. Next: inject only the finished Diff Module into the fixed CLI/main composition manifest.
- 2026-08-24: slice `Diff CLI composition` completed. Modified files: updated `cmd/ycy/main.go`. Verification: uncached compile-only `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -run '^$' ./cmd/ycy ./internal/cliapp` passed; complete uncached `cmd/ycy`/CLI regression passed (37.616s); `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. The composition root now builds the concrete Diff Module and supplies its typed `Run` handler to the fixed `cliapp` manifest, so `diff` is a real command while all preexisting command registration remains unchanged. Risk: standalone root/port/bind/output/signal evidence, production browser workflows, native filesystem evidence, Common Unit closeout, and final Integration Commit evidence remain unimplemented; Vite's existing large-chunk advisory remains informational. Next: add only standalone CLI/lifecycle coverage using disposable roots and local listeners.
- 2026-08-24: slice `Diff standalone CLI and signal lifecycle` completed. Modified files: added `cmd/ycy/diff_integration_test.go` and `diff_integration_unix_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./cmd/ycy -run '^TestDiffStandaloneBinary'` passed. Disposable-root standalone coverage proves root help and leaf help, no `--address`, invalid/out-of-range ports, operand counts, equal/non-directory roots, bind conflict, port-zero selected URL/startup output, local state access, public localhost URL, SIGINT, and SIGTERM normal exit. Complete uncached Diff/CLI/`cmd/ycy` regression passed (`cmd/ycy` 44.431s); `make check` passed (33 Web tests, all Go packages, and 424 reachable Vite assets); `make build` passed. Risk: production browser/SSE/worker workflows, broader native filesystem evidence, Common Unit closeout, and final Integration Commit evidence remain unimplemented; Vite's existing large-chunk advisory remains informational. Next: run only production browser workflows against the standalone Diff binary.
- 2026-08-24: slice `Diff production browser workflow and worker assets` completed. Modified files: none; verification used disposable baseline/target roots and a temporary standalone `build/ycy diff --port 0` server only. Verification: the production server selected a loopback ephemeral port and served the direct `/deep/link` fallback with title `HACKYCY CLI — DIFF SERVER`; it loaded the built hashed JavaScript/CSS, Pierre theme assets, and `worker-portable-CISYzss2.js`. After adding one target-only file and one changed file, the production React refresh showed Added `1` and Modified `1`; opening `changed.txt` rendered its `before`/`after` text diff. Unified mode, wrapping, theme, search, and refresh were exercised; refresh invalidated the stale open tab as designed. Browser diagnostics remained empty and exactly one `#root` mounted. The temporary page and server were closed cleanly. Risk: Gate-close raw-protocol/native filesystem audit, standalone artifact smoke, six-target cross-build, and Acceptance Ledger evidence remain outstanding; Vite's existing large-chunk advisory remains informational. Next: run the complete G20 focused raw REST/SSE/MCP/browser/native-host verification, then the required repository and Common Unit checks.
- 2026-08-24: final G20 acceptance completed. Exit 1: all recorded focused workspace, traversal/snapshot, query, content/blob, and bounded text-difference suites passed. Exit 2: all recorded raw REST/SSE/MCP adapter suites passed without arbitrary-path exposure or framework-default security reliance. Exit 3: standalone CLI/lifecycle suites cover parser, bind, port, root, public URL, SIGINT, and SIGTERM behavior; the production browser run proved the embedded deep link, worker/assets, refresh/state/tab invalidation, text presentation, display controls, and empty diagnostics. Exit 4: final uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/diff ./internal/cliapp ./cmd/ycy` passed (Diff 1.060s, CLI 1.437s, `cmd/ycy` 45.858s); `make check` passed with 33 Web tests and 424 reachable Vite assets; `make build` passed; current-host `build/ycy` is macOS arm64 SHA-256 `d0bdb952b813047d6cdb9d5f4135e1aadf8e580c53d8d460591522e89c75247c` and an isolated `diff --port 0` smoke reached ready `/api/state` then exited 0 on SIGINT; `make cross-build` produced and inspected all six nonempty Mach-O/ELF/PE artifacts with matching `CGO_ENABLED=0` Go metadata. The Diff row is `integrated` in `acceptance.md`. Manual acceptance is none. G20 is `passed`; G21 is activated but not started. Risk: the existing Vite large-chunk advisory is informational; release-accepted native Linux/Windows execution remains explicitly outstanding. Next: end this Goal; G21 requires a new Goal.

### G21: FS Foundation

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-24: 已激活，尚未开始实施。
- 2026-08-24: slice `Workspace Path grammar` completed. Modified files: added `internal/commands/fs/path.go` and `internal/commands/fs/path_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs`, `make check`, and `make build` passed. Tests prove root-relative empty and duplicate-separator normalization, Unicode preservation, and rejection of NUL, backslash, leading-slash, drive-prefix, dot, and dotdot inputs. Risk: no Browse Root identity, opened-handle containment, filesystem operation, session state, or public `fs` leaf exists yet; Vite's existing large-chunk advisory remains informational. Next: add only Browse Root identity and opened-handle containment over parsed Workspace Paths.
- 2026-08-24: slice `Workspace Path fail-closed encoding` completed. Modified files: updated `internal/commands/fs/path.go` and `internal/commands/fs/path_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs`, `make check`, and `make build` passed. The path value is now opaque outside its owner and rejects invalid UTF-8 bytes before any future JSON or filesystem use, selecting the inventory-required visible fail-closed policy. Risk: no Browse Root identity, opened-handle containment, filesystem operation, session state, or public `fs` leaf exists yet; Vite's existing large-chunk advisory remains informational. Next: add only Browse Root identity and opened-handle containment over parsed Workspace Paths.
- 2026-08-24: slice `Browse Root opened-handle containment` completed. Modified files: added `internal/commands/fs/workspace.go` and `internal/commands/fs/workspace_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs`, `make check`, and `make build` passed. Tests prove one-time root symlink resolution, missing/non-directory classification, internal-link access, external-link rejection, and stable original-root access after the pathname is moved and replaced. Risk: listing/basic identity, session state, native permission/restart evidence, and public `fs` absence checks remain; Windows reparse-point execution requires its native fixture and is not represented as a passed host test. Vite's existing large-chunk advisory remains informational. Next: add only directory listing and basic file identity through the contained root handle.
- 2026-08-24: slice `directory listing and basic identity` did not complete. Modified files: updated `internal/commands/fs/path.go` and `workspace.go`; updated `workspace_test.go`; added `workspace_unix_test.go`. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs` failed before repository checks: the current macOS case-insensitive volume merged `Alpha.txt` and `alpha.txt`, and the attempted invalid-UTF-8 filename fixture failed with `illegal byte sequence`. Risk: current host fixture assumptions, rather than the intended root-handle behavior, prevented evidence collection. Next: prove case-tie ordering at its direct sort seam and fail-closed name handling at the List validation seam, then rerun the same directed test.
- 2026-08-24: slice `directory listing and basic identity` completed. Modified files: updated `internal/commands/fs/path.go` and `workspace.go`; updated `workspace_test.go`; added `workspace_unix_test.go`. Verification: after replacing host-incompatible fixture assumptions with direct production-seam regression tests, uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs`, `make check`, and `make build` passed. Tests prove directory/file/unavailable identity, internal/broken/escaping links, staging-name hiding, directory-first case-tie ordering, regular-file opened-handle byte stability after replacement, non-file rejection, fail-closed invalid-name handling, and Unix FIFO classification without exposing absolute paths. Risk: fresh-Go session ownership/lifecycle and native permission/restart evidence remain; Windows reparse-point execution remains a native-target outstanding item rather than a passed host test. Vite's existing large-chunk advisory remains informational. Next: implement only Go-owned file-session lock/key/record lifecycle.
- 2026-08-24: slice `fresh-Go session storage lock and key` did not complete. Modified files: added `internal/filesession/store.go`, `store_test.go`, `process_unix.go`, and `process_windows.go`. Directed verification `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/filesession` failed before repository checks because the stale-lock fixture used `math.MaxInt`; Darwin PID conversion treated it as a live special PID. Risk: no lifecycle evidence is accepted until a portable stale-owner fixture proves recovery. Next: use an in-range but impossible `MaxInt32` positive PID and rerun the focused test.
- 2026-08-24: slice `fresh-Go session storage lock and key` completed. Modified files: added `internal/filesession/store.go`, `store_test.go`, `process_unix.go`, and `process_windows.go`. Verification: after using a portable `MaxInt32` stale-owner fixture, uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/filesession`, `make check`, and `make build` passed. Tests prove that only a `go-v1` child is accessed below the supplied base directory, unrelated parent state remains untouched, directory/key permissions are restrictive, live ownership is exclusive, stale Go-owned locks recover, replacement lock ownership is preserved, and HMAC credential revisions persist through only the Go-created 32-byte key. Risk: durable records, issue/resume/refresh/revocation/pruning/limits/observation, and native restart evidence remain; no Bun-written session fixture or compatibility path was introduced. Vite's existing large-chunk advisory remains informational. Next: implement only Go-owned record issuance, resume refresh, and explicit revocation.
- 2026-08-24: slice `fresh-Go record issue refresh and revocation` completed. Modified files: updated `internal/filesession/store.go` and `store_test.go`; added owner-local `replace_unix.go` and `replace_windows.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/filesession`, `make check`, and `make build` passed. Tests prove opaque 32-byte base64url tokens, SHA-256 record filenames, token non-disclosure, v1 JSON record fields/timestamps/modes, fresh use refresh with atomic replacement, explicit revocation, and credential-revision revocation. Risk: startup loading/restart, expiry, malformed/candidate pruning, 8/128 LRU, observers, concurrent refresh, and native permission evidence remain; no Bun-written session fixture or compatibility path was introduced. Vite's existing large-chunk advisory remains informational. Next: implement only Go-owned startup load, expiry/pruning, limits, observation, and concurrent refresh.
- 2026-08-24: slice `fresh-Go record startup recovery and pruning` completed. Modified files: updated `internal/filesession/store.go` and `store_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/filesession`, `make check`, and `make build` passed. Tests prove a Go-issued v1 record resumes after restart and refreshes its idle deadline; startup removes expired, malformed, and interrupted Go-owned candidates, leaves no stale JSON candidate behind, and re-applies mode `0600` to valid records. Risk: active expiry scheduling, 8/128 LRU, observers, concurrent refresh, and broader native permission evidence remain; no Bun-written session fixture or compatibility path was introduced. Vite's existing large-chunk advisory remains informational. Next: implement only active expiry, LRU limits, observation, and concurrent refresh.
- 2026-08-24: slice `fresh-Go session maintenance` completed. Modified files: updated `internal/filesession/store.go` and `store_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/filesession` passed (1.447s); `make check` passed with 33 Web tests, all Go packages, and 424 reachable Vite assets; `make build` passed. Tests prove the exact default eight-per-subject and 128-total least-recently-accessed eviction order, observer notification for LRU, explicit revocation, credential-revision revocation, cancellation, active timer expiry, and serialized concurrent record refresh without leftover candidates. Risk: current-host Unix permission evidence, Windows ACL/lock execution, architecture/no-public-`fs` evidence, standalone smoke, and six-target build evidence remain; no Bun-written session fixture or compatibility path was introduced. Vite's existing large-chunk advisory remains informational. Next: collect only the remaining G21 native permission/restart and ownership/no-command evidence.
- 2026-08-24: slice `FS Foundation ownership boundary` completed. Modified files: updated `internal/cliapp/app_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/cliapp -run '^TestFSFoundationDoesNotExposeACommand$'` and `go test -count=1 ./cmd/ycy -run '^TestActiveArchitecture$'` passed; `make check` and `make build` passed. The CLI help does not list `fs`, direct `fs` invocation remains an unknown command, and the standard-library architecture assertion passed. Risk: no public leaf was added, but current macOS cannot execute Windows ACL, lock, or reparse fixtures; this remains explicitly native-target evidence rather than inferred parity. Next: collect only current-host session permission/restart and cross-target compilation evidence.
- 2026-08-24: slice `native session permission and restart evidence` completed. Modified files: updated `internal/filesession/store_test.go`. Verification: uncached current-host permission/lock/restart suite, `go test -race -count=1 ./internal/filesession -run '^TestManagerSerializesConcurrentResumes$'`, and `go test -count=1 ./internal/commands/fs` passed; `make check` and `make build` passed. The actual Unix owner now asserts `0700` state-directory and `0600` key, lock, and record modes; live/stale lock handling and Go-issued restart recovery passed. Windows amd64/arm64 `filesession` and `fs` test packages each compiled to the inspected PE target format without attempting to execute them on macOS. Risk: actual Windows ACL/lock/reparse execution remains a native-target outstanding item, not a passed host test; no Bun-written state fixture or compatibility path was introduced. Next: run only G21 aggregate focused, standalone-host, and six-target closeout evidence.
- 2026-08-24: final G21 acceptance completed. Exit 1: Workspace Path grammar, Browse Root identity, opened-handle containment, internal/broken/escaping links, root replacement, stable opened-file bytes, listing identity, and Unix special-file behavior have focused evidence. Exit 2: Go-owned session creation, lock/key/record lifecycle, restart, refresh, revocation, pruning, exact 8/128 LRU, observation, active expiry, concurrent refresh under `-race`, and current-host Unix modes have focused evidence without any Bun fixture. Exit 3: the standard-library architecture test passed; CLI regression and standalone `build/ycy` help confirm no public `fs` leaf, and direct `build/ycy fs` returned the expected unknown-command error. Exit 4: the FS Foundation row is `integrated` in `acceptance.md`; final uncached focused tests, `make check`, `make build`, host version/help smoke, and six nonempty CGO-free cross-build artifacts with matching format/target metadata passed. Manual acceptance is none. G21 is `passed`; G22 is activated but not started. Risk: actual Linux/Windows native execution, including Windows ACL/lock/reparse work, remains explicitly outstanding in the Acceptance Ledger and is not treated as passed evidence. Next: end this Goal; G22 requires a new Goal.

### G22: fs

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-24: activated as `active`; not started.
- 2026-08-24: slice `read-only workspace HTTP` completed. Modified files: added `internal/commands/fs/readonly.go`, `http.go`, `http_response.go`, and `readonly_test.go`; retained the FS Foundation root-handle API while extending its structured HTTP adaptation. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs`, `make check`, and `make build` passed. The focused suite proves contained directory listing fields/URLs/MIME/preview/archive classification, strict UTF-8 and UTF-16 text previews with binary and 10 MiB outcomes, structured API headers/errors, and original GET/HEAD/OPTIONS, conditional, range, CORS, directory redirect, Unicode-safe disposition, and HTML/SVG policy behavior. Risk: public command registration, login/session, writes, uploads, download, archive, thumbnail, tasks/SSE, retained React production routing, and standalone behavior remain absent. The existing Vite large-chunk advisory remains informational. Next: implement only authentication/session/edit/management operations.
- 2026-08-24: slice `authentication and fresh-Go sessions` completed. Modified files: added `internal/commands/fs/auth.go` and `auth_test.go`; extended the read-only HTTP adapter with `/api/session`, protected-read resumption, cookie refresh/revocation, and same-origin login/logout checks; added direct `golang.org/x/crypto v0.53.0` for Argon2id and its `go.sum` records. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs ./internal/filesession`, `make check`, and `make build` passed. The initial repository pass correctly stopped at a stale module graph; `GOTOOLCHAIN=go1.26.7 GOWORK=off go mod tidy` generated the exact dependency metadata, then all directed and repository checks were rerun successfully. Tests prove account grammar/UTF-16 boundary path, case-insensitive identity, Argon2id unknown/wrong-password work, Go-owned session issuance/resumption/revocation, protected reads, cookie attributes, and origin validation, without a Bun session read or fixture. Risk: conditional editing and management operations, uploads, download, archive, thumbnail, tasks/SSE, public CLI, React routing, and standalone behavior remain absent. Next: implement only conditional text editing and management operations.
- 2026-08-24: slice `conditional text editing` completed. Modified files: added `internal/commands/fs/edit.go` and `edit_test.go`; extended `/api/text` with the Management/Origin/media-type/If-Match guarded `PUT` path and structured conflict results. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs`, `make check`, and `make build` passed. Tests prove stable source revision checks, no final-link edit target, destination-local exclusive `.edit-*` staging, sync and root-confined rename publication, mode and UTF-8 BOM/UTF-16LE preservation, newline normalization/trailing policy, 10 MiB limits, stale conflict, and HTTP 403/415/428/412 outcomes. Risk: directory/file management operations, uploads, download, archive, thumbnail, tasks/SSE, public CLI, React routing, and standalone behavior remain absent. Next: implement only management operations.
- 2026-08-24: slice `management operations` completed. Modified files: added `internal/commands/fs/operations.go`, `operations_http.go`, and `operations_test.go`; extended the FS HTTP adapter with strict `POST /api/operations`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs ./internal/filesession`, `make check`, and `make build` passed. Tests prove root-confined create/rename/copy/move/delete semantics, collision suffixes, recursive directory and link-aware copy, permanent final-link deletion, root immutability, self/descendant refusal, ordered partial results, strict schema/extra-field rejection, and Management/Origin/method gates. Risk: direct/chunked uploads, remote download, target 7-Zip, thumbnail workers, tasks/SSE, public CLI, retained React production routing, and standalone behavior remain absent. Next: implement only direct upload.
- 2026-08-24: slice `direct upload` completed. Modified files: added `internal/commands/fs/upload.go` and `upload_test.go`; extended the HTTP adapter with managed same-origin `POST /api/upload`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs`, `make check`, and `make build` passed. Tests prove trimmed single-name validation, destination-local exclusive `.upload-*` staging, 1 GiB guarded streaming path, hard-link publication, collision suffixing without overwrite, cleanup, multipart file selection, and Management/Origin/media-type failures. Risk: retryable chunked upload, remote download, target 7-Zip, thumbnail workers, tasks/SSE, public CLI, retained React production routing, and standalone behavior remain absent. Next: implement only chunked upload.
- 2026-08-24: slice `retryable chunked upload` completed. Modified files: added `internal/commands/fs/chunked_upload.go`, `chunked_upload_http.go`, and `chunked_upload_test.go`; extended directory capability output and `/api/uploads` session endpoints. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs`, `make check`, and `make build` passed. Tests prove the strict >20 MiB threshold, 4/8 MiB configured chunks, owner isolation, ordered confirmed offsets, body/range mismatch refusal, process-local staged files, idempotent completion/replay, no-overwrite publication, and advertised capability. Risk: the 30-minute/5-minute record pruning runs on protocol access rather than a dedicated timer; remaining remote download, target 7-Zip, thumbnail workers, tasks/SSE, public CLI, retained React production routing, and standalone behavior are absent. Next: implement only remote download.
- 2026-08-24: slice `remote download core queue` completed. Modified files: added `internal/commands/fs/download.go`, `download_http.go`, and `download_test.go`; extended the adapter with `/api/downloads` create/list/cancel/retry/terminal-clear controls. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs`, `make check`, and `make build` passed. Tests prove prohibited local targets, validated HTTP(S) input, two-slot asynchronous scheduling, terminal publication/cleanup, task snapshots, and HTTP Management/Origin/schema handling. Risk: response stream progress is committed at publication rather than 250-ms intervals, and timeout/header filename RFC coverage plus SSE remain required before leaving the remote-download stage; target 7-Zip, thumbnail workers, public CLI, retained React routing, and standalone behavior are absent. Next: add only remote response streaming, timeout, and task observation behavior.
- 2026-08-24: slice `remote download stream progress` completed. Modified files: updated `internal/commands/fs/upload.go` and `download.go` to feed destination-local streamed writes into task byte/progress snapshots. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/commands/fs ./internal/filesession`; `make check`; and `make build` passed. Risk: progress publication throttling, per-read idle timeout, complete RFC filename parsing, and task SSE remain required before leaving remote download; later target 7-Zip, thumbnail workers, public CLI, retained React routing, and standalone behavior remain absent. Next: add only remote idle-timeout and filename behavior.
- 2026-08-24: slice `remote response timeout and metadata` completed. Modified files: updated `internal/commands/fs/download.go` and `download_test.go`. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/filesession`; `make check`; and `make build` passed. Focused tests prove the 60-second header and per-read idle timeout paths with injected test intervals, malformed redirect handling without panic, literal reserved IPv4/IPv6 rejection, request filename precedence, limited UTF-8 `filename*`/plain/final-URL filename fallback, encoded/unsafe response-size behavior, and throttled plus forced-boundary task progress/speed snapshots. Risk: queue cancellation still retains stale queue entries, terminal history has no deterministic oldest-first cap, and task SSE remains absent; target 7-Zip, thumbnail workers, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only remote task lifecycle, queue cleanup, and terminal-history retention.
- 2026-08-24: correction `remote header timeout context lifetime` completed. Modified files: updated `internal/commands/fs/download.go` and `download_test.go`. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/filesession`, `make check`, and `make build` passed. The regression test proves that stopping the header timer does not cancel the established response body; cancellation now remains deferred until streaming finishes while header acquisition still times out. Risk: queue cancellation/history retention and task SSE remain absent. Next: implement only remote task lifecycle, queue cleanup, and terminal-history retention.
- 2026-08-24: slice `remote task lifecycle and history` completed. Modified files: updated `internal/commands/fs/download.go` and `download_test.go`. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/filesession`; `make check`; and `make build` passed. Focused tests prove active cancellation closes a stalled response body and staging work, terminal retry creates a different UUID while retaining the old record, the two active slots plus 100 waiting-slot limit, queued cancellation releasing capacity, close-time cancellation, deterministic newest-first snapshots, and oldest-first terminal pruning to 100 records. Risk: remote download has no task-subscription interface yet, so its complete snapshots/SSE are deferred to the designated tasks/SSE slice; target 7-Zip, thumbnails, public CLI, retained React routing, and standalone behavior remain absent. Next: audit the completed remote-download contract against frozen vectors before moving to target 7-Zip.
- 2026-08-24: slice `remote download protocol parity` completed. Modified files: updated `go.mod`, `internal/commands/fs/download.go`, `download_http.go`, `http_response.go`, and `download_test.go`. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/filesession`; `make check`; and `make build` passed. Focused vectors prove strict required JSON fields, filename trimming, `DOWNLOAD_UNAVAILABLE` 502 mapping, WHATWG-style scheme/IDNA/dot-path normalization, decimal/octal/hex numeric IPv4 private-target rejection, the five manual redirect statuses only, per-hop private redirect rejection, five-hop refusal, explicit no-auto-follow behavior even with an injected client, and HTTP retry/missing-control/terminal-clear outcomes. Remote URL/response, streaming, queue/task, cancellation/retry/clear, and progress contracts now have focused evidence; download SSE remains intentionally deferred to the ordered tasks/SSE slice. Risk: target 7-Zip, thumbnails, tasks/SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only the target 7-Zip runtime/payload and extraction contract.
- 2026-08-24: slice `7-Zip pinned target payload inputs` completed. Modified files: added `internal/sevenzipmanifest`, target-gated `internal/commands/fs/sevenzipruntime` embedding sources/tests, and `tools/prepare-sevenzip`; updated `Makefile` and `.gitignore`. Verification: official 7-Zip 26.02 archives for all six targets matched the frozen archive SHA-256 pins; extracted `7zz`/`7z.exe`/`7z.dll`/license bytes were individually SHA-256 pinned; `make prepare-7zip-all` passed; current-target manifest and embedded-byte tests passed; six CGO-free target test binaries compiled and were inspected as the expected Mach-O, ELF, and PE CPU formats; `make check` and `make build` passed. The 94 MB disposable cross-test directory was moved to the local Trash after inspection. Risk: the payload package is intentionally not yet reachable from the binary until state materialization is implemented; archive inspection/extraction, task HTTP, thumbnails, tasks/SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only verified 7-Zip runtime state materialization and source-mode fallback.
- 2026-08-24: slice `7-Zip verified runtime materialization` completed. Modified files: updated `internal/commands/fs/sevenzipruntime` with state-root selection, locked candidate publication, restrictive modes, file-type/digest verification, regeneration, and PATH fallback tests. Verification: uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; six target runtime-package test binaries compiled and were inspected as the expected Mach-O, ELF, and PE CPU formats; `make check`; and `make build` passed. Tests prove the versioned state location, `0700` executable/`0600` auxiliary modes on Unix, concurrent materialization, corrupt-file and symlink regeneration, and fallback only for an absent embedded payload. The 94 MB disposable cross-test directory was moved to the local Trash after inspection. Risk: archive naming, inspection, extraction, workspace publication, task HTTP, thumbnails, tasks/SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only archive suffix recognition and extraction destination naming.
- 2026-08-24: slice `archive suffix and destination naming` completed. Modified files: added `internal/commands/fs/archive.go` and `archive_test.go`; updated `readonly.go` to use the complete shared archive classifier. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Tests prove the full case-insensitive suffix set, longest-suffix selection, layered-TAR recognition, safe `Extracted` fallbacks, and directory-listing integration. Risk: inspection/extraction subprocesses, workspace publication, extraction task HTTP, thumbnails, tasks/SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only 7-Zip archive inspection and normalized failure classification.
- 2026-08-24: slice `7-Zip archive inspection and failure classification` completed. Modified files: added `internal/commands/fs/archive_inspection.go` and `archive_inspection_test.go`; extended the existing command-wide `ServiceError` with an optional internal cause. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Focused tests prove the fixed `7z l -slt -sccUTF-8 -- <archive>` argv and C locale, streamed `Path`/`Size`/`Encrypted` parsing, safe integer byte/count accounting, encrypted/split/multi-volume/oversized rejection, process cancellation, bounded stderr tailing, and every frozen normalized stderr/exit class. Risk: actual extraction staging, capacity, layered TAR, workspace publication, extraction task HTTP, thumbnails, tasks/SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only 7-Zip extraction subprocess staging and progress parsing.
- 2026-08-24: slice `7-Zip extraction subprocess and progress` completed. Modified files: updated `internal/commands/fs/archive_inspection.go` and `archive_inspection_test.go`. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Focused tests prove fixed `7z x -y -sccUTF-8 -bso0 -bse2 -bsp1 -o<staging> -- <archive>` argv and C locale, CR-delimited progress parsing across read chunks with 100% clamping/final completion, and normalized process failure reuse. Risk: this internal executor accepts command-owned physical paths but exposes none through HTTP/React; workspace-local staging, capacity, layered TAR, validation/publication, extraction tasks, thumbnails, tasks/SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only archive capacity preflight.
- 2026-08-24: slice `archive capacity preflight` completed. Modified files: added `internal/commands/fs/archive_capacity.go` and `archive_capacity_test.go`. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Focused tests prove byte and inode capacity calculations preserve the frozen 10% reserves capped at 1 GiB/1024, exact-fit behavior, both normalized insufficient-space messages, and absent inode reporting. Risk: no platform statfs adapter has been wired; workspace-local staging, layered TAR, validation/publication, extraction tasks, thumbnails, tasks/SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only workspace-local archive staging and validated publication.
- 2026-08-24: slice `workspace-local archive extraction and publication` completed. Modified files: added `internal/commands/fs/archive_staging.go`, `archive_staging_test.go`, `archive_extract.go`, `archive_extract_test.go`, and target capacity adapters; updated `workspace.go` for private command-only subprocess path resolution and temporary-name hiding. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Six CGO-free `internal/commands/fs` target test binaries compiled and inspected as Mach-O x64/arm64, ELF x64/arm64, and PE x64/arm64; their temporary directory was moved to the local Trash. Focused tests prove stable contained source copying, hidden same-directory staging cleanup, 10% reserve capacity checks including platform-query failure, normal 7-Zip inspect/extract with progress, layered-TAR flow support, symlink/special-file validation, collision-safe destination publication, and no staged/output leak after failures. Absolute paths remain private workspace state used only for subprocess invocation. Risk: single-worker task lifecycle and HTTP/SSE controls, real format/native extraction matrix, thumbnails, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only extraction task lifecycle and history.
- 2026-08-24: slice `single-worker extraction task lifecycle` completed. Modified files: added `internal/commands/fs/extraction.go` and `extraction_test.go`; updated `archive_extract.go` with internal inspection notification. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Focused tests prove 1-100 validated paths, newest-first snapshots, one active extraction, queued/active cancellation, final inspection/progress/destination fields, terminal retry with new UUID and preserved old record, terminal clearing, history pruning basis, and stopped-service refusal. Risk: extraction HTTP/SSE controls, real format/native extraction matrix, thumbnails, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only extraction task HTTP controls.
- 2026-08-24: slice `extraction task HTTP controls` completed. Modified files: added `internal/commands/fs/extraction_http.go` and `extraction_http_test.go`; updated `http.go` and `http_response.go`. Verification: `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Focused raw HTTP tests prove absent/disabled behavior, task list/create/cancel/retry/terminal-clear response shapes/statuses, strict JSON/body fields, API headers, and Management plus same-Origin enforcement with normalized extraction error mapping. Risk: extraction SSE, real format/native extraction matrix, thumbnails, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only the selected thumbnail decoder/normalization boundary.
- 2026-08-24: slice `thumbnail decoder and WebP normalization` completed. Modified files: added `internal/commands/fs/thumbnail.go` and `thumbnail_test.go`; updated `go.mod` and `go.sum` with the fixed `gav1d v0.2.5`, `vpx v0.2.1`, and `x/image v0.45.0` graph. Verification: `GOTOOLCHAIN=go1.26.7 GOWORK=off go mod tidy`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Focused tests prove PNG/JPEG/GIF/AVIF/WebP decode paths, GIF first-frame static behavior, JPEG EXIF Orientation parsing/transform, unsupported/malformed/64 MiB rejection, and 160x160 center-cover static WebP output. The selected module graph raises the module language minimum from Go 1.26.0 to 1.26.4, which remains below the fixed Go 1.26.7 toolchain. Risk: pixel corpus/10-bit and format-orientation fixtures, self-exec worker pool/timeout/replacement, cache/HTTP, task SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only hidden thumbnail worker framing.
- 2026-08-24: slice `hidden self-exec thumbnail worker framing` completed. Modified files: added `internal/commands/fs/thumbnail_worker.go` and `thumbnail_worker_test.go`; updated `cmd/ycy/main.go`. Verification: `git diff --check`; focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs -run 'TestRunThumbnailWorker|TestIsThumbnailWorkerInvocation|TestConvertThumbnail'`; complete uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; `make build`; and `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go run ./cmd/ycy --internal-thumbnail-worker < /dev/null` all passed. Focused pipe tests prove sequential request reuse, normal conversion errors returned in-frame without ending the worker, malformed/truncated/oversized frame refusal, bounded binary payloads, and exact-only private argument recognition; the executable intercepts that exact argument before web/application initialization. Risk: the required two-child pool, queue, five-second kill/reap/replacement, coalescing/cache, thumbnail HTTP, task SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only the two-worker self-exec lifecycle, including timeout/replacement and queue behavior.
- 2026-08-24: slice `two-worker self-exec thumbnail lifecycle` completed. Modified files: added `internal/commands/fs/thumbnail_pool.go` and `thumbnail_pool_test.go`. Verification: `git diff --check`; focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 -timeout 30s ./internal/commands/fs -run '^TestThumbnailWorkerPool'`; complete uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. The subprocess suite proves two lazily started persistent slots, one-task-per-slot dispatch, source-copy queueing, 128 waiting tasks, normal conversion error delivery, 5-second-configured timeout cancellation, `Process.Kill` plus exactly-once `Wait`, different-PID replacement after timeout or crash, queued failure when every worker fails, and close-time queued/in-flight rejection with child reaping. stderr is separately bounded and drained. Risk: workspace thumbnail lookup and input prevalidation, in-flight coalescing/LRU, HTTP response/cache behavior, task SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only root-confined thumbnail workspace retrieval, validation, coalescing, and cache behavior.
- 2026-08-24: slice `thumbnail workspace retrieval and process-local cache` completed. Modified files: updated `internal/commands/fs/thumbnail.go` and `thumbnail_test.go`; added `thumbnail_service.go` and `thumbnail_service_test.go`. Verification: `git diff --check`; focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs -run 'TestThumbnailService|TestConvertThumbnail|TestRunThumbnailWorker|TestThumbnailWorkerPool'`; complete uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Tests prove root-confined opened-file reads, extension/MIME and 64 MiB rejection before conversion, a valid oversized PNG header rejected at the 50M-pixel boundary before full decoding, coalescing on Workspace Path plus size plus mtime milliseconds, defensive cached-result copies, LRU promotion, 1000-entry eviction, 32 MiB byte eviction, and stopped-service cache disposal. Risk: thumbnail HTTP conditional/HEAD/status/header behavior, final integration of the pool through the public FS lifecycle, task SSE, public CLI, retained React routing, and standalone behavior remain absent. Next: implement only thumbnail HTTP response behavior and its raw HTTP vectors.
- 2026-08-24: slice `thumbnail HTTP presentation` completed. Modified files: updated `internal/commands/fs/http.go` and `http_response.go`; added `thumbnail_http_test.go`. Verification: `git diff --check`; focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs -run 'TestReadOnlyHandlerServesConditionalHeadThumbnailResponses|TestReadOnlyHandlerMapsThumbnailFailuresAndAuthentication|TestThumbnailService|TestConvertThumbnail|TestRunThumbnailWorker|TestThumbnailWorkerPool'`; complete uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Raw HTTP tests prove protected `/thumbnails/*` routing, GET/HEAD only, full static WebP output without Range/CORS, exact weak thumbnail ETag shape, Last-Modified/conditional 304, no-cache/nosniff/no-referrer headers, status-family normalization to `THUMBNAIL_ERROR`, and Login Mode protection. Risk: pool/service wiring into the future public FS lifecycle, task SSE, public CLI, retained React routing, six-target native thumbnail artifact tests, and standalone integration remain absent. Next: implement only shared download/extraction task subscriptions and SSE framing.
- 2026-08-24: slice `download and extraction task SSE` completed. Modified files: added `internal/commands/fs/task_stream.go`, `task_events.go`, and `task_stream_test.go`; updated `download.go`, `extraction.go`, `download_http.go`, and `extraction_http.go`. Verification: `git diff --check`; focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs -run 'Test(Download|Extraction)TaskSubscription'`; complete uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. Tests prove each manager emits an immediate complete snapshot, publishes complete state changes, and closes subscribed streams on manager close. The precise `/api/downloads/events` and `/api/extractions/events` GET routes now send unlabelled `data: {"version":1,"tasks":[...]}` frames with SSE cache/connection/security headers and client cancellation unsubscribes. Risk: authenticated session-revocation stream closure, built React EventSource integration, public CLI lifecycle, retained FS production routing, six-target native/standalone evidence, and final common integration remain absent. Next: implement only authenticated session-revocation stream closure.
- 2026-08-24: slice `authenticated task SSE revocation lifecycle` completed. Modified files: updated `internal/commands/fs/auth.go`, `auth_test.go`, `download_http.go`, and `extraction_http.go`; added `task_events_test.go`. Verification: `git diff --check`; focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs -run 'TestAuthentication|Test(Download|Extraction)TaskSubscription|TestAuthenticatedTaskEventsCloseWhenSessionIsRevoked'`; complete uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. The existing fresh-Go token observer is now exposed through Authentication and bound to each authenticated task-event subscription. Network tests prove an authenticated download and extraction stream each emits its immediate complete snapshot and reaches EOF after `SignOut`; the observer also covers store-driven expiry, LRU eviction, and credential-revision revocation. Risk: built React EventSource integration, public CLI lifecycle, retained FS production routing, six-target native/standalone evidence, and final common integration remain absent. Next: implement only public `fs` lifecycle composition and command registration.
- 2026-08-24: slice `FS production route composition` completed. Modified files: updated `web/readiness_handler.go`; added `web/fs_handler_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./web`; then `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. The new production composition requires an FS adapter and gives it unmodified ownership of `/api`, `/api/*`, `/files`, `/files/*`, `/thumbnails`, and `/thumbnails/*`; it gives only `/`, `/browse`, and `/browse/*` to the embedded FS shell, with existing CSP/no-store headers, while exact embedded assets retain immutable headers. Tests cover shell/HEAD/assets, route rejection, adapter query/method preservation, and non-rewritten adapter headers. The existing Vite large-chunk advisory remains informational. Risk: this handler is not yet bound to the FS runtime or public CLI; browser task-stream interaction, lifecycle/signals, standalone/native evidence, and final Integration Commit remain absent. Next: inspect and implement only the retained React task-stream interaction that remains unverified.
- 2026-08-24: slice `FS adapter-to-shell composition` completed. Modified files: updated `internal/commands/fs/http.go`; added `server_handler_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs -run 'TestServerHandler|TestReadOnlyHandler|TestFSProductionHandler'`; then `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. `fs.NewServerHandler` now composes the actual FS HTTP adapter through the production route boundary and refuses a nil Workspace; focused tests prove the live directory JSON API, original-file route, and embedded browse shell remain independently owned. The existing Vite large-chunk advisory remains informational. Risk: no listener/service shutdown ownership or public CLI binding exists yet; browser task-stream interaction, standalone/native evidence, and final Integration Commit remain absent. Next: implement only owned FS runtime/listener lifecycle and cleanup.
- 2026-08-24: slice `FS listener lifecycle` completed. Modified files: added `internal/commands/fs/server.go` and `server_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs -run 'Test(RunningServer|StartServer)'`; then `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. `RunningServer` binds the composed FS handler, exposes the concrete port/URL, force-closes HTTP on shutdown, waits for serving to stop, and invokes a supplied resource release exactly once on normal close, unexpected listener termination, and bind failure. Tests cover a real port-zero API request, idempotent close, unexpected listener close, occupied-port cleanup, and joined cleanup error evidence. The existing Vite large-chunk advisory remains informational. Risk: the individual FS services are not yet collected under this release hook or bound to a CLI; browser task-stream interaction, standalone/native evidence, and final Integration Commit remain absent. Next: implement only owned FS runtime resource construction and cleanup.
- 2026-08-24: slice `FS runtime resource ownership` completed. Modified files: added `internal/commands/fs/runtime.go` and `runtime_test.go`; updated `server.go`, `chunked_upload.go`, `chunked_upload_test.go`, `thumbnail_pool.go`, and `thumbnail_pool_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs -run 'Test(Runtime|NewRuntime|StartServer|RunningServer|Chunked|ThumbnailWorkerPoolClosesBefore)'`; then `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. The new Runtime owns workspace, optional fresh-Go authentication/chunk uploads, downloads, extraction, and thumbnails; its server release stops every service before closing the workspace. Runtime tests exercise an authenticated port-zero session endpoint and assert stopped task services. This slice exposed and fixed a nil-slot panic when a lazy thumbnail pool closed before first work, and it adds incomplete chunk-staging cleanup plus stopped-state refusal. The existing Vite large-chunk advisory remains informational. Risk: the typed command lifecycle/presentation and public Cobra binding remain absent; browser task-stream interaction, standalone/native evidence, and final Integration Commit remain absent. Next: implement only the unregistered FS typed command lifecycle.
- 2026-08-24: slice `unregistered FS command lifecycle` completed. Modified files: added `internal/commands/fs/input.go`, `presentation.go`, `run.go`, and `run_test.go`. Verification: focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs -run 'Test(Module|Runtime|NewRuntime|StartServer|RunningServer|Chunked|ThumbnailWorkerPoolClosesBefore)'`; then `git diff --check`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/commands/fs ./internal/commands/fs/sevenzipruntime ./internal/filesession ./internal/sevenzipmanifest`; `make check`; and `make build` passed. The typed Module creates Runtime, binds it, presents concrete port-zero local/network URLs, and waits for either context cancellation or an unexpected server exit; presentation and host-dependency failures close the listener/runtime. Tests cover port-zero, public-network filtering, stop presentation, and lifecycle error propagation. The existing Vite large-chunk advisory remains informational. Risk: `fs` remains absent from the public Cobra tree; standalone/browser, six-target, and acceptance-ledger evidence remain absent. Next: final Integration Commit, beginning with the public CLI binder and main composition.
- 2026-08-24: slice `public FS CLI binding and standalone lifecycle` completed. Modified files: added `internal/cliapp/fs.go`, `internal/cliapp/fs_test.go`, `cmd/ycy/fs.go`, and `cmd/ycy/fs_integration_test.go`; updated `internal/cliapp/app.go`, `cmd/ycy/main.go`, and `internal/commands/fs/session_directory.go` with its tests. Verification: `gofmt -d` produced no diff; focused uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./internal/cliapp ./cmd/ycy ./internal/commands/fs -run 'Test(FS|Module|Runtime|NewRuntime|StartServer|RunningServer|DefaultSession)'`; uncached `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -race -count=1 ./cmd/ycy -run '^TestFSStandaloneBinary' -timeout 90s`; `make check`; and `make build` passed. The public `ycy fs [directory]` binder has strict port, idle-day, boolean, and 4-16 MiB options with CLI/environment/default precedence, derives a Go-owned session directory only for configured accounts, and starts the typed Runtime with terminal URL/lifecycle presentation. Standalone tests prove help and rejected startup input, port-zero public shell/API behavior, fresh-Go login protection, and SIGINT shutdown. Risk: production browser task-stream behavior and six-target CGO-free/native artifact evidence remain outstanding; the Vite large-chunk advisory is informational. Next: verify production shell and task EventSource behavior through a built standalone FS binary, then run the Gate-close native/payload matrix.
- 2026-08-24: slice `standalone production browser, service, and artifact evidence` completed. Modified files: extended `cmd/ycy/fs_integration_test.go`; added `THIRD_PARTY_NOTICES.md`; updated `acceptance.md`. Verification: a built standalone binary served the production FS shell; Browser loaded the embedded shell, exercised managed directory creation and the `/browse` route, and reported no diagnostics. The uncached standalone suite proved SSE headers/initial frames, multipart publication, WebP thumbnail/304, bundled 7-Zip extraction, private-download rejection, fresh-Go session protection, and SIGINT cleanup; the complete uncached FS/session/7-Zip/CLI race suite passed. `make check`, `make build`, and `make cross-build` passed; six CGO-free artifacts were inspected as Mach-O x64/arm64, static ELF x64/arm64, and PE x64/arm64; the host artifact recorded Go 1.26.7, `CGO_ENABLED=0`, selected thumbnail sums, and embedded worker/7-Zip/Web markers. `THIRD_PARTY_NOTICES.md` records the selected dependency and Go license/patent texts. Risk: matching Linux/Windows native execution remains a future Release-accepted Artifact Gate, not a G22 prerequisite; the Vite large-chunk advisory is informational. Next: record final G22 acceptance and activate only G23 without starting it.
- 2026-08-24: final G22 acceptance completed. Exit 1: public CLI, raw HTTP, fresh-Go authentication/session, original/text, operations, direct/chunked upload, remote download, and task contracts have focused `-race` and standalone evidence. Exit 2: target 7-Zip payload/materialization/extraction and the selected pure-Go two-worker thumbnail graph, timeout/replacement, pinned sums, and notices have focused artifact evidence. Exit 3: task SSE, embedded production React/browser routing, lifecycle/signals, and current-host standalone behavior passed without Bun state. Exit 4: the `fs` Unit is `integrated` in `acceptance.md`, with common `make check`, `make build`, host smoke, and six-target cross-build evidence. G22 is `passed`; G23 is activated but not started. Risk: release-accepted matching native evidence belongs to the later Artifact Gate. Next: end this Goal; G23 requires a new Goal.

### G23: Tunnel Foundation

- 2026-08-23: initialized as `planned`; no implementation evidence.
- 2026-08-24: activated as `active`; not started.

### G24: tunnel server

- 2026-08-23: initialized as `planned`; no implementation evidence.

### G25: tunnel connect

- 2026-08-23: initialized as `planned`; no implementation evidence.

### G26: upgrade

- 2026-08-23: initialized as `planned`; no implementation evidence.

### G27: Final Artifact Gate

- 2026-08-23: initialized as `planned`; no implementation evidence.
